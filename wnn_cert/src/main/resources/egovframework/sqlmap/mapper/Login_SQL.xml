<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.wnn_cert.login.mapper.LoginMapper">

	<select id="getLoginUser" parameterType="UserDTO" resultType="UserDTO">
		SELECT *
		  FROM TW_USER
		 WHERE USER_ID = #{userId}
		   AND USER_PW = #{userPw}
		   AND USE_YN = 'Y'
	</select>

	<select id="getUserList" resultType="UserDTO">
		SELECT *
		  FROM TW_USER
		 WHERE USE_YN = 'Y'
		 ORDER BY USER_NM
	</select>

	<insert id="insertUser" parameterType="UserDTO">
		INSERT INTO TW_USER
		       (USER_ID, USER_NM, USER_PW, DEPT_NM, ROLE_CD, USE_YN)
		VALUES (#{userId}, #{userNm}, #{userPw}, #{deptNm}, #{roleCd}, 'Y')
	</insert>

	<update id="updateUser" parameterType="UserDTO">
		UPDATE TW_USER
		   SET USER_NM = #{userNm}
		     , DEPT_NM = #{deptNm}
		     , ROLE_CD = #{roleCd}
		     , USE_YN = #{useYn}
		 WHERE USER_ID = #{userId}
	</update>

</mapper>
