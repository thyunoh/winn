-- ============================================================================
-- 여수시립(36280704) 2026-07 환자평가표 109건 복구  (2026-08-03)
--
-- [사고 경위 — TBL_MAGAM_DEL_HIS 로 확정]
--   8/1 17:04:05  109건 업로드 ①(작업키 20260801170333) 완료
--   8/1 17:04:08  같은 파일이 한 번 더 올라감 ②(20260801170408, 파일 다중선택 중복)
--                 → upsert 가 같은 환자 102명의 소속(CHUNGSEQ)을 ② 로 가져감
--   8/1 17:04:48  담당자가 목록의 '중복' ② 를 삭제
--                 → 삭제 SP 가 CHUNGSEQ=② 인 평가표를 지우면서 109건 전체 소실
--   8/1 17:55:47  별도 환자 17명 파일 업로드(같은 파일명, 내용 다름) → 현재 이 17건만 남음
--
-- [복구 원리] 원본 줄은 TBL_FILES_DATA 에 배치별로 그대로 남아 있다(109줄 확인됨).
--   업로드 SP 를 그 배치(jobs_dt)로 다시 태우면 평가표가 재적재된다(upsert — 17건과 충돌 없음).
--
-- 실행 후 화면에서 '적정성평가 자료생성' 재실행할 것.
-- ============================================================================

CALL SP_UPLOAD_MAGAM_SAMFILES('36280704','2026','07','9','20260801170333','recover0803',109,@ec,@em);
SELECT @ec AS errcode, @em AS errmess;   -- errcode '0' 또는 빈값이면 성공

-- 확인: 202607 배치별 행수 — 170333 이 ~102건, 175516 이 17건이면 정상(합계 ~119)
SELECT CHUNGSEQ, COUNT(*) cnt
  FROM TBL_PATVAL_MST
 WHERE HOSP_CD='36280704' AND MED_START LIKE '202607%'
 GROUP BY CHUNGSEQ;
