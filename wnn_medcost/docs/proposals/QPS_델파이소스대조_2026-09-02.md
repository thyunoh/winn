# QPS — SUNWOO 델파이 원본 소스 대조 (2026-09-02)

> 지금까지 서식 385종(점검표 309 · safeRpt 76)은 **전부 SUNWOO 화면 캡처를 판독**해 옮겼다.
> 2026-09-02 에 **델파이 원본 소스**(`D:\sunwoo\sunwoo`, .pas 1,531 · .dfm 1,486)가 생겼다 —
> 캡처로는 못 보던 두 가지가 보인다 : ①**동작**(더블클릭·Enter·전월복사·서명·목록) ②**보류 서식의 진짜 구조**.
> 사용자 말대로 「검증된 소스라 사용자 위주 기능이 많다」 — 이 문서는 그것을 세어 WinCheck+ 에 무엇을 얹었고
> 무엇을 일부러 안 얹었는지 적은 것이다.

## 0. 한 장 요약

| | |
|---|---|
| **소스 구조** | SUNWOO 에는 **서식 엔진이 없다.** 서식마다 `TShape`·`TLabel`·`TcxTextEdit` 를 픽셀 배치한 **종이 복제 폼**(1,373개)이고, 저장·조회·인쇄·결재·편의기능은 전부 부모 `TfrmParentChartMulti`(301KB) 한 클래스가 맡는다. ⇒ WinCheck+ 의 데이터 기반 엔진(`TBL_QPS_CHK_*`)이 **설계로는 더 앞선다.** 소스로 보강할 것은 「모양」이 아니라 **「동작」**이다 |
| **보류 6종 판정** | 소스로 **3건의 전제가 바뀌었다** — XR23≠XR08(갈음 불가) · w42 는 SUNWOO 도 기기 마스터 없음 · RN25 약품 목록은 `t_list`(병원별 등록). w15 는 「셀 고정문」에 **행 배타 체크** 동작이 하나 더 있다. §2 |
| **이식한 것** | 부모 편의기능 중 **격자에 바로 얹을 수 있는 5가지**를 `qpsChk.jsp` 에 넣었다 — 더블클릭 O/X 토글(칸·세로줄·가로줄) · 전체 O/지움 · Enter/Ctrl+Enter 복사 · 일괄 서명 · 이름 빈 행 잠금. **JSP만 → WAR 재빌드 불필요.** §4 |
| **일부러 안 옮긴 것** | 온습도 **랜덤 채움**(측정값을 지어낸다) · 근무표 서명(B4 결정 사안) · 공휴일 표(DDL) · 행 배타체크(DDL+서식 UI). §5 |

## 1. SUNWOO 는 어떻게 저장하나 — 소스로 확정한 구조

| 무엇 | 소스 근거 | 뜻 |
|---|---|---|
| **문서** `t_patient_chart_m` | `f_save` → `SaveChartMastM(company, patient_id, chart_id, class_nm, ver, w_date, …)` | 키 = 병원 + 서식(`chart_id`) + **`w_date`(YYYYMMDD + 2자리 순번)**. `GetMaxWDate` 가 같은 날 문서에 `+1` 순번을 준다 ⇒ **하루에 문서 여럿**(개인별 서식이 이걸로 사람을 가른다) |
| **값** `t_patient_chart_d` | `SaveChartCompoM(컴포넌트, …, page)` / `p_ChartViewer_Parent_001` 이 `obj_nm, text, memo, date, checked, img, caption` 반환 | **컨트롤 이름 → 값** K/V. `pnl_chart_N` 안의 `Tag ≥ 1` 인 보이는 컨트롤만. 저장 = **전부 지우고 다시 넣기**(`f_delete` → insert) |
| **Tag 규약** | `Tag 1` 입력 · `2` 서명(`Hint='SIGN'`) · `9` 체크 · `32` 날짜 · `88` 콤보 · `≥100` 편의기능 제외 | WinCheck+ 의 `INPUT_GB`·행/열 예약 번호(890·900·1000·2000·9000)에 해당 |
| **결재라인** | `t_co_chart.line_cd` → `t_template(seq, title_caption)`; 폼의 `img_A~D / lbl_A1~D1 / shp_A1..D2 / edt_signD_A(날짜) / edt_signT_A(시간)` | 4칸 고정, 템플릿이 3·2·1칸이면 **왼쪽(A)부터 감춘다**. `E1` = 결재 없음. WinCheck+ `TBL_QPS_APPR_LINE(HOSP_CD, STEP_NO)` 와 같은 발상(병원별 단계) — 서식별 템플릿 지정(`t_co_chart`)만 우리에 없다 |
| **병원별 목록** `t_list` | `f_checkList(폼, 병원, class_nm, '목록')` 가 열 때 `obj_nm→note` 를 칸에 채움 · 우클릭 「목록 등록」= `f_add_list` | 약품명·물품명 같은 **병원마다 다른 항목**을 서식이 아니라 병원이 등록. **75종**이 쓴다. WinCheck+ = `LIST` 축 + `CARRY_YN`(전월복사로 딸려 옴) |
| **전월 복사** | `f_get_patientchart` → `p_ChartViewer_Parent_002` — 값 전부 복사, **서명(`Hint='SIGN'`) 만 제외** | ⚠**SUNWOO 는 점검 결과(O)까지 가져온다.** WinCheck+ `ckPrevSeed` 는 일부러 **틀만**(기기명·열·상단·자산열) 가져온다 — 「아무도 안 한 점검이 기록이 되는」 것을 막으려는 결정(2026-08-12). **유지한다** |
| **DB 두 벌** | 모든 SQL 이 `{$IFNDEF SQL_SERVER_DB}`(MySQL) / `{$ELSE}`(SQL Server) 두 벌 | 운영은 SQL Server(`SUNWOO.dbo.…`), 프로시저 `p_ChartViewer_Parent_001/002` |
| **공휴일** `t_holiday(date, name)` | `f_GetHolidayName` — 일괄 서명·전체 체크·온습도 채움에서 **휴일 칸 건너뛰기** | WinCheck+ 에 표 없음(관리자모듈 대조 08-17 ④). 지금은 **토·일만**(`dowCls`) |

## 2. 보류 6종 — 소스로 다시 본 것

| # | 서식 | 델파이 유닛 | 소스에서 확인한 것 | 판정이 바뀌나 |
|---|---|---|---|---|
| ① | **w15** 인사고과평가표 | `Employee_Chart_011` | 체크박스 **50개**(10행×5열) + `cxCheckBox1Click` : **같은 `Hint` 묶음 안에서 하나만 체크**(행 단위 라디오). 열 머리 `Label15DblClick` = 그 열 전체 토글. 고정 문구는 `TLabel` | 「셀 고정문」 조각에 **행 배타 체크 동작**이 하나 더 필요하다. 배타 체크는 SUNWOO 전체 **173종**이 쓰는 패턴 — 조각을 만든다면 w15 하나가 아니라 이 173종의 값어치다(§5 ④) |
| ② | **RN08~10** 검사 성적서 | `KIDNEY_Chart_008~010` (미생물 검사결과 보고서 · 내독소 참고치 · 미세물질 검사결과) | 이름만 확인(개별 화면 판정은 `HEAD_MAX` 라는 **우리 쪽** 제약이라 소스가 답을 바꾸지 않는다) | **그대로** — 개별 화면 후보 |
| ③ | **RN25** 병동비치약품점검표 | `WARD_Chart_069`(+변형 `_1·_B~_H` 8개) · `KIDNEY_Chart_025` | 탭 4장(`pnl_chart_1~4`) · 입력칸 343+ · **날짜별 서명 이미지 31개** · `btn_getBeforeM` 전월복사 · `chk_duty` 근무표 서명 · `f_checkList('목록')` = **약품 목록을 `t_list` 에서 불러옴** · `cxTextEdit1PropertiesChange` = **약품명 빈 행은 그 줄 비활성** | 「위 표는 병원 약품 목록」임이 확정. `RN25_SPLIT` 의 `RNL028` LIST + `CARRY_YN` 설계가 **원본 동작과 정확히 맞는다.** 물음(#43 두 화면 수용)은 그대로 |
| ④ | **w42** 의료기기 자체정기점검 | `Employee_Chart_036` | 탭 5장. **p2 기기 목록 = `TcxTextEdit` 438개 자유 입력**, 고정 글자는 머리(사용부서·의료기기 명칭·점검주기·수량·점검구분)뿐. 전월복사 단추 **없음**, `t_list` **안 씀** | ★**SUNWOO 도 기기 마스터가 없다** — 병원이 문서마다 40줄을 친다. ⇒ SUNWOO 동등성은 `LIST`(행=기기·열 5)+`CARRY_YN` 으로 **마스터 없이** 맞춰진다. #45 는 「이식 전제」가 아니라 「SUNWOO 보다 낫게 할지」로 뜻이 바뀐다 |
| ⑤ | **XR23** 개인 피폭선량 측정부 | `Ray_Chart_023` | 머리 4칸(**성명·성별·생년월일·입사년월일**) + 4분기 × (피폭량 심부/표층 · 연간선량 심부/표층) = 입력 20. 키 = 연(`cmb_year`→`w_date=YYYY0101+순번`) ⇒ **연 1장 × 사람 수** | ★**XR08 로 갈음 불가.** `Ray_Chart_008`(TLD 관리대장)은 **성명 칸이 없고** 배지 도착/회송/착용일 + 연간·5년 누계 + 담당자 서명 + 결재 4칸의 **배지 물류·집계 대장**이다. 겹치는 것은 「분기 심부/표층」뿐. ⇒ #44 물음이 **「개별 화면으로 만들지」** 로 바뀐다(RN08~10 묶음). 개인별 문서 = SUNWOO 도 `w_date` 순번으로 푼다 — 우리 키 설계(병원+서식+기간)에 **사람 축** 하나면 된다 |
| ⑥ | **MR13** 퇴원환자 의무기록 현황 | `HEALTH_Chart_014`(+A~D 요양·정신·재활·4주기·한방) · `_015` 완결도 조사 | `t_discharge`(퇴원환자) × `t_form_complete`(완결도) 조인, 저장 함수 `f_get_complete_1`, **10명/쪽** 페이징, 1차/2차, 항목 **41개**(입원초기 9·경과 5·간호초기 12·간호기록 5·퇴원요약 10). 퇴원환자는 `TfrmHLP_DisCharge` 팝업으로 가져온다 | **기능 화면 확정**(B3 그대로). 다만 구조가 「환자 × 항목 41 체크」라 **개별 화면을 만든다면 LIST 축 렌더를 재사용**할 수 있다 |

## 3. 부모 편의기능 12종 — 사용 빈도와 WinCheck+ 대응

`ParentChartMulti.InitFunctionValues` 가 정의하고 각 폼이 배열로 켠다. **CHT/*.pas 1,373개 전수** 검색 결과 :

| 편의기능 (SUNWOO) | 쓰는 서식 | WinCheck+ | 비고 |
|---|---|---|---|
| **전월 복사** `btn_getBeforeM` | **416** | ✅ 있음(`ckPrevSeed`) | 단 **틀만** 복사(§1). 의도된 차이 |
| **결재라인** `img_A~D` | **642** | ✅ 있음(`APPR_LINE`) | 서식별 템플릿 지정만 없음 |
| **전체 마킹** `chk_all` | **300** | ⬜ → ✅ **이식** | 「전체 O · 전체 지움」 |
| **Hint 묶음 배타 체크**(행 라디오) | **173** | ⬜ | DDL(서식 옵션) 필요 — §5 ④ |
| **병원별 목록** `t_list`/`f_checkList` | **75** | ✅ 대응(LIST+CARRY_YN) | |
| **O/X 더블클릭 토글** `switchFlag` (칸·세로줄·가로줄) | **61** | ⬜ → ✅ **이식** | 빈→O→X→빈 |
| **Enter 가로복사 · Ctrl+Enter 세로복사** `copyLine` | **61** | ⬜ → ✅ **이식** | |
| **일괄 서명** `signLine` (+휴일 제외 19) | **61** | ⬜ → ✅ **이식** | 사인 행 머리 더블클릭 |
| **온습도 랜덤 채움** `tempHumi` | 55 | ⛔ **안 옮김** | §5 ① |
| **근무표 서명** `chk_duty`/`f_call_dutysign` | 53 | ⬜ | B4(DUTY) 결정 사안 |
| **이름 빈 행 비활성** `blankDisableLine` | 47 | ⬜ → ✅ **이식** | 값은 안 지움 |
| **요일별 날짜창** `weekList` / **주간 자동체크** `weekCheck` | 22 / 13 | ⬜ | 주차 서식의 날짜 자동 채움 — `PRDH_NO`(890) 행에 붙일 수 있다. 다음 후보 |
| **토/일 제외** · **공휴일 제외** | 8 / 8 | ✅ 토·일만 이식 | 공휴일 표 없음 |

## 4. 이번에 이식한 것 — `qpsChk.jsp` 「편의 기능」 절 (2026-09-02)

**규칙은 SUNWOO 그대로**, 단 이 화면 원칙 둘을 지켰다 :
①**이미 적힌 값은 덮지 않는다**(전체 O·일괄 서명은 빈 칸만. SUNWOO 는 덮어쓴다 — 전월복사와 같은 원칙)
②**대상은 격자 값칸뿐**(`input[data-r]` 중 `.ltxt` 아닌 것. 사인 900·기간머리 890·자유행 9000+·이름칸·글자칸 제외 —
  글자 칸에 Enter 복사를 허용하면 대장의 이름이 사유 칸으로 번진다. SUNWOO 는 칸 **너비**로 걸렀고 우리는 `.ltxt` 로 가른다)

| 손짓 | 동작 | SUNWOO 원본 |
|---|---|---|
| 값칸 **더블클릭** | 빈→O→X→빈 | `f_switch_flag(2)` |
| 날짜/열 머리 더블클릭 | 그 **세로줄** 전부 토글(사인·890 제외) | `OnSwitchFlagLineV` |
| 항목/날짜 행 머리 더블클릭 | 그 **가로줄** 값칸 토글(옆 글자칸 제외) | `OnSwitchFlagLineH` |
| 사인 행 머리 더블클릭 | 빈 사인칸에 **로그인 사용자명**(`s_usernm` 쿠키) · 토·일 제외 옵션 | `OnSignLineV` + `f_signlist_V_1p(휴일 제외)` |
| 사인 칸 더블클릭 | 그 칸만(빈 칸일 때) | `f_get_sinimgEn` |
| **Enter** | 같은 행 **오른쪽** 값칸에 복사 | `OnCopyLine_EditKeyDown` |
| **Ctrl+Enter** | 같은 열 **아래** 값칸에 복사(사인 행 제외) | 〃 |
| [☑ 전체 O] | 빈 값칸 전부 O(토·일 제외 옵션) | `chk_all` |
| [☐ 전체 지움] | 값칸 전부 비움 — **묻고** 한다 | 〃 |
| 이름칸(`data-rn`) 빈 행 | 그 행을 **흐리게만** 표시(입력·토글은 됨). 이름 넣으면 풀림 — 처음엔 readOnly 잠금이었으나 새 서식이 통째로 회색이라 **사용자 요청으로 잠금을 뺐다**(09-02) | `blankDisableLine`(SUNWOO 는 비활성+값 지움) |
| 「토·일 제외」 | localStorage 에 기억 | `isCheckExcludeSaturdays`·`signLine_isExcludeHolidays` |

- 구현 = 리스너를 `#ckGridWrap` 에 **한 번만 위임**(표가 다시 그려져도 살아남는다). 열 머리에 `data-col` 을 달아 세로줄 토글의 기준으로 썼다(ITEM_COL·LIST·DAY_ITEM). 날짜 격자는 기존 `data-day` 를 쓴다.
- **검증** = 배포 JSP 에서 그 블록을 그대로 꺼내 **jsdom 가짜 DOM 12경우**(scratch `sim_qpsChk_tools.js`) · JSP 함수검사(정의 94·호출 32 전부 정의) · 인라인 JS 문법 · 구조검사(div 43/43·script 2/2·JSP주석 25/25, 내 블록에 `--%>` 0).
  ⚠`docs/tools/JSP구조검사.py` 는 이 PC 에서 못 돈다 — `python` 이 **Windows 스토어 스텁**이라 「Python」만 찍고 끝난다. 같은 검사를 Node 한 줄로 대신했다(위 수치).
- ⛔**남은 것 = 브라우저로 눈으로 보기**(로컬 8080 · Eclipse **F5** 후). 특히 더블클릭이 글자 선택과 안 부딪히는지, `td.hd` 손 모양.

## 5. 일부러 안 옮긴 것 — 이유

| # | 기능 | 왜 |
|---|---|---|
| ① | **온습도 랜덤 채움**(`tempHumi`, 55종) | 더블클릭/Enter 로 세로줄 전체에 **Min~Max 사이 난수**를 넣는다 — **측정값을 지어내는 기능**이다. 점검 기록의 뜻을 해치고 인증 자료로서 문제가 된다. SUNWOO 가 그렇게 쓰였다는 사실은 기록해 두되 **WinCheck+ 에는 넣지 않는다.** 「한 줄에 같은 값 채우기」는 Enter/Ctrl+Enter 복사가 대신한다 |
| ② | **근무표 서명**(`chk_duty`, 53종) | 근무표(DUTY)의 그 달 근무자를 날짜별 서명칸에 자동 배치 — **DUTY 화면이 먼저**(남은할일 B4·관리자모듈 대조 ④). 근무표가 생기면 사인 행 채우기에 붙인다 |
| ③ | **공휴일 제외**(`t_holiday`, 8~19종) | 공휴일 표가 없다(관리자모듈 대조 ④ 「소소한 개선 후보」). 표가 생기면 `ckCellOff` **한 곳**만 넓히면 된다. 지금은 토·일만 |
| ④ | **행 배타 체크**(Hint 묶음, 173종) | ITEM_COL 의 CHECK 열에서 「한 행에 하나만 O」. **서식 옵션(DDL 1칸 `EXCL_YN`)+서식 관리 입력칸+매퍼** = WAR 재빌드. 173종이면 값어치는 충분하다 — **다음 배포 묶음 후보 1순위**. w15 셀 고정문(`CELL_TXTS`)과 같은 배포에 실으면 w15 가 그날 들어간다 |
| ⑤ | **주간 자동체크·요일별 날짜창**(13·22종) | 주차(N) 격자의 `PRDH_NO`(890) 행에 날짜 범위를 자동으로 넣는 것 — 다음 후보 2순위 |
| ⑥ | 전월복사가 **값까지** 가져오는 것 | §1 — 우리 결정(틀만)이 옳다. 바꾸지 않는다 |

## 6. 델파이 소스 찾아 읽는 법 (다음에 또 볼 때)

- **색인** = `D:\sunwoo\sunwoo\sunwoo2026.txt` — `insert into t_unit`(1,310줄) 이 **유닛코드 ↔ 화면 이름**, `insert into t_menu` 가 **화면 이름 ↔ `TfrmXXX_Chart_NNN` 클래스**. 서식 이름으로 grep 하면 유닛이 나온다. 뒤쪽(3005행~)에 **전자결재 설계·WEB 전환 목록**.
- 폼 = `SmartChart\CHT\<모듈>\<유닛>.pas`(동작) + `.dfm`(배치·고정 글자). 모듈 = QPS 230 · WARD 244 · FAC 95 · INF 92 · Pharm 88 · Meal 84 · Employee 74 · QI 52 · WEL 50 · PAT 48 · KIDNEY 45 · RPT 45 · HEALTH 43 · Service 43 · HCare 35 · CTL 29 · Ray 27 · REHAB 22 · Nurse 12.
- ⚠**DFM 의 한글은 `#51032#47308` 유니코드 이스케이프** — 그대로 grep 하면 안 잡힌다. PowerShell `[regex]::Replace($s,"#(\d+)",{[char][int]$m.Groups[1].Value})` 로 디코드해서 본다(캡션 뽑기 = `Caption =`/`Hint =`/`Tag =`/`object` 줄).
- 공용 = `SmartChart\COM\ParentChartMulti.pas`(부모 — 저장 `f_save` 5024 · 조회 `f_load` 5325 · 결재 `f_decision_line` 6725 · 편의기능 `InitFunctionValues` 756 / `InitControlFunctions` 1001) · `GlobalFunc.pas`(전월복사 `f_get_patientchart` 2024) · `COM\ALLQFunc.pas`(토글 `f_switch_flag` 1437 · 일괄서명 `f_signlist_V_1p` 1138 · 휴일 `f_GetHolidayName` 775 · 목록 `f_checkList` 1910).
- ⚠PS7 에서 `Select-String … | % { $matches[..] }` 는 **빈 배열** — `$_.Matches[0].Groups[..].Value` 로 꺼낼 것(한 번 통째로 헛돌았다).
- `SUNWOO_프로젝트_분석_보고서.pdf` 는 이 PC 에서 못 읽는다(pdftoppm·pypdf 없음) — 필요하면 사용자에게 열어 달라고 할 것.
