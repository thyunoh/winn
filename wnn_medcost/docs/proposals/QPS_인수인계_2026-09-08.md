# QPS 인수인계 — 2026-09-08 (일괄 3묶음 · 결재란 · 결재 권한 · 근무표를 끝낸 날)

> 앞 문서 : [09-02 델파이 대조](QPS_인수인계_2026-09-02.md) · [08-14 서식 이식](QPS_인수인계_2026-08-14.md)
> 배경은 `.claude/CLAUDE.md` 의 「★[2026-09-07] QPS 점검표 일괄 …」 절과 「▶ 이어서」 5·6번이면 충분하다.
> **읽는 차례** : §1(하루 요약) → 낮 것은 §2~6 · 저녁·밤 것은 §7~12. 배포는 §3 + §8.

---

## 1. 지금 상태 한 장 (하루 전체)

**낮 — 일괄 3묶음**
- **「일괄 작성 · 일괄 출력 · 일괄 사인」 세 묶음이 모두 끝났다.** 화면 안 [🖨 일괄 출력] = 21개 화면, 옛 「일괄 출력」 화면은 [🖨 서식 묶음 인쇄]로 역할을 갈랐다.
- 곁가지로 저장 버그 1건(자바 `jsonRows`) · 경주 결함 2종(조회 순번 가드 18곳 · 못 연 문서)을 잡았다. 부서 양식 복사·배정 화면도 「길이 둘」을 하나로 모았다.

**저녁·밤 — 결재란 · 결재 권한 · 근무표**
- **결재란 도장·서명**(⑧) — 사람마다 도장 한 장(`TBL_QPS_SIGN`) + 문서×단계 기록(`TBL_QPS_CHK_APPR`). 본인 것만, 서버가 막는다.
- **서식마다 결재란 켜고 끄기**(⑨) · **결재 권한 부서×단계×사람**(⑩) → **한 표로 개편**(⑪, 서식 × 단계 · 16줄 스크롤 · 기본 줄 고정 · [결재 단계 설정] 이관).
- **근무표 신설**(⑫) — 사람 × 1~31일 · 붓·패턴·전월 가져오기 · 마감 · 인쇄. **점검표 일괄 사인이 「그날 근무자」로** 채운다(O·V·R 제외 · 적은 차례).
- **격자 사인 칸 작은 도장**(⑬, 이름으로 잇는다 · 없으면 이름 그대로) · **서식 아래 결재란**(⑭, 예약 대역 901~910 · **날짜 없음**).

**상태**
- DB — DDL 4개(SIGN_APPR · FORM_APPR · APPR_AUTH · DUTY) **전부 실행 완료**. 새 SQL 은 DB 에 미리 태워 봤다.
- 로컬 — 재기동 네 번, 매번 실왕복 통과(`build=20260908-DUTY`). 시험 자료 잔여 0(도장·기록·문서·근무표·권한).
- 상시 검사 — QPS `Swal.fire` 0 · 매퍼 XML 정형성 310문·id 중복 0 · javac 0 · 시뮬 **378 통과**(오늘 새 시뮬 4벌).
- ⛔**운영 미배포 · 미커밋 21건**(신규 9 · 수정 12). 자바·매퍼가 섞여 **WAR 재빌드 + 재기동**(§3·§8). 커밋·배포는 사용자 몫.

---

## 2. 오늘 바뀐 것 — 묶음별

### ① 화면 안 [🖨 일괄 출력] — 20화면 확장 (JSP만)

09-07 점검표에 붙인 뼈대를 그대로 넓혔다 : **툴바 단추 + 조건 띠 + 「열고 → 그 화면의 낱장 인쇄 함수 호출」 을 `window.QPS_BULK_CB` 로 모아
끝에 `qpsPrintMerge(parts,title)`(sidebar.jsp)로 한 문서**. 사이드바 인쇄 관문(`qpsPrintOut`)이 QPS_BULK_CB 를 먼저 보므로 **JSP 만 고치면 된다.**

★**조건 띠는 화면마다 그 업무의 말로** 지었다 — 이것이 이 작업의 핵심이다(사용자 지시 「등록화면마다 업무에 맞게」).

| 호 | 화면 | 문서 단위 | 조건 띠 |
|:--:|---|---|---|
| 1 | qpsSafeRpt | 건 단위(주기 없음) | 이 유형 / 이 계열(SORT 대역) / 전체 × **발생일** 월 범위 |
| 2 | qpsMinutes | 위원회×연도 목록 | 이 위원회 / 전체 13 × **회의일** 월 범위 × 정기·임시 |
| 3 | qpsRound · qpsSecLog · qpsCathDay | 월 1부 | 연도 + 월 범위 (라운딩은 구분 추가) |
| 4 | qpsPlan · qpsSrvPlan · qpsCmplPlan | 연 1부 | 연도 범위 (활동계획서는 구분 추가) |
| 5 | qpsQiPlan · qpsQiRpt · qpsQiTopic · qpsQiFund | 각각 다름 | 연도 범위 / 종류(중간·최종)×연도 / 연도+☑집계표 ☑기준표 / 연도 범위 |
| 6 | qpsFmea · qpsRca | 구분×연도 / 건 단위 | 계획서·보고서 × 연도 / 연도 + **발생일** 월 범위 |
| 7 | qpsCmpl · qpsCmplRpt | 대장+건별 / 반기 | 연도+접수일 범위+☑대장 ☑처리결과 / 반기 × 연도 범위 |
| 8 | qpsSrvImpr · qpsDef | 연도 목록 / 지표별(연도 없음) | 연도 범위 / 이 영역·전체 + ☑우리 병원이 채운 것만 |
| 9 | qpsSurvey | 연도×조사, 조사당 보고서 2종 | 연도 범위 + ☑조사결과 ☑지표분석 |
| 10 | qpsFall | 지표 × 연도 × 회차 | 연도 범위 + 회차(지금/분기 4/분기·반기 6) + ☑서술 작성된 회차만 |

**화면마다 손봐야 했던 것**(새 화면에 붙일 때 먼저 볼 5가지) :
①열기·조회 함수가 **프라미스를 돌려주는가** ②목록이 변수에 남는가(`LIST`) ③**늦게 온 옛 응답**을 막는 순번 가드 ④사진/blob(revoke 보류·대기) ⑤실패를 catch 가 삼켜 **앞 문서가 찍히지 않는가**.

특이했던 화면 :
- **qpsQiRpt** — 결과분석 수치를 저장하지 않고 지표에서 **비동기로 다시 센다** ⇒ `qrPickIndi` 가 프라미스를 돌려주게 하고 **수치가 온 뒤** 찍는다.
- **qpsQiTopic** — `qtPrint` 가 **보이는 탭**으로 집계표(가로)/기준표(세로)를 가른다 ⇒ 탭을 돌려 가며 찍고 끝에 원래 탭으로.
- **qpsSurvey** — 이 화면만 `post` 가 **콜백**이고 인쇄물이 **화면 칸**을 읽는다 ⇒ `post(…,fail)` · 인쇄 함수 `done` · `applySurveyDoc` 공용화로 **프라미스 다리**를 놓았다.
- **qpsFall** — 목록이 없고 해마다 수치를 **통째로 다시 계산**한다. 낱장 인쇄의 **병원 확인창**은 뜻이 옳아 없애지 않고 `qfPrint({noAsk})` 로 **맨 처음 한 번만** 묻는다.

### ② 저장 버그 — 자바 (⛔재빌드 필요)

`roundSave` · `planSave` · `infRptSave` · `infRiskSave` 4곳이 `items/members` JSON 을 **원문 파싱**해
HTMLTagFilter 가 바꾼 `&quot;` 에서 `Unexpected character ('&')` 로 **저장이 통째로 실패**하고 있었다(화면의 rdSave 로 실측).
⇒ `jsonRows()` 로 통일. `BUILD = "20260908-JSONROWS"`.
★규칙 : **@RequestParam 으로 JSON 문자열을 받는 자리는 반드시 `jsonRows`/`unesc`** — 원문 `ObjectMapper.readValue` 금지(`grep readValue(` 로 잡는다).

### ③ 조회 순번 가드 — 18곳 (JSP만)

일괄 출력 실왕복에서 **3장이어야 할 것이 4장**으로 나왔다. 원인 = 문서를 연 뒤 뒤따라 나가는 목록 재조회가 아직 응답 전인데 일괄이 다른 해 목록을 다시 청했고,
**jQuery 3 의 `.then` 이 setTimeout 으로 미뤄지는 사이 늦게 온 옛 응답이 `LIST` 를 덮은 것**. 일괄만의 문제가 아니라 「문서를 열자마자 해를 바꾸면 옛 목록이 덮는」 기존 결함.
⇒ 조회 함수 18개(srLoad·qmList·rdLoad·slLoad·cdLoad·plLoad·spLoad·cpLoad·qpList·qrList·qtLoad·qfLoad·fmLoad·rcLoad·cmLoad·crLoad·siList·qdLoad)에
`var my = ++LOAD_REQ;` … `.then(function(res){ if (my !== LOAD_REQ) return; …` — **더 새 조회가 나갔으면 옛 응답을 버린다.**

### ④ [🖨 서식 묶음 인쇄] — 옛 「일괄 출력」 화면 개편 (JSP 2 · **미커밋**)

등록 화면 21곳에 [🖨 일괄 출력] 이 생기며 **같은 이름이 두 군데**가 됐다. 하는 일이 다르다 :

| | 하는 일 |
|---|---|
| 등록 화면 [🖨 일괄 출력] | 한 서식의 **여러 건**(업무 조건으로 걸러) |
| 메뉴 [🖨 서식 묶음 인쇄] | **여러 서식**을 한 벌로(실사·인증 준비). 서식 하나에 한 장씩(회의록만 기간 안 여러 건) |

- 메뉴·제목 이름 변경 + 첫 화면에서 두 길을 가르는 안내 띠 + 서식 줄마다 **[화면에서 →]**(그 등록 화면을 새 창으로, 고른 해를 `?yy=` 로 넘김).
- 이어서 **주기·찾기를 「작성 현황」 업무로 갈랐다** — 화면 안 **업무 탭 2개**(`🖨 묶음 인쇄` / `📋 작성 현황`).
  ⚠**화면 파일은 쪼개지 않았다** — 찾기는 인쇄와 **같은 기계**(`grab`·`frameSrc`·`message`)를 쓴다. 떼면 서식 표 127종과 그 기계가 두 벌이 되어 반드시 어긋난다.
  ⇒ 기계는 한 벌, **보이는 것만 CSS 로 가른다**(`#qpsPrintAll.m-stat` / `.m-print` · `paMode()`). JSP 가 `${param.mode}` 를 읽으므로 **자바 변경 없음.**
  ⚠★**메뉴는 한 줄이다** — 처음엔 「📋 서식 작성 현황」을 **메뉴로 따로** 냈는데 이름도 화면도 닮아 **중복으로 읽혔다**(사용자 「2개 메뉴가 살짝 중복인 것 같은데」).
    ⇒ 메뉴는 [🖨 서식 묶음 인쇄] 하나로 되돌리고 **탭으로 옮겼다.** `?mode=stat` 으로 바로 여는 길은 살려 두었다(옛 링크 호환).
    ★교훈 : **업무가 다르다고 메뉴를 나누면, 이름이 닮은 순간 되레 헷갈린다** — 화면 안에서 가르는 편이 나을 때가 있다.

### ⑤ [✍ 일괄 사인] — 세 묶음의 마지막 (JSP 1 · **미커밋**)

**뜻(사용자 확정)** = 「점검표 여러 문서의 **빈 사인 칸**을 내 이름으로 한 번에」.
⛔결재(승인)가 아니다 — 결재선이 실제로 도는 것은 **지표분석보고서뿐**이고(`TBL_QPS_APPR`+`_LINE`, 키=지표×회차), 나머지 서식은 인쇄물에 **빈 결재란만** 나간다.
이건 종이 서식의 **점검자 사인 칸**(예약 행 `SIGN_NO=900`)이다.

- 조건 = 이 서식만 / 이 부서 서식 전부 · 연도+월 범위(일괄 출력과 **같은 규약**, 「이 서식만」은 병동 필터를 따름) · 이름은 `s_usernm`.
- ★★**일괄 출력과 다르다 — 자료를 고쳐 저장한다.** ①실행 전 **반드시 묻는다** ②**빈 칸만**(이미 적힌 사인 안 덮음) ③화면의 **「토·일·공휴일 제외」** 를 따름
  ④**채울 칸이 없는 문서는 저장조차 안 한다**(수정일시를 안 건드린다) ⑤상한 200건 ⑥조건 띠는 **초록**(파란 일괄 출력과 눈으로 구분).
- `ckSave(opts)` 에 **`{quiet:true}`** 를 열었다(묻지·알리지·재조회 안 하고 저장만 + 프라미스). **저장 payload 는 여전히 한 곳에서만** 만든다.

### ⑥ 같이 잡은 결함 — 못 연 문서

`ckPickDoc` 은 실패를 `catch(err)` 가 삼켜 **앞 문서 번호(curSeq)가 그대로 남는다.**
**일괄 출력이 그 상태로 그냥 찍어 앞 문서가 한 번 더 실릴 수 있었다**(다른 QPS 화면엔 이미 있던 가드가 여기만 빠져 있었다).
⇒ 일괄 출력·일괄 사인 두 곳 모두 **열기 직전 `curSeq = 0`** 으로 내리고 열린 뒤 번호를 대조해 어긋나면 건너뛴다.

### ⑦ 부서 양식 복사·배정(qpsDeptForm) — 시연에서 나온 결함 2건 (JSP 1 · **미커밋**)

화면을 「어디 것을 어디로 복사해서 쓴다」 중심으로 다시 구성한 뒤, 사용자가 **[복사]와 [부서·분류 저장]의 차이**를 묻고 실제 동작을 시켜서 드러났다.

1. **부서를 옮기면 분류가 조용히 비워졌다.** 공통 서식 `LAB001`(진단검사·기타)의 부서만 물리재활로 바꿔 저장했더니 분류가 `기타` → **빈 값**이 됐다.
   원인은 부서 change 처리의 「새 부서 규칙에 없는 분류는 비운다」 한 줄인데, 바로 옆 분류 셀렉트는 **「지금 값은 규칙 밖이어도 남긴다」**로 만들어 두어 원칙이 서로 어긋나 있었다.
   ⇒ **비우지 않는다.** 규칙 밖이면 셀렉트에 「(규칙 밖)」으로 남고, 줄 밑에 빨간 글로 알린다. **자동 정리는 지우는 쪽이 아니라 보여 주는 쪽으로.**
2. **「고친 것 없음」일 때 저장 단추를 막아 두어 「눌러도 아무 동작하지 않는다」**는 지적을 받았다. 막힌 단추는 눌러도 아무 일이 안 일어나 **고장으로 읽힌다.**
   ⇒ 흐리게만 두고 **눌리게** 한다. 누르면 왜 할 일이 없는지 알림창이 뜬다 — 체크해 둔 양식이 있으면 **「복사하려면 위쪽 [복사]를 누르세요」**로 안내한다.

3. **저장 단추가 복사 흐름의 한 단계처럼 읽혔다.** 이 화면의 일은 「여러 부서에서 고르기 → 대상 부서로 복사」인데, 저장 단추가 머리줄에 있어 복사 띠 바로 옆에 붙어 있었다.
   ⇒ 저장 단추를 **표 위로 내리고** 색을 달리해 두 업무를 갈랐다. 이름도 **「표에서 바꾼 것 저장」**으로 바꾸고 바꾼 건수를 단추에 붙였다. 종전 이름은 무엇을 저장하는지 읽히지 않았다.
4. **고른 것만 모아 볼 길이 없었다.** 고른 양식은 부서를 옮겨 다녀도 남지만(코드로 기억한다) 다른 부서 것은 표에서 사라져 무엇을 담았는지 확인할 수 없었다.
   ⇒ 부서 칩마다 **고른 수 배지**를 띄우고, 복사 띠에 **[고른 것만 보기]** 를 넣었다. 켜면 부서와 상관없이 담은 것만 한 표에 모인다. 복사 전에 이걸로 확인하면 된다.

시연으로 바꾼 `LAB001` 은 원래 값(진단검사·기타)으로 **되돌려 놓았다**. 작성 문서가 0건인 서식을 골라 시연했다.

---

## 3. 배포 — 무엇을 해야 하나

| 대상 | 조치 |
|---|---|
| 자바 1(`QpsController`) + JSP 21 | ⛔**WAR 재빌드 + 재기동** — 절차는 [배포_가이드](../배포_가이드.md) §2·§4 |
| JSP 3(qpsChk·qpsPrintAll·sidebar, §2 ④⑤) | 같은 WAR 에 실어 나간다 |
| 매퍼 XML · 설정 | 변경 없음 |
| DB | **변경 없음** (시험 자료는 전부 삭제, 잔여 0) |

- 재기동은 `systemctl` 금지, `/usr/share/tomcat9/bin/shutdown.sh` → `startup.sh`. 대상은 `webapps/wnn_medcost-1.0.0`(ROOT 아님).
- **반영 확인** = `codeList.do` 응답 `build` 가 **`20260908-JSONROWS`** 인지. 화면 쪽은 아무 QPS 등록 화면 툴바에 [🖨 일괄 출력] 이 보이면 된다.
- ⚠**미커밋 6파일**(qpsChk·qpsPrintAll·sidebar·CLAUDE.md·시뮬 2) — 배포 전에 커밋하는 편이 되돌리기에 안전하다(커밋은 사용자가 한다).
- ⚠**Eclipse 톰캣이 떠 있으면** 명령줄 maven 과 겹쳐 로컬 배포 폴더의 클래스가 빌 수 있다 — 빌드 전 서버를 멈추거나 뒤에 Servers ▸ Clean.
- ⚠배포 문서 **§3(서버 업로드 방법)은 아직 비어 있다** — 이 PC 에서는 서버 SSH 가 열쇠·비밀번호 모두 거부된다. 쓰시는 방법을 알려 주면 채워 넣는다.

---

## 4. 검증

- **jsdom 시뮬 315 통과**(`docs/tools/sim` → `node run_all.js`). 오늘 **신설 10벌**(safeRpt·minutes·monthlog·yearplan·qi·fmea_rca·cmpl·srvimpr_def·survey·fall) + **일괄 사인 1벌**, `sim_qpsChk_ward.js` 앵커 1줄 수정.
- **로컬 8080 실왕복** — 21화면 전부 가짜병원(99999998) 자료로 「만들기 → 일괄 → 결과 확인 → 복귀 → 삭제」. 내장 브라우저에서 `window.open` 을 가로채 합쳐진 HTML 을 직접 읽어 장수·차례·내용·종이 방향을 봤다.
- **시험 자료는 전부 삭제**(잔여 0) — safeRpt·minutes·round·plan·QI 4·fmea·rca·cmpl·srvimpr·indi_def·survey·report·점검표 문서.
- JSP 를 고칠 때마다 **인라인 JS 문법(node --check) · 태그 짝 · 함수 정의 검사**를 돌렸다.

---

## 5. 다음 사람이 알아야 할 함정 (오늘 새로 얻은 것)

1. ★**응답이 전역을 덮는 조회 함수엔 순번 가드를 둔다** — `.catch(err)` 로 실패를 삼키는 구조라 순서 뒤집힘이 조용히 지나간다(§2 ③).
2. ★**실패를 catch 가 삼키면 「앞 문서가 찍힌다」** — 열기·조회 뒤에는 반드시 **키를 대조**한다(seq·연도·회차). 표식은 조회 첫머리에서 **먼저 비우고** 성공했을 때만 세운다.
3. ★**git-bash `curl` 로 한글 파라미터를 보내면 깨진 채 저장된다**(CP949) — 한글 시험 자료는 **내장 브라우저 `fetch`** 로 넣는다.
4. ★**작업본은 CRLF** — 시뮬이 `\n  };` 를 앵커로 잡으면 `\r` 때문에 빗나간다. 읽자마자 `replace(/\r\n/g,'\n')`.
5. ★**소스 시그니처를 바꾸면 시뮬 앵커도 함께 본다** — `ckSave` 에 인자를 더하자 `sim_qpsChk_ward.js` 가 깨졌다.
6. **`saveQiRpt/Topic/Fund` 는 머리 INSERT 뒤 항목 INSERT 인데 트랜잭션이 없다** — 항목에 `sort` 가 없으면(NOT NULL) 항목만 실패하고 **머리 행이 고아로 남는다.** 화면은 늘 sort 를 보내 실사용 무해, API 직접 호출 때만 문제(미수정).
7. **`surveyAnsSave.do` 의 `items` 는 `qsort` 키다** — `sort` 로 보내면 응답 머리만 저장되고 문항 점수가 안 들어간다(응답은 OK 로 온다).
8. **UI 를 지우거나 옮길지 판단할 때 「그 상태가 저장되는가」를 먼저 본다**(07-23 에 두 번 겪은 것과 같은 뿌리).

---

## 6. 남은 일

- **운영 배포**(§3) — 사용자가 직접 한다.
- **배포 가이드 §3(서버 업로드)** 채우기 — 방법만 알려 주면 된다.
- (선택) **`saveQiRpt/Topic/Fund` 에 `@Transactional`** — 위 함정 6. 자바·재빌드 사안이라 급하지 않다.
- (선택) **점검표 [📋 작성 현황]** 은 점검표 전용(문서 목록·작성자·[열기])으로 그대로 두었다. 새 「📋 작성 현황」 탭(서식 127종)과 역할이 다르다 — 합칠지는 쓰면서 판단.
- 새 서식이 생기면 §2 ①의 **「먼저 볼 5가지」** 로 같은 뼈대를 붙이면 된다.

---
---

# ▶ 같은 날 저녁·밤 — 결재란 · 결재 권한 · 근무표

윗글(§1~6)은 **일괄 3묶음**을 끝낸 데까지다. 그 뒤 저녁부터 밤까지 한 것을 여기 잇는다.
***이 절만 읽으면 저녁 이후 배경은 충분하다.***

## 7. 저녁·밤에 바뀐 것 — 묶음별

### ⑧ 점검표 결재란 — 도장·서명 (자바·매퍼·DDL · ⛔재빌드)

사용자 「마우스로 사인 가능한가요 / 아님 스캔 도장 / 여러 사인 들어가는 경우」 → **「결재란부터 진행해줘」**.

★**사인이 들어가는 자리가 셋인데 성격이 다르다**(먼저 읽을 것) :
- ① **격자의 점검자 사인 행**(`SIGN_NO=900`) = 날짜 칸마다 이름 — 날마다 다른 사람이 이미 된다. 칸이 6mm 라 도장은 점이 된다 ⇒ **범위 밖**.
- ② **서식 아래 결재란**(`FORM.SIGN_LINE`) = 「점검자 ____ (인)」 빈 줄 — 손도장 전제(그대로 둠).
- ③ **상단 결재 상자**(`TBL_QPS_APPR_LINE` 단계) = 칸만 그려지고 **누가 결재했는지 저장하지 않았다** ⇒ **여기를 만들었다.**

- **표 2** : `TBL_QPS_SIGN`(병원+계정 = **사람마다 한 장**, LONGBLOB) · `TBL_QPS_CHK_APPR`(병원+문서+단계 = **문서×단계 한 줄**)
- ★★**기록에 그림을 복사하지 않는다** — 도장을 새로 등록하면 옛 문서도 새 도장으로 인쇄된다(「그때 그 도장」이 필요해지면 그때 사본 칸을 더한다).
- **엔드포인트 6** = `signGet/signSave/signDel` · `chkApprList/chkApprSave/chkApprDel`
- ★★**본인 것만** — 도장 등록·삭제도, 결재 찍기·취소도 **로그인 계정으로 강제**한다(화면이 보낸 userId 는 안 받는다). 취소는 매퍼 WHERE 의 `USER_ID` 가 막는다 — **그 한 줄을 빼면 남의 결재를 지운다.**
- ★`selectChkApprBox` 는 **정의에서 사라진 단계에 찍힌 것도 뒤에 붙인다**(`gone='Y'`) — 버리면 결재선을 줄인 순간 찍은 도장이 화면에서 사라진다.
- DDL = [QPS_DDL_SIGN_APPR_2026-09-08.sql](../sql/qps/ddl/QPS_DDL_SIGN_APPR_2026-09-08.sql) ✅사용자 실행.
  ⚠`CHK_SEQ` 를 INT 로 적었다가 `TBL_QPS_CHK_DOC.CHK_SEQ`(**BIGINT**)와 안 맞아 ALTER 를 따로 돌렸다 —
  ★**남의 표를 가리키는 칸은 그 표의 형을 그대로 베낀다**(INFORMATION_SCHEMA 로 확인하고 적을 것).

### ⑨ 서식마다 결재란 켜고 끄기 (DDL · JSP)

사용자 「사인 하다 보니 **서식마다 결재란 관리**가 필요함 — 담당자 하는 것이 있고, 결재란이 있는 것이 있고」.

- `TBL_QPS_CHK_FORM.APPR_YN`(기본 **'Y'**) · `APPR_STEPS`(그 서식만의 단계, 비면 병원 결재선) — [DDL](../sql/qps/ddl/QPS_DDL_FORM_APPR_2026-09-08.sql) ✅실행.
- ★**기본을 'Y' 로 둔 까닭** — 지금 종이에 결재 상자가 나가고 있어 기본을 뒤집으면 **그날부터 전 서식의 종이 모양이 바뀐다.**
- 화면 = 서식 관리 [결재란 씀] 체크 + 단계 칸 · 작성 화면은 `apprOn()`·`apprSteps()` 로 **화면 상자와 인쇄 결재표를 함께** 가른다.

### ⑩ 결재 권한 — 부서 × 단계 × 사람 (DDL · 자바 · 새 화면)

사용자 「결재는 **권한 관리**로 — 서식별 해당 권한 있는 내용 보여주고 결재하게」 · 단위는 **부서**(사용자 확정).

- `TBL_QPS_APPR_AUTH` — [DDL](../sql/qps/ddl/QPS_DDL_APPR_AUTH_2026-09-08.sql) ✅실행. 화면 = **QPS ▸ 관리(설정) ▸ 결재 권한**(`main/qpsApprAuth.do`, **위너넷 전용**).
- ★★**이김은 단계 단위다**(`apprAuthMap`) — 서식에 「팀장」만 적어 두면 팀장만 그 서식 지정을 쓰고 **나머지 단계는 부서 기본을 그대로 따른다.**
- ⚠**지정이 하나도 없는 단계는 종전처럼 누구나** — 도입하는 날 결재가 멈추면 안 된다(`qpsUserDept` 와 같은 원칙).
- ★**막는 것은 서버**(`saveChkAppr` 가 다시 본다) — 화면의 흐리게·이름 표시는 편의일 뿐이다.

### ⑪ 결재 권한 화면을 **한 표로 개편** (JSP 1 · 저녁 이후 사용자 지적 6번)

「서식을 뿌리고 **우측에 결재 단계를 횡으로**」 · 「지금 이것은 **한 건 한 건**이고 **밑에 별도 보여줄 필요도 없는데**」

옛 판은 한 번에 한 건(부서 하나 또는 서식 하나)만 다뤄, 서식마다 예외를 주려면 「고르기 → 담기 → 저장」을 되풀이하고 그 결과를 아래에 또 목록으로 보여 줬다.
⇒ **줄 = 서식(맨 위가 부서 기본) · 열 = 결재 단계 · 칸 = 결재자** 한 표. **아래 목록은 없앴다**(표가 곧 현황).

- 서식 줄의 빈 칸은 *「부서 기본 : 마스터」* 처럼 **무엇을 따르는지** 기울임으로 보여 준다.
- ＋ 를 누른 **그 칸에만** 셀렉트가 나온다 — 칸마다 미리 깔면 셀렉트가 수백 개다.
- 바꾼 줄에 **●**, 단추는 **「저장 (n줄)」** — 바꾼 줄만 서버에 보낸다(서버는 부서·서식 단위 통째 교체라 한 줄씩 차례로).
- 이어진 지적들 : **「스크롤되게」**(카드 안에서만) · **「15개까지」→「16개까지」**(머리줄 + 앞 16줄을 **실제로 재서** 높이 결정) ·
  **「부서기본 스크롤 안 되게」**(기본 줄 `position:sticky`, top 은 머리줄 높이를 실측해 넣는다) · **「맨앞에 숫자 표시」**(# 열, 걸러도 보이는 차례로 다시 매김).
- **[결재 단계 설정]을 이 화면으로 옮겼다**(사용자 「네」·「결제선설정」) — 표의 **열이 곧 결재선**이라 여기가 제자리다.
  낙상(지표분석) 화면의 같은 단추는 **뺐다**(사용자 「낙상화면 결재선설정은 빼줘」). ★함수 `qfApprLineEdit` 와 `show('#qfBtnLine')` 은 남겨 뒀다 — **단추만 되넣으면 부활**한다.
  ⚠확인창에 못 박은 것 : 결재선은 **QPS 전체가 함께 쓰고**(점검표 결재란·지표분석 결재), **줄이면 그 단계 권한이 표에서 사라진다**(자료는 남는다).

### ⑫ 근무표 — 새 화면 (DDL · 매퍼 17문 · 자바 · JSP 2 · ⛔재빌드)

사용자 「근무표에 따른 사인도 매치가 되어야 하고」 · 「근무표는 SUNWOO 에도 있었음」 · **「더 효율적으로」**.

**SUNWOO 원본**(`D:\SUNWOO\SUNWOO`) : `t_duty`(사람×하루) + `t_duty_group`(월별 근무조) · `HLP/HLP_DutySign.pas` 가
`ROW_NUMBER() OVER (PARTITION BY work_dt ORDER BY user_id DESC)=1` 과 `SUBSTRING(work_cd,1,1)` 로 날짜별 한 사람을 뽑아 서식에 넣는다.

**우리가 다르게 한 것 네 가지**
1. 표를 셋으로(머리·사람·값) — **사람 줄이 곧 그 달의 근무조 명단**이다(명단을 따로 두면 두 곳이 어긋난다).
2. 근무 기호를 **공통코드 `QPS_DUTY_SHIFT`** 로(원본은 사인 SQL 에 병동 이름이 박혀 있다 — 그 병원 전용).
3. 날짜마다 여럿이면 **적은 차례(SORT_NO)** 로 고른다(원본의 `user_id DESC` 는 우연이다).
4. 원본에 없는 **전월 가져오기 · 반복 패턴 · 기호 붓**을 넣었다(「더 효율적으로」).

- **DDL** [QPS_DDL_DUTY_2026-09-08.sql](../sql/qps/ddl/QPS_DDL_DUTY_2026-09-08.sql) ✅사용자 실행 — `TBL_QPS_DUTY`(+마감 3칸) · `_ROW` · `_VAL` · 기호 12종.
- **매퍼 17문** : `selectDuty`·`Rows`·`Vals`·`Wards`·`List` / `insert·updateDuty` / `updateDutyLock` / `delete·insertDutyRows·Vals` / **`selectDutyDayNames`**
- **엔드포인트 4** : `dutyGet`·`dutySave`·`dutyLock`·`dutyDayNames` · 화면 `main/qpsDuty.do` · 사이드바 **[▸ 근무표]**(점검표 작성 바로 아래, **병원도 보인다**)
- **화면**(qpsDuty.jsp) : 사람 × 1~31일 격자 · 기호 붓(끌어 칠하기) · 반복 패턴(`DDNNOO`) · 전월 사람/통째 가져오기 · 더블클릭 기호 돌리기 · Enter·화살표 이동 ·
  근무일수(쉬는 기호 제외)·그날 인원 자동 합계 · **마감 잠금** · A4 가로 인쇄(기호 범례·그날 인원 포함)
- ★**사인 매치** = 부서(그 서식의 부서, 공통이면 화면 부서) · 병동(그 문서의 병동) · 달(그 문서의 연월) · **O·V·R 제외** · 같은 날 여럿이면 SORT_NO 순.
- **점검표 일괄 사인**에 「**그날 근무자**」 선택지 + 근무 기호 고르기. 한 달 한 번만 물어본다(`DUTY_CACHE`) · **날짜 격자 서식에만**(사인 칸에 `data-day` 가 있는 서식) ·
  근무표가 없는 달·근무자가 없는 날은 **빈 칸으로 두고 저장조차 안 한다**.
- ⚠`saveDuty` 는 **마감(LOCK_YN='Y')이면 거절**한다. 저장은 사람 줄·값을 **통째로 다시 깐다**(줄이면 뺀 사람이 남지 않아야 한다).

---

## 8. 배포 — 저녁 이후 몫

| 갈래 | 파일 | 필요한 일 |
|---|---|---|
| **DB** | 위 DDL 4개(SIGN_APPR·FORM_APPR·APPR_AUTH·DUTY) | ✅**전부 실행 완료** |
| **자바** | QpsController · QpsService(Impl) · QpsMapper | ⛔**WAR 재빌드 + 재기동** |
| **매퍼** | `Qps_SQL.xml`(309문) | ⛔같이 |
| **JSP** | qpsChk · qpsChkForm · qpsFall · **qpsApprAuth(신규)** · **qpsDuty(신규)** · sidebar | 파일 교체 |

반영 확인 = `codeList.do` 응답 `build` = **`20260908-DUTY`** (로컬은 확인 완료).
⚠**커밋은 아직 안 했다** — 위 파일 전부 미커밋(신규 7 · 수정 11).

## 9. 검증 (저녁 이후)

- 매퍼 XML 정형성 **309문** · id 중복 0 · javac(WAR lib) exit 0
- JSP 인라인 문법 · 태그 짝 · 미정의 함수 0 · **QPS `Swal.fire` 0**
- 시뮬 **349 통과**(315 → 일괄 사인 근무자 **+9** · 새 `sim_qpsDuty.js` **+25**)
- **DB 실왕복**(가짜병원, 넣고 지움) : 1일=김간호(D) · 2일=**박간호**(김간호 O 라 빠짐) · 3일=김간호(N, 박간호 V) · 기호 `N` 만 걸러도 정확 · 마감 저장 거절 · `UX_QPS_DUTY` 유니크 확인
- **재기동 뒤 화면 실왕복** : 근무표 저장·재조회·합계 · 점검표 문서 만들고 **일괄 사인(그날 근무자) → 1건 30칸** · 인쇄물의 점검자 확인란이 **날짜마다 그날 사람**(5·6일만 다른 사람) · 다시 돌리면 0건(안 덮음) · 결재 권한 표 저장·상속·비우기
- 시험 자료 **전부 삭제**(듀티·점검표 문서·결재 권한 — 잔여 0)

## 10. 저녁 이후 새로 얻은 함정

1. ★**남의 표를 가리키는 칸은 그 표의 형을 그대로 베낀다** — `CHK_SEQ` 를 INT 로 적어 BIGINT 와 안 맞았다.
2. ★**날짜가 31칸인 표는 폭을 못 박아야 한다** — 안 그러면 성명·직종 칸이 눌려 「김:」 「수:」 로 잘린다(실측). 두 열은 `position:sticky; left` 로 붙여 둔다.
3. ★**화면 높이 실측은 한 번으로 부족하다** — 타일 레이아웃이 뒤늦게 자리를 옮겨 표가 화면 밖으로 이어졌다(사용자 캡처). **CSS 로 먼저 막고**(`max-height:calc(100vh - …)`) JS 로 다듬는다. 다시 그린 뒤에도 잰다.
4. ★**sticky 를 행·열에 함께 쓰면 z-index 를 층으로 준다** — 머리줄 4 · 고정 줄 3 · 왼쪽 열 2. 배경을 안 주면 밑 줄이 비쳐 보인다.
5. ★**시뮬에서 `window.x = function` 은 그냥 안 불린다** — 브라우저는 window 속성이 곧 전역이지만 Function 스코프는 아니다. `with (window) { … }` 로 태우거나 별칭을 만든다.
6. ★**화면의 `toast()` 는 `window._toast` 가 있는지 본다** — 시뮬에서 인자로만 넘기면 알림으로 떨어져 검사가 헛돈다.
7. ★**`<c:url value="…"/>` 를 시뮬에서 치환할 때 따옴표를 덧붙이지 말 것** — 소스가 이미 `'…'` 안에 넣어 두어 URL 이 `"…"` 째로 온다(응답 스텁이 안 걸려 한참 헤맸다).
8. ★**저장 뒤 재조회와 「전월 가져오기」가 경주한다** — 늦게 온 재조회가 방금 가져온 명단을 덮었다(시뮬이 잡았다) ⇒ 가져오기가 **조회 순번을 올려** 날아가던 응답을 버린다(§2 ③과 같은 규칙).

## 11. 남은 일 (저녁 이후 기준)

- **커밋**(사용자 몫) · **운영 배포 = WAR 재빌드+재기동**(자바·매퍼).
- (후보) **격자 사인 칸에 작은 도장** — 지금은 이름 글자만 들어간다(칸이 6mm 라 도장은 점이 된다. 인쇄 배율을 손봐야 한다).
- (후보) **서식 아래 결재란(`SIGN_LINE`)도 같은 뼈대로** — 지금은 빈 줄로 나가 손도장을 받는다.
- (후보) **근무표 사람 고르기** — 계정이 없는 병원은 직원 콤보가 비어 [+ 빈 줄]로 이름을 친다(그래도 도장은 계정이 이어져야 붙는다).
- 위너넷 병원(w1234567)에 남은 **결재 기록 4건**(문서 321, 전부 USE_YN='N')과 **도장 1장** — 사용자가 직접 만든 것이라 그대로 두었다. 지울지 확인 필요.

## 12. 밤 후속 — 격자 사인 칸 도장 · 서식 아래 결재란 (§11 후보 둘을 끝냈다)

### ⑬ 격자 사인 칸의 작은 도장 (매퍼 1문 · 자바 · JSP · ⛔재빌드)
- **열쇠 문제** : 사인 칸(`SIGN_NO=900`)에 저장된 것은 **이름 글자**뿐, 도장은 **계정**마다다. ⇒ **이름으로 잇는다**(종이도 「이름 = 그 사람」. SUNWOO `HLP_DutySign` 도 같은 자리).
- `selectQpsSignsByNames` — **문서에 실제로 적힌 이름만** 묻는다(전 직원 = 수백 KB). `/qps/signNames.do`(`names` JSON, 50개 상한, jsonRows).
- 툴바 **[☑ 사인 칸 도장]** 기본 켬(브라우저 기억). **종이에만** 찍는다 — 화면 칸은 `input`(글자로 고친다). `img.stmp` 4.6mm + `print-color-adjust:exact`.
- ★**도장 없는 사람은 이름 그대로**(빈 칸이면 누가 점검했는지 사라진다) · 한 번 물어본 이름은 다시 안 묻는다(없다는 답도) · 옛 서버면 조용히 이름으로.
- ⚠같은 이름 둘이면 앞 사람 도장(매퍼 주석). 실왕복 : 도장 30 / 끄면 이름 30 / 섞이면 없는 사람만 이름. 시뮬 `sim_qpsChk_stamp.js` 14.
- 시뮬이 잡은 결함 : 초기화 `catch` 안에서 없는 요소를 또 만져 죽었다 → 널 가드.

### ⑭ 서식 아래 결재란(SIGN_LINE) — 같은 뼈대 (자바 · JSP · ⛔재빌드, 새 SQL 없음)
- 종이 아래 「점검자 ______ (인)」 줄(서식 `SIGN_LINE`, 쉼표)을 **화면에서 찍는다.** 위 결재 상자와 규칙 같음(빈 칸 = 내 도장 · 내 것만 취소 · 남이 찍은 칸은 알림).
- ★**새 표 없음** — 기록은 `TBL_QPS_CHK_APPR` **예약 대역 901~910**(`SLINE_BASE=900`, 자리 i → 901+i). 서버 `selectChkApprBox` 가 `sline` 을 따로 돌려주고 900 대는 위 상자로 안 샌다(자리가 줄어도 `gone` 으로 남긴다).
- 화면 `#ckSlineBox`(격자 아래) · `apprTake(res)` 가 두 상자를 한 응답으로 · **권한으로 안 좁힌다** · 저장된 문서에만.
- 인쇄 = 서명한 자리 도장 9mm, 빈 자리 **빈 줄 그대로**(손도장). ★★**날짜 없음**(사용자 「일자는 들어가면 안 됩니다」 — 옆→밑→삭제 세 번 만에 확정. 종이의 「(인)」 자리는 도장만. 위 결재 상자는 날짜 그대로).
- 실왕복(가짜병원 · NUR048 응급약품 대장 3자리) : 찍기 → 도장 · 종이 = 도장 1 + 빈 줄 2 · 날짜 없음 · 시험 자료 삭제(잔여 0). 시뮬 `sim_qpsChk_signline.js` 15 · 전 시뮬 **378**.

### 배포·남은 것(갱신)
- ⑬⑭ 모두 **자바 → WAR 재빌드+재기동**(로컬은 재기동·실왕복 완료). DDL 없음.
- §11 의 후보 둘(격자 도장 · SIGN_LINE)은 **완료**. 남은 것 = **커밋 · 운영 배포**(사용자 몫) · 근무표 사람 고르기(계정 없는 병원은 이름만).

## 13. 2026-09-09 — 인사 등록 · 담당자별 사인·도장 (근무표 사람 고르기의 답)

> 📘 **정식 인수인계는 [QPS_인수인계_2026-09-09.md](QPS_인수인계_2026-09-09.md)** — 아래는 오전 요약이고, 시연·지적 반영·배포 절차·함정은 그 문서에 있다.

사용자 지시 세 번 : 「담당자별 사인(작성해서) 및 도장 관리 필요함」 → 「사인은 작성해서 저장 기능」 → 「**인사등록도 필요함**」 → 「TBL_QPS_SIGN 이 사원정보로 가능할듯 확인해줘」.

### ⑮ 설계 — 새 표 없이 `TBL_QPS_SIGN` 을 「직원 표」로 (DDL 2 · 매퍼 7문 · 자바 · 새 화면 1 · ⛔재빌드)
- **왜 이 표인가** : 도장은 계정 본인만 등록할 수 있었는데, 사인 칸에 이름이 오르는 간호사·조무사는 **계정이 없다**. 근무표 사람 콤보도 같은 뿌리로 비었다.
  `TBL_QPS_SIGN` 은 이미 「사람마다 한 줄(병원+USER_ID)」이라 여기에 직원 칸을 얹으면 **근무표·사인·결재가 한 사람을 가리킨다**. 직원 표를 따로 두면 사람이 두 표에 갈린다(이 저장소가 여러 번 겪은 함정).
- **기존 `TBL_HOSPEMP_MST` 와 대조**(사용자 물음에 DB 로 확인) : 그 표는 차등제 **면허 인력 신고**(면허종류·면허번호·입사·퇴사·주당근무시간, 키 = 면허번호+입사일). 면허자만 다루고 사번·직책이 없어 목적이 다르다 — 합치지 않았다. 나중에 「면허등록에서 가져오기」 다리는 놓을 수 있다.
- **DDL 2** (둘 다 사용자 실행) : [QPS_DDL_SIGNER_2026-09-09.sql](../sql/qps/ddl/QPS_DDL_SIGNER_2026-09-09.sql) = JOB_NM·DEPT_CD·ACCT_YN·SORT_NO ·
  [QPS_DDL_EMP_2026-09-09.sql](../sql/qps/ddl/QPS_DDL_EMP_2026-09-09.sql) = EMP_NO·POS_NM·JOIN_DT·RETIRE_DT·REMARK. 주민번호·연락처는 일부러 안 받는다(개인정보 최소화).
- **키 규칙** : 계정이 있으면 USER_ID = 계정(결재란 도장과 **같은 줄**, ACCT_YN=Y) · 없으면 서버가 `P`+UUID 12자리(ACCT_YN=N). **이미 만든 줄의 USER_ID 는 바꾸지 않는다**(결재 기록·근무표가 그 키를 가리킨다) — 계정 잇기는 새 사람일 때만.
- **퇴직자** = RETIRE_DT 가 오늘 이전. 근무표 콤보(`selectQpsSignerNames`)에서 빠지고, 인사 화면은 「퇴사자 포함」을 켜야 보인다. **사인 칸 도장 찾기(`selectQpsSignsByNames`)는 그대로** — 옛 종이의 도장이 사라지면 안 된다.
- **엔드포인트 4** `signerList / signerSave / signerImgSave / signerDel.do`. 고치기 권한 = 자료실과 같은 `canEditLib`(위너넷 · QPS 담당자 · 담당자 없으면 병원관리자). 보기는 병원 전원. 본인 사인은 본인이. 그림 저장은 **`saveQpsSign` 재사용**(저장 경로 하나).
  날짜는 어떤 꼴이든 숫자만 남겨 8자리(`hrDate`), 퇴사일<입사일 거절.
- **화면** `main/qpsSigner.do`(qpsSigner.jsp, 관리(설정) ▸ **인사 등록 · 사인·도장**, 병원도 보임) : 부서 필터 · 찾기(이름·사번·직종·직책) · 퇴사자 포함 · [+ 사람 추가] 폼(이름·사번·직종·직책/직급·부서·계정 잇기·입사·퇴사·차례·비고) ·
  **[📋 여러 명 붙여넣기]**(엑셀 열 복사 → 탭/쉼표, 머리줄 건너뜀, 부서는 이름·코드 모두, 못 읽는 줄은 건너뛰고 표시, 한 줄 실패해도 나머지 등록 — SUNWOO 사원등록의 「엑셀 불러오기」 갈음) ·
  표(사인 미리보기·계정 배지·퇴직 배지) · **사인 그리기 창**(qpsChk [🖋 내 도장] 창과 같은 캔버스 규칙 : devicePixelRatio·스캔 그림 축소) · 그림 내리기 · 삭제(「퇴사자는 빼지 말고 퇴사일을」 안내).
  동명이인은 경고만(막지 않음 — 사인 칸 도장은 이름으로 붙으니 앞 사람 것이 쓰인다고 말해 준다). 순번 가드 있음.
- **근무표(qpsDuty)** : `dutyGet` 이 `signers` 를 함께 주고 사람 콤보가 `<optgroup>` 두 묶음(인사 등록 직원 ✎ / 명단에 없는 계정 직원). 이름·직종은 명단에서 먼저 채운다. 툴바에 「인사 등록 →」.
- 검증 = 매퍼 XML 317문·중복 0 · javac · JSP 문법·태그·미정의·Swal 0 · 시뮬 `sim_qpsSigner.js` **48** + 근무표 콤보 5 → 전 시뮬 **431** ·
  **재기동 뒤 실왕복 20항목**(가짜병원, PowerShell UTF-8 로 한글 왕복) : 등록 2명(P+12자리, 사번·직책·입사·비고 되읽기, 점 날짜 → 8자리) · 거절 3(깨진 날짜·퇴사<입사·이름 없음) · 계정 없는 사람 사인 저장 → 목록 hasimg·`signNames` 로 도장 찾힘 · 근무표 콤보에 둘 다 ·
  퇴사 처리 → 기본 목록·콤보에서 빠지고 포함하면 retired=Y, **도장은 여전히 찾힘** · 그림 내리기·줄 삭제·없는 사람 FAIL · 권한 없는 계정 저장 FAIL(목록은 보임) · **내장 브라우저**로 붙여넣기 등록(확인창 「2명을 등록합니다」) → 사인 창에 마우스로 그려 저장 → 미리보기 · 근무표 콤보 「홍길동 · 간호사 ✎ | 임꺽정 · 조무사」. 시험 자료 삭제(잔여 0).
- ⚠겪은 것 : 시뮬 본문 추출이 `</style>` 뒤부터라 **`<style>` 앞에 있는 래퍼 div 를 잘라** `gel('qpsSigner')` 가 null — 새 화면 시뮬은 인라인 `<script>` 앞까지 통째로 잡는다. 붙여넣기 시뮬의 점 날짜 `2026.4.1` 은 6자리라 건너뜀 → `until` 이 영영 안 풀려 멎었다(시뮬 픽스처 문제, 화면은 정상 — 서버도 8자리만 받는다).
- 남은 것 = **커밋 · 운영 배포(DDL 2 → WAR 재빌드+재기동)**. 결재란 부서별 단계 이름 여부는 사용자 확인 뒤.
