# QPS 인수인계 — 2026-08-14 (safeRpt 엔진 확장 3건 · ⓪ 은 못 했다)

> ## ★★★[이정표 2026-08-14 심야] SUNWOO 18개 모듈 — **서식 시드 전량 완료**
>
> **점검표 표준 서식 300종 · safeRpt 유형 61종 · 회의록 위원회 5종 · 부서 코드 13개** 가 운영 DB 에 있다.
> 마지막 3모듈(물리재활 5 · 진료 2 · 사회복지 15+9)은 [QPS_SEED_PT_DR_SW](../sql/QPS_SEED_PT_DR_SW_2026-08-14.sql).
> 이 세션의 상세는 **§5-4 완료 블록 1~7** — 사진첨부·LBL_JSON·EDURPT·연간격자·보건관리자·원무총무·
> 인공신장·의무기록·방사선사·마지막 3모듈 순서로 쌓였다.
>
> **남은 백로그(서식 아님)** — *2026-08-14 심야 갱신 : ②③은 같은 날 풀렸다* :
> ① 사회복지 **설문 7종** — ⛔**시드 불가 확정**(TBL_QPS_SRV_DEF 가 설문지 축 없는 고정 20문항, DB 실측).
>    설문지 축 확장은 개발 사안 ⇒ **병원확인 #40**(실사용 여부부터)
> ② ~~「항목×날짜아닌 고정열」 조각 설계~~ ✅**새 조각 불필요했다** — 재판정으로 4종이 기존 축에 들어감
>    ([RETRY 시드](../sql/QPS_SEED_RETRY_2026-08-14.sql) : w12=ITEM_COL · w20/21=LIST(행=직원이 곧 LIST) ·
>    w29=ITEM_COL(업체 12열) · RN16=LIST). 진짜 조각 사안은 **w15(셀 고정문)·MR13(기능 조사화면)** 2건뿐
> ③ ~~보고서 5종 집계표 검증~~ ✅**등록 완료**([HBRPT 시드](../sql/QPS_SEED_HBRPT_2026-08-14.sql) —
>    표 1개짜리 3종은 그대로, h01·h02 는 열 접두어로 합침. 배치 차이는 **병원확인 #41**). safeRpt **66종**
> ④ ~~반복행 여러 벌~~ ✅**2026-08-15 조각 완성** — `TBL_QPS_SAFERPT_SUB`(벌 정의) + **값은 기존 ROW 표에
>    `ROW_NO=벌×1000+행`**(LIST 블록 규칙과 같은 발상 — 단벌 유형·옛 문서 무변경). DDL·매퍼·화면·인쇄 적용,
>    `build=`**`20260815-SRSUBS`**. [SUBS 시드](../sql/QPS_SEED_SUBS_2026-08-15.sql)로 **w13 인사기록카드
>    (`HRCARD` 9벌+사진, ⚠주민번호 칸은 최소화 원칙으로 뺌 — 병원확인 #42) · MR14 완결도(`MRCOMPL` 2벌) ·
>    RPTBHEP/RPTCHK 집계표 원형 복원(2벌/3벌)** 등록. safeRpt **68종**.
>    **[이어서] 점검표 사진칸도 완성** — `TBL_QPS_CHK_FORM.PHOTO_NMS`(칸 이름 목록 = 수·라벨, 최대 12) +
>    `TBL_QPS_CHK_FILE`. DDL 운영 적용 · 매퍼 3쿼리 · `chkPhotoUpload/Delete.do` · qpsChk 화면(칸 클릭 업로드·
>    blob 표시)·인쇄(있는 칸만 2칸씩 + 사진 로드 대기). [CHKPHOTO 시드](../sql/QPS_SEED_CHKPHOTO_2026-08-15.sql) =
>    **`RAD022` 납가운 관리대장(XR07 — LIST+평가기준+사진 5칸) 등록** · **`PHA024` 에 봉인스티커 월별 12칸**
>    (RN27 p2 흡수 — 부서 공유 판정 그대로).
>    **[이어서] h19 업무일지도 등록**(`HLTDIARY`) — 문항 전문을 **캡처(h19.png) 구역별 Crop→Read 재판독**으로
>    확보(체크 6그룹 50항목 + 통계 소표 3벌·상담 반복행 = SUBS 4벌). safeRpt **69종**.
>    ⚠긴 문항 3곳은 저해상 판독 — 시드([HLTDIARY](../sql/QPS_SEED_HLTDIARY_2026-08-15.sql)) 주석의 실물 대조 표시.
>    ★재판독 기법 확립 : 전체화면 PNG 를 구역별로 잘라 읽으면 작은 문항도 읽힌다(이 PC 캡처 사본으로 가능).
>    ⇒ 서식 보류는 **XR23(문서 키=사람)·w15(셀 고정문)·MR13(기능화면) 3건**만 남는다 — 전부 설계·범위 사안
>
> ### ✅[2026-08-15 재개] **실서버 왕복 검증 + 혈액 반납/폐기 신청서**
>
> - 재기동된 8080(`build=20260815-SRSUBS`)에서 새 조각 2건 **실왕복 통과** :
>   반복행 여러 벌(HLTDIARY — 벌 1·2·4 분배·한글·조도 부기 복원) · 점검표 사진(RAD022 —
>   문서 저장→칸3 업로드→복원, 한글 파일명 보존). 테스트 자료 삭제(08-13 `devtest` 잔재 2건도 정리).
> - **`BLOODRTN` 혈액 반납/ 폐기 신청서 등록**([시드](../sql/QPS_SEED_BLOODRTN_2026-08-15.sql)) —
>   간호 [201]·진단검사 11·인공신장 RN37 이 같은 종이(3부서 공용). RN37.png **구역 재판독**으로
>   주의문 전문 확보 + ★서명 4칸(수간호사~혈액은행담당자)이 별도 서명란이 아니라 **표의 열**임을
>   확인해 반복행 8열로 폈다. 폐기/반납 2택 + 사유 7(복수). safeRpt **70종**.
> - DRUGRTN(의약품 반납) 확인 2건은 여전히 남는다 — **약국 캡처가 이 PC 에 없다**(§7 그대로).
> ⑤ 기능 화면 계열(재택복귀율 그리드·PDF 보관함·설문 집계) — 이식 여부 자체가 범위 결정 사안
> ⑥ 브라우저 눈 검증(사진 2×2 인쇄·연간격자·새 부서 서식 표본) · 운영 WAR 재배포(사용자 몫)
>
> ### ✅[2026-08-15 오후] **담당자 위주 메뉴 개편 + 남은 일 총정리**
>
> - 사용자 지시 「메뉴 구성은 직관적으로, 해당 담당자 위주로」 →
>   ①사이드바 **「부서별 점검표」 15부서 그룹** 신설(`qpsChk.do?dept=코드` — 그 부서로 걸러 열림)
>   ②「보고서」→**「보고서 · 서식」 계열 6링크**(`qpsSafeRpt.do?gb=유형` 프리셀렉트)
>   ③유형 셀렉트 70종 **optgroup 계열 묶음** — ★**SORT 대역 = 계열** 시드 규약
>   (1~19 사고·20대 보건·31~ 인사총무·51~ 의무기록·71~ 영양·73~ 사회복지·91~ 검진).
>   새 유형은 대역 안 SORT 로 등록하면 화면 무수정으로 제 묶음에 들어온다.
> - 매퍼 selectQpsCodes 에 `sort` 추가 · qpsChk/qpsSafeRpt 컨트롤러에 dept/gb 파라미터.
>   ⚠qpsChk 프리셀렉트는 **FORM.formid 대조**로 3번째 chkBase — 셀렉트 값 비교는 어긋남을 못 잡는다
>   (목록 재구성 시 첫 서식이 저절로 보임). Node 로 4시나리오+70종 묶음 검증 통과.
> - **자바+매퍼 → WAR 재빌드 필수**, 반영 확인 `build=20260815-DEPTMENU`.
> - **남은 일 총정리** = [QPS_남은할일_2026-08-15.md](QPS_남은할일_2026-08-15.md) — 사용자 몫(A)·
>   회의 사안(B)·자료 대기(C)로 갈라 두었다. ***다음 재개 때 이 문서부터 본다.***
>
> ### ✅[2026-08-15 낮] **약국 실물 캡처 — 의약품 계열 5유형 적재 (safeRpt 75종)**
>
> - 사용자가 집 PC SUNWOO 약국 모듈을 열어 줌 → 캡처 4서식(+원외 4쪽) · 대조 5건 전부 판정 완료.
>   상세·판정표 = [약국보충 판독](QPS_서식판독_약국보충_2026-08-15.md) ·
>   시드 = [QPS_SEED_DRUG_2026-08-15.sql](../sql/QPS_SEED_DRUG_2026-08-15.sql)(운영 적재).
>   DRUGRTN 시드도 ⛔ 해소·적재. **SORT 10~19 「의약품 · 혈액」 대역 신설**(BLOODRTN 26→11).
> - ★기법 : 작은 글씨는 **SUNWOO [폰트 확대] 후 재캡처**(사용자 제보) — 크롭 확대보다 정확.
> - 잔여 : **[274] 병동의약품 신청서 = 간호/병동 모듈을 열어 줘야 캡처 가능**(약국엔 없음 확인) ·
>   원외 법정 4쪽(별지 77호의2) 등록 여부 = 병원확인.
>
> ### [메뉴 반영 2026-08-14] 시드 뒤 메뉴 작업은 **회의록 3형제뿐**이었다 — 완료
>
> - **점검표 300종·safeRpt 61종은 메뉴 작업 0** — 기존 메뉴(점검표 작성·사고 유형별 보고서)가
>   부서 셀렉트·유형 셀렉트를 **DB 에서 그려** 재기동만 하면 나온다(코드 확인).
> - **회의록 위원회만 3곳 하드코딩** — ①qpsMinutes.jsp 셀렉트+제목 ②sidebar.jsp 부서 위원회 메뉴
>   ③`putFormGb` 허용 목록(⚠빠뜨리면 **조용히 Q 로 열린다** — 08-12 약사·영양 실사고 재발 지점).
>   운영위(`gb=W`)·중독연구소(`gb=C`)·인사위(`gb=H`) 셋 다 반영했다.
> - ★★**코드 충돌 실사고 1건 잡음** — 인사위원회를 'P' 로 넣었다가 **약사위원회가 같은 화면에서
>   P 를 먼저 쓰는 것**을 메뉴 작업 중 발견, **'H' 로 정정**(DB·시드 파일·화면·허용 목록 일치).
>   문서 0건 상태라 피해 없음. ***회의록 위원회 글자는 코드 테이블이 아니라 화면·사이드바·허용 목록
>   3곳이 정본이다 — 새 위원회를 넣을 때 반드시 셋을 함께 본다.***
> - 배포 : qpsMinutes/sidebar = JSP 교체, `putFormGb` = **자바 → WAR 재빌드**(대기분 합류).

> ***이 문서가 지금 인수인계 정본이다.*** 앞 문서 [08-13](QPS_인수인계_2026-08-13.md) 은
> 「간호/병동 완주까지 어떻게 왔나」로 남긴다 — **SUNWOO 조작 절차(§4) · 예약 번호 표 · 로컬 반영 절차(§5)**
> 는 그 문서에 있고 **여전히 유효하다.**
> 오늘 일의 **모든 상세**는 [safeRpt 반복행·서명 설계](QPS_safeRpt_반복행서명_설계_2026-08-14.md) 에 있다 —
> 이 문서는 그 지도다.

---

## 0. ★★가장 먼저 — DB 는 적용했다, **WAR 는 이틀치가 밀려 있다**

| | 상태 |
|---|---|
| **DB(운영 WNN)** | ✅ **적용 완료** — `QPS_DDL_SAFERPT_ROWSIGN` 실행함(표 2개 생성 확인). ***다시 돌리지 말 것*** |
| **운영 WAR** | ⛔ **옛 코드.** 08-13분 + **08-14분**이 쌓였다 — **이틀치다** |
| **로컬(8080)** | ⛔ **오늘 것 미반영** — 이 PC 에서 톰캣에 올려 보지 못했다 |

DB 선적용이 안전한 이유는 08-13 §0 그대로 — **표 2개를 더하기만** 해서 옛 코드는 읽지도 쓰지도 않는다.
⚠`QPS_SAFERPT_SEED_DRUGRTN` 은 **안 돌렸다**(확인 2건 대기 — §7).

> **[적용 기록 2026-08-14]** `114.108.153.178:3306/WNN`(gcloud-seoul, MySQL 8.0.41, 병원 120곳) 에 실행.
> `TBL_QPS_SAFERPT_FORM`(10칸, PK `RPT_GB`) · `TBL_QPS_SAFERPT_ROW`(4칸, 복합 PK) 생성 확인.
> 한글 주석은 **`CHAR_LENGTH` 로 검증**했다(콘솔 cp949 깨짐은 표시 문제일 뿐 — 08-13 §7).
> ⚠**이 프로젝트는 dev/prod DB 분리가 없다** — 프로파일 이름이 헷갈리게 붙어 있다 :
> `local` → **원격 운영 DB(114.108.153.178)** · `prod` → `localhost`(운영 서버 기준).
> ***로컬에서 개발해도 운영 DB 에 붙는다.*** 계정·비밀번호는 `context-datasource.xml` 에 평문이다.

### 배포하면 되는 것 (사용자가 직접 · [배포_가이드](../배포_가이드.md) §2)

오늘 늘어난 대기분 : **자바 4**(QpsController · QpsService · QpsServiceImpl · QpsMapper) ·
**매퍼 1**(Qps_SQL.xml) · **JSP 1**(qpsSafeRpt). 자바가 바뀌었으니 **WAR 재빌드 + 재기동**이다.
(DDL 은 위에서 이미 적용했다.)

**반영 확인** — QPS 응답의 `build` 가 **`20260814-SRPROW`** 이면 새 WAR 다.
(어제 것 `20260813-SUBCOL`, 그제 `20260812-FACSEED`.)

---

## 1-0. ★★[당일 반전] 화면 조작이 **PowerShell 로 복구됐다** — 진단검사 캡처 착수

⓪ 이 "환경 문제로 불가"였는데(§1), **PowerShell `CopyFromScreen` + `user32 mouse_event` 로
SUNWOO 를 몰 수 있다는 것이 같은 날 실측됐다.** computer-use 도구 없이도 된다.
방법·함정(가상 화면 원점 `Top=-61` 보정 · 매 호출 Add-Type 재정의)은
[진단검사 판독 문서](QPS_서식판독_진단검사_2026-08-14.md) §캡처 방법에 있다.

**진단검사(임상병리) 진행 현황** :
- 트리 3갈래 확인 — **보고서 11종 = safeRpt 9유형과 일치(부서 공용 폴더)** · 고유 39종
- **39종 캡처 완료** → `D:\워너넷자료\caps\LAB_2026-08-14\` (영구 보관)
- 판독 ~10종 — **「매일/매주/매월 복합 격자」 근거 2종 확보**(공통판+원심분리기) ⇒ 새 엔진 조각 후보
- ⚠일괄 캡처 중 클릭이 밀려 **재캡처 ~8건** 필요(목록·원인·개선책은 판독 문서에)

### ✅[2026-08-14 오후] **진단검사 판독 완주 · 재캡처 완료** — 남은 것은 「구현」이다

| | |
|---|---|
| **재캡처** | ✅ **12건 완료** — `r01·r06·r08·r09·r10·r11·r14·r15·r18·r20·r29·r31·r32` (파일명 = 트리 번호) |
| **판독** | ✅ **39종 갈래 확정** — [판독 문서 §4-2](QPS_서식판독_진단검사_2026-08-14.md) |
| **캡처 어긋남** | ✅ **원인 확정 + 해법 문서화**(판독 문서 §캡처가 어긋난 진짜 원인) |
| **새 조각 설계** | ✅ [주기 복합 설계](QPS_점검표엔진_주기복합_설계_2026-08-14.md) |
| **새 조각 구현** | ✅ **끝** — `GRP_PRD` 한 칸. DDL·매퍼·컨트롤러·작성화면·서식관리 |
| **진단검사 시드** | ✅ **운영 DB 적재** — 서식 **28종** · 항목 185 (`LAB001~LAB028`) |
| **재기동·반영** | ✅ 로컬 8080 `build=`**`20260814-GRPPRD`** 확인(매퍼 3곳·JSP 반영) |
| **검증** | ✅ **DB→렌더 사슬** — 배포된 JSP 에 실제 적재 서식 3종을 물려 칸 수·복원 확인 · **한글 왕복 확인** |
| **화면 검증** | ⛔ **브라우저로는 못 했다** — 로그인이 필요하다(아래). **인쇄가 특히 남았다** |

### 「주기 복합」 — `GRP_PRD` 한 칸으로 끝냈다

***묶음(GRP_NM)에 기간 축을 붙인다*** — `GRP_PRD = '매일>D,매주>N,매월>1'`.
묶음 이름은 **이미 항목표에 있다**(v2 행 그룹). 없던 것은 「그 묶음이 어떤 기간으로 뻗는가」뿐이었다.

> ⚠**설계 초안의 「`ROW_BLKS` 확장」은 틀렸다** — 코드를 열어 보니 `ROW_BLKS` 는 ***LIST 전용***이고
> 행번호를 `블록×1000` 으로 띄우는 **다른 뜻의 장치**였다. 거기 얹었으면 뜻이 둘이 됐다.
> ***설계를 코드로 옮기기 전에 그 칸이 실제로 무엇인지 확인할 것.***

- 비면 **기존 서식 177종이 하나도 안 바뀐다**(확인함).
- **값 자리는 안 건드렸다** — 행번호가 곧 묶음을 가르므로 `collect()`·전월복사·빈행 정리가 종전대로 돈다.
- ⚠**자바·매퍼가 바뀌었다 ⇒ WAR 재빌드 + 재기동 필요.** 매퍼는 기동 시 로드라 **재기동 전엔 화면에 안 온다.**
  반영 확인 — QPS 응답의 `build` 가 **`20260814-GRPPRD`** 이면 새 WAR 다.

### 진단검사 시드 — 39종 중 **28종**만 넣었다 (⛔11종은 일부러 뺐다)

주기 복합 8 · EQUIP_DAY 3 · ITEM_DAY 6 · DAY_ITEM 6 · LIST 5.
***반쪽 등록 금지*** — 뺀 11종과 이유는 [시드 파일 머리말](../sql/QPS_CHK_SEED_LAB_2026-08-14.sql)에 그대로 적어 두었다.
요약 : **대조 대기 4**(9 혈액관리대장↔NUR011 · 11↔[201] · 24 부서=병동 · 26↔FALLENV) ·
**조각 대기 3**(12 연문서·월행 · 20 시약교체시 블록 · 39 사진첨부) · **불필요 4**(1↔2 중복 · 33·38 오연결 · 36 자리표시자).

***★이번에 얻은 가장 값진 것 두 가지*** :

1. **캡처 진위는 「창 제목」으로 본다** — `MainWindowTitle` 앞머리가 열린 문서 이름이다.
   ***이미지를 읽지 않고도 무엇이 열렸는지 즉시 안다.*** 어제 39장이 통째로 밀린 것도 이걸 안 봐서였다.
   어긋남의 원인은 셋이었다 — 창이 움직인다 · **트리 맨 위 「보고서」 폴더가 펼쳐지면 18칸 밀린다** ·
   「열린창 모두 닫기」 뒤 트리가 직전 문서 쪽으로 스크롤된다.
2. ⚠***서식 이름으로 갈래를 가를 수 없다*** — 「일상=EQUIP_DAY · 일일=주기 복합」으로 깔끔히 갈리는 줄 알았는데
   **`14 EKG 일일점검표`가 EQUIP_DAY** 이고 **이름에 갈래어가 없는 `8 롤 믹스기`가 주기 복합**이었다.
   ***한 장씩 열어 본 것만 믿는다.*** (어긋난 캡처가 판정까지 틀리게 만든 실례 — 판독 §4-2)

**갈래별 시드 대상** : DAY_ITEM 7 · LIST 6 · EQUIP_DAY 3 · ITEM_DAY 7 · **주기 복합 10(조각 신설 필요)** ·
safeRpt 쪽 2(`11 혈액 반납/폐기` · `39 직원 교육 결과 보고서`) · 판정 보류 1(`12 혈당측정기` = 연 문서·월행).

⛔**등록 전 대조 5건** — `9 혈액 관리 대장 ↔ NUR011`(**거의 같은 종이다**) · `11 ↔ 간호/병동 [201]` ·
`24 약품냉장고`(부서가 **병동**으로 뜬다) · `26 낙상 ↔ FALLENV` · `39 ↔ 시설 196·영양`.
(`32 청소·소독` 은 대조 끝 — 항목이 임상병리 전용이라 **별건**이다.)

## 1. ★★⓪ 「남은 모듈 캡처」를 **못 했다** — 환경 문제다 *(→ §1-0 으로 당일 뒤집힘)*

08-13 §6 의 사용자 지시는 ⓪ **SUNWOO 를 직접 몰아 감염·진단검사부터 캡처**였다. **못 했다.**

- **어제 작업한 노트북과 오늘 PC 가 다르다**(사용자 확인). 어제 그 환경에는 화면 전체를 조작하는
  **computer-use** 가 있었고 — 08-13 §4 의 "computer-use 로 직접 몰았다"가 그 흔적 — **이 PC 에는 없다.**
- 실측 : `~/.claude.json` 전역 `mcpServers` **없음** · 프로젝트 `.mcp.json` **없음** ·
  `.claude/settings.local.json` 은 권한 목록뿐. 붙어 있는 도구는 브라우저 자동화 2종(웹페이지 전용)이라
  **네이티브 `D:\SUNWOO\SUNWOO.exe` 를 열지도 캡처하지도 못한다.**

> ***★다음 세션에 기억할 것 : ⓪ 계열(SUNWOO 캡처·재캡처)은 「어제 그 노트북」에서만 된다.***
> 08-13 §4 절차는 그 PC 를 전제로 쓰인 것이다. 다른 PC 에서 열면 여기서 막힌다.
> 확인법 — 어제 노트북의 `~/.claude.json` 에서 `mcpServers` 를 보면 무엇이 붙어 있었는지 나온다.

⇒ 대신 **ⓓ 개별 화면**으로 갔다(사용자 선택).

---

## 2. 오늘 한 일 — 한 장

| # | 한 일 | 결과 |
|---|---|---|
| ① | **`safeRpt` 항목표 구조 확인** (시설 판정이 남긴 지시) | **전제는 사실이었다** — 유형은 이미 데이터다. §3 |
| ② | 없는 것 **3가지** 발견 — 반복행 표 · 서명란 · 정형문구 | 그 중 **저장이 필요한 건 반복행뿐** |
| ③ | **DDL 2표** `TBL_QPS_SAFERPT_FORM` · `_ROW` | ***안 돌렸다.*** 더하기만 하는 DDL |
| ④ | **서버단** — 매퍼 4쿼리 · 서비스 · 컨트롤러 | `saveSafeRpt` 에 `rows` 인자 추가 |
| ⑤ | **화면** — 반복행 입력표(행 추가/삭제) + 인쇄(표·문구·서명) | `SUB_COLS` 없으면 카드가 숨는다 |
| ⑥ | **유형 시드** 의약품 반납(DRUGRTN) | ⛔**머리말에 정지 표시** — 확인 2건 남음 |

---

## 3. `safeRpt` 구조 — 확인 결과 (다음에 또 물을 것이다)

| 조각 | 무엇 | 유형 추가 시 |
|---|---|---|
| `TBL_CODE_DTL` / `QPS_SAFERPT_GB` | 유형 목록 (9종 + DRUGRTN 대기) | 행 1개 |
| `TBL_QPS_SAFERPT` | 본문 **고정 26칸** | 손 안 댐 |
| `TBL_QPS_SAFERPT_DEF` | 체크 묶음·항목 (`RPT_GB='*'`=공유) | 행 N개 |
| `TBL_QPS_SAFERPT_USE` | 그 유형이 쓰는 묶음 + 순서 | 행 N개 |
| `TBL_QPS_SAFERPT_CHK` | 답 = `GRP_CD`+`ITEM_NM` **문자열** | — |
| **`TBL_QPS_SAFERPT_FORM`** | **신설** — 반복행 표·서명란·정형문구 | 행 0~1개 |
| **`TBL_QPS_SAFERPT_ROW`** | **신설** — 반복행 값 | — |

화면에 **유형별 하드코딩이 없다** — `qpsSafeRpt.jsp` 가 USE 를 순회해 그리고 유형 셀렉트는 공통코드다.

⇒ ***「시설환경 안전사고 발생보고서」(시설 189·190) 는 지금도 데이터만으로 흡수된다.*** 코드 0.
   (아직 시드를 안 만들었다 — 하려면 유형 1줄 + 묶음 몇 줄이면 끝난다.)

---

## 4. 새 부품 — 점검표 엔진과 **같은 이름**으로 맞췄다

`SUB_NM` · `SUB_COLS` · `SIGN_LINE` · `FOOT_TXT` — 전부 `TBL_QPS_CHK_FORM` 에서 그대로 가져온 이름이다.
두 엔진이 같은 것을 다른 이름으로 부르면 다음 사람이 반드시 헷갈린다.

- **`SIGN_LINE`·`FOOT_TXT` 는 값을 받지 않는다 — 인쇄 전용.**
  점검표 엔진([qpsChk.jsp:1353-1357](../../src/main/webapp/WEB-INF/jsp/main/qpsChk.jsp))이 이미 그렇게
  `보고자 _____________ (인)` 로 찍고 있다. **종이에 손으로 서명하는 칸**이라 저장할 값이 없다.
- **결재 장치(`TBL_QPS_APPR`)는 쓸 수 없다** — 키가 `INDI_CD+PRD_GB+PRD_KEY` 로 **지표 보고서 전용**이라
  `SRP_SEQ` 문서에 안 붙는다. 성격도 다르다(그건 전자결재, 이건 종이 서명칸).

### ★`9000` 예약대를 쓰지 않았다 — 이유

점검표 엔진이 자유행 표를 `9000+i` 에 두는 것은 **격자 값과 한 표**(`TBL_QPS_CHK_VAL`)를 공유해
번호가 겹치면 안 되기 때문이다. safeRpt 는 **전용 표**(`_ROW`)라 `ROW_NO` 가 1부터인 것이 자연스럽다.
***이유 없이 9000을 흉내 내면 다음 사람이 "어디서 9000이 나왔나"를 찾게 된다.***

---

## 5. ⚠검증 상태 — **화면 검증만 못 했다**

| | |
|---|---|
| ✅ 인라인 JS `node --check` | 통과 |
| ✅ 매퍼 XML | 구문 230개, 중복 id 없음, 새 4개 확인 |
| ✅ 자바 4개 | **배포 classpath 로 `javac` 통과**(기존 unchecked 경고만) |
| ✅ **로컬 배포 반영** | Eclipse 가 클래스·매퍼·JSP 를 이미 배포했다. `build` = **`20260814-SRPROW`** 응답 확인 |
| ✅ DB | 표 2개 생성 · 컬럼·PK·콜레이션 확인 · 임시행으로 **한글 저장 확인**(`CHAR_LENGTH`) |
| ❌ **화면 저장→복원→인쇄** | **못 했다 — 세션을 못 만들었다**(아래) |

### [2026-08-14 오후 재시도] 절반은 풀렸다 — **로그인만 사용자 몫이다**

내장 Browser 패널은 **화면 표시가 안 돼 스크린샷은 여전히 실패**하지만,
***`read_page` 와 페이지 JS 실행은 된다***(어제는 이걸 확인 못 했다). 로그인 화면까지 띄웠다 —
`http://localhost:8080/user/main` 의 폼 필드는 `hospid` · `userid` · `passwd` · `saveyn` 이다.
로컬 8080 은 살아 있고 `safeRptBase.do` 가 **`build=20260814-SRPROW`** 로 답한다(POST 전용 — GET 은 500).

⛔**로그인(비밀번호 입력)은 내가 하지 않는다.** ***사용자가 한 번 로그인해 주면 그 뒤 저장→복원→인쇄는
JS 로 확인할 수 있다.*** (패널이 안 보이면 사용자도 못 누르니, **사용자 Chrome 으로 8080 에 로그인**하고
그 브라우저에서 확인하는 편이 빠를 수 있다.)

### 왜 못 했나 — 브라우저가 갈렸다

화면 검증에는 로그인 세션이 필요한데 **자동화 브라우저와 사용자의 Chrome 이 세션을 공유하지 않는다.**
- 내장 Browser 패널 = 쿠키 별도. 게다가 **패널이 화면에 표시되지 않으면 사용자가 로그인할 수 없다**
  (스크린샷도 `the Browser pane is not displayed` 로 실패한다).
- `Claude in Chrome`(실제 Chrome, 기존 로그인 재사용)은 **확장이 이 PC 에 설치/로그인되어 있지 않다.**
- ⚠**세션 판정은 쿠키로 하지 말 것** — `JSESSIONID` 가 **HttpOnly** 라 `document.cookie` 에 안 보인다.
  ***`/qps/safeRptBase.do` 를 same-origin fetch 로 때려 `"로그인이 필요합니다"` 인지 보는 것이 확실하다.***

⇒ **다음 세션의 첫 일감**(§6 ⓐ). 화면만 열면 5분이면 끝난다.

### 남은 확인 항목 (그대로 유효)

***특히 한글 왕복을 확인해야 한다*** — `rows` 는 `chks` 와 같은 `jsonRows()` 경로를 타는데,
08-13 §5-4 가 「curl 로 넣은 한글은 깨져 저장될 수 있다 — 한글 왕복은 화면 저장으로 확인한다」고 남겼다.
반복행에는 약품명이 들어가므로 **반드시 화면에서** 확인할 것. 가짜병원 `99999998`.

**검증 방법** : `TBL_QPS_SAFERPT_FORM` 에 임시 1행을 넣어 아무 유형에나 `SUB_COLS` 를 걸고 →
화면에서 한글 품목 저장 → 다시 열어 복원 → 인쇄 → ***임시행 삭제***.
(오늘 `SECU` 로 그렇게 해 두었다가 **검증을 못 해 지웠다** — 지금 설정표는 0행이다.)

### 🧹오늘 남긴 것 없음 (확인함)

`TBL_QPS_SAFERPT_FORM` 0행 · `_ROW` 0행 · `TBL_QPS_SAFERPT` 0행 · 유형 코드 **9개**(원래대로).
테스트로 넣었던 행은 `REG_USER='claude-test'` 로 표시해 두었다가 전부 지웠다.

---

## 5-1. 로컬 환경 실측 (2026-08-14) — 기록된 것과 다르다

| | 실측 |
|---|---|
| **Eclipse 워크스페이스** | **`D:\egv`** (sejong 쪽 메모의 `C:\Users\HYUN\eclipse-workspace` 아님) |
| 배포 폴더 | `D:\egv\.metadata\.plugins\org.eclipse.wst.server.core\tmp1\wtpwebapps\wnn_medcost` |
| 톰캣 3개 | **tmp1 = wnn_medcost(8080)** · tmp0 = wnn_consult(**9090**) · tmp4 = Sejong_APP(9060) |
| **로그인 화면** | **`http://localhost:8080/user/main.do`** (`/login.do`·`/main.do` 는 404) |

⚠**`http://localhost:8080/` 는 잔재다** — 205바이트 static 페이지가 `/wnn_consult/login.do` 로 넘긴다.
포트도 안 맞아(consult 는 9090) 따라가면 막힌다. **wnn_medcost 와 무관하니 무시할 것.**
⚠**Eclipse 를 띄우면 CLI 로 고친 소스도 자동 빌드·배포된다** — 오늘 서버 Start 만으로
클래스·매퍼·JSP 가 전부 반영됐다(수동 복사 불필요했다).

---

## 5-2. ★★[2026-08-14 저녁] 남은 모듈 캡처 — **보건관리자·원무총무 완료**

| 모듈 | 캡처 | 판독 |
|---|---|---|
| **보건관리자** | ✅ **33종 + 여러 쪽 32장** → `D:\워너넷자료\caps\HB_2026-08-14\` | ✅ **끝** — [판독 문서](QPS_서식판독_보건관리자_2026-08-14.md). safeRpt 쪽 19 · LIST 12 · 연간격자 1 · 보류 1 |
| **원무총무** | ✅ **59종 + 여러 쪽 29장** → `D:\워너넷자료\caps\WM_2026-08-14\` (파일명=트리 번호 `wNN`) | ✅ **끝** — [판독 문서](QPS_서식판독_원무총무_2026-08-14.md). safeRpt 쪽 21 · 회의록 8 · 연간격자 4 · LIST 6 · 신규 다수 · **중복 3**(낙상=LAB029·AED=NUR014·UPS=FAC004, 등록 금지) · 제외 4 |
| 진단검사 | — | ✅ **여러 쪽 재점검 끝 — 39종 전부 1쪽**(빠진 페이지 없음 확증) |

***★이번에 확립한 것 — 「여러 쪽 판별」을 캡처 절차에 넣었다*** (사용자 지적) :
전체화면 캡처에는 문서 상단 **페이지 탭(1 Page·2 Page…)이 같이 찍힌다** — 탭 띠만 잘라
**콘택트시트**로 이어 붙이면 33~59종을 이미지 1~2장으로 판별할 수 있다.
보건관리자 33종 중 **11종**, 원무총무 59종 중 **16종**이 여러 쪽이었다(명부류 뒤쪽 = 연속 격자).
⚠f계열(문서 패널만 찍은 옛 캡처)은 탭이 잘려 판별 불가 — **전체화면으로 찍어야 한다.**

**캡처 함정 추가분** : 트리 항목 클릭 x 는 **120**(짧은 이름 「롤 믹스기」가 x=165 에서 글자를 지나쳐 빗나갔다) ·
「열린창 모두 닫기」 뒤 트리가 직전 문서 위치로 튀므로 **되감기는 클릭 직전에** · 원무총무처럼 긴 트리(59종)는
**스크롤 2단계**(20~39 = -8노치 · 40~ = -16노치)로 나눈다. 스크립트 : 스크래치패드 `grab_hb.ps1`·`grab_wm.ps1`(소멸 — 절차만 기억).

**원무총무 판독 때 주의** : ⚠`w20`·`w21` 이 **동명**(직원교육현황표 — w20 은 3쪽, w21 은 1쪽 = 구/신판 의심) ·
`w35 사원등록`·`w48 의료기기목록`·`w56 서명대장` 은 **페이지 탭이 아예 없다**(격자 아닌 다른 UI — 열어 봐야 안다) ·
운영위원회회의록이 **8종**(월별 4 + 본판 + 규정 + 중독연구소 + 인사위원회) — 회의록 `FORM_GB` 계열(세션요약 §회의록)과 대조 ·
`w54 직원 교육 결과 보고서` = 부서 공통 후보 **5곳째**.

## 5-3. ★★[2026-08-14 밤] **SUNWOO 모듈 18개 전체 캡처·판독 완료**

- ✅ 오늘 마지막 11개까지 끝 — [기타모듈 판독](QPS_서식판독_기타모듈_2026-08-14.md)(요약표+대조 목록+절차서 통짜).
  기능 화면 4(관리자·규정집·DUTY·인증) · 공용 일치 1(보고서) · 서식 모듈 6(진료 3 · **인공신장 42** ·
  **의무기록 30** · **방사선사 25** · 물리재활 6 · **사회복지 45항목**).
- **기능 화면 4개의 처리 방향**(서식이 아니라 **범위 결정 사안** — 회의/사용자 결정 후 착수) :
  | 모듈 | 정체 | 방향 |
  |---|---|---|
  | 관리자 | 기초코드·사용자·권한·서식관리 등 29건 | ⚠**위너넷 관리자 기능과 중복**(사용자 확인) — 구현 시 어느 쪽이 정본인지부터. **위원회 구성(024)만** QPS 기준정보로 가져올 후보 |
  | 인증 | 규정집과 **같은 PDF 관리 화면**(조직도+위원회 탭) | 규정집과 한 몸으로 판단 — 병원확인목록 #17(규정집 범위)과 같은 결정 |
  | DUTY | 월간 근무표 편성(엑셀 IO·결재란) | 점검표 엔진 밖 — 이식하면 **별도 기능 개발**. 필요 여부부터 확인 |
  | 규정집 | PDF 업로드·열람(등록 0건) | 병원확인목록 #17 그대로 |
- ⚠함정 셋(다음 캡처 때) : 모듈이 **SUNWOOPass 자식 창**으로 뜨는 것이 있다(EnumWindows 로 찾기) ·
  의료기기 점검표 로딩 **20~40초**(제목 폴링) · PDF 계열은 로딩 중 창 제목이 수십 초 빈다.
- **캡처 총량** : `D:\워너넷자료\caps\` 아래 **14개 폴더**(LAB·HB·WM + AD·RG·DT·RP·RN·MR·DR·XR·CT·PT·SW).
- **다음 = 시드 작업 순서** : ①대조 목록(기타모듈 문서 머리) 처리 ②직원 교육 결과 보고서 **공용판 9곳째 확정** —
  사진첨부 구현과 함께 `EDURPT` 로 ③인공신장·방사선사(격자 다수 — 기존 축으로 대부분 담김) ④의무기록·사회복지(safeRpt 쪽 다수).

### ★사용자 요청사항 2건 (2026-08-14 밤 — 다음 작업 때 반영)

1. ~~QPS JSP 그룹핑~~ — ✅**끝**(2026-08-14 밤, 사용자 지정 이름 **`qpsmgr`**).
   `qps*.jsp` **28개**를 `jsp/main/qpsmgr/` 로 `git mv`(rename 추적), `QpsController` 뷰 문자열 **29곳**을
   `.main/qpsmgr/qpsXxx` 로 치환. 그 밖의 참조는 **없음을 전수 확인**했다 —
   인클루드는 절대경로(`/WEB-INF/jsp/main/inc/qpsFileBox.jsp`, `inc/` 는 안 옮겼다) · 사이드바는 `.do` URL ·
   타일즈는 기존 2단 패턴(`.main/*/*`)이 그대로 받는다(`mangr/` 전례). `build=20260814-QPSDIR`.
   ⚠**배포 = WAR 재빌드+재기동**(자바 변경). ⚠**Eclipse 배포 폴더에 옛 경로의 qps*.jsp 가 남는다** —
   지워지지 않고 접근도 안 되지만(뷰가 새 경로) 헷갈리니 **Servers ▸ Clean...** 권장.
   ✅**화면 검증 끝**(재기동 뒤) — **28개 화면 전수**를 렌더까지 확인했다.
   ★방법(로그인 불필요 — 다음에도 이렇게) : `qpsScreen` 가드는 세션이 아니라 **`s_hospid` 일반 쿠키**다 —
   가짜병원 쿠키(`s_hospid=99999998; s_wnn_yn=Y`)를 실어 `GET /main/qpsXxx.do` 를 때리면
   컨트롤러→타일즈 2단 패턴→`qpsmgr/*.jsp` Jasper 컴파일→렌더 **전 체인**이 돈다.
   28개 전부 200 · 160~250KB · 로그인화면 아님 · 표본 4종(qpsChk `ckGridWrap`·qpsChkForm `f_grpPrd`·
   safeRpt·지표현황)은 **화면 고유 마커까지 확인**. 배포 폴더도 qpsmgr 28개·옛 잔재 0(Clean 완료 상태였다).
2. **관리자 모듈 = 위너넷(WinCheck+ 관리자 기능)과 중복** — SUNWOO 관리자의 기초코드·사용자·권한·서식관리는
   WinCheck+ 에 이미 대응 기능이 있다. **구현 시 겹치는 것(어느 쪽을 정본으로 쓸지) 확인 후 진행.**
   위원회 구성(024)처럼 QPS 기준정보로 쓸 것만 골라 온다.

### 캡처 자료를 다른 PC 로 옮겨야 하나 — **판독 안 끝난 것만 필요하다**

| | 다른 PC 에 필요한가 |
|---|---|
| **git 문서**(판독·설계·시드) | ✅ push/pull 로 간다 — 판독이 끝난 모듈은 **문서만으로 시드 작업 가능** |
| **`D:\워너넷자료\caps\`** (이 PC 로컬) | ✅**[2026-08-14] 사용자가 다른 PC 로 복사 완료**(14개 폴더 · 약 108MB). 판독 문서가 참조하는 캡처 파일명(`RN07.png` 등)을 그 PC 에서도 열어 볼 수 있다 — **경로만 그 PC 의 복사 위치로 읽을 것** |
| 스크래치패드 스크립트 | ❌ 소멸 — 절차서(위 문서)에 통짜로 보존해 뒀다 |

## 5-4. ★★[사용자 지시 2026-08-14 마감] 다음 재개 첫 작업 = **사진첨부 구현** (집 PC 에서)

> ### ✅[2026-08-14 심야 · 집 PC] 사진첨부 **구현 완료** — 남은 것은 재기동 후 화면 확인
>
> | | |
> |---|---|
> | **DDL** | ✅ [QPS_DDL_SAFERPT_PHOTO_2026-08-14.sql](../sql/QPS_DDL_SAFERPT_PHOTO_2026-08-14.sql) — **운영 적용 완료**(FORM.PHOTO_YN + `TBL_QPS_SAFERPT_FILE`, 한글 주석 CHAR_LENGTH 확인). ***다시 돌리지 말 것*** |
> | **매퍼** | ✅ `selectSafeRptForm` 에 photoyn · `selectSafeRptFile`/`upsertSafeRptFile`/`deleteSafeRptFile` 3쿼리(XML 정형성·중복 id 없음 확인) |
> | **자바** | ✅ Mapper·Service·Impl(`files` 동봉·saveSafeRptPhoto/deleteSafeRptPhoto)·Controller(`safeRptPhotoUpload/Delete.do`, **소유 확인 후** sftp `QPS/{병원}/SAFERPT_PHOTO/{문서}` 저장). `BUILD=`**`20260814-SRPHOTO`** |
> | **화면** | ✅ qpsSafeRpt.jsp — `PHOTO_YN='Y'` 유형만 카드 표시(기본 숨김), 2×2 칸 클릭=업로드·재업로드=교체·✕ 지우기, **개인정보 안내 1줄**, 인쇄 2×2(원본 비율·있는 줄만·사진 로드 대기 후 print). 인라인 JS `node --check` 통과 |
> | ⚠**표시 방식** | `/sftp/download.do` 가 attachment 강제라 `<img src>` 직결 불가 → **fetch→blob→objectURL**(월보고서 PDF 와 같은 해법). 인쇄창도 같은 blob URL 사용(같은 출처 창이라 접근됨) |
> | ⛔남은 검증 | 자바 CLI 컴파일은 사용자 취소 — **Eclipse 자동 빌드로 확인**(F5→서버 기동, build 응답으로 판별). 화면 왕복(업로드→복원→인쇄)은 재기동 뒤 — **EDURPT 가 적재돼 있어 임시행 불필요**(그 유형으로 바로 검증). sftp 는 로컬 Windows 에서 원격(114.108.153.178:22)으로 붙는다 |
> | 배포 | **WAR 재빌드+재기동 필수**(자바+매퍼) — 운영 대기분에 합류(§0 이틀치 + 오늘분) |
>
> ### ✅[같은 세션 이어서] **라벨 오버라이드(LBL_JSON) + EDURPT 시드** — §5-4 ③ 까지 완료
>
> - **왜 라벨 장치가 먼저였나** : EDURPT 는 본문 칸은 다 맞는데 **이름이 사고 서식**(발생일·사건경위…)이다.
>   화면·인쇄가 「교육일시·교육내용」으로 나와야 서식이 된다 — 유형별 하드코딩 대신
>   **FORM 한 칸(`LBL_JSON`)** 으로 데이터화했다(상담일지 계열 19종도 이 장치를 쓴다).
> - **DDL** : [QPS_DDL_SAFERPT_LBL_2026-08-14.sql](../sql/QPS_DDL_SAFERPT_LBL_2026-08-14.sql) — ✅**운영 적용**.
>   키 = 화면 `data-lbl`(occurDt~note 22키), **값 `'-'` = 그 칸을 화면·인쇄에서 숨김**. 비면 종전 그대로.
> - **화면** : qpsSafeRpt.jsp — 본문 라벨 전부에 `data-lbl`, `applyLabels` 가 기본 라벨 복원→직원 보정→JSON
>   오버라이드 순으로 적용. 육하원칙 6칸을 전부 숨긴 서식은 **사건개요 카드째** 걷는다(`cardSix`).
>   인쇄 머리표를 [라벨,값] 짝 조립식으로 재작성(숨긴 칸은 짝 자체를 안 만든다) · 서술 줄도 `rowL()`.
> - **EDURPT 시드** : [QPS_SAFERPT_SEED_EDURPT_2026-08-14.sql](../sql/QPS_SAFERPT_SEED_EDURPT_2026-08-14.sql) —
>   ✅**운영 적재**(JSON_VALID·한글 왕복 확인). 유형 1행(SORT 20) + FORM 1행(`PHOTO_YN='Y'`+LBL_JSON).
>   대응 : 교육일시→OCCUR_DT · 교육장소→PLACE · 교육자→TARGET_NM · 교육대상자→DEPT_NM ·
>   교육방법→POSITION_NM · 교육주제→DIAG_NM · 교육내용→SUMMARY · 첨부자료→NOTE. 체크 묶음 없음.
>   결재란 4칸은 병원 결재선(APPR_LINE)이 그대로 찍는다. ⚠옛 WAR 에서는 유형만 뜨고 사고 라벨로
>   보인다(이식 단계라 병원 노출 없음 — 재배포로 해소).
>
> ### ✅[같은 세션 이어서 2] **연간(월12) 격자** — §5-4 순서 4까지 완료 (설계 §② 전부 소화)
>
> - **`chkFormSave` 한 줄** : 날짜 격자에서 `PRD_KIND='M'` 이면 `PRD_GB='Y'`(연 문서) — 설계 §②의
>   「막힌 곳 한 군데」 해소. **자바 변경** ⇒ WAR 재빌드 대상(사진첨부와 같은 배포분).
> - **시드** : [QPS_CHK_SEED_YEARGRID_2026-08-14.sql](../sql/QPS_CHK_SEED_YEARGRID_2026-08-14.sql) —
>   ✅**운영 적재**(6종·항목 100). 부서 코드 2건 신설(**HEALTH 보건관리자 · ADMIN 원무·총무**).
>   | 서식 | 원본 | 갈래 |
>   |---|---|---|
>   | `HLT001` | h30 관리활동 계획서 | ITEM_MONTH + POST(예산·비고) |
>   | `ADM001` | w19 직원교육 연간계획서 | ITEM_MONTH + DESC(교육담당) + POST(대상·예산) |
>   | `ADM002`/`ADM003` | w23/w25 (같은 주제 **2벌** — 병원확인 #38) | ITEM_MONTH |
>   | `ADM004` | w55 의료기기 예방점검 | ITEM_MONTH + **PRE**(점검업체·시행) + POST(예산) |
>   | `LAB030` | 진단검사 12 혈당측정기 | **DAY_ITEM·Y·M·PRD_SUB(용액A,B)** — 연 문서·월행 |
> - **원본 축약 3가지**(시드 머리말에 상세) : h30 3단 계층→2단(`이름 - 꼬리`) · w19 하단 블록→HEAD_NMS/FOOT_TXT ·
>   인쇄된 업체·대상 값은 문서 값이라 제외(FAC003 전례). 병원확인 **#38·#39** 로 올림.
> - **검증** : qpsChk.jsp 의 실제 격자 함수(prdCells/prdFlat/usesMm)를 Node 로 태워 운영 적재값을 물림 —
>   LAB030 = **연 문서(월 셀렉트 없음)·12월×용액A/B 24칸**, ITEM_MONTH 5종 = 12칸. ⛔브라우저 확인은 재기동 뒤.
>
> ### ✅[같은 세션 이어서 3] **보건관리자(HB) 시드 완주** — 33종 중 담을 수 있는 것 전부
>
> - [QPS_SEED_HB_2026-08-14.sql](../sql/QPS_SEED_HB_2026-08-14.sql) ✅**운영 적재** :
>   **LIST 10종**(`HLT002~011` = h04~h08·h10 명부 6 + h12·h20·h21·h22) +
>   **safeRpt 유형 4종**(`CONSULT` 검진 이상자 상담일지 — **h14~16·h18·h23~28 열 장이 한 판** ·
>   `CONSULTB` B형간염 · `CONSULTF` 유소견자 · `HLTRPT` 결과보고서 — 전부 **LBL_JSON** 라벨,
>   상담일지 2종은 **PHOTO_YN='Y'**). h30=`HLT001` · h33=`EDURPT` 는 앞 블록에서 완료.
> - **LIST 실측 확인** : 열 묶음(GRP_NM) **2단까지** 지원(qpsChk L606) · 행 번호 자동 ·
>   블록 띠 `ROW_BLKS`(h20 부서 5블록 · h22 「추가관리 대상자」 구간) 그대로 담김.
> - ⛔**보류 6종**(시드 머리말에 이유) : 보고서 5종(h01~03·09·11 — **본문 중간 집계표**가 SUB_COLS 로
>   되는지 화면 검증 대기) · h17 치료안내문(2쪽 고정문 — 자료실 후보) · h19 업무일지(판정 보류) ·
>   h29 관리대장(**3단 머리** — LIST 는 2단까지).
> - 한계 4가지(명부 결재란 4칸은 엔진 밖 · h20 블록명은 캡처 병원 구성 · h22 첫 띠 이름 창작 ·
>   상담일지 질환별 기본 문구 미담김)는 시드 머리말 ⓐ~ⓓ.
>
> ### ✅[같은 세션 이어서 4] **원무총무(WM) 시드 완주** — 59종 처리
>
> - [QPS_SEED_WM_2026-08-14.sql](../sql/QPS_SEED_WM_2026-08-14.sql) ✅**운영 적재** :
>   **회의록 위원회 코드 3건**(`QPS_FORM_GB` W·C·P) + **점검표 8종**(`ADM005~012` : LIST 6 · ITEM_DAY 1 · EQUIP_DAY 1)
>   + **safeRpt 유형 17종**(w09·11·14·16·17·18·24·27·30·31·32·33·39·40·50·52·58) + 체크 묶음 6유형 37항목.
>   연간 격자 4종(w19·23·25·55)은 YEARGRID 시드에서 완료 ⇒ **safeRpt 유형 누계 31종.**
> - ★**회의록은 엔진 변경 0** — 표(TBL_QPS_MINUTES)에 `MEET_GB`(정기/임시)·`CLERK_NM`(간사)·`AGENDA`·
>   `DECISION`·`MEMBERS`(참석자 서명)·`ATTACH_TXT` 가 **이미 다 있다**(실측). w01~06 은 한 판이라
>   **위원회 코드 하나**로 받는다(월은 회의 일시가 담는다).
> - ⛔**등록 금지 3종**(w36=LAB029 · w53=NUR014 · w59=FAC004) · **이식 제외 4종**(내규·조직도=자료실,
>   사원등록·의료기기목록=마스터 화면) · **보류 8종**(w12·15·20/21·29 = 「항목×날짜아닌 고정열」이나
>   **셀 타입이 갈려**(수치/5택1/체크/점수+합계) 조각 설계 후 · w13 반복행 7벌 · w42 · w43~45 · w46/47 라벨).
>
> ### ✅★★[검증 완료 2026-08-14 심야] 로컬 8080 재기동 — **사진첨부 왕복 실증**
>
> `build=`**`20260814-SRPHOTO`** 확인. 가짜병원 `99999998` 로 전 경로 확인하고 **테스트 자료는 지웠다**(DB 0행).
>
> | 확인 | 결과 |
> |---|---|
> | 화면 렌더 | `qpsSafeRpt.do` 200 · 196KB · `cardPhoto`·`ph-grid`·`data-lbl`·`cardSix`·개인정보 안내문 **전부 있음** |
> | EDURPT 라벨 | 서버가 `photoyn='Y'` + LBL_JSON 22키를 그대로 응답 |
> | **한글 왕복** | 저장→복원 : 교육주제·교육자·교육대상·교육내용 **깨짐 없음** |
> | **사진 업로드** | OK — `/home/winner/upload/QPS/99999998/SAFERPT_PHOTO/{seq}/{uuid}_교육사진.png` (**한글 파일명 보존**) |
> | **같은 칸 재업로드** | 행이 안 늘고 **교체**(UPSERT) 확인 — 1건 유지, 원본명만 바뀜 |
> | 다운로드 | `/sftp/download.do` 200 · **70 bytes = 원본과 일치** |
> | 그림 아닌 파일 | 거부 — "그림 파일(JPG·PNG 등)만…" |
> | 시드 서식 9종 | `chkFormGet` 으로 전부 정상(LAB030 `DAY_ITEM/Y/M` · HLT009·HLT011 블록 문자열까지) |
>
> ⛔**남은 것은 눈으로 보는 것뿐** — 사진 2×2 배치·인쇄물 모양·연간 격자 12칸 그리드.
> (⚠파일서버의 테스트 사진 2개는 남는다 — 메타만 지우는 공통첨부 정책 그대로.)
>
> ### ✅[같은 세션 이어서 5] **인공신장(RN) 시드** — 42종 중 25종 등록
>
> - [QPS_SEED_RN_2026-08-14.sql](../sql/QPS_SEED_RN_2026-08-14.sql) ✅**운영 적재**(`RNL001~025`, 부서 `RENAL` 신설) :
>   EQUIP_DAY 5 · ITEM_DAY 8(**주기복합 4**) · DAY_ITEM 6(**PRD_SUB 시간대 4**) · ITEM_MONTH 5 · LIST 1.
> - ★**등록 금지 7종 — 이름이 아니라 「항목 문자열」로 DB 대조해 확정** :
>   RN38=`LINEN` · RN40=`NUR014` · RN41=`NUR025`(5항목 글자까지) · RN26=`PHA025~028`(제형 4벌) ·
>   RN27=`PHA024` · RN33=`NUR011`(13열) · RN34=`NUR012`(9열). **부서 공유로 쓴다.**
>   ⚠반대로 **RN24 멸균물품장은 `NUR008` 과 항목이 달라 별건**이었다 — 이름만 보고 뺐으면 잃을 뻔했다.
> - ⛔**보류 10종**(시드 머리말) : RN08·09·10 검사 성적서 · **RN16**(장비행×체크+결과 2단 = 원무총무
>   w43~45 와 같은 계열 ⇒ 「항목×날짜아닌 고정열」 조각에 합산) · RN25 복합 · RN28·29(약국 대조) ·
>   RN31(행별 유효기간 = 앞/뒤 열 조각) · RN37(혈액 반납 — DRUGRTN 확인 2건과 함께) · RN42=`EDURPT` 중복.
> - **검증** : `chkFormGet` 로 주기복합(`매일 1회>D,주 1회>N,분기별>1,년 1회>1,즉시>1`) ·
>   시간대 세분(`9am,5pm,2am`) · 주차 격자(`kind=N`) 전부 정상 응답 확인.
> - ⚠**냉장고 4종은 기존 등록분과 같은 구조·다른 부서** — 「부서마다 다른 판을 쓰나」는 병원확인 **#36** 과 같은 사안.
>
> ### ✅[같은 세션 이어서 6] **의무기록(MR)·방사선사(XR) 시드**
>
> | 모듈 | 등록 | 파일 |
> |---|---|---|
> | **의무기록** | 점검표 **11**(`MRC001~011` : LIST 7·DAY_ITEM 2·ITEM_MONTH 2) + safeRpt 유형 **15**(`MRPROXY`~`CCTVREQ`) + 체크묶음 8 | [QPS_SEED_MR](../sql/QPS_SEED_MR_2026-08-14.sql) |
> | **방사선사** | 점검표 **21**(`RAD001~021` : ITEM_DAY 10·DAY_ITEM 5·EQUIP_DAY 1·ITEM_MONTH 1·LIST 3·**ITEM_COL 1**) | [QPS_SEED_XR](../sql/QPS_SEED_XR_2026-08-14.sql) |
>
> - **부서 코드 2건 신설** — `MEDREC`(의무기록) · `RADIO`(방사선). **safeRpt 유형 누계 46종.**
> - MR 은 **법정 양식 3종**(의료법 시행규칙 별지 제9호의3 위임장 · 개인정보보호법 유출신고/통지)을 포함한다 —
>   근거 조문을 `FOOT_TXT` 로 그대로 실었다.
> - XR **TLD 관리대장은 `ITEM_COL`** 로 담았다(분기 4열 = 날짜 아닌 고정 열) — 이 축의 첫 실사용.
> - ★**같은 판이어도 따로 넣은 것** : XR04↔12(CR/DR — 열 1개와 **모델명**이 다르다) ·
>   XR14↔16(고주파온열암/고압산소). **모델명이 서식에 박혀 있어** 한 벌로 묶으면 병원이 장비를 못 가른다.
> - ⛔**MR 제외 3**(MR29 껍데기·MR30=`EDURPT` 중복·MR13 기능 조사화면) · **MR 보류 1**(MR14 통계표 2벌) ·
>   **XR 제외 1**(XR25=`EDURPT`) · **XR 보류 3**(XR07 사진 2쪽 — 점검표엔 사진칸이 없다 ·
>   XR18 엑셀 연동 그리드 · XR23 **문서 키가 「사람 1명」** — 기존 키 설계에 없다).
> - ⚠원본 오타·중복 행(「Aaging」·「Gnadll shield」·「Gird Moving」·「대기 의자」 2행·「인터폰작동」 2그룹)은
>   **그대로 옮겼다** — 병원이 쓰던 종이와 글자가 달라지면 안 된다.

> 사무실 PC 작업은 여기서 마감했다(전부 커밋됨 — `git pull` 부터).
> **"QPS 이어서" = 아래 순서대로 사진첨부를 구현한다.** 배경 재설명 불필요.

1. **설계 정본** : [QPS_사진첨부_연간격자_설계_2026-08-14.md](QPS_사진첨부_연간격자_설계_2026-08-14.md) §① — 그대로 구현한다.
   요지 : `TBL_QPS_SAFERPT_FILE`(SRP_SEQ·FILE_SEQ 1~4·FILE_PATH·ORG_NM) 신설 + `TBL_QPS_SAFERPT_FORM.PHOTO_YN` 한 칸.
   저장은 **기존 sftp 파일서버**(`/sftp/upload·download.do` — 월보고서 PDF 와 같은 장치) 재사용. 새 저장소 금지.
2. **손댈 파일** : DDL 1(docs/sql, 더하기만) · `Qps_SQL.xml`(FORM 조회에 PHOTO_YN + FILE 4쿼리) ·
   `QpsServiceImpl`/`QpsController`(safeRptSave 에 파일 연결·삭제) · `qpsSafeRpt.jsp`(2×2 칸·업로드·인쇄).
   ⚠**개인정보** : 업로드 안내문구 1줄 + 병원별 폴더(설계 §① 그대로).
3. **이어서** : `EDURPT` 공용 유형 시드(직원 교육 결과 보고서 — **9곳 확인된 공용판**. 본문 칸 대응은
   보건관리자 판독 h33 절 참조) → 연간 격자는 §② 대로 **시드 위주**(`chkFormSave` 한 줄 포함).
4. **검증** : 화면 로그인 없이 가능 — **가짜병원 쿠키 렌더 검증법**(§5-3 사용자 요청 1 에 적어 둔
   `s_hospid=99999998` 쿠키 + `GET /main/qpsSafeRpt.do`). 저장 왕복은 DB 로(08-14 주기 복합 전례).
   업로드는 sftp 가 붙는 환경이어야 한다 — 안 붙으면 그 부분만 표시하고 남긴다.
5. **환경 주의(집 PC)** : 캡처 사본은 사용자가 복사해 둔 위치로 경로만 바꿔 읽는다(§5-3) ·
   DB 도구는 08-11 §3 `RunSql.java` 레시피로 재생성 · 프로파일 `local`=**원격 운영 DB**다(§0 함정).
   ⚠DDL 적용 전 `QPS_DDL_` 최신분이 이미 운영에 들어가 있는지 §0 표부터 볼 것.

## 6. 다음 세션 첫 지시

> **"QPS 이어서"** — 아래에서 고른다.
> ★**환경부터 보라** : ⓪ 계열은 **어제 그 노트북**에서만 된다(§1).

| 후보 | 무엇 | 이 PC 에서? |
|---|---|---|
| ~~★ⓐ 진단검사 재캡처·판독~~ | ✅**끝**(§1-0 오후) | — |
| ~~★ⓐ″ 주기 복합 구현·진단검사 시드~~ | ✅**끝** — DB 적용까지 | — |
| **★ⓐ‴ 브라우저 검증** | ①**LAB011 인쇄**(표가 여러 벌이라 처음 겪는 모양) ②서식 관리 미리보기 ③safeRpt 한글 왕복(§5)<br>①의 **저장·복원·한글은 DB 로 이미 확인**했다 — 남은 것은 **눈으로 보는 것**이다 | ⛔ **사용자 로그인 필요** — 아래 |
| ~~★ⓑ′ 대조 5건~~ | ✅**끝**(판독 문서 §4-3) — 결과 : 9·10 은 간호와 **같은 종이**(LAB008 뺌·9 미등록) ·
26 은 **별건**(`LAB029` 등록) · 32 별건 확인 · 11 은 safeRpt 몫 · 24 는 병동 서식 ·
39 는 등록된 교육 서식이 없어 safeRpt 사진첨부 조각이 먼저.
**부서 소속 어긋남 발견**(NUR018·NUR029 = 임상병리 서식이 간호 소속) → 병원확인목록 #35~37 로 올림 | — |
| ~~ⓑ 남은 모듈 캡처~~ | ✅**보건관리자·원무총무 끝**(§5-2). 보건관리자는 **판독까지** 끝 | — |
| ~~★ⓒ′ 원무총무 판독~~ | ✅**끝**(§5-2 표) — 시드는 다음 세션. ⚠재캡처 2장(`w20_p2·p3`·`w49_p2`) 먼저 | — |
| ~~★ⓓ′ safeRpt 확장 2건 설계~~ | ✅**끝** — [설계](QPS_사진첨부_연간격자_설계_2026-08-14.md). ①사진첨부 = `_FILE` 표+`PHOTO_YN`+파일서버 재사용 ②연간 격자 = **새 조각 불필요** — 항목×월 5종은 기존 `ITEM_MONTH` 그대로, 혈당측정기만 `chkFormSave` 한 줄(`PRD_KIND='M'` 때 `PRD_GB='Y'` 허용). 재캡처 2장(w20_p2·p3/w49_p2)도 완료(탭 검증) | — |
| ★ⓔ′ 보건관리자·원무총무 시드 | 판독 완료분에서 — 회의록 신형(`FORM_GB`)·w13 반복행 7벌 한계·w37 기기 키는 설계 먼저 | ✅ |
| ⓑ 남은 모듈 캡처·판독 | ~~노트북에서만~~ → **이 PC 에서 가능해졌다**(§1-0). 감염은 완료된 모듈이므로 다음은 보건관리자·원무총무 등 | ✅ |
| ⓒ 시설환경 안전사고 유형 시드 | 데이터만. 코드 0 (§3) | ✅ |
| ⓓ DRUGRTN 시드 확정 | **확인 2건** 먼저 — §7 | ⛔ 캡처 필요 |
| ⓔ 「블록 반복」 구현 | 보류 4종의 공통 뼈대. [v3 설계 §11](QPS_점검표엔진_v3_설계_2026-08-12.md) — 설계만 있다 | ✅ |
| ⓕ QPS 지표 잔여 4종 | 신체보호대(NUR050)·재택복귀·불만고충·만족도 | ✅ |
| ⓖ 위너넷 협의 반영 | [병원확인목록](QPS_병원확인목록_2026-08-13.md) 34항목 | 답이 와야 |
| ⓗ 운영 WAR 재배포 | **사용자 몫** — §0. **이틀치가 쌓였다** | — |

---

## 7. 막혀 있는 확인 2건 (DRUGRTN 시드)

[QPS_SAFERPT_SEED_DRUGRTN_2026-08-14.sql](../sql/QPS_SAFERPT_SEED_DRUGRTN_2026-08-14.sql) 머리말에도 적어 두었다.

1. **[290]「의약품 반납 신청서」(간호/병동) ↔ 약국「병동의약품 반납신청서」가 같은 것인가**
   판독 §385 가 ***이름이 다르다***고 적었고 **캡처로 본 것은 간호/병동 판뿐**이다.
   같으면 유형 하나, 다르면 둘로 갈라 `SUB_COLS`·`SIGN_LINE` 을 각각 맞춘다.
2. **[274]「병동의약품 신청서」**(반납 아님) — 채록이 `신청 일자·병동` 뿐이라 **정보가 모자란다.**

둘 다 **SUNWOO 캡처가 있어야 풀린다** ⇒ 노트북 세션 몫.

---

## 8. 오늘 새로 배운 함정

| 함정 | 한 줄 |
|---|---|
| **도구는 PC 마다 다르다** | 「어제 됐으니 오늘도 된다」가 아니다 — 세션 시작 때 **환경부터** 본다 |
| **브라우저가 두 개다** | 자동화 브라우저 ≠ 사용자 Chrome. **세션을 공유하지 않는다**(§5) |
| **로그인 판정은 쿠키로 하지 말 것** | `JSESSIONID` 가 HttpOnly 라 `document.cookie` 에 안 보인다 — API 응답으로 본다 |
| **결재 장치는 재사용 못 한다** | `TBL_QPS_APPR` 키가 지표 전용(`INDI_CD+PRD_GB+PRD_KEY`) |
| **서명은 저장할 값이 아니다** | 종이 서명칸이다 — 점검표 엔진이 이미 인쇄 전용으로 풀어 놨다 |
| **예약번호를 흉내 내지 말 것** | 9000은 *한 표를 공유해서* 생긴 번호다. 전용 표면 1부터가 맞다 |
| **`반납경위` ≠ 육하원칙 6칸** | 「육하원칙에 따라 기록」*안내문구가 붙은 큰 칸 하나*다 → `SUMMARY` |
