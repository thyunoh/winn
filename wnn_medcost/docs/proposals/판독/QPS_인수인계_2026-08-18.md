# QPS 인수인계 — 2026-08-18

> ## 다음 세션은 여기부터
>
> | 먼저 | 무엇 | 누가 |
> |---|---|---|
> | **①** | ⛔***운영 WAR 재빌드*** — 오늘 것이 전부 여기 걸려 있다(클래스·매퍼 변경). 이걸 안 하면 **병원 화면은 어제 그대로**다 | 사용자 |
> | ② | safeRpt 시드 3종 실행 — `STAFFVIO`·`PSYIMPR`·`PSYVISIT` ([파일](../../sql/qps/seed/QPS_SAFERPT_SEED_PSY_2026-08-18.sql)) | 사용자 |
> | ③ | **결정거리 하나** — 병원이 한 번 저장한 뒤 들어온 새 표준 서식을 자동으로 켤지(§3-0b) | 위너넷 |
> | ④ | 남은 이식 — `UT01~04` 감염환자 기록지 · `PS04`·`PS05` 격리·강박 기록지(**다른 판 확정** §5-③) | 에이전트 |
>
> ★***읽는 순서*** : **§2-5 자리 나눔** → §6 지표분석보고서 → §4 함정.
> 오늘 정해진 큰 줄기 둘 :
> 1. ***설정 화면은 병원에 내지 않는다*** — 「병원은 설정 못 한다, 전산 요원이 없어서」(§2-5).
>    병원에 보이는 점검표 메뉴는 **[점검표 작성] 하나**다.
> 2. ***새 화면을 만들기 전에 있는 것을 찾는다*** — 오늘 「부서 관리」와 「지표분석보고서」 두 번,
>    만들려던 화면이 **이미 있었다**(§2-5 · §6).

---

## 1. ✅부서 점검표 3종 — **등록 완료** (2026-08-18 저녁)

| SUNWOO | 우리 서식 | 부서 | 항목 |
|---|---|---|---:|
| `RD02` 외래 | **`CLI001`** | `CLINIC`(진료) ★사용자 지정 | 27 |
| `RD03` 병동 | **`NUR069`** | `NURSE` | 27 |
| `RD04` 영양 | **`NUT017`** | `NUTRI` | 19 |

- 분류는 셋 다 `ENV`(환경·시설) · 축 `ITEM_COL` · 고정 열 **`상태>양호,상태>불량,조치내용(불량 시)`**(2단 머리글) ·
  맨 아래 **특 이 사 항**(`NOTE_YN='Y'`).
- ★***원본의 왼쪽 두 칸을 이렇게 옮겼다*** : 「구분」 → **블록 띠**(`BLK_NM`, 가로 띠) ·
  「점검항목」 → **왼쪽 세로 묶음**(`GRP_NM`) · 「점검 내용」 → `ITEM_NM`.
  ⚠원본은 「구분」도 세로 칸이지만 엔진의 바깥 단은 **가로 띠**다 — ***정보는 그대로, 모양만 다르다.***
- ⚠`소화기 정 위치` 행 셋은 **`INPUT_GB='NUM'`**(단위 `개(투척소화기포함)`) — `CHECK` 면 숫자 1 이 O 로 바뀐다.
- ★**외래 항목 문구는 판독 문서에 없어 캡처에서 직접 읽었다**(`RD02_p2.png`) — 판독의 「25항목」은
  어림이었고 **실제 27항목**이다.
- 시드 = [QPS_CHK_SEED_RD_2026-08-18.sql](../../sql/qps/seed/QPS_CHK_SEED_RD_2026-08-18.sql)
  ⛔***아직 운영에 안 돌렸다*** — 돌려야 공통 서식이 된다(§4 함정).
- 검증 : `chkBase` 로 셋 다 열림(27·27·19 · 열 정의·특이사항 이름 확인).

<details><summary>옛 절차 메모(등록 전에 쓰던 것)</summary>

### (이전) 바로 이어갈 작업 — 부서 점검표 3종

### 입력 원본
[QPS_서식판독_신규분_2026-08-18.md](QPS_서식판독_신규분_2026-08-18.md) **§3-2** 에 항목 문구가 전부 적혀 있다.
- `RD02` 외래 25항목 (§3-2 본문) · `RD03` 병동 27항목 (§3-2-2) · `RD04` 영양 19항목 (§3-2-3)

### 절차 — `INF001`·`INF002` 로 이미 검증된 흐름
로컬 `http://localhost:8080/main/qpsChkForm.do` (쿠키 `s_hospid=99999998` · `s_wnn_yn=Y` · `s_userid=…`)

1. **[＋ 새 서식]** → 서식 정의 채우기
   - 부서 : `NURSE`(병동) / `NUTRI`(영양) / 외래는 `COMMON` 또는 `CLINIC` — **판단 필요**
   - 분류 : `ENV`(환경·시설) 쪽이 맞다(요로감염 2종은 `SAFE` 를 썼다)
   - 표 모양 : **`ITEM_COL`** · 고정 열(`f_colNms`) = `양호,불량,조치내용`
   - 행 묶음 : **왼쪽 세로 칸**을 켜고 「구분」을 `GRP_NM` 으로 넣는다 (요로감염 2종은 묶음이 없었다 — ***여기서 처음 쓴다***)
   - 표 위 안내(`f_guideTxt`) · 하단(`f_footTxt`) = 「특 이 사 항」
   - **[자동]** 눌러 서식코드 발번
2. **[📥 일괄 붙여넣기]** → `cfPasteTxt` 에 점검 내용을 줄바꿈으로 넣고 **[넣기]**
3. ⚠**`소화기 정 위치` 행만 입력종류를 `NUM` 으로 바꾼다**
   `CHECK` 로 두면 ***숫자 「1」이 「O」로 바뀐다***(대장 §2 경고). 오류가 안 나서 티도 안 난다.
4. **[저장]** → 서버 목록으로 확인
   `curl -b s_hospid=99999998 -X POST http://localhost:8080/qps/chkFormList.do -d ""`
5. **[💾 시드 SQL 내보내기]** → `docs/sql/qps/seed/QPS_CHK_SEED_<부서>_2026-08-18.sql`
   내보내기는 `HOSP_CD='*'`(공통)로 나온다 — 그대로 쓰면 된다.

### 이미 만든 것 (참고 틀)
[QPS_CHK_SEED_INFECT_2026-08-18.sql](../../sql/qps/seed/QPS_CHK_SEED_INFECT_2026-08-18.sql) — `INF001`(11문항)·`INF002`(6문항)

</details>

---

## 2. 오늘 끝난 것

| 갈래 | 결과 |
|---|---|
| 캡처 | 46종 / PNG 102장 — `D:\위너넷\caps\QPS_2026-08-18\` + `manifest.txt` |
| 판독 | [신규분](QPS_서식판독_신규분_2026-08-18.md) · [변경대조](QPS_서식목록_변경대조_2026-08-18.md) |
| safeRpt 시드 3종 | [QPS_SAFERPT_SEED_PSY_2026-08-18.sql](../../sql/qps/seed/QPS_SAFERPT_SEED_PSY_2026-08-18.sql) — **파일만 · 미실행** |
| 점검표 시드 2종 | `INF001`·`INF002` — 로컬 저장 완료 · 시드 파일 완료 |
| `PS03`·`UT05` | DDL·매퍼·서비스·컨트롤러·JSP·사이드바 **전부 완료** · 로컬 검증 완료 |
| `qpsChkForm` 손질 | 서식 목록 탭에서 제외 · 탭 3개 · 오른쪽 정렬 · 기본 전체보기 |

### 오후·저녁에 더 한 것 (한 장)

| 갈래 | 결과 | 절 |
|---|---|---|
| **자료** | 공통 서식 **309 → 314종**(`RD02~04`+`INF001·002` 시드 실행) · **기본 세트 314종** 신설 · `TBL_QPS_DEPT_CATE` **42칸** | §1 · §2-3 · §2-5 |
| **화면(위너넷)** | **우리 병원 사용 서식** 3단 개편(부서·쓰는 서식·추가할 양식) · **부서별 양식** 신설 · **부서별 쓰는 분류** 신설 | §2-3 · §2-5 |
| **화면(모두)** | 지표 **분석 탭에 격리·강박 분포**(19세·시행시간) — 화면·**인쇄물 모두** | §6 |
| **메뉴** | 사이드바 부서줄 **자동화**(손질 없어짐) · 중복 2줄 감춤 · ***설정 화면 4개를 위너넷 전용으로*** | §2-4 · §2-5 |
| **대조** | `T_MENU` 3건 확정 — `HA01~04` **다른 서식** · 근접오류 **오연결** · `PS04`≠`PS05` | §5 |
| **되돌아본 것** | 「부서 관리」·「지표분석보고서」 두 화면은 ***이미 있어서 안 만들었다*** · 분포 3축은 ***중복이라 걷어냈다*** | §2-5 · §6 |

⚠***전부 로컬(8080)에서만 돈다*** — 클래스·매퍼가 바뀌어 **운영 WAR 재빌드 전에는 병원 화면이 어제 그대로**다.

### `PS03`·`UT05` 화면 — 지표와 물린 지점
- **격리·강박 시행일지** 저장 → `TBL_QPS_MONITOR` 에 그 달 행을 다시 넣는다
  (`OBS_CNT`=전체 건수=분모 · `PASS_CNT`=`GUIDE_YN='Y'`=분자) ⇒ `ISOLATION`·`SECLUSION` 이 코드 변경 없이 돈다
- **유치도뇨관 월별 기록지** 저장 → `TBL_QPS_CENSUS(CENSUS_GB='CATHDAYS')` 그 달 칸에 `CATH_CNT` 합계

---

## 2-1. ★결정됨 — 「우리 병원 사용 서식」 화면 신설 (2026-08-18 사용자 결정)

### 왜
서식(공통 `'*'`)은 **우리가** 등록하지만, **어느 병원이 어느 서식을 쓸지**는 병원별이다.
그런데 그 스위치가 있는 `서식 관리` 는 ***위너넷 전용 화면***(`s_wnn_yn=Y`)이라
***병원 담당자가 스스로 켤 수 없다.*** 지금은 병원마다 우리가 켜 주어야 한다.

세 갈래(①우리가 계속 켜 준다 ②서식 관리를 병원에 연다 ③사용 목록만 켜는 가벼운 화면을 만든다)
중 **③으로 결정**. ②는 병원이 공통 서식을 고칠 위험이 있어 뺐다.

### 만들 것 — `qpsChkUse.jsp` (가칭 「우리 병원 사용 서식」)
- **체크와 저장, 그것뿐인 화면.** 서식을 만들거나 고치는 기능은 **넣지 않는다**(그것이 ②를 뺀 이유다).
- 목록 : 부서·분류 거르기 + 서식명 + 체크. `서식 관리` 의 왼쪽 목록만 떼어 온 모양.
- **서버는 새로 만들 것이 거의 없다** — 이미 있는 것을 그대로 쓴다 :
  - 목록 : `/qps/chkFormList.do` (cateCd·deptCd)
  - 저장 : **`/qps/chkUseSave.do`** ([QpsController.java:1439](../../../src/main/java/egovframework/wnn_medcost/qps/web/QpsController.java))
  ⇒ 새로 필요한 것은 **화면 진입 `main/qpsChkUse.do` 하나**와 JSP 뿐이다.
- 사이드바 : `점검표` 그룹, `점검표 작성` 바로 아래. **`(위너넷)` 게이트를 걸지 않는다** — 병원이 봐야 한다.

### ★한 화면을 둘이 같이 쓴다 (2026-08-18 추가 결정)
***병원 전용으로 만들지 않는다.*** 병원이 못 하면 결국 우리가 대신 설정해야 하는데,
그때 화면이 따로 있으면 두 벌을 만들고 두 벌을 고쳐야 한다.

| 보는 사람 | 화면 |
|---|---|
| **병원 담당자** | 자기 병원 것만. 병원 고르는 칸이 **없다** |
| **위너넷**(`s_wnn_yn=Y`) | 맨 위에 **병원 고르기** 칸이 하나 더 뜬다 — 병원 대신 켜 준다 |

⚠**서버에서 권한을 반드시 다시 본다.** 화면에서 칸을 숨기는 것만으로는 막이 아니다 —
  `hospCd` 를 파라미터로 받는 순간 ***병원 사용자가 값을 바꿔 남의 병원 설정을 건드릴 수 있다.***
  ⇒ `chkUseSave` 에서 **`s_wnn_yn='Y'` 일 때만 파라미터 `hospCd` 를 받고, 아니면 쿠키 값을 강제**한다.
  (`hospCd(request, p)` 헬퍼가 지금 어떻게 고르는지 먼저 확인할 것 — 그대로면 고쳐야 한다.)
- ⚠`chkUseSave` 가 **화면에 안 뜬 서식(부서 필터로 걸러진 것)까지 지켜 주는지** 확인할 것.
  `서식 관리` 의 `cfUseSave` 는 전체를 다시 받아 합치는 방식이다(qpsChkForm.jsp §cfUseSave) — 같은 규칙을 따라야
  ***부서를 걸러 놓고 저장했다가 다른 부서 서식이 통째로 꺼지는 사고***가 안 난다.

---

## 2-2. ✅`qpsChkUse` — 해소 (2026-08-18 `build=20260818-CHKUSE`)

전 세션이 「거르기가 안 든다」로 남겨 둔 것을 **실브라우저로 재현해 갈랐다.**

| 물음 | 실측 |
|---|---|
| 카드가 정말 안 줄어드나 | ***줄어든다*** — `#cuList .cu-item` 을 직접 세어 311 → 79(간호) → 11(간호+멸균) 확인. 개수 글자와 카드 수가 **언제나 같다** ⇒ 종전 의심 1·2번은 **아니었다** |
| 그럼 무엇이 문제였나 | ***3번(부서·분류 연동 없음)*** — 사용자 말 그대로 **「부서를 골라도 분류 칸이 6개 그대로」**. 시설을 골라 놓고 「멸균·소독」을 고르면 **0종**이 되는, ***고를 값어치가 없는 선택지***가 남아 있었다 |

### 고친 것 ① 부서 ↔ 분류가 서로 좁힌다 (`qpsChkUse.jsp` `refillSel`·`put`)

- 한쪽을 고르면 **다른 쪽 칸을 다시 짓는다** — 남은 종 수를 괄호로 적고, **0종은 아예 안 내놓는다.**
  (예 : 시설 → `전체 분류(45) · 의료기기 점검(9) · 환경·시설(21) · 안전·감염(14) · 기타(1)` — 멸균·소독은 사라진다)
- ⚠각 칸의 후보는 ***자기 조건을 뺀 나머지***로 센다 — 자기 조건까지 넣으면 **고른 하나만 남는다.**
- ⚠지금 고른 값이 0종이 되어도 **지우지 않는다.** 칸이 저 혼자 바뀌면 「내가 안 골랐는데」가 된다.
- 검색어(`cuQ`)도 같은 규칙으로 두 칸에 반영된다.
- 검증(실브라우저) : 시설→분류 4개 · 시설+환경·시설 **21종** · 부서를 전체로 되돌리면 분류 6개 복귀(70종) ·
  **분류만 골라도 부서 칸이 좁아진다**(환경·시설 → 감염관리·보건관리자 사라짐) · 검색어 겹침(12종) 전부 통과.

### 고친 것 ② ★★병원 계정이 목록을 아예 못 받고 있었다 (`QpsController.chkFormList`)

***이 화면을 만든 이유가 「병원이 스스로 켠다」인데, 정작 병원 계정에는 목록이 안 왔다.***

- `chkFormList.do` 에 **위너넷 전용 게이트**(`isWnn`)가 남아 있어 `s_wnn_yn≠Y` 면
  `FAIL — 서식 관리는 위너넷 담당자만` 을 돌려줬다. 화면은 경고창만 뜨고 **텅 빈다.**
- ⇒ **읽기 하나만 열었다.** 서식을 만들고 고치는 5개(`chkFormSave`·`chkFormCopy`·`chkFormDel`·
  `chkCodeAdd`·`chkCodeNext`)는 게이트 그대로다.
- 검증 : `s_wnn_yn=N` 으로 목록 **OK(311종)** · 같은 계정으로 `chkFormSave` 는 여전히 **FAIL** ·
  병원 계정이 `hospCd` 를 위조해도 `hospCd()` 가 쿠키 값을 강제한다.

### ⚠`INF001`·`INF002` — **공통이 아니라 가짜병원 전용으로 들어가 있다** (정정 2026-08-18 저녁)

목록에 `deptcd=INFECT` 2종이 잡혀 「반영됐다」고 적었으나, ***가짜병원(99999998) 시점에서 본 것***이었다.
기본 세트를 만들며 `HOSP_CD='*'` 로 조회해 보니 **309종뿐 — 두 서식이 없다.**

- 원인 : **서식 관리 화면의 저장은 언제나 「병원 전용 행」**이다(`chkFormSave` 주석 : 공통('*')은 어떤 경우에도 안 건드린다).
  ⇒ 화면으로 만든 서식은 그 병원에만 생긴다.
- ***[시드 SQL 내보내기]로 뽑은 파일이 `HOSP_CD='*'` 로 나오는 이유가 이것이다*** —
  [QPS_CHK_SEED_INFECT_2026-08-18.sql](../../sql/qps/seed/QPS_CHK_SEED_INFECT_2026-08-18.sql) 첫 행이 `('INF001','*',…)`.
  **그 파일을 운영에 돌려야** 진짜 병원에 생긴다. ***아직 안 돌렸다.***
- ★***RD02~04 도 같은 함정을 밟는다*** — 화면에서 만들고 **반드시 시드 SQL 을 뽑아 운영에 실행**할 것.

### 남은 판단거리 — 부서를 통째로 끄는 스위치
지금은 **서식 단위**로만 켜고 끈다(`TBL_QPS_CHK_USE` = 병원+서식). 「우리 병원엔 방사선과가 없다」는
**부서를 고르고 [보이는 것 전체 끔] → [저장]** 으로 되지만, ***한 줄짜리 부서 스위치는 없다.***
필요하다고 하면 그때 만든다(자료 구조는 그대로 두고 화면 단추만 늘리면 된다).

---

## 2-3. ★부서별 「쓰는 분류」 (2026-08-18 사용자 결정 · `build=20260818-DEPTCATE`)

> 사용자 : 「부서별로 어느 분류 서식으로를 정하고 관리해야 할 듯 (기본은 설정하고)」
> 「메뉴 관리(부서별 및 서식 관리) 중요하니까 이틀을 잡아 놓아야 편할 듯」

### 왜 — 84칸 중 42칸만 쓴다

서식 311종의 **부서 × 분류** 실측(2026-08-18) :

| 부서 | 의료기기 | 멸균·소독 | 환경·시설 | 약품·마약 | 안전·감염 | 기타 | 계 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 간호·병동 | 18 | 11 | 14 | 11 | 25 | · | 79 |
| 약국 | 2 | · | 5 | **29** | · | · | 36 |
| 영양 | · | · | **15** | · | · | · | 15 |
| 시설 | 9 | · | 21 | · | 14 | 1 | 45 |
| 진단검사 | 18 | · | 5 | · | 1 | 5 | 29 |
| 인공신장 | 12 | 2 | 3 | 4 | 6 | · | 27 |
| 방사선 | 15 | · | 1 | · | 3 | 3 | 22 |
| 원무·총무 | 8 | · | 1 | · | 2 | 7 | 18 |
| 보건관리자 | · | · | · | · | **11** | · | 11 |
| 의무기록 | · | · | 1 | · | 1 | **9** | 11 |
| 사회복지 | · | · | 1 | · | · | **8** | 9 |
| 물리재활 | 2 | · | 2 | · | 1 | · | 5 |
| 감염관리 | · | · | · | · | 2 | · | 2 |
| 공통 | · | · | 1 | · | 1 | · | 2 |

★**부서 14 × 분류 6 = 84칸 중 42칸만 차 있다.** 나머지 절반은 ***고르면 0종이 되는 헛 조합***이다.
⚠눈에 걸리는 어긋남 3 : **영양 15종이 전부 「환경·시설」**(급식위생인데) · **사회복지 8/9·의무기록 9/11 이 「기타」** ·
「기타」가 통틀어 33종. ⇒ ***분류 코드 자체를 손보는 것은 따로 판단***(사용자 선택지 ③, 이번에는 안 함).

### 정한 것 (사용자 선택)

| 물음 | 정한 것 |
|---|---|
| 관리 방식 | **마스터 표로 관리** — 자동 유도만으로는 「이 부서는 이 분류를 쓴다」를 사람이 정해 둘 수 없다 |
| 강제 범위 | ***등록 화면만*** — 보는 화면(사용 서식·점검표 작성)은 **손대지 않는다** |

★**보는 화면까지 막지 않은 이유** : 규칙과 자료가 어긋나는 날 ***서식이 목록에서 통째로 사라진다.***
  등록에서만 좁히면 최악의 경우도 「고를 수 있는 분류가 좁다」에서 끝난다.

### 만든 것

| 갈래 | 무엇 |
|---|---|
| 자료 | `TBL_QPS_DEPT_CATE`(DEPT_CD, CATE_CD) — [DDL](../../sql/qps/ddl/QPS_DDL_DEPT_CATE_2026-08-18.sql). ★**기본값을 손으로 안 적는다** — `TBL_QPS_CHK_FORM` 에서 `GROUP BY DEPT_CD, CATE_CD` 로 뽑는다. 재실행 안전 |
| 매퍼 | `selectDeptCate`·`selectDeptCateCnt`(실제 종수)·`deleteDeptCate`·`insertDeptCate` |
| 서버 | `deptCateList.do`(읽기 · 안 막음) · `deptCateSave.do`(**위너넷 전용**) · 진입 `main/qpsDeptCate.do`(병원 계정은 작성 화면으로) |
| 화면 | `qpsDeptCate.jsp` — **부서(행) × 분류(열) 체크 한 판**. 칸마다 **지금 서식 종수**를 같이 적고, ***규칙엔 없는데 서식이 있는 칸은 붉게***. [지금 서식대로 맞추기] 로 처음 한 번에 채운다 |
| 등록 | `qpsChkForm.jsp` `cfCateNarrow()` — 부서를 고르면 분류 셀렉트가 그 부서 것만 남는다 |
| 메뉴 | 사이드바 `점검표 ▸ 부서별 쓰는 분류 (위너넷)` |

### ★설계에서 지킨 것 — 다시 건드릴 때 깨지 말 것

1. ***정한 것이 없는 부서는 「전 분류」다*** — `TBL_QPS_USER_DEPT` 와 같은 규칙. 빈 표는 지금과 똑같이 돈다.
   **모두 지우고 저장 = 전 분류로 복귀**(해제 기능이 따로 필요 없다).
2. ***이미 등록된 서식의 분류는 안 지운다*** — 규칙 밖이면 셀렉트에 **「(규칙 밖)」**으로 남긴다.
   안 그러면 규칙을 바꾼 날 ***옛 서식을 열었다 저장하는 순간 분류가 조용히 바뀐다.***
3. **규칙을 못 받으면 좁히지 않는다**(`RULECAT=null`) — 표가 없던 시점의 실측으로 확인했다.
   ***표가 없어도 등록 화면이 멀쩡히 돌았다.***
4. 저장은 **화면에 있던 부서를 전부** 보낸다 — 체크를 다 지운 부서도 보내야 「전 분류 복귀」가 전달된다.

### 검증

| 무엇 | 결과 |
|---|---|
| DDL·기본값 (사용자가 운영 DB 실행) | ✅ **정해진칸수 42 · 부서수 14** · 어긋난 서식 **0행** |
| 매퍼 XML | ✅ 구문 258개 파싱 OK · 새 구문 4개 확인 |
| JSP 스크립트 | ✅ `qpsDeptCate`·`qpsChkForm` 둘 다 Nashorn `PARSE OK` |
| 규칙 없을 때 등록 화면 | ✅ 분류 6개 그대로 · `INF002` 정상 열림 · 내 코드발 콘솔 오류 0 |
| **관리 화면 격자** | ✅ 체크 **42/90칸**(15부서×6) · 붉은 칸 **0** · 「전 분류」 배지는 **진료(CLINIC)** 하나 — DB 실측(42·14)과 일치 |
| **저장 왕복** | ✅ 시설에 멸균·소독을 켜고 저장 → 다시 받아 **43칸·시설 5개** · 되돌려 저장 → **42칸·시설 4개**(양방향 확인, 자료 원상복구) |
| **등록 화면 좁힘** | ✅ 시설 → **4개**(멸균·소독·약품·마약 사라짐) · 진료(규칙 없음) → **6개 전부** · 부서 미선택 → 6개 전부 |
| ★**규칙 밖 값 지키기** | ✅ 영양(규칙=환경·시설)에서 분류가 `안전·감염` 인 채로 열리면 **「안전·감염 (규칙 밖)」**으로 남는다 — ***값이 안 지워진다*** |
| 화면 권한 | ✅ 병원 계정이 `/main/qpsDeptCate.do` 를 직접 쳐도 **작성 화면(`id="qpsChk"`)** 이 온다 · 위너넷은 관리 화면 |

⚠`deptCateSave` 의 위너넷 게이트는 **코드로만 확인**했다(`chkFormSave` 와 같은 한 줄).
  병원 계정으로 저장 API 를 직접 때리는 시험은 **운영 DB 에 쓰는 시험**이라 하지 않았다.

## 2-4. ✅사이드바 부서 메뉴 자동화 (`build=20260818-DEPTMENU`)

사이드바 **「부서별 점검표」 15줄이 손으로 적혀 있었다.** 그래서 :
- 2026-08-15 : 서식 0종인 **감염관리·진료** 두 줄을 손으로 지웠다
- 2026-08-18 : 감염관리에 `INF001`·`INF002` 가 생겨 ***다시 넣어야 했다***

⇒ **자료에서 그린다.** `/qps/deptMenu.do` 가 **줄을 만들 부서만** 돌려준다.

| 무엇 | 어떻게 |
|---|---|
| 세는 규칙 | ★**작성 화면과 같아야 한다** — ⓐ이 병원이 쓰기로 켠 것만(지정 없으면 전부) ⓑ병원 전용 서식이 있으면 그것, 없으면 공통(`'*'`) ⇒ 눌렀을 때 나오는 수와 메뉴가 어긋나지 않는다 |
| 담당 부서 | **`TBL_QPS_USER_DEPT` 도 반영**한다(등록 없으면 전 부서) — 담당자 메뉴에 남의 부서가 안 뜬다 |
| 공통(COMMON) | ★**담당 부서와 무관하게 남긴다** — 작성 화면이 어느 부서에서나 함께 보여주는 서식이라, 메뉴에서만 사라지면 「있는데 못 찾는」 것이 된다 |
| 부르는 때 | ★**[부서별 점검표] 를 펼칠 때 한 번**만. 사이드바는 **전 화면에 뜨는 타일**이라 화면마다 조회가 붙으면 안 된다. `sessionStorage` 에 담아 다음 화면에서는 곧바로 그리고, 그 뒤 조용히 다시 받아 고쳐 그린다 |
| ⚠**폴백** | ★***못 받으면 아무것도 하지 않는다*** — JSP 에 적힌 하드코딩 줄이 그대로 보인다. ***그 줄을 지우지 말 것.*** 「조회 실패 = 빈 메뉴」로 만들면 업무가 멈춘다 |

- 검증 : `deptMenu.do` **14부서**(감염관리 2종 **포함** · 진료 0종 **제외**) · 사이드바 블록 Nashorn `PARSE OK`.

## 2-5. ★★「우리 병원 사용 서식」으로 통일 (2026-08-18 사용자 확정 · 2회 반복 지시)

> 「우리 병원 사용 서식으로 해서 부서별에 맞는 서식 매칭해서 쓰는 것으로 통일
>  (서식은 일반적으로 위너넷에서 만들고 기존처럼)」 · 「서식 관리에서 기본 분류에서 정하고」

***새 「부서 관리」 화면은 만들지 않는다.*** 같은 일을 두 자리에서 하게 된다. 자리를 이렇게 갈랐다 :

| 누가 | 어디서 | 무엇을 |
|---|---|---|
| **위너넷** | [서식 관리] | 서식을 **만들고 고친다** — 부서·**기본 분류**를 여기서 정한다 |
| 〃 | [부서별 쓰는 분류] | 부서마다 **고를 수 있는 분류**(등록 화면에서만 걸린다) |
| 〃 | [부서별 양식] | 서식이 **어느 부서 것인가**(한 양식 = 한 부서. 두 부서면 **복제**) |
| 〃 | ★**[우리 병원 사용 서식]** | 그 병원이 **무엇을 쓰나** — ⚠**[2026-08-18 저녁 확정] 위너넷 전용으로 돌렸다** |

### ★★운영 흐름 — 「기본 설정 해 두고, 요구를 들으면 그 병원만 고친다」 (2026-08-18 확정)

> 「우리가 정한다」 · 「사용자 요구를 듣고」 · 「먼저 이야기한 것처럼 기본 설정하고」
> ★***「병원은 설정 못 할 겁니다 — 전산 요원이 없어서」*** (2026-08-18 사용자)

★★***이 한 줄이 QPS 화면 설계의 기준이다.*** 요양병원에는 전산 담당이 없다 ⇒
  **설정하는 화면은 병원에 내지 않는다.** 병원이 보는 것은 ***쓰는 화면***(작성·조회·인쇄)뿐이고,
  기준을 정하는 일은 전부 위너넷이 맡는다. 새 화면을 만들 때마다 **「이건 설정인가 작성인가」**를 먼저 묻는다.
✅**[사용자별 담당 부서] 도 같은 기준으로 위너넷 전용**으로 돌렸다(2026-08-18 저녁, `build=20260818-WNNONLY2`).

### 지금 점검표 메뉴 — 병원에는 **[점검표 작성] 하나**

| 줄 | 병원 | 위너넷 |
|---|:---:|:---:|
| 점검표 작성 | ○ | ○ |
| 우리 병원 사용 서식 (위너넷) | — | ○ |
| 서식 관리 (위너넷) | — | ○ |
| 부서별 양식 (위너넷) | — | ○ |
| 사용자별 담당 부서 (위너넷) | — | ○ |

- 검증(병원 계정으로 주소 직접 입력) : `qpsUserDept`·`qpsChkUse`·`qpsChkForm`·`qpsDeptForm`·`qpsDeptCate`
  **다섯 곳 모두 작성 화면(`id="qpsChk"`)** 으로 돌아온다. 위너넷은 그대로 열린다.
- ★되살리는 법은 화면마다 같다 : **사이드바 게이트 두 줄 + 컨트롤러 `isWnn` 한 줄**.

1. **기본 설정**(`HOSP_CD='*'`)을 깔아 둔다 — 지금 **314종 전부**. 병원은 아무것도 안 해도 이대로 돈다.
2. 병원이 **「우리는 방사선실이 없다」**처럼 요구하면, 위너넷이 [우리 병원 사용 서식]에서
   **병원코드로 불러와** 그 병원만 끈다.
3. ***병원은 이 화면을 못 본다*** — 사이드바에서 감췄고 서버도 막는다(주소를 직접 쳐도 작성 화면으로).
   ★되살리려면 : 사이드바 `qpsChkUseMenu` 게이트 두 줄 + `QpsController.qpsChkUse` 의 `isWnn` 줄만 빼면 된다.
   ⚠읽기 API(`chkFormList`)는 연 채로 둔다 — 막아도 얻는 것이 없고 되살릴 때 다시 열어야 한다.
- 검증 : 병원 계정이 `/main/qpsChkUse.do` 를 직접 쳐도 **작성 화면(`id="qpsChk"`)** · 위너넷은 그대로(`build=20260818-WNNONLY`).

### 화면 개편 — ★세 칸 (2026-08-18 사용자 지시 · `qpsChkUse.jsp`)

> 「좌측 부서별 리스트 보여주고 · 가운데 해당 서식 보여주고 · 우측에 전체 양식 보여주고
>  가운데로 추가 · 가운데는 빼는 기능 · 우측에 어디 양식인지 보여주고」

| 칸 | 무엇 | 단추 |
|---|---|---|
| **좌** | 부서 목록 — 부서마다 ***쓰는 수 / 전체 수***. 한 종도 안 쓰는 부서는 **붉게**. 양식이 없는 부서는 줄이 없다 | 눌러서 고른다 |
| **가운데** | 고른 부서가 ***쓰는*** 서식 | **[－ 빼기]** · [전체 빼기] |
| **우** | **추가할 양식** = ★***지금 가운데에 그려진 줄만 빼고 나머지 전부***를 부서별로 묶어 보여준다. 줄마다 **부서 뱃지**(고른 부서와 다르면 **주황**) · **서식코드 · 분류** · 이미 쓰는 것은 **「쓰는 중」**. 머리에 **「n종 (안 쓰는 것 m) · 전체 N종」** | 안 쓰면 **[＋ 추가]** · 쓰는 중이면 **[－ 빼기]** · [보이는 것 전체 추가] |

★★***우측 규칙은 세 번 고쳐 여기 왔다*** — 다시 손대기 전에 왜 이 모양인지 읽을 것 :

| 사용자 말 | 그래서 |
|---|---|
| 「전체를 보여주고 그룹핑해서 — **해당 부서는 아니지만 갖다 쓸 경우**」 | ***고른 부서로 가두지 않는다.*** 시설을 보고 있어도 간호·영양·진료 양식이 함께 뜬다 |
| 「**가운데 없는 것만** 보여주기」 | 가운데에 이미 있는 줄은 뺀다 — 같은 것이 두 군데 보이지 않게 |
| 「**우측이 안 보이네요**」(약국 36/36 을 다 쓰는 화면) | 「안 쓰는 것만」으로 걸렀더니 ***칸이 텅 비었다.*** ⇒ **쓰는 중인 다른 부서 양식도 내놓는다** |
| 「가운데 **빼기 하면 우측에 부서별 서식 표시**」 | 뺀 줄이 곧바로 **제 부서 묶음**으로 간다(실측 : 약국에서 빼니 우측에 「약국 1종」 묶음이 생김) |

⚠**이름을 「전체 양식」으로 두지 말 것**(사용자 지적) — 309종을 이미 쓰는 병원에서 「전체 양식 5종」으로 보여
  ***사실과 어긋났다.*** 지금 이름은 **「추가할 양식」**이고 숫자에 전체를 함께 적는다.
★**찾기는 세 칸 모두**(가운데 `cuQM` · 우측 `cuQ`). ⚠***가운데 찾기는 「보여줄 때만」 거른다*** —
  우측 목록을 정하는 계산(`midRows(false)`)에는 안 쓴다. 안 그러면 글자를 치는 순간
  가운데에서 빠진 줄이 우측에 나타나 **「두 군데 있는」 것처럼** 보인다.

⚠묶음 차례는 ***공통코드 차례 고정***이다 — 고른 부서를 위로 끌어올리지 않는다.
  자리가 움직이면 「아까 거기 있던 것」을 다시 찾게 된다.
⚠공통코드에 없는 부서코드가 붙은 양식도 **제 이름의 묶음으로 내놓는다** — 조용히 숨기면 「목록에 없는 서식」이 된다.
★분류는 ***서식 관리에서 만든 값 그대로*** 보여만 준다(「서식 관리에서 만든 분류」) — 이 화면에서 정하지 않는다.

⚠★***양식의 부서는 서식이 가진 값이다*** — 이 화면에서 바꾸지 않는다(부서·분류를 정하는 곳은
  위너넷 [서식 관리] 하나뿐. **「부서별 생성은 위너넷 서식에서」** 사용자 확정).
  그래서 **다른 부서 양식을 추가하면 그 부서 목록으로 간다** — 가운데에는 안 나타난다.
  모르면 「추가했는데 사라졌다」가 되므로 ***추가한 순간 어디로 갔는지 알려 준다***(Swal, 작게).

### ★★두 층 — 위너넷 **기본 설정** + 병원 **수정** (2026-08-18 사용자 지시)

> 「가운데는 기본 설정 보여주고 수정할 수 있게 (위너넷 설정 있고 병원이 설정한 것 있고) 기본으로 쓰면 상관없고」

***새 표를 만들지 않았다*** — `TBL_QPS_CHK_USE` 의 **`HOSP_CD='*'` 행이 곧 기본 세트**다.

| 층 | 무엇 | 누가 |
|---|---|---|
| ① **기본 설정** | `HOSP_CD='*'` — 병원이 아무것도 안 했을 때의 목록 | 위너넷 (화면 위 **[기본 설정] 체크**) |
| ② **병원 수정** | `HOSP_CD=병원코드` | 병원 (없으면 ①을 따른다) |

- 판정 규칙 : ***병원 지정이 있으면 그것 · 없으면 기본 세트 · 기본도 없으면 전부.***
  ⇒ **「기본으로 쓰면 상관없다」** 가 그대로 성립한다.
- ⚠***세 곳이 같은 규칙을 써야 한다*** (목록 `useyn` · **작성 화면 필터** · 사이드바 부서 수).
  한 곳만 고치면 「메뉴엔 있는데 열면 없는」 어긋남이 난다 ⇒ 매퍼 조각 **`<sql id="chkUseOn">`** 하나로 빼서
  세 곳이 `<include>` 한다.
- 화면 : 가운데 줄에 **「기본」/「추가」** 뱃지 · 우측에 **「기본에서 뺌」** · 위에 상태 한 줄
  (「기본 설정 그대로 쓰는 중」 / 「기본과 다릅니다 — 더한 것 n · 뺀 것 m」 + **「아직 저장 전입니다」**) ·
  **[기본으로 되돌리기]**(= 병원 지정을 지운다).
- ⚠**한 종도 안 켜고 저장하면 지정이 지워져 기본으로 되돌아간다** — 「전부 뺐는데 그대로네」가 되므로
  그때는 **되돌리기 확인창**으로 넘긴다. ***병원이 「한 종도 안 쓰기」를 고를 수는 없다***(그런 병원은 이 화면을 안 쓴다).
- ⚠기본 모드에서는 **뱃지를 달지 않는다**(자기 자신과 견줄 수 없다) · 보이는 것은 **공통 서식뿐**이다
  (병원 전용 서식은 기본 세트에 넣을 수 없다 — 실측 309 vs 병원 311).

### ★★부서별 양식 화면 신설 (`qpsDeptForm` · `build=20260818-DEPTFORM`)

> 「부서별 양식 저장관리하는 내용만 — **서식 관리(위너넷)는 한눈에 안 들어와서**」
> 「양식은 **같은 양식 두 군데 못 쓴다**. 쓸려면 **별도 서식** 만든다, 아님 **복사** 가능」 (사용자 확정)

- **한 양식은 한 부서다.** 부서 셀렉트를 바꾸면 ***옮겨진다***(둘 다에 남지 않는다).
  두 부서에서 쓰려면 줄마다 있는 **[복제]** 로 별도 서식을 만든다(이미 있는 `chkFormCopy` 를 그대로 쓴다).
  ★***이 규칙이 자료 구조와 맞다*** — 문서 키가 `병원+서식+기간` 이라 한 서식을 두 부서가 같이 쓰면
  ***같은 달 문서가 서로 덮인다.***
- 화면 : 위에 **부서별 양식 수 칩**(한눈에) · 표는 `양식 | 분류 | 부서 | [복제]` 넉 줄뿐 ·
  고친 줄은 **노란 띠**로, 머리에 「고친 것 n종 — 저장 전」 · 찾기.
- 분류 후보는 **부서별 쓰는 분류 규칙**을 따른다(등록 화면과 같은 규칙). 부서를 옮기면 규칙에 없는 분류는 **비운다**.
- 서버 : `chkFormDeptSave.do`(위너넷 전용) → `updateChkFormDept` — ***부서·분류 두 칸만*** 고친다.
  ⚠`saveChkForm` 과 달리 **공통(`'*'`) 행을 고친다**(배정을 정하는 것이 이 화면의 일).
- ★**부서를 옮겨도 작성한 문서는 흔들리지 않는다**(문서 키에 부서가 없다).
- ⚠★***복제로 만든 공통 서식은 화면에서 못 지운다*** — `deleteChkForm` 이 `HOSP_CD <> '*'` 로 막혀 있다.
  잘못 만들면 SQL 로 지워야 한다 ⇒ 화면 안내문과 복제 확인창에 경고를 적어 두었다.
- 검증 : 칩 16개·314종 렌더 · 감염관리 서식의 분류 후보가 **「안전·감염」 하나**로 좁혀짐 ·
  `CLI001` 을 **진료 → 간호·병동** 으로 옮겨 저장(칩 진료 0 / 간호 81) → **되돌려 저장**(진료 1 / 간호 80, `CLINIC`·`ENV` 원복).
  ⚠**복제는 시험하지 않았다** — 공통 서식이라 지울 길이 없어 시험 흔적이 남는다.

### 그 밖에 지킨 것

- 위에 **부서 단추**가 늘어서고 단추마다 ***켠 것 / 전체***가 적힌다 — **어느 부서를 아직 안 골랐는지 한눈에** 보인다.
  ★**한 종도 안 켠 부서는 붉게** 나온다(빠뜨린 곳이 눈에 걸리게). 서식이 없는 부서는 단추를 안 만든다.
- 부서를 누르면 그 부서만 남고, **분류 셀렉트가 그 부서 것으로 좁혀진다**(0종 분류는 사라진다).
- `?dept=코드` 로 **부서가 골라진 채** 열린다 — ⚠주소 숨김이 `location.search` 를 지우므로
  **서버가 모델(`chkDeptCd`)로 내린다**(qpsChk 와 같은 방식).
- 저장은 종전 그대로 **전체를 훑어 보낸다** — 걸러서 안 보이던 서식이 꺼지지 않는다.

### 기본 분류 (`qpsChkForm.jsp`)

- ★***새 서식이고 그 부서가 쓰는 분류가 하나뿐이면 그것으로 채운다***(예: 영양 = 환경·시설).
  ⚠**이미 저장된 서식은 건드리지 않는다** — 사람이 안 고른 값이 조용히 들어갔다 저장되면 분류가 바뀐 줄도 모른다.
- 분류 칸 옆에 **[부서별 분류 정하기]** 링크 — 규칙을 정하는 자리를 못 찾아 헤매지 않게.

### 검증 (실브라우저)

| 시험 | 결과 |
|---|---|
| `?dept=FACIL` 로 열기 | ✅ 좌측 시설이 골라진 채 가운데 **45종** · 우측 0종 |
| **빼기** | ✅ 가운데 2건 빼니 **가운데 43 / 우측 2**, 좌측 줄이 **시설 43/45** 로 |
| **부서 뱃지** | ✅ 영양에서 뺀 것이 우측에 **「영양」 주황 뱃지**로 섞여 보인다 · [고른 부서 것만] 켜면 시설 2종만 |
| ★**다른 부서 양식 추가** | ✅ 시설을 고른 채 영양 양식을 [추가] → **가운데는 43 그대로** · 좌측 **영양 15/15** 로 · ***어디로 갔는지 알림***이 뜬다 |
| **부서별 묶음** | ✅ 세 부서에서 뺀 6종이 우측에 **영양 2 · 시설 2 · 방사선 2** 로 묶여 나온다(공통코드 차례 고정) · 줄마다 **서식코드 · 분류** 표시 |
| 되돌리기 | ✅ 311/311 복귀 |
| ★**두 층 — 저장 왕복** | ✅ 시설 3종(FAC001~003)을 빼고 **저장** → 우측에 **「기본에서 뺌」** 뱃지 3 · 좌측 **시설 42/45** · 상태 「기본과 다릅니다 — 뺀 것 3종」 |
| ★**작성 화면까지 같은 규칙** | ✅ `qpsChk.do?dept=FACIL` 서식 셀렉트에 **FAC001 없음 · FAC004 있음**(42+공통) — 목록·작성·메뉴가 어긋나지 않는다 |
| **저장 전/후 구분** | ✅ 한 종 되돌리면 「…뺀 것 2종. **아직 저장 전입니다(고친 것 1종)**」 · 원복하면 문구도 사라진다 |
| **[기본으로 되돌리기]** | ✅ 병원 지정이 지워져 **311 전부** · 「**기본 설정 그대로** 쓰는 중」 · 단추 자동 숨김 |
| **기본 설정 모드**(위너넷) | ✅ 병원코드 칸 숨김 · 「기본 설정(전 병원 공통)을 고치는 중」 · 뱃지 없음 · **공통 서식 309종**만 |
| 뒷정리 | ✅ 가짜병원(99999998)을 **원래대로**(311행 지정) 되돌려 놓았다 |

### ★기본 세트 만듦 — **현재 상태 그대로 309종** (2026-08-18 사용자 지시로 실행)

- 화면 [기본 설정] 모드에서 **공통 서식 309종 전부 켜서 저장** ⇒ `TBL_QPS_CHK_USE` 에 `HOSP_CD='*'` **309행**.
- ★***지금 동작은 종전과 똑같다*** — 기본이 「전부 켜짐」이라 병원이 보는 것이 달라지지 않는다.
  ***앞으로 여기서 빼면*** 그것이 전 병원의 처음 목록이 된다(밑그림을 깔아 둔 것이다).
- 검증 : 기본 모드 **309/309** · 병원 시점 `defyn` = **Y 309 / N 2**(N = 그 병원 전용 `INF001`·`INF002`) ·
  상태 문구 「기본과 다릅니다 — 더한 것 2종」 · 가운데 뱃지 「기본」.
- 화면 저장 경로도 왕복 확인 : 기본 모드에서 `FAC001` 을 빼고 [저장] → 서버 **308/309** → 되돌려 **309/309**.
| 기본 분류 | ✅ 새 서식 + 영양 → **환경·시설 자동 선택** · 시설(4개) → 자동 선택 없음 · 옛 서식은 그대로 |

---

## 3. 남은 일 (우선순위)

0. ~~시드 SQL 2개 실행~~ ✅**2026-08-18 저녁 사용자가 실행 · 확인 완료** —
   공통 서식 **309 → 314종**(`CLI001`·`NUR069`·`NUT017`·`INF001`·`INF002`).
   기본 세트도 **314종 전부**로 맞췄다(「공통 서식 전부」 규칙 유지).
0-b. ★**결정거리 — 병원이 한 번 저장한 뒤 들어온 새 서식은 자동으로 안 켜진다**
   실측 : 가짜병원(지정 311행)에서 새 3종이 **꺼진 채**로 왔다(「뺀 것 3종」).
   지금 모델은 ***병원 지정 = 그때의 목록 통째 스냅샷***이라, 뒤에 위너넷이 표준 서식을 더해도
   **이미 한 번 손댄 병원에는 영영 안 간다.**
   | 길 | 뜻 |
   |---|---|
   | 지금 그대로 | 병원 목록은 병원 것 — 새 서식은 병원이 [＋추가] 할 때만 들어온다 |
   | 「뺀 것만 기록」으로 바꾼다 | 병원 지정을 **끈 서식 목록**으로만 두면, 새 서식은 기본 세트를 따라 **자동으로 들어온다** |
   ⇒ 자료 구조는 그대로 두고 판정만 바꾸면 된다(`chkUseOn` 조각 한 곳). **위너넷 결정 사안.**
1. ~~**`RD02~04` 점검표 3종 등록**~~ ✅**완료**(§1)
1-b. ~~**`qpsChkUse.jsp` 신설**~~ ✅**완료**(§2-1 설계 · §2-2 거르기·권한 해소, `build=20260818-CHKUSE`).
   ⚠**운영 배포 대기분에 합류** — `QpsController`(클래스) 변경이라 WAR 재빌드가 필요하다
2. **safeRpt 시드 3종 운영 실행** — `STAFFVIO`·`PSYIMPR`·`PSYVISIT` (SORT 96~98) ← **사용자 몫, 아직**
3. ⛔**운영 WAR 재빌드** — 오늘 바뀐 클래스·매퍼가 전부 여기 걸려 있다 ← ***가장 급한 것***
   (사용 서식 2층 · 부서별 분류 · 부서별 양식 · 사이드바 자동화 · 위너넷 전용 게이트 · 지표 분포 · `PS03`·`UT05`)
4. ~~**`T_MENU` 대조 3건**~~ ✅**2026-08-18 저녁 완료 — §5 참조**(SUNWOO MSSQL 읽기전용 조회로 셋 다 확정)
5. ~~**지표분석보고서 화면**~~ ✅**분포까지 완료 — §6**. ***새 화면은 안 만들었다***(분석 탭이 이미 있었다).
   남은 것은 **요로감염(`UT01~12`) 계열**과, 원본 3쪽 「개선 전략 및 실행」 칸이 따로 필요한지 **원본 대조**.
6. **`UT01~04` 감염환자 기록지 · `PS04`·`PS05` 격리·강박 기록지** — 전용 화면(진단 알고리즘·회차 반복).
   ★`PS04`·`PS05` 는 §5-③ 으로 **다른 판임이 확정**됐다 — **둘 다** 만든다.
7. **근접오류 보고서** — §5-② 로 ***원본 서식을 확보할 수 없음***이 밝혀졌다(SUNWOO 오연결).
   만들려면 **병원에 「근접오류를 어떤 양식으로 쓰는지」** 물어야 한다.

---

## 5. ✅`T_MENU` 대조 3건 — 확정 (2026-08-18 저녁)

조회 = SUNWOO MSSQL `saynice.co.kr:1433 / SUNWOO` **읽기 전용**(jjs + mssql-jdbc).
⚠콘솔이 cp949 라 한글이 깨진다 ⇒ **결과를 UTF-8 파일로 써서 읽었다.**
`T_MENU` 칸 = `COMPANY · GRP · ID · PARENT_ID · IDNAME · CHART_ID · VER · CLASS_NM · SEQ · USE_YN`
(**`CLASS_NM` = 델파이 폼 이름** — 메뉴가 실제로 무엇을 여는지는 `CHART_ID` 가 아니라 ***이 값***이 말한다).

### ★★먼저 — ***오늘 판독한 신규분은 `COMPANY='0000'`(선우) 트리다***

| COMPANY | QPS 메뉴 | 그중 서식 |
|---|---:|---:|
| `0001` (SRC) — ***프로젝트 기준선 203종*** | 272 | **203** |
| **`0000` (선우)** — 오늘 캡처한 트리 | 328 | **248** |
| `1000`·`2000` | 328 | 248 (0000 과 **ID·CHART_ID 까지 같은 복제본**) |

⇒ ***0000 이 0001 보다 45종 많다.*** `RD`·`PS`·`UT`·`HA`·`SU`·`IS`·`RS` 가 **0001 에는 아예 없다**
(그래서 0001 로 조회하면 격리·강박·정신이 **0건**으로 나온다 — 없는 게 아니라 **다른 회사 트리**다).
★***앞으로 T_MENU 를 볼 때 COMPANY 를 먼저 정할 것.***

### ① `HA01~04` ↔ `B000050~53` — ***다른 서식이다*** (판독의 추정은 틀렸다)

| 무엇 | 자리 | CHART_ID | 폼(CLASS_NM) |
|---|---|---|---|
| 오늘 캡처한 **환자안전분석보고서 1/4~4/4** | `0000` ▸ 지표분석보고서 ▸ **위해사건분석보고서** | `B000142~145` | **`TfrmQPS_Insane_033_1~4`** |
| 기존 **`<QPS 지표분석> - 환자안전 1/4~4/4`** | `0001` ▸ **환자안전사고유형별분석보고서** | `B000050~53` | `TfrmQPS_Chart_021_1~4` |

⇒ **폼이 다르다**(`Insane_033` vs `Chart_021`) ⇒ ***같은 서식이 아니다.*** `HA01~04` 는 **정신(Insane) 계열 신규**다.
판독 §1-4 가 「위해사건분석보고서 밑에 있다」고 적은 것은 **정확했다**(`030515`).

### ② 근접오류 보고서 — ***메뉴가 학대·폭력 폼을 가리킨다*** (오연결 확정)

| COMPANY | 메뉴 | CHART_ID | 폼 |
|---|---|---|---|
| `0000` | **근접오류 보고서**(`050901`) | `D000009` | **`TfrmRPT_Chart_009`** |
| `0000` | 환자 학대 및 폭력 발생 신고서(`050801`) | `D000008` | `TfrmRPT_Chart_008` |
| `0001` | 환자 학대 및 폭력 발생 신고서(`040801` 등) | `D000008` | **`TfrmRPT_Chart_009`** |

★`0001` 에서 **학대·폭력이 `Chart_009` 를 쓴다** ⇒ ***`TfrmRPT_Chart_009` 는 학대·폭력 서식이다.***
그런데 `0000` 의 **근접오류 보고서가 같은 폼을 가리킨다** ⇒ 열면 학대·폭력이 나온다.
***2회 재현된 현상이 자료로 설명됐다 — SUNWOO 원본의 오연결이다.***

⇒ **근접오류 보고서는 원본 서식을 확보할 수 없다.** 우리가 만들 근거가 없다.
  필요하다면 **병원에 물어 「근접오류를 어떤 양식으로 쓰는지」** 확인해야 한다(확인목록 후보).
  ⚠`0000` 의 학대·폭력이 가리키는 `TfrmRPT_Chart_008` 이 무엇인지는 **캡처로만 확인된다**(미확인).

### ③ `PS04` ↔ `PS05` — ***다른 판 확정***

| 메뉴 | ID | CHART_ID | 폼 |
|---|---|---|---|
| 격리.강박 기록지 | `051104` | `D000028` | `TfrmRPT_Insane_011` |
| 격리.강박 기록지 | `051105` | `D000029` | `TfrmRPT_Insane_013` |

***이름이 같고 서식·폼이 다르다*** ⇒ 판독의 추정(4쪽 / 1쪽)이 맞다. **둘 다 이식 대상**이다.

### 덤 — 격리·강박 지표분석보고서 자리

`0000` ▸ 지표분석보고서 ▸ **격리지침**(`B000129~134`, `Insane_025_1~6`) · **강박지침**(`B000135~140`, `Insane_026_1~6`).
분기 4 + **중간보고서 + 최종보고서** = 계열마다 **6종**이다(판독의 `IS01~06`·`RS01~06` 과 수가 맞는다).

## 6. 지표분석보고서 — **분포 표 API 완성** (2026-08-18 밤 · `build=20260818-INDIDIST`)

보고서 화면에 필요한 것 중 ***없던 것은 분포 표뿐***이었다 — 나머지는 이미 있다 :

| 보고서의 자리 | 이미 있는 것 |
|---|---|
| 지표정의 · 목표 · 모니터링 · 결재란 | `indiDefGet.do` |
| 월별 격자(분자·분모·율) · 분기 · 현황 | `indiCalc.do` |
| 분류별 집계(분기 4벌) | `indiBreakQtr.do` |
| ***분포 표(2·3·4번)*** | ⇒ **`indiDist.do` 신설** |

### ★★새 화면을 만들지 않았다 — 있던 것을 찾아 거기 붙였다

처음엔 `qpsIndiRpt.jsp` 를 새로 만들려 했으나, **이미 다 있었다** :

| 이미 있던 것 | 어디 |
|---|---|
| 지표분석보고서 **현황판**(18종 작성·결재 상태) | `qpsRpt.jsp`(메뉴 「지표분석목록」) |
| 지표별 **분석 탭** — 정의 · 월별 집계 · 분기/반기/연 · 추이 차트 · **분류별 집계** · **서술(개선활동 1~4·분석·개선계획)** · **결재** · **인쇄(A4)** | `qpsFall.jsp` 탭 ③ |

⇒ ***새 화면은 중복이었다.*** 분석 탭에 **분포 표만** 덧붙였다.

### ⚠★그리고 — 내가 만든 것 중 절반은 이미 있었다 (걷어냄)

처음에 `indiDist` 를 **세 축(연령·장소·시간)** 까지 만들었는데, ***그 셋은 `selectBreakdown` 이 이미 세고 있었다***
(분류별 집계의 `AGE`·`PLACE`·`TIME` 축). **같은 수를 두 곳에서 세면 언젠가 갈린다** —
실제로 등급 필터를 한쪽은 `LV`, 한쪽은 `Level` 로 써서 **같은 자료에서 다른 수가 나올 뻔**했다.
⇒ ***사고보고 계열은 원래 자리에 두고***, `indiDist` 는 **격리·강박 전용**으로 줄였다(SQL·매퍼도 삭제).

| 지표 | 분포 | 세는 곳 |
|---|---|---|
| 사고보고 계열(자살·자해·낙상 등) | 연령대 · 발생장소 · 시간대 · 유형 · 손상 · 부서 · 위해등급 | **기존 `selectBreakdown`** |
| **격리**(`ISOLATION`) | 19세 이상·미만 · **시행 시간 13칸** | **새 `indiDist`** |
| **강박**(`SECLUSION`) | 19세 이상·미만 · **시행 시간 5칸** | 〃 |

⚠***격리 13칸 / 강박 5칸을 한 표로 합치지 말 것***(판독 §1-3). 코드에서도 갈라 두었다.

### 설계에서 지킨 것

1. ★***구간 나누기는 SQL 이 아니라 자바***가 한다 — 시각이 `HHMM`·`HH:MM` **문자열**이라
   SQL 로 재면 형변환 함정이 줄줄이 붙는다. SQL 은 **원자료만** 꺼낸다(`selectIncidDistRows`·`selectSecLogDistRows`).
2. ★***빈 칸도 0 으로 내놓는다*** — 원본이 고정 칸 표라, 없는 구간을 빼면 표가 짧아져 어긋난다.
   단 **발생장소는 칸을 안 깐다**(병원이 적은 값이 그대로 온다 — 분류집계와 같은 방식).
3. ★***칸에 없는 값이 와도 버리지 않고 줄을 늘린다*** — 조용히 빠지면 합계가 안 맞는다.
4. ⚠★**등급 필터를 `selectBreakdown` 과 글자까지 맞췄다**(`LV` 를 떼는 방식).
   한쪽만 `Level` 로 적으면 ***같은 자료에서 다른 수가 나온다*** — 실제로 처음 그렇게 썼다가 고쳤다.
5. 기간은 `YYYY` · `YYYY-Q1..4` · `YYYY-H1..2` 한 가지 규칙으로 자른다.

### 검증

| 무엇 | 결과 |
|---|---|
| API 범위 | ✅ 격리 `ADULT 2 + DUR 13` · 강박 `ADULT 2 + DUR 5` · **자살자해·낙상은 `dists: []`**(기존 분류집계가 담당) |
| 화면(격리) | ✅ 분류별 집계 카드에 **「연령 구분」 3행(2칸+합계) · 「시행 시간대별」 14행(13칸+합계)** 이 붙는다 |
| 화면(강박) | ✅ **6행(5칸+합계)** — 구간 수가 갈린다 |
| ★**인쇄물** | ✅ [인쇄(A4)] 로 만들어지는 HTML(30,738자)에 **「분류별 집계」 절 + 연령 구분 + 시행 시간대별** 표가 실린다 — ***화면과 종이가 같다*** |

⏳**수치 검증은 자료가 생긴 뒤** — 가짜병원엔 시행일지가 없어 전부 0이다.
  격리·강박 자료는 **오늘 만든 `PS03` 화면**으로 들어오므로, 첫 입력 뒤 곧바로 대조할 수 있다.
⚠인쇄는 **확인창을 거쳐** 창이 열린다(`_confirmBox` → `doPrint`) — 자동 확인에서 창이 안 열려 헤맸다.
  ***인쇄 내용을 확인할 때는 `_confirmBox` 를 눌러 준 다음***에 봐야 한다.

### ⚠병원에 물어야 할 것 (확인목록 후보)

- **시간대 경계** — 지금 `DAY 07~15 · EVENING 15~23 · NIGHT 23~07`(표준 3교대)로 두었다. 병원 교대가 다르면 바꿔야 한다.
- **연령 칸** — 원본은 30~90대뿐이다. 20대 이하는 **「기타·미상」**으로 담았다(버리지 않으려고).

### 다음

- **자살·자해(`SUICIDE`)** 는 분석 탭이 그대로 돌아간다 — 분포도 기존 분류집계가 낸다. ***더 만들 것이 없다.***
- 남은 것은 **요로감염(`UT01~12`) 계열**과 원본 3쪽 「개선 전략 및 실행」(자살·자해 전용 칸) 정도다.
  ⇒ 지금 서술 칸은 **개선활동 1~4 · 분석 · 개선계획** 여섯이다. 「개선 전략」이 따로 필요한지는 **원본 대조 후 판단**.

## 4. 이번에 밟은 함정 — ***다시 밟지 말 것***

- **`curl` 로 저장 시험하지 말 것.** 한글이 깨진 채 ***운영 DB*** 에 들어간다(로컬 프로파일이 운영을 본다).
  오늘 실제로 넣었다가 지웠다. 저장 검증은 **화면(jQuery)** 으로 한다. 읽기(GET)만 curl 로.
- ★★**화면에서 만든 서식은 「그 병원 것」이다** — `chkFormSave` 는 언제나 병원 전용 행으로 쓴다.
  ***[시드 SQL 내보내기] 로 뽑아 운영에 돌려야 공통(`'*'`) 서식이 된다.*** 안 돌리면 만든 사람 눈에만 보인다
  (2026-08-18 `INF001`·`INF002` 로 실제로 겪었다 — 「반영됐다」고 적었다가 정정).
- **서식은 등록해도 작성 화면에 안 뜬다** — 「이 병원 사용 목록」이 따로 있다.
  서식 관리에서 체크를 켜고 **[사용 저장]** 을 눌러야 한다(이제 병원도 **[우리 병원 사용 서식]** 에서 할 수 있다).
- ★**「안 줄어든다」는 목록이 아니라 셀렉트 이야기일 수 있다** — 카드는 줄고 있었는데
  **분류 칸이 그대로**여서 「거르기가 안 든다」로 읽혔다(§2-2). ***먼저 DOM 을 세어 두 관찰을 가른다.***
- ★**화면을 새로 만들 때는 그 화면을 쓸 계정으로 열어 볼 것** — `qpsChkUse` 는 병원용인데
  목록 API 가 위너넷 전용이라 **병원 계정에서는 텅 비었다**(§2-2 ②). 만든 사람은 늘 위너넷 쿠키로 본다.
- **지표를 새로 만들지 말 것** — `SUICIDE`·`ISOLATION`·`SECLUSION`·`UTI` 는 `QPS_DDL_2026-08-08.sql` 에
  **이미 마스터·정의·영역·하위코드까지 다 있다.** 없던 것은 지표가 아니라 **보고서 서식**이다.
- **safeRpt SORT 는 99부터** — 기존 최대가 95다. 51~53 은 `MRPROXY`·`IDREQ`·`ABBRRPT` 가 쓴다.
- **JSP 주석 `*/` 를 두 번 닫으면 스크립트가 통째로 죽는다** — 오늘 한 번 냈다.
  고친 뒤 **주석 열림/닫힘 개수를 세고** Nashorn 으로 `PARSE OK` 를 확인할 것.
- **클래스·매퍼 반영은 `web.xml` touch 까지** — 확인은 `QpsController.BUILD` 마커
  (지금 `20260818-DEPTCATE`). 값이 안 바뀌면 리로드가 안 된 것이다.
- ★**새 규칙(마스터)을 만들 때는 「없으면 전부 허용」으로 짤 것** — 「등록 안 했으니 아무것도 못 한다」로
  만들면 도입하는 날 업무가 멈춘다. 사용자별 담당 부서(08-15)·부서별 분류(08-18) 둘 다 같은 규칙이다.
- ⚠**운영 DB DDL 은 에이전트가 못 돌린다**(자동 승인 정책이 막는다 — 2026-08-18 Bash·PowerShell 둘 다 차단).
  ***SQL 파일을 만들어 사용자에게 넘기는 것이 정상 절차다.***
  ★단 **읽기(SELECT)는 통과한다** — SUNWOO T_MENU 대조를 jjs+mssql-jdbc 로 그렇게 했다(§5).
- ★**`T_MENU` 는 회사(COMPANY)마다 다른 트리다** — `0001`(SRC) 203종 · `0000`(선우) **248종**.
  ***COMPANY 를 안 정하고 조회하면 「없는 서식」으로 오판한다***(격리·강박이 0건으로 나왔다).
- ★**메뉴가 실제로 무엇을 여는지는 `CHART_ID` 가 아니라 `CLASS_NM`(델파이 폼)이 말한다** —
  근접오류 오연결이 이 값으로 드러났다(§5-②).
