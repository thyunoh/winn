# 표준 서식 등록 대장 — 점검표 엔진 (2026-08-12)

> 엔진(v3)은 **다 만들었다**(순서 1~9 완료). 남은 일은 **원본 서식을 등록하는 것**이다.
> 판정 : [약국](QPS_서식판정_약국_2026-08-12.md) · [영양](QPS_서식판정_영양_2026-08-12.md) ·
> [시설](QPS_서식판정_시설_2026-08-12.md) · [간호/병동](QPS_서식판정_간호병동_2026-08-12.md)

---

## 0-A. ✅**막혔던 곳이 풀렸다 — 캡처는 세션 기록 안에 그대로 있었다 (2026-08-12)**

「캡처가 손에 없다」고 적었던 것은 **내 작업 맥락에서 밀려났다는 뜻**이었지, 사라진 것이 아니었다.
***세션 기록(`.jsonl`)에 이미지가 base64 로 박혀 있다.*** 꺼내서 다시 읽으면 된다.

```
꺼내는 법 : scratchpad/ExtractImgs.ps1  →  caps/cap001.png … (306장, 36MB)
부서 경계는 사용자 메시지 줄번호로 가른다 —
  약국 : 첫 이미지 ~ line 866 「영양 캡쳐 올리겠음」 앞      → 62장
  영양 : ~ line 1202 「영양 다올렸음 … 시설 올리겠음」 앞    → 63장
  시설 : ~ line 1531 「큰것은 다올림 (간호/병동)」 앞
  간호/병동 : 그 뒤 ~ line 1991 「간호/병동 다올림」
```

⚠**다음에 또 밀려나면 이 방법을 먼저 쓴다.** 사용자에게 다시 올려 달라고 하기 전에.

### 지금까지 등록한 것 — **약국 11종**

| 코드 | 서식 | 축 | 이 서식이 처음 쓰는 것 |
|---|---|---|---|
| PHA001~003 | 냉장고 온도 · 마약류비치서 · 반납 대장 | `DAY_ITEM`/`LIST` | ⚠**1차 시드가 운영에 실행된 적이 없었다.** 이번에 같이 넣었다 |
| PHA004 | 수액보관실 온,습도 점검일지 | `DAY_ITEM` | |
| PHA005 | 잔여 마약류 폐기 대장 | `LIST` | 열 묶음(약사·참관직원) |
| PHA006 | 조제 전 / 후 감사 대장 | `LIST` · **연** | ※문구 ①~⑦ |
| PHA008 | 의료용 마약류 저장시설 점검부 | `LIST` | ***1차에서 `GUIDE_TXT` 200자에 막혔던 것*** — 1000자로 넓혀 들어갔다 |
| PHA009 | 약제과 환경 점검표 | `ITEM_DAY` | 행 그룹 3 |
| PHA010·011 | 자동 / 반자동 조제기 일일 점검 | `ITEM_DAY` | **인쇄 15칸 위아래** — 원본의 1~15/16~31 두 토막 그대로 |
| PHA022 | 백신 보관 냉장고 일일체크리스트 | `ITEM_DAY` | ★**행 블록(가로 띠)** 첫 실물 — 결정 대기 ④가 가리키던 서식 |

⚠**PHA022 는 판정표에 빠져 있었다.** 결정 대기 ④에 이름만 남아 있던 것을 캡처에서 찾았다.
   ⇒ ***판정표의 종수를 캡처 수와 대조해야 한다는 것이 실제로 확인됐다.***

### 2차로 더한 것 — **약국 16종이 됐다**

| 코드 | 서식 | 축 · 주기 | 이 서식이 처음 쓰는 것 |
|---|---|---|---|
| PHA007 | 유효기간 임박 약물 관리서 | `LIST` · 월 | 열 묶음(유효기간 경과 후 관리) |
| PHA012 | 약제과 약사부재중 조제 및 출입관리대장 | `LIST` · **연** | |
| PHA013 | 회수의약품 관리대장 | `LIST` · **연** | |
| PHA014 | 폐기 의약품 대장 | `LIST` · **일** | ***문서 단위가 일*** — v3 순서 3 의 첫 약국 실물 |
| PHA023 | 비치의약품 보관상태 점검대장(약국용) | `ITEM_COL` · **일** | ★***v2 때 「2종뿐이라 미룬다」고 했던 `ITEM_COL` 의 첫 근거 서식.*** 이제 들어갔다 |

★**PHA013(회수, 연) 과 PHA014(폐기, 일) 는 이름이 닮았지만 문서 단위가 다르다** — 합치지 않았다.

### 3차로 더한 것 — **약국 18종**

| 코드 | 서식 | 축 · 주기 | 이 서식이 처음 쓰는 것 |
|---|---|---|---|
| PHA015 | 알러지 환자 내역 관리대장(약국) | `LIST` · 월 | |
| PHA021 | 냉장고온도관리기록지 | `DAY_ITEM` · 월 | ★★***`HEAD5~8`(v3 순서 1)을 만들게 한 그 서식.*** 상단 8칸이 다 들어갔다 |

★PHA021 은 원본이 **평평한 6열**인데 「확인온도」·「점검자」가 **두 개씩 같은 이름**이라
  그대로 두면 추출에서 어느 시간 값인지 사라진다. ⇒ `10시`/`19시` 묶음을 세워 뜻을 지켰다.

### ★★등록 중에 나온 큰 것 — **`EQUIP_MONTH` 가 3종이 됐다**

**응급 약품 점검 기록부**(약품 자유행 × 3하위항목 × 1~12월)가 나오면서
소화기 관리대장 · 월별비치의약품보관상태점검기록부에 이어 **세 번째**가 됐다.
***축을 만드는 기준(3종)을 넘었다.*** → [v3 설계 3-B](../설계/QPS_점검표엔진_v3_설계_2026-08-12.md)

⇒ 필요한 것은 **「문서가 정하는 행 묶음」 하나뿐**이다. 나머지 조각은 다 있다.
   `CHK_ROW`·`CHK_COL` 을 이미 두 번 만들어 봤으므로 **세 번째도 값싸다.**

### 4차 — **`ROW_SRC` 를 만들고 5종을 더 넣었다 · 약국 23종**

| 코드 | 서식 | 축 · 주기 | |
|---|---|---|---|
| PHA024 | 응급 약품 점검 기록부 | `ITEM_MONTH` · 연 | 약품 자유행 × 수량·유효기간·파손유무 × 12월 |
| PHA025~028 | 월별비치의약품보관상태점검기록부 — **주사제/경구제/시럽제/수액** | `ITEM_MONTH` · 연 | ***1차 시드에서 「엔진 밖. 보류」라고 적어 둔 그 서식.*** 앞 열(비치량) + 문서가 정하는 행 묶음으로 풀렸다 |

★**약품 이름은 서식에 넣지 않았다.** 원본에 `라식스주20mg/2ml` 처럼 채워져 있지만 그건
  **그 병원이 쓰는 목록**이지 서식이 아니다. 항목으로 박으면 약을 바꿀 때마다 서식을 고쳐야 하고
  ***고치는 순간 지난해 기록까지 같이 바뀐다.*** (영양 「교육 계획서의 주제」와 같은 가름)

⚠**검증에서 버그를 하나 잡았다** — 앞/뒤 열이 `r.sort` 를 행 번호로 써서
  묶음이 열여섯이어도 **비치량 16칸이 전부 row 1** 이었다. ***한 칸에 겹쳐 쓰는 자료 손실***인데
  화면으로는 멀쩡해 보인다. 행 번호를 밖에서 받도록 고쳤다(`sideTd(..., rowNo)`).

### 5차 — **탭 서식 · 병동 비치 · 약국 28종**

| 코드 | 서식 | 축 · 주기 | |
|---|---|---|---|
| PHA016~019 | 의약품 관리대장 — **PO / 외용제 / ING / 수액** | `LIST` · 월 | 열 묶음(사용기한/재고) |
| PHA020 | 병동 비치 약물 관리 대장 | `LIST` · **연** | 열 묶음 4(응급/고위험 고주의/향정/일반) |

★PHA020 은 **서명 칸 넷의 이름이 다 같다**(`확인자 서명`). 그대로 두면 추출에서 어느 약물의
  서명인지 사라진다 ⇒ 열 묶음으로 네 이름을 세웠다. PHA021(10시/19시)과 **같은 처방**이다.

⚠**「양호/불량」·「유/무」가 계속 나온다.** 저장은 되지만 이행 요약의 O/X 건수에서
  전부 「기타」로 빠진다. ***결정 대기 ②가 실제로 걸리는 자리가 늘고 있다.***

### 아직 안 한 약국

### 6차 — **병동 비치 약물 목록 8탭 · 약국 36종**

| 코드 | 서식 | 축 |
|---|---|---|
| PHA029 | 병동 비치 약물 목록 - **확인** | `ITEM_COL` — 일곱 갈래에 체크 하나씩(다른 탭들의 표지) |
| PHA030~036 | 〃 — 응급약물 / 고위험고주의 / 향정신성 / 일반 1~4 | `LIST` — 약품명·제형·보유수량·유효기간·개봉일자·보관방법·라벨링·필요시 경고문·물량 및 파손여부 |

⚠⚠**탭 서식에서 내가 한 가정을 밝혀 둔다.** 캡처에 열려 있던 탭만 보았다 —
`병동 비치 약물 목록`은 **확인·응급약물 둘**, `의약품 관리대장`은 **PO 하나**,
`월별비치의약품`은 **주사제 하나**. 나머지는 ***열이 같다고 보았고, 본 것은 아니다.***
같은 화면의 갈래 탭이라 그럴 가능성이 높지만 확인이 필요하다.
⇒ 다르면 **그 탭 서식만** 고치면 된다(서식이 갈려 있어 다른 탭에 영향이 없다).

---

## 0-B. 영양 — **4종 등록** (2026-08-12, 진행 중)

| 코드 | 서식 | 축 · 주기 | 이 서식이 처음 쓰는 것 |
|---|---|---|---|
| NUT001 | 영양초기평가 불량환자 관리 리스트 | `LIST` · **연** | 상단 자유칸(담당자) |
| NUT003 | 일일 위생점검표 | `ITEM_DAY` | **행 그룹 11 · 항목 29** — 판정표 숫자와 정확히 맞았다 |
| NUT004 | 청소계획표 | `ITEM_DAY` | ★★**항목 설명 열(청소방법)의 첫 실물** — v3 순서 7 이 여기서 쓰인다 |
| NUT005 | 안전 점검 일지 | `ITEM_DAY` | 행 그룹 5 · 항목 27 |

★NUT004 의 「청소방법」을 **입력칸으로 만들었다면** 병원이 매달 「중성세제로 세척」을
  스물몇 번 다시 쳤을 것이다. ***항목의 속성으로 둔 판단이 실물에서 값을 했다.***

⚠**원본 오타를 그대로 옮겼다** — `전치리씽크대` · `쌩크대` · `받드정리`(NUT004).
  고치면 원본과 글자가 달라져 대조가 안 된다. ***고칠지는 병원이 정한다***(결정 대기 ⑤).

⚠**재캡처 목록에 한 줄 더한다** — NUT005 의 `4. 절단,다듬기,뼈 발라내기를 할 때 칼의` 는
  원본 화면에서 **글자가 잘려 보인다.** 보이는 데까지만 옮겼다. ***뒤를 지어내지 않았다.***

### 영양 등록 현황 — **13종** (2026-08-12)

| 코드 | 서식 | 축·주기 | 이 서식이 처음 쓰는 것 |
|---|---|---|---|
| NUT001 | 영양초기평가 불량환자 관리 리스트 | `LIST`·연 | |
| NUT003 | 일일 위생점검표 | `ITEM_DAY` | 행 그룹 11 · 항목 29 |
| NUT004 | 청소계획표 | `ITEM_DAY` | ★**항목 설명 열(청소방법)** |
| NUT005 | 안전 점검 일지 | `ITEM_DAY` | 행 그룹 5 · 항목 27 |
| NUT006 | 개인위생 확인 기록지 | `LIST` | 열 묶음 4 |
| NUT007 | 조리실 냉장/냉동고 온도 및 위생 관리일지 | `ITEM_DAY` | 3단→2단 눌러 담기 |
| NUT008 | 개인위생 점검일지 | `LIST` | ⚠NUT006 과 **다른 서식**(체온 칸·「구열」) |
| NUT010 | 음식물 쓰레기 관리대장 | `DAY_ITEM` | |
| NUT011 | 식기 세척기 점검 일지 | `DAY_ITEM` | ★**정규화 고침의 실증**(헹굼온도만 NUM) |
| NUT012 | 감량의무 사업장 음식물 쓰레기 관리대장 | `DAY_ITEM` | 열 묶음 2 |
| NUT013 | 방역 일지 | `LIST`·연 | 열 묶음 |
| NUT015 | 건강관리 현황표 | `LIST`·연 | ★**행 블록 3** |
| NUT016 | 식중독 예방 위생 점검 일지 | `ITEM_DAY`·**주** | ★**주 문서 + 요일 격자 + 뒤 열**이 한꺼번에 |
| COM001 | MSDS보관소 점검표(일단위) | `LIST`·일 | ★**부서 공통 첫 서식** + 행 블록 2 |

| NUT002 | 밀라운딩일지 | `LIST`·연 | ⚠NUT009 와 **띄어쓰기만 다른 이름, 다른 서식** |
| NUT009 | 밀라운딩 일지 (일단위) | `LIST`·**일** | 상단 칸 3(점검 사항) · 원본은 머리글을 6번 되풀이 |

★**「밀라운딩일지」와 「밀라운딩 일지」** — 문서 단위(연/일)도, 열도, 상단도 다르다.
  ***이름만 보고 합쳤다면 두 서식의 기록이 한 곳에 섞였을 것이다.***

### ⏭ 영양에서 **아직 못 담는 것** — 새 모양이 하나 더 나왔다

| 서식 | 왜 못 담나 |
|---|---|
| **일일위생안전청소점검일지** | ***항목마다 고를 값이 다르다*** — `이상없음/이상발견후 조치` · `청결/미흡/불량` · `실시/미실시` · `확인/미확인` · `작동/미작동` · `양호/불량` · `부착/미부착`.<br>우리 `ITEM_COL` 은 **모든 행이 같은 열**을 쓴다.<br>게다가 ①개인위생 묶음 안에 **직원 이름 11칸 확인표**가 끼어 있고 ②조치사항에 **미리 찍힌 안내**가 있다(식당 포함·바퀴작동상태 포함·오후 3시 30분 실시 …) |
| 검식 \* 보존식 일지 | ***판정 그대로 개별 화면***이다. 끼니 × 구분(밥/국/찬3/김치) × 검식 6 × 보존식 2 + 시정조치 — **한 칸 안에 또 구조가 있다** |
| ~~냉장고/냉동고/주방 온도·습도관리 기록지~~ | ✅**이제 담긴다**(`PRD_SUB` 가 생겨 행이 풀렸다).<br>열 3단은 **두 조각을 이어 붙여** 2단으로 누른다 — `묶음='온도(℃) 냉장고2'` · `항목='냉장','보존식냉동'`.<br>NUT007(온도 9am)에서 쓴 방법 그대로다. ***다음 세션에서 등록할 것.*** |
| 온도 관리 기록지 (냉장고,냉동고,식품창고) | 〃 같은 모양이다 — `PRD_GB='W'`(주차 탭) · `PRD_KIND='W'`(요일) · `PRD_SUB='AM 09:00,PM 17:00'`.<br>열은 `묶음='온도(℃) 식품저장용'` · 항목 `냉장고 1·냉동고 1…` 로 누른다 |

### ★새 후보 — **「항목마다 선택지가 다르다」 · 1종**

`PHA023`(비치의약품)은 모든 행이 `Y/N` 으로 같았는데, 일일위생안전청소점검일지는 **행마다 다르다.**
⇒ 「항목이 자기 선택지를 갖는다」(`ITEM.OPT_NMS` 같은 칸). ***1종이라 아직 만들지 않는다.***
   같은 모양이 2종 더 나오면 만든다 — 지금까지의 기준 그대로다.

### ⏭ 영양에서 **처음으로 등록이 막혔다** — 「기간 세분」

**냉장고/냉동고/주방 온도·습도관리 기록지** — 행이 `요일 × 시각(10시/15시)` 두 겹이다.
우리 격자는 기간 행이 한 겹이라 담을 자리가 없다. 억지로 눕히면 24열이 되어 원본과 아주 달라진다.
⇒ ***반쪽으로 넣지 않고 이유를 적어 둔다.*** → [v3 설계 3-C](../설계/QPS_점검표엔진_v3_설계_2026-08-12.md)
   근거는 **8종**(간호/병동 7 + 영양 1)으로 이미 기준을 넘었다.

### ★작은 글자는 **확대해서 읽는다** (2026-08-12에 정한 것)

캡처를 3~4배로 늘려 읽으니 그냥 볼 때와 **글자가 달랐다** :

| 그냥 보면 | 실제 |
|---|---|
| 천장 팬**크**일 | 천장 팬**코**일 (NUT016) |
| 방역전 식개**멸**개 | 방역전 식개**덮**개 (NUT013) |
| 장신구 **작**용 | 장신구 **착**용 (NUT008) |
| **고**열 | **구**열 (NUT008 — NUT006 은 「고열」이라 **두 서식이 다르다는 증거**이기도 하다) |

***없는 낱말을 서식에 박을 뻔했다.*** 앞으로 작은 글자는 반드시 늘려서 읽는다.

### 영양에서 새로 드러난 것 — **판정표에 없던 서식이 또 있다**

메뉴에 이런 것들이 더 있다 : `급식업무일지` · `조리원위생교육일지` · `식중독예방위생점검일지` ·
`냉장고/냉동고/주방 온도·습도관리 기록지` · `안전교육일지` · `위생교육일지` ·
`온도관리기록지(냉장,냉동,식품창고)` · `검식일지` · `일일위생안전청소점검일지` · `청소계획점검표` ·
`위생교육계획서`.
⇒ ***약국에서와 같다 — 등록은 판정표가 아니라 캡처를 기준으로 한다.***

---

### 아직 안 한 약국 — **둘뿐**

| 서식 | 왜 아직인가 |
|---|---|
| 조제장비 일일점검대장 · 약국사용 약물목록 2종 | ***판정표에 없던 것.*** 메뉴에는 있는데 **캡처에서 화면을 연 적이 없다**(62장을 다 훑음) |

**개별 화면으로 판정된 것**(등록 대상 아님, 캡처로 재확인함) — 회수확인서 · 사고마약류 폐기 신청서 ·
의약품부작용보고서(원외) · 의약품 부작용 보고 평가서 · 처방내역 변경 관리대장 · 긴급/신규의약품 신청서 ·
약물 교육 일지 · 유사외관약품(사진) · 약사위원회 조직도·내규·회의록(자료실/기존 화면).

⚠***판정표에 없던 서식이 계속 나온다*** (PHA022 백신 · 응급약품점검기록부 · 조제장비 일일점검대장 · 약국사용 약물목록).
   판정 때는 「무엇을 만들지」를 정하는 것이 목적이라 목록을 완결하지 않았다.
   ⇒ **등록은 판정표가 아니라 캡처를 기준으로 한다.**

---

## 0. ⛔당시 기록 — ***왜 한때 막혀 있었나*** (위 0-A 로 풀림)

서식 하나를 등록하려면 **세 가지**가 필요하다.

| 필요한 것 | 지금 있나 | 어디서 오나 |
|---|---|---|
| 서식 **이름 · 부서** | ✅ 있다 | 판정 문서 |
| **축 · 주기 · 쓰는 기능**(행 그룹·열 묶음·인쇄 나누기 …) | ✅ 있다 | 판정 문서 |
| ⚠**점검항목 문구** (그리고 열 이름 · 표 위 안내 · ※문구) | ❌ **없다** | ***원본 캡처에만 있다*** |

***판정 문서는 「무엇을 어떻게 만들지」를 적은 것이지 원본을 옮겨 적은 것이 아니다.***
「항목 29」·「행 그룹 11묶음」처럼 **개수와 구조**는 적혀 있지만, 항목 **문구**는 없다.

⇒ **캡처 없이 등록하면 항목을 지어내게 된다.**
   ***지어낸 항목은 병원이 진짜인 줄 안다.*** 점검표는 「무엇을 점검하는가」가 내용의 전부라,
   틀린 항목이 든 점검표는 빈 점검표보다 나쁘다. **그래서 여기서 멈춘다.**

⚠판정 문서에 열 이름이 몇 개 적혀 있는 서식도 있다(예 「월·실시주·주제·비고」).
   ***그것으로 등록하지 않는다*** — 판정표의 열 나열은 **요지를 적은 것**이지 전사(轉寫)가 아니다.
   요약을 원본인 척 넣는 것은 지어내는 것과 결과가 같다.

---

## 1. 그래서 지금 할 수 있는 것 — **등록 큐**

캡처가 오면 **곧바로 등록**할 수 있도록, 판정 문서에 흩어져 있는 것을 한 자리에 모았다.

### ① 서식코드 체계 — ***먼저 못 박는다***

| 접두어 | 부서 | 비고 |
|---|---|---|
| `PHA` | 약국 | **001~003 이미 등록됨** |
| `NUT` | 영양 | |
| `FAC` | 시설 | |
| `NUR` | 간호/병동 | |
| `COM` | **공통** | 두 부서 목록에 똑같이 있는 것 — MSDS보관소 점검표 · 직원 교육 결과 보고서 |

`접두어 + 3자리`. **번호는 판정 문서의 순서를 그대로 따른다** — 나중에 대조할 수 있어야 한다.
⚠서식 관리 화면에 **자동 코드 제안**이 있으니 손으로 번호를 세지 말 것(중복이 나면 조용히 덮어쓴다).

### ② 부서별 종수와 **이름이 남아 있는 정도**

| 부서 | 엔진 담당 | 이름이 판정 문서에 있다 | 항목 문구 |
|---|---|---|---|
| 약국 | **17** | 17 (전부) | ❌ (PHA001~003 만 있음) |
| 영양 | **14** | 14 (전부) | ❌ |
| 시설 | **약 49** | 약 30 (축별 표의 예시) | ❌ |
| 간호/병동 | **약 65** | 약 43 (축별 표의 예시) | ❌ |
| **합계** | **약 145** | 약 104 | **3종만 등록됨** |

⚠***시설·간호/병동은 이름조차 절반쯤만 남아 있다.*** 판정 문서가 축별 종수를 세고
   예시만 적었기 때문이다. 그때는 「무엇을 만들지」를 정하는 것이 목적이라 그것으로 충분했다.

---

## 2. 등록 절차 — ***손으로 SQL 을 쓰지 않는다***

서식 관리 화면이 이미 시드 SQL 을 만들어 준다. 그 길을 쓴다.

1. **서식 관리** 화면에서 [＋ 새 서식] → 이름·부서·분류·축·주기를 넣는다
2. 미리보기를 보며 **쓰는 기능**을 켠다 (행 그룹 / 열 묶음 / 인쇄 나누기 / 앞뒤 열 / 행 블록 …)
3. 점검항목을 **원본 문구 그대로** 넣는다
   ⚠**입력종류를 반드시 맞춘다** — 숫자·글자 칸을 `CHECK` 로 두면 서버가 한 글자를 O/X 로 바꾼다.
     ***온도 「1」이 「O」가 되고 이름 「V」가 「O」가 된다.*** 오류가 안 나서 티도 안 난다.
4. [시드 SQL 내보내기] → `docs/sql/QPS_CHK_SEED_<부서>_<날짜>.sql` 에 붙인다
5. 운영에 그 SQL 을 실행한다 (공통 서식이므로 `HOSP_CD='*'`)

★**만드는 동안은 가짜 병원(`99999998`)에 저장해 두고**, 모양이 맞으면 시드로 뽑아 `'*'` 로 넣는다.
  ⚠로컬 톰캣이 **운영 DB** 를 본다 — [로컬 반영 절차](../이력/QPS_인수인계_2026-08-13.md) §5 참고.

---

## 3. 캡처를 어떤 차례로 받으면 좋은가

***이름이 다 남아 있는 부서부터*** 하면 받는 쪽도 주는 쪽도 헷갈리지 않는다.

| 순 | 부서 | 종수 | 왜 이 차례인가 |
|---|---|---|---|
| 1 | **약국** | 14 (PHA001~003 제외) | 이름이 전부 남아 있다. **엔진 검증도 여기서 끝났다** — 처음 3종이 이미 돌아간다 |
| 2 | **영양** | 14 | 이름 전부. `ITEM_DAY` 행 그룹이 많아 **행 그룹·항목 설명 열**이 실물로 확인된다 |
| 3 | **시설** | 약 49 | ***가장 크고 `ITEM_COL` 이 압도적*** — v3 에서 만든 것 대부분이 여기서 쓰인다 |
| 4 | **간호/병동** | 약 65 | 가장 크다. ★「일상점검」 9종은 시설과 **틀이 같아** 한 번에 간다 |

### ★캡처 한 장에 담겨야 하는 것

| 담겨야 함 | 왜 |
|---|---|
| **표 머리글 전부** (2단이면 2단 다) | 열 묶음·행 그룹이 여기서 갈린다 |
| **점검항목 전 행** | 잘리면 그만큼 지어내야 한다 ⇒ ***잘린 채로는 등록 안 한다*** |
| **표 위 안내** (「양호 O 불량 X」 같은 줄) | 없으면 표시 규칙을 모른다 |
| **하단 ※문구 · 서명란** | 법정 문구가 있는 서식이 여럿이다 |
| **상단 칸** (기기명·모델명·점검주기 …) | `HEAD1~8` 로 들어간다 |

⚠**한 서식이 여러 장이면 첫 장과 이어지는 장 모두** — 앞서 정한 대로.

---

## 4. 등록 큐 — 약국 (14종, PHA001~003 제외)

> 축·기능은 판정 문서에서 그대로 가져왔다. **항목 문구만 캡처를 기다린다.**

| 코드(안) | 서식 | 축 | 주기 | 쓰는 기능 |
|---|---|---|---|---|
| PHA001 | 온도. 습도. 의약품 냉장고 온도 점검일지 | `DAY_ITEM` | 월 | ✅**등록됨** — 열 묶음(10am/06pm/02am) |
| PHA002 | 마약류관리보조자 지정 및 임시마약류비치서 | `LIST` | 월 | ✅**등록됨** — 상단 자유칸 3 · ※문구 · 서명란 |
| PHA003 | 잔여 마약류 반납 대장 | `LIST` | 월 | ✅**등록됨** — 열 묶음(반납자·약사) |
| PHA004 | 수액보관실 온,습도 점검일지 | `DAY_ITEM` | 월 | 표 위 안내 |
| PHA005 | 잔여 마약류 폐기 대장 | `LIST` | 월 | 열 묶음(약사·참관직원) |
| PHA006 | 조제 전 / 후 감사 대장 | `LIST` | **연** | ※문구(처방 감사 항목 ①~⑦) |
| PHA007 | 유효기간 임박 약물 관리서 | `LIST` | 월 | 열 묶음(유효기간 경과 후 관리) |
| PHA008 | 의료용마약류 저장시설 점검부 | `LIST` | 월 | 긴 표 위 안내(법정 유의사항 4줄) |
| PHA009 | 약제과 환경 점검표 | `ITEM_DAY` | 월 | **행 그룹**(업무전·조제환경·업무후) |
| PHA010 | 자동 조제기 일일 점검 대장 | `ITEM_DAY` | 월 | **인쇄 나누기** 15칸·열 |
| PHA011 | 반자동 조제기 일일 점검표 | `ITEM_DAY` | 월 | **인쇄 나누기** 15칸·열 |
| PHA012 | 약제과 약사부재중 조제 및 출입관리대장 | `LIST` | **연** | |
| PHA013 | 회수의약품 관리대장 | `LIST` | **연** | |
| PHA014 | 폐기 의약품 대장 | `LIST` | **일** | 문서 단위가 「일」 |
| PHA015 | 알러지 환자 내역 관리대장(약국) | `LIST` | 월 | ⚠「Allergy 환자 내역」과 **다른 서식** |
| PHA016~019 | 의약품 관리대장 — PO / 외용제 / ING / 수액 | `LIST` | 월 | 열 묶음(사용기한/재고) · ***4탭 → 4서식*** |
| PHA020 | 병동 비치 약물 관리 대장 | `LIST` | **연** | 열 묶음(응급/고위험고주의/향정/일반) |
| PHA021 | 냉장고온도관리기록지 | `DAY_ITEM` | 월 | 열 묶음(10시/19시) · **상단 8칸** |

⚠**판정 문서의 종수가 서로 안 맞는다** — 한 장 요약은 14, 절 제목은 11, 실제 표는 17행이다.
   ***세 숫자 중 실제 표가 맞다.*** 위 큐가 그 표에 냉장고온도관리기록지(상단 8칸 때문에
   따로 적혀 있던 것)를 더한 것이다. **캡처를 받을 때 이 목록으로 대조한다.**

---

## 5. 등록 큐 — 영양 (14종)

| 코드(안) | 서식 | 축 | 주기 | 쓰는 기능 |
|---|---|---|---|---|
| NUT001 | 영양초기평가 불량환자 관리 리스트 | `LIST` | **연** | 상단 자유칸(담당자) |
| NUT002 | 밀라운딩 일지 | `LIST` | **연** | ※문구(참조 사항 4줄) |
| NUT003 | 일일 위생점검표 | `ITEM_DAY` | 월 | **행 그룹 11** · 항목 29 · 안내(O/△/X) |
| NUT004 | 청소계획표 | `ITEM_DAY` | 월 | 행 그룹(매일/1주1일/월1회/년1회) · **항목 설명 열**(청소방법) |
| NUT005 | 안전 점검 일지 | `ITEM_DAY` | 월 | **행 그룹 5** · 항목 27 · 긴 안내 |
| NUT006 | 개인위생 확인 기록지 | `LIST` | 월 | **열 묶음 4** · ※문구(관리기준 3줄) |
| NUT007 | 조리실 냉장/냉동고 온도 및 위생 관리일지 | `ITEM_DAY` | 월 | 행 그룹(냉장고/냉동고) · ***3단 머리글을 2단으로 눌러 담는다*** |
| NUT008 | 개인위생 점검일지 | `LIST` | 월 | 열 묶음 · ※문구 |
| NUT009 | 밀라운딩 일지 (일단위) | `LIST` | **일** | 상단 자유칸 3 · ⚠NUT002 와 **다른 서식** |
| NUT010 | 음식물 쓰레기 관리대장 | `DAY_ITEM` | 월 | 단위 Kg · ⚠누계는 **사람이 적는다** |
| NUT011 | 식기 세척기 점검 일지 | `DAY_ITEM` | 월 | 항목 8 · ★**헹굼온도는 반드시 `NUM`** |
| NUT012 | 감량의무 사업장 음식물 쓰레기 관리대장 | `DAY_ITEM` | 월 | 열 묶음(자가감량/재활용) · ※작성요령 |
| NUT013 | 방역 일지 | `LIST` | **연** | 열 묶음(시작·종료) · ※참고사항 |
| NUT014 | 위생 교육 계획서 | `LIST` | **연** | 기본 24행 · ★**주제는 항목이 아니라 데이터** |
| COM001 | MSDS보관소 점검표 (일단위) | `LIST` | **일** | **행 블록**(spill kit / 보관소 내부) · 영양·시설 공통 |
| COM002 | MSDS보관소 점검표 (연단위) | `ITEM_MONTH` | **연** | ⚠위와 **다른 서식** · 인쇄만 연간 모아 |
| NUT015 | 건강관리 현황표 | `LIST` | 월 | **행 블록**(병원 근무자 17 / 배송기사 3 / 대체인력 3) |

⚠**건강관리 현황표도 둘이다**(3블록판 / 3단 열묶음판) — 캡처를 받을 때 어느 쪽인지 확인한다.

---

## 0-C. 시설 — **판독 진행 중** (2026-08-12)

> 판독 결과는 통째로 [QPS_서식판독_시설_2026-08-12.md](QPS_서식판독_시설_2026-08-12.md) 에 있다.
> ***항목 문구까지 다 적어 두었으므로 그 문서만 있으면 시드를 쓸 수 있다.***

**cap126~151(26장) 판독 완료 — 등록할 서식 22종 확정.** 남은 것은 cap152~196(45장).
남은 45장은 **제목 띠만 모아 지도를 만들어 두었다**(판독 문서 끝) — 다음 세션은 한 장씩 열 필요가 없다.

### ★도구를 저장소에 넣었다 — ***스크래치패드는 세션마다 사라지므로***

| 파일 | 무엇 |
|---|---|
| [docs/ExtractImgs.ps1](../../tools/ExtractImgs.ps1) | 세션 기록(.jsonl)에서 캡처를 꺼낸다 (306장 확인) |
| [docs/Zoom.ps1](../../tools/Zoom.ps1) | 캡처 일부를 3~5배로 늘린다. ***작은 글자는 반드시 이걸로 읽는다*** |
| [docs/Montage.ps1](../../tools/Montage.ps1) | 여러 캡처의 제목 띠만 붙여 한 장으로. **45장을 3장으로 줄였다** |

### 시설에서 새로 드러난 것

| | 내용 |
|---|---|
| ⚠**「고정 띠」** | 연간시설물안전관리계획은 **행 절반이 12칸 격자가 아니라 「매월 1회 실시」 같은 고정 문구**다.<br>격자를 열면 원본에 없던 점검칸이 264개 생기고 ***「매월 실시한다」가 「매달 O를 찍어라」로 뜻이 바뀐다.***<br>⇒ **보류.** 후보 `ITEM.SPAN_TXT`. 근거 1종(연간소방안전관리계획이 2종째일 가능성 높다 — cap159) |
| ⚠**`MONTH_ITEM` 은 아직 빈자리** | `qpsChk.jsp:315` `isMonthly(){ return axis()!=='ITEM_MONTH' }` — ***연 1장인 축은 `ITEM_MONTH` 뿐***이다.<br>「`DAY_ITEM`+`PRD_KIND='M'`」은 12월 행을 그려도 **문서는 월 1장**이라 12월짜리 표가 열두 개 생긴다.<br>⇒ 냉·난방필터청소점검일지는 **`ITEM_MONTH` 로 축을 돌려서** 넣는다(자료는 같고 인쇄만 90도 돈다) |
| ★★**쉼표 함정** | 원본 열 이름에 쉼표가 있으면(`점검결과 (양호 ○, 불량× )` · `적,부 (O,X)`) **`COL_NMS` 가 쪼갠다.**<br>***오류가 안 난다.*** ⇒ 안내는 `GUIDE_TXT` 로 옮긴다. `HEAD_NMS`·`PRE_COLS`·`POST_COLS`·`SIGN_LINE`·`ROW_BLKS`·`PRD_SUB` **전부 같다** |
| ⛔**MSDS 관리대장** | `LIST` 인데 **열 묶음(물질명)을 문서가 정한다.** `COL_SRC='D'` 는 `ITEM_COL` 에서만 켜진다.<br>***근거 1종이라 만들지 않는다.*** |
| ⛔**재캡처 2건 추가** | 의료가스및진공설비안전점검표-월단위(**좌우 머리글이 둘 다 「자동운전」**, 행이 한 칸 밀림) ·<br>유해화학물질 적정 취급상태 모니터링(**항목 12에서 잘림**) ⇒ 재캡처 **4건**이 됐다 |
| ✅**확인 필요 #2 해소** | cap174 제목이 **「사용중지 -의료폐기물실 관리점검표」** — ***중복 4신호가 또 맞았다.***<br>#3·#4·#5 도 두 판이 다 캡처에 있어 대조하면 풀린다 |
| ★**판정표에 없던 서식 3종** | 의료폐기물실 냉장고 온도 점검표 · 개인영상정보 관리대장 · 액상 폐기물실 관리점검표<br>⇒ 약국·영양과 똑같다. **등록은 판정표가 아니라 캡처를 기준으로 한다** |
| ⚠**판정표와 어긋난 것 2건** | CCTV(판정=주차 열 / 실물=단순 목록) · 유해화학물질목록점검(판정=행 블록 / 실물은 **머리글 되풀이**, 블록 아님) |

---

## 6. 등록 큐 — 시설 · 간호/병동

***이름이 절반쯤만 남아 있어 큐를 완성할 수 없다.*** 캡처를 받으면서 채운다.
아래는 판정 문서가 적어 둔 **축별 종수**다 — 캡처 수와 이 숫자를 대조하는 데 쓴다.

### 시설 (약 49)

| 축·기능 | 종수 | 판정 문서에 이름이 있는 것 |
|---|---|---|
| `ITEM_COL` | **13** | 발전기 · 건축물 · 가스시설 · 저수조 · 공기조화설비 3 · 소방시설월 · 방화보안순찰 · 냉난방기 · 시설물정기점검 2 · 의료/유해물질 취급부서 |
| 주차(1~5) 열 | 4 | CCTV · 가스보일러 · 전기시설주간 · 세탁물관리 |
| 항목 설명 열 | 3 | 연간시설물안전관리계획 · 연간소방안전관리계획 · 청소계획표(영양) |
| 항목 뒤 열 | 3 | 연간시설물(예산) · 연간소방(예산) · U.P.S(조치사항) |
| 행 블록 | 6 | 유해화학물질목록점검 · 공기조화설비 혼합형 · 시설물정기점검 2 · 세탁물관리 (+영양 2) |
| 인쇄 나누기 | 7 | 카트및엘리베이터 · 세탁물보관소 · 소화전 · 육안일일 · U.P.S(6칸) · 의료가스및진공설비(좌우) |
| 문서가 정하는 열 | 2 | MSDS관리대장(물질명) · 소방시설월점검표(층·병동) |
| 일상점검 계열 | 7 | 냉각탑 · 의료용산소발생기 · 의료가스 2 · 진공펌프 · 온수보일러 · 공기조화장치 |
| `EQUIP_MONTH` 조합 | 3 | 소화기 관리대장(연) (+약국 2) |
| `MONTH_ITEM` 조합 | 1 | 냉·난방필터청소점검일지 |

### 간호/병동 (약 65)

| 축 | 종수 | 판정 문서에 이름이 있는 것 |
|---|---|---|
| `ITEM_DAY` | **약 24** | EO GAS 멸균기 · 멸균물품보관장 · 청소 · 환기 · 낙상예방활동 · 뇌파검사기 · 혈당측정기 · O2탱크 · iSyncWave · 오물처리실 · 세탁물보관실 |
| `LIST` | **17** | 혈액 관리 · 혈액 입/출고 · 지참약 마약 · CVR · TAT · 멸균물품 회수 · 응급약물 봉인해제 · 위해도구 3 · PRN · 유효기간 임박 |
| `ITEM_COL` | 6 | 비치의약품 보관상태 · 고위험 병실 순회 · 병동 순회일지 · 공기정화 및 환기 · 정신과적 고위험 명단 · 병동 위해도구 |
| `DAY_ITEM` | 5 | 혈액/검체 냉장고 온도 · 고압증기멸균기 CI · 냉장고 온도관리 · 세탁물 운반차 소독 |
| `ITEM_MONTH` | 5 | 억제대 · 응급 벨 · 낙상예방 시설환경 · 낙상예방 환경시설물 · 일회용 소독물품 |
| `EQUIP_DAY` | 3 | 병동비치약품 · 물품 Count · 위해도구 일일 |
| 주 단위 | 3 | 병동 위생관리 계획표 · 병동 환경 위생 관리 · 세탁물집하장 운반용기 |
| 일상점검 계열 | 2 | AED · 고압증기 멸균기 — ***시설 7종과 틀이 같다*** |
| v3 조합 | 2 | 화재대피 순서 환자명단 · 향정 비치 의약품 |

---

## 7. 등록하면서 같이 처리할 것

| | 내용 |
|---|---|
| ✅**부서 「공통」** | **고쳤다(2026-08-12, `build=20260812-COMMON`).** 부서를 고르면 **그 부서 + 공통**이 나온다.<br>목록·**추출**·**이행 요약** 셋 다 같은 규칙 — 하나만 고치면 ***화면엔 보이는데 뽑으면 빠진다.***<br>자동 코드 접두어도 `CHK` → **`COM`** 으로 맞췄다(이 대장과 대조되도록). |
| ⚠**중복 4신호** | 「사용중지」 · 「○○ 내용 동일」 · **2022-12 로 굳은 빈 화면** · 같은 화면을 메뉴만 둘로. ***이름이 비슷하면 먼저 이걸 본다*** |
| ⚠**결정 대기 5건** | 범위 · 「유/무」 집계 · 자동 계산(재고량·총 수) · 원본 오타 4건 · 재캡처 2건 — [인수인계](../이력/QPS_인수인계_2026-08-11.md) ⑥ |
| **연간 모아 찍기 2종** | v3 순서 6에서 유일하게 남은 것. 세탁물집하장 소독점검대장 · MSDS보관소(연). ***문서는 월별로 두고 인쇄만 모은다*** |

---

## 8. 한 줄 요약

***엔진은 끝났고, 남은 것은 옮겨 적는 일인데, 옮겨 적을 원본이 지금 손에 없다.***
캡처를 부서 하나씩 주면 그 부서를 통째로 등록한다. **약국 14종부터가 가장 빠르다.**

---

## ⑧ 캡처 꺼내는 스크립트 — ***스크래치패드는 세션마다 사라지므로 여기 남긴다***

```powershell
# 세션 기록(jsonl)에 박힌 캡처를 파일로 꺼낸다. 사용자가 이미 올린 것을 되찾는 것이다.
param(
  [string]$Jsonl = 'C:\Users\SRC\.claude\projects\D--egv\<세션ID>.jsonl',
  [string]$Out   = '<scratchpad>\caps'
)
if (-not (Test-Path $Out)) { New-Item -ItemType Directory -Path $Out | Out-Null }
$rx = [regex]'"type":"image","source":\{"type":"base64","media_type":"image/png","data":"([^"]+)"'
$seen = @{}; $n = 0; $ln = 0
$log = New-Object System.Collections.Generic.List[string]
foreach ($line in [System.IO.File]::ReadLines($Jsonl)) {
  $ln++
  if ($line.IndexOf('"type":"image"') -lt 0) { continue }
  foreach ($mm in $rx.Matches($line)) {
    $b64 = $mm.Groups[1].Value
    $key = $b64.Length.ToString() + '_' + $b64.Substring(0, [Math]::Min(160, $b64.Length))
    if ($seen.ContainsKey($key)) { continue }      # 압축 요약에 다시 실린 것은 거른다
    $seen[$key] = $true; $n++
    $name = ('cap{0:D3}.png' -f $n)
    [IO.File]::WriteAllBytes((Join-Path $Out $name), [Convert]::FromBase64String($b64))
    $log.Add("$name`tline=$ln")
  }
}
$log | Set-Content (Join-Path $Out '_index.txt') -Encoding UTF8
"꺼낸 이미지 = $n 장"
```

**작은 글자 확대** (읽기 전에 반드시) :

```powershell
Add-Type -AssemblyName System.Drawing
$b=[System.Drawing.Bitmap]::FromFile($src)
$r=New-Object System.Drawing.Rectangle($x,$y,$w,$h)      # 읽을 곳만 잘라낸다
$crop=$b.Clone($r,$b.PixelFormat)
$big=New-Object System.Drawing.Bitmap(($r.Width*3),($r.Height*3))
$g=[System.Drawing.Graphics]::FromImage($big); $g.InterpolationMode='HighQualityBicubic'
$g.DrawImage($crop,0,0,$big.Width,$big.Height); $g.Dispose()
$big.Save($out,[System.Drawing.Imaging.ImageFormat]::Png)
```

⚠**배포 함정** : 클래스·JSP 복사와 `web.xml` touch 가 겹치면 **컨텍스트가 통째로 죽는다**(전부 404).
   이클립스 톰캣은 로그를 콘솔로만 내보내 파일에 흔적이 없다. ⇒ **web.xml 을 한 번 더 touch** 하면 산다.
