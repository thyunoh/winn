# QPS 인수인계 — 2026-08-12 (시설 완주 · 엔진 숙제 5건 · 시드 43종)

> ***이 문서가 지금 인수인계 정본이다.*** 앞 문서 [QPS_인수인계_2026-08-11.md](QPS_인수인계_2026-08-11.md) 는
> 「그때까지 어떻게 왔나」로 남긴다 — **저장 엔드포인트 15개 함정 · 예약 번호 표 · 로컬 도구**는
> 아직 그 문서에만 있으므로 **필요할 때 그쪽을 본다.**
> 왜 그렇게 만들었는지·확정사항은 [QPS_세션요약_2026-08-08.md](QPS_세션요약_2026-08-08.md).

---

## 0. ★★가장 먼저 — **운영은 아직 옛 WAR 다**

| | 상태 |
|---|---|
| **DB(운영 WNN)** | ✅ **전부 적용 완료** — DDL 3개 + 시드 43종. ***다시 돌리지 말 것*** |
| **운영 WAR** | ⛔ **아직 옛 코드.** 오늘 고친 자바·매퍼·JSP 가 안 들어가 있다 |
| **로컬(8080)** | ✅ 오늘 것 전부 반영·검증 완료 |

**DB 가 먼저 들어간 것은 사고가 아니라 순서다** — 오늘 DDL 은 전부 *더하기만 하는 ALTER* 라
새 컬럼이 NULL 이거나 기본값(`'N'`)이고 **옛 SQL 은 그 컬럼을 읽지도 쓰지도 않는다.**
⇒ 지금 운영은 **평소대로 돌아간다.** 배포하면 그때 화면에 나타난다.

### 배포하면 되는 것 (사용자가 직접)

자바 2 · 매퍼 XML 1 · JSP 2 가 섞였으므로 ***WAR 재빌드 + 재기동***이다
([배포_가이드](../배포_가이드.md) §2 표 그대로). DDL·시드는 **다시 돌리지 않는다.**

```
① mvn clean package
② webapps/wnn_medcost-1.0.0 에 전개   ← ★ROOT 아님(2026-08-07 에 여기서 한참 헛돌았다)
③ /usr/share/tomcat9/bin/shutdown.sh → ps 로 0 확인 → startup.sh
   ⚠systemctl restart tomcat9 금지(유령 인스턴스를 건드린다)
```

**반영됐는지 확인** — QPS 응답의 `build` 표식이 **`20260812-FACSEED`** 면 새 WAR 다.
(옛 값은 `20260812-PRDSUB`. 다르면 clean 을 빼먹었거나 다른 폴더를 본 것이다.)

---

## 1. 오늘 한 일 — 한 장

| # | 한 일 | 결과 |
|---|---|---|
| ① | **시설 캡처 판독 완주** (cap126~196) | 등록 **44종** 확정 · 확인 필요 5건 중 **4건 해소** |
| ② | **엔진 숙제 5건** 구현 | `BLK_NM` · `SPAN_TXT`/`SPAN_ALL_YN` · `PRD_HEAD_*` · `NOTE_NM` · `CARRY_YN` |
| ③ | **시드 43종 적재** | FACIL 42 + COMMON 1(COM002). ***돌렸다*** |
| ④ | 메일 발송 계정 교체(운영) | 서버 `/home/winner/conf/mail.properties` — winner797, 보낸이름 「위너넷 컨설팅」 |

***★엔진 숙제가 보류 서식 둘을 되살렸다*** — 시드 직전에 다시 세어 보니
FAC003(고정 띠)·FAC025(블록 띠)가 풀려 있었다. **막던 조각이 생기면 보류 목록을 다시 센다.**

---

## 2. 오늘 만든 엔진 칸 — **무엇을 위해 만들었는지**

> 전부 기준 그대로다 — ***근거 3종을 넘으면 만든다.*** 그 아래는 안 만든다.

| 칸 | 무엇 | 어디에 쓰이나 |
|---|---|---|
| `ITEM.BLK_NM` | **가로 띠** 한 단(묶음 `GRP_NM` 위) | FAC025 공기조화 혼합형 · COM002 MSDS보관소 |
| `ITEM.SPAN_TXT` | 격자를 대신하는 **고정 문구** (입력 불가) | FAC003 연간 시설물계획(11줄) · FAC027 소방시설 월(2줄) |
| `FORM.SPAN_ALL_YN` | 그 띠가 **뒤 칸(POST_COLS)까지** 덮나 | FAC027 만 `Y`(상태 칸까지) · FAC003 은 `N`(예산 칸 남김) |
| `FORM.PRD_HEAD_YN`·`_NM` | 기간 열마다 **문서가 적는** 머리글 한 줄 | FAC029 CCTV · FAC030 가스보일러 · FAC039 전기시설(주차 날짜) |
| `FORM.NOTE_NM` | 표 아래 칸의 이름 | 조치사항 · 특이사항 및 조치사항 · ※ 조치가 필요한 사항 · 기타 이상내용 |
| `ITEM.CARRY_YN` | **전월복사로 가져올 열**(자산) | FAC031 냉난방기 · FAC033 소화기 · FAC017 유해물질 · FAC014 CCTV현황 |

### ★★값이 사는 자리 — **예약 번호에 하나 늘었다**

앞 문서 §「예약 번호」 표에 **한 줄을 더한다.** ***여기를 안 보고 칸을 더하면 값이 겹친다.***

| 번호 | 뜻 |
|---|---|
| `900` | 점검자 사인 행/열 (기존) |
| **`890`** | **기간 열 머리글 입력 행**(`PRD_HEAD`) — 사인 바로 앞에 붙였다 |
| `1000·2000` 대 | 격자 앞/뒤 열 (기존) |
| `블록×1000 + 항목` | 행 블록 · 문서가 정하는 묶음 (기존) |

### ★전월복사 — **판단 하나를 뒤집었다**

옛 코드에 「수량처럼 자산에 가까운 것이 섞여 있지만 ***칸마다 갈라 줄 근거가 없다*** ⇒ 전부 안 한다」
고 적혀 있었다. **그 근거가 생겼다** — FAC031 냉/난방기는 행이 **60줄**, FAC033 소화기 대장도
수십 줄이라 ***매달 다시 치라는 것은 「못 쓴다」와 같다.***

⇒ 근거 없이 반만 복사하는 대신 **서식이 열을 지목한다**(`CARRY_YN`). 어느 칸이 따라오는지
서식 관리 항목표 체크와 미리보기 `↩전월` 배지에 드러나므로 **「아무도 모른다」가 성립하지 않는다.**

**안전장치 셋 — 실측 확인** :
1. 기본이 `N` — 켜지 않으면 지금까지와 똑같다(옛 서식 12종 무영향)
2. **빈 칸에만 채운다** — 적다 말고 눌러도 이미 친 글자를 안 덮는다
3. **`LIST` 축만** — 사인 행(900)·앞뒤 열(1000/2000대)도 안 온다

---

## 3. 시드 43종 — 어디에 무엇이

| 파일 | 종수 | 비고 |
|---|---|---|
| [QPS_CHK_SEED_FAC1_2026-08-12.sql](../sql/QPS_CHK_SEED_FAC1_2026-08-12.sql) | **19** (FAC001~022) | 카트·세탁물·연간계획·UPS·폐기물·일상점검 계열 등 |
| [QPS_CHK_SEED_FAC2_2026-08-12.sql](../sql/QPS_CHK_SEED_FAC2_2026-08-12.sql) | **24** (FAC023~047 + COM002) | 공기조화 3 · 소방 4 · CCTV · 냉난방기 · 소화기 · 시설물정기점검 2 등 |

DB 확인 : `DEPT_CD='FACIL'` **42종** · `COMMON` 2종(COM001 기존 + COM002 신규).

### ⛔시드에 **안 넣은 것**과 이유

| 서식 | 왜 |
|---|---|
| **FAC026** 연간 소방안전관리 계획 | 월 칸이 **체크 + 글** 둘이다(`CHECK_TXT` 후보, 근거 1종). `CHECK` 로만 넣으면 「(재직원)/(신규직원)」이 사라진다 ⇒ ***반쪽으로 넣지 않는다*** |
| **FAC034** 소화기 관리대장(연) | **자유행 × 1~12월.** `LIST` 엔 기간 열이 없고 `ITEM_MONTH` 는 행을 서식이 정한다. v3 조합 작업 뒤 |
| **FAC009** 의료가스 일상점검(평면판) | **뒷판 FAC042 를 넣었다**(확인 필요 #3). 「사용중지」 표시가 없어 확정은 아니다 — 병원 확인 1순위 |
| **FAC016** 유해화학물질 모니터링 | 캡처가 **잘렸다.** 13번부터가 있는지 알 수 없다 ⇒ 재캡처 뒤 |
| MSDS 관리대장 · cap142 · cap147 · cap150 | 각각 「LIST 의 열을 문서가 정한다」 없음 · 개별 화면 · 깨진 화면 · 입력칸 없음 |

---

## 4. ★★병원(위너넷) 확인 목록 — **답이 오면 서식이 바뀐다**

우선순위 순. 1~3 은 **어느 판을 쓰는지**라 답에 따라 서식을 내리거나 되살린다.

1. **의료가스 일상점검일지, 어느 판?** — 평면판(FAC009, 압력·알람 있음) / 보관실·이동용판(**FAC042 로 넣어 둠**)
2. **시설물 정기점검 둘 다 쓰나** — FAC037(건축물, 결과 1열) · FAC038(결과 2열·부서 5). **뼈대가 달라 둘 다 넣어 둠**
3. **소방시설 일일점검표** — cap183(화기취급판)으로 넣음. 구판(cap161, 「경보설비」 오기)을 쓰면 알려 달라
4. 세탁물집하장 소독점검대장(FAC043) — **연도가 2022 에 굳어 있다.** 아직 쓰나
5. **우리가 붙인 이름** — FAC022 「1차·2차」 순찰 · FAC027 「비상경보설비 (경종)/(싸이렌)」. 원본엔 구별 이름이 없다
6. FAC047 `구분` 칸 — 체크 4개(이용/제공/열람/파기)를 **글자 입력으로 바꿨다**
7. FAC014 CCTV 설치현황 — **전월복사를 켰다**(판독 문서에 없던 판단. 결과 열이 없어 위험이 없다고 봤다)
8. FAC040 안내문이 「의료폐기물실」로 찍혀 있다(서식은 사업장폐기물실) — 원본 오류, 그대로 둠
9. 공기조화설비 안내표(사진 4묶음·2장) — 서식에서 뺐다. 필요하면 자료실
10. **「주말서명 삭제」 버튼**(FAC036·040·041 원본에 있다) — 주말 자동서명이 필요한가
11. FAC007 청소·검사·약품투입 일자 — 원본은 **표 아래**인데 **상단 자유칸**으로 올렸다(인쇄 모양이 다르다)
12. COM002 주기 — `일 단위`로 봤다. 제목의 `( 2026 )년` 은 해 표시로 읽었는데 맞나

**재캡처 4건** — 청소점검 일상점검표 · 자해타해 예방활동 기록지 ·
자동산소발생기 및 진공설비 일일점검표 · 의료가스 및 진공설비 안전점검표-월단위[cap147].

---

## 5. 다음 세션 첫 지시

> **"QPS 이어서"** — 이 문서를 읽고 아래에서 고른다. 경로도 배경도 말할 필요 없다.

| 후보 | 무엇 |
|---|---|
| **ⓐ 간호/병동 서식 등록** | 🔨**착수함** — 캡처를 찾았고(**`cap197~306`**, 시설과 같은 폴더) **지도 110장 완성.** 이어받기 = [QPS_서식판독_간호병동_2026-08-13.md](QPS_서식판독_간호병동_2026-08-13.md) |
| ⓑ 보류 2종 | `CHECK_TXT`(근거 1종) · 자유행×12월(v3 조합). ***근거가 더 모이면*** |
| ⓒ 개별 화면 8종 | 비상발전기 · 소방훈련결과 · 승강기 고장 · 시설환경 안전사고(→`safeRpt` 유형 추가로 끝날 가능성 높음) 등 |
| ~~ⓓ 회의록 `FORM_GB`~~ | ✅**끝났다** — 아래 §5-1 |
| ⓔ 연간 모아 인쇄 | 문서는 월별인데 **인쇄만 12개월을 한 장에**(근거 2종 — FAC043 · COM002) |

## 5-1. ✅[2026-08-13] 위원회 3형제 — **회의록 화면 하나로 끝냈다**

***새 화면을 만들지 않았다.*** 약사·영양관리·소방안전관리 회의록이 우리 화면과 판박이라
**`FORM_GB` 값 세 개 + 사이드바 링크 세 줄**이 전부다
(약국 판정 §3-2 · 영양 판정 §1-4 · 시설 판정 §5 — 「회의록은 단일 서식으로 간다」가 또 값을 돌려줬다).

| 값 | 위원회 | 하단 조치표 |
|---|---|---|
| `P` | 약사 | 없음 |
| `N` | 영양관리 | 없음 |
| `S` | 소방안전관리 | ✅**있다** — 원본에만 있는 네 칸 한 줄 |

### 미뤄 뒀던 질문의 답 — **칸이 없었다. 그래서 만들었다**

시설 판정 §5 가 「`TBL_QPS_MINUTES` 에 그 칸이 있는지 확인해야 한다. 없으면 `FORM_GB` 추가만으로는
안 된다」고 남긴 자리다. **확인 결과 없었다** ⇒ [QPS_DDL_MINUTES_ACT_2026-08-12.sql](../sql/QPS_DDL_MINUTES_ACT_2026-08-12.sql)
로 `ACT_GB`·`ACT_DEPT`·`ACT_DUE`·`ACT_COST` 를 더했다(***돌렸다***).
원본이 **한 줄**이라 자식 표를 만들지 않고 본문에 붙였다 — 여러 줄이었으면 표를 따로 뒀을 것이다.

⚠**`ACT_GB` 는 머리의 `MEET_GB`(정례/임시 체크)와 다른 칸이다.** 원본이 둘 다 「회의구분」이라 부를 뿐 —
  합치면 서로를 덮어쓴다. 화면 라벨을 「조치사항」으로 갈라 두었다.
★조치표는 **`S` 에서만** 그린다(`ACT_GBS` 목록 한 곳에서 정한다) — 다른 위원회 원본에는 없는 표라
  ***원본에 없던 칸을 만들지 않는다.*** 표가 안 보이는 위원회에서는 **빈 값으로 저장**해
  위원회를 바꿔 저장할 때 옛 값이 조용히 따라가지 않게 했다.

### ★찾아 고친 결함 2건 (이 작업 중)

1. **`putFormGb` 화이트리스트에 `P`·`N` 이 없었다** — 없는 값은 조용히 `Q` 가 되므로
   ***약사·영양관리 링크가 질향상위원회로 열렸을 것***이다. 링크를 더할 때 **반드시 여기도 더한다.**
   (옛 주석의 `S=만족도` 는 사실과 다르다 — 만족도·불만고충 화면은 이 헬퍼를 안 쓴다. 주석도 고쳤다)
2. **인쇄 제목이 「QPS위원회 회의록」으로 박혀 있었다** — 감염·QI·RCA·FMEA 를 인쇄해도 전부 그렇게 찍혔다.
   고른 위원회를 따라가게 고쳤다(**기존 4종도 같이 나아졌다**).

### ✅화면 검증 — **전부 통과** (2026-08-13 실측)

| 본 것 | 결과 |
|---|---|
| 사이드바 **▸ 부서 위원회** 3줄(`?gb=P`·`N`·`S`) | ✅ |
| `?gb=P` → **약사위원회 회의록**, 조치표 숨김 | ✅ |
| 셀렉트 `N` → **영양관리위원회 회의록**, 조치표 숨김 | ✅ (화이트리스트 결함 1 이 고쳐진 자리) |
| 셀렉트 `S` → **소방안전관리위원회 회의록**, 조치표 네 칸 | ✅ |
| 저장 → 재조회 왕복(조치표 4칸 그대로) | ✅ |
| 인쇄 — 제목이 **소방안전관리위원회 회의록**, 조치표 찍힘 | ✅ |
| **약사 목록을 보다 `S` 문서를 열면 셀렉트가 문서를 따라가나** | ✅ `P`→`S`. 그대로 저장해도 **조치표 4칸 살아남음**(DB 확인) |

QPS 관련 JS 오류 0(뜬 것은 전 화면 공통인 대시보드 차트 플러그인).
가짜 병원 `99999998` 회의록은 **지웠다**(잔여 0건).

### ★★결함 3 — **검증 중에 잡았다. 조치표가 조용히 지워질 수 있었다**

상단 셀렉트는 **「목록 필터」와 「이 문서의 위원회」 두 역할을 겸한다.**
`S` 문서를 열어 둔 채 셀렉트를 다른 위원회로 바꾸고 [저장]하면
***조치표 네 칸이 빈 값으로 덮인다*** — 실제로 재현했다(DB 에서 네 칸이 `[]` 가 됐다).

- 저장은 `FORM_GB` 를 안 고치므로 **문서는 늘 만든 위원회에 남는다** — ***화면만 어긋났던 것***이다.
- 원인 : `minutesGet` 이 **문서의 `FORM_GB` 를 안 돌려줬다.** 화면이 알 길이 없었다.
- 고침 : `selectMinutes` 에 `FORM_GB AS formgb` 추가 → `qmOpen` 에서 **셀렉트를 문서에 맞춘다.**

⚠**매퍼 XML 은 기동 때 한 번만 읽는다** — 이 고침은 `web.xml` touch 로는 안 올라왔다
  (앞선 리로드는 앱이 000→200 으로 내려갔다 왔는데 이때는 줄곧 200 이었다).
  ⇒ ***`web.xml` touch 는 믿을 수 없다.*** 매퍼를 고쳤으면 **서버를 재기동한다.**
    확인법 : `minutesGet.do` 응답에 `formgb` 가 오면 새 매퍼다. **재기동 뒤 실제로 통과했다.**

---

### 판독을 이어받을 때 (시설과 같은 방식)

- 캡처는 **다시 받지 않는다** — 세션 기록 jsonl 에 base64 로 박혀 있고
  [ExtractImgs.ps1](../ExtractImgs.ps1) 이 그대로 꺼낸다. [Zoom](../Zoom.ps1) · [Montage](../Montage.ps1) 도 저장소에 있다.
- **제목 띠만 모아 보는 것**(Montage)이 45장을 3장으로 줄였다. 한 장씩 열기 전에 이것부터.
- 판독 정본 = [QPS_서식판독_시설_2026-08-12.md](QPS_서식판독_시설_2026-08-12.md) — 그 형식 그대로 쓴다.

---

## 6. 이 세션에서 새로 배운 함정

| 함정 | 증상 |
|---|---|
| **열 이름 속 쉼표** | `COL_NMS` 는 쉼표로 열을 가른다 — `점검결과 (양호 ○, 불량× )` 를 그대로 넣으면 **열이 둘이 되어 조용히 표가 틀어진다.** ***오류가 안 난다.*** `COL_NMS`·`HEAD_NMS`·`PRE_COLS`·`POST_COLS`·`SIGN_LINE`·`ROW_BLKS`·`PRD_SUB` 전부 해당 |
| **체크박스의 `value`** | 항목표에서 `el.value` 로 읽으면 체크박스는 늘 `'on'` — 그대로 담아 `CARRY_YN` 이 전 열에 켜질 뻔했다(`el.checked` 로 고침) |
| **머리글과 몸통은 같이 숨긴다** | 항목표에 칸을 더할 때 `<th>` 만 숨기면 표가 한 칸씩 밀린다 — `data-cell` 로 몸통도 같이 |
| **블록이 갈리면 묶음도 끊는다** | 안 끊으면 이웃 블록의 같은 이름 묶음이 rowspan 으로 이어져 **묶음 칸이 띠를 뚫고 내려간다** |
| **같은 이름 띠 둘은 띠가 아니다** | 원본이 머리글을 되풀이한 것이다(FAC017·FAC038). 블록으로 세우면 없는 구조를 만든다 |
| **원본 트리에 같은 이름이 두 번** | 소방시설 일일점검표 · 의료가스 일상점검 · 소화기 관리대장 · 시설물 정기점검. **꼬리 쪽이 늘 새것은 아니다**(cap174 는 꼬리가 「사용중지」) ⇒ **내용으로 가른다** |

### ⛔`.claude/worktrees` 가 커밋되면 **push 가 500 으로 막힌다** (2026-08-12 실제 발생)

Claude Code 가 만든 임시 worktree 폴더를 git 이 ***SHA 가 전부 0 인 gitlink***(`160000 000000…`)로
인덱스에 넣고, 그게 **커밋까지 되면** 그 뒤로 `status`·`commit`·`push` 가 전부 막힌다.

- 증상 ① `error: cache entry has null sha1: .claude/worktrees/…`
- 증상 ② **eGit push 가 `500 Internal Server Error`** — GitHub 이 null-sha 객체를 거부하는 것이다.
  ***네트워크·권한 문제로 오진하기 딱 좋다.***
- ⚠**나중에 지우는 커밋을 얹어도 안 풀린다** — push 는 커밋을 통째로 보내므로
  ***옛 커밋 안의 나쁜 트리가 그대로 전송된다.*** **그 커밋들을 고쳐야 한다.**

**푼 방법** (아직 원격에 없는 로컬 커밋이라 안전했다) :
```bash
git branch -f backup/before-worktree-fix HEAD          # ★먼저 백업
git filter-branch -f --index-filter \
  'git update-index --force-remove .claude/worktrees/fav-max-2 .claude/worktrees/fav-menu-2 2>/dev/null || true' \
  -- origin/main..HEAD
git diff backup/before-worktree-fix HEAD               # ★비어야 한다(파일 내용 동일 확인)
```
⇒ 커밋 6개 유지, 해시만 바뀜, **파일 내용 차이 0**. 재발 방지로 `.gitignore` 에
`**/.claude/worktrees/` 를 넣었다(왜 막히는지 주석과 함께).

### ⚠못 하는 조합 하나

**고정 띠 + 인쇄 열-끊기(`SPLIT_DIR='C'`)** — 띠 칸에 `data-day` 가 없어 조각마다 통째로 남는다.
근거 서식 3종이 전부 안 끊는 판이라 **지금은 부딪히지 않는다.** 부딪히면 그때 고친다.

---

## 7. 검증할 때 지킨 것 (다음에도)

- 가짜 병원 **`99999998`** 로만 저장하고 **끝나면 지운다**(잔여 0건 확인). 실제 병원 자료에 테스트 행을 남기지 않는다.
- `DELETE ... WHERE HOSP_CD='...'` 로 싹 지우지 않는다 — **내가 만든 행만** 지정해서 지운다
  (전에 사용자가 방금 저장한 자료까지 날려 「저장이 안 된다」 오진의 원인이 됐다).
- 순서는 ①API ②DB ③화면. 쿠키 두 개(`s_hospid`·`s_wnn_yn`)면 로그인 없이 API 를 직접 때려볼 수 있다.
- 클래스·매퍼를 고쳤으면 **`web.xml` touch 로 리로드**하고, 화면이 아니라 **`build` 표식**으로 반영을 확인한다.
- ⚠**Eclipse 서버가 도는 중에 명령줄 `mvn compile` 을 돌리지 말 것**(2026-08-12 실제 발생).
  Eclipse 자동배포와 겹치면 `.class` 를 쓰는 순간을 배포기가 읽어
  ***`Publishing failed … File not found: …QpsController$1.class`*** 가 나고
  **배포 폴더의 그 패키지가 통째로 비어 버린다.** 8080 은 옛 클래스가 메모리에 남아 200 을 돌려주므로
  ***재기동 전까지 티가 안 난다.***
  ⇒ 고치는 법 : **Servers ▸ 서버 우클릭 ▸ Clean...**(전체 재배포). 소스는 멀쩡하다.
  ⇒ 피하는 법 : 검증용 빌드는 **Eclipse 빌드에 맡기거나** 서버를 멈추고 돌린다.
