# QPS 이식 — 작업 기록 (2026-08-08 시작 · 최종 갱신 2026-08-10)

> 이 문서 + [전체지도 HTML](QPS_업무_전체지도_2026-08-08.html) 만 보면 어느 PC에서든 맥락이 잡힌다.
> **아래 「▶ 다음은 여기부터」만 읽고 시작해도 된다.** 나머지는 필요할 때 찾아보는 근거다.

---

## ▶ 다음은 여기부터 (2026-08-10 최종)

### ★범위 변경 — 감염관리 포함 (2026-08-10 오후, 사용자 지시)
**8/9 「감염 폴더 1차 제외」 확정을 뒤집었다. 이제 QPS 전체 적용 = 감염관리 포함이다.**
아래 「[확정 2026-08-09] 범위 — 감염 폴더 제외」 절은 **무효**(대조표만 근거로 남긴다).

**끝난 것 — 서식 1·2·3호에 서식구분(FORM_GB) 얹기**
[QPS_DDL_INF_2026-08-10.sql](../sql/QPS_DDL_INF_2026-08-10.sql) 적용·검증 완료. BUILD=`20260810-1600`.
- 회의록·계획서·라운딩에 `FORM_GB`(Q=질향상 / I=감염관리). 기존 행은 전부 `Q`.
- ***★왜 컬럼부터였나*** — 계획서·라운딩은 유니크 키가 `병원+년도`, `병원+년월` 이라
  **감염용을 만들면 저장이 막힌다.** 키를 `병원+구분+년도(년월)` 로 바꿨다. 화면부터 손댔으면
  자료가 엉킨 뒤에 발견됐을 문제다.
- ***★첨부 키도 같이 바꿨다*** — 계획서 REF_KEY `2026` → `2026|Q`, 라운딩 `202608` → `202608|Q`.
  안 그러면 감염 라운딩 사진이 질향상 라운딩 첨부에 섞인다. 기존 첨부 1건 이관 완료.
- 회의록의 `MEET_GB`(정기/임시)는 **회의 성격**이라 "어느 위원회냐"를 담지 못한다 → 별도 컬럼.
- 자료실 분류에 「감염관리 자료」(INF) 추가.

**감염 폴더 전체 구조** — ***2026-08-10 전부 완료(화면 확인 ✅)***
| 원본 | 우리 구현 |
|---|---|
| 감염관리계획서 · 감염관리위원회 · 감염라운딩 | 서식 2·1·3호에 **구분(FORM_GB)** 을 얹어 해결 — 새 화면 없음 |
| 손위생수행률 · 요로감염 · 직원감염노출사고분석보고서 | 지표 3종으로 이미 있음 |
| 감염관리 우선순위 사정 도구(기준표+집계표) | `qpsInfRisk.do` — **자동계산 + 집계표 탭** |
| 감염종합보고 3종 | `qpsInfRpt.do` — **한 서식으로 통합**(종류 선택 시 쓰지 않는 카드 숨김) |
| 감염병환자 월별 리스트 | `qpsInfPat.do` — 12행 + 발생자 통계, **[찾기] 환자검색** |
| 감염관리 전담자 3종 | `qpsInfStaff.do` — **한 사람 = 서류 한 벌**(탭 3개), 단위업무 5종 자동 |
| 결핵환자등 신고·보고서 | ***서식 만들지 않음.*** 사용자 확인(2026-08-10) — **사본만 보관**한다. |

***★결핵 신고서를 안 만든 이유*** — 질병관리청 법정 서식(항목 100+·체크박스)이라 옮기면 큰 작업인데,
실제로는 **질병보건통합관리시스템에 신고하고 사본만 보관**한다. 사본 보관은 이미 있는 첨부로 끝난다
(감염병환자 화면 첨부 또는 자료실 「감염관리 자료」). **다시 만들자는 말이 나오면 이 문단부터 볼 것.**

**신규 화면 4개 — DDL·5계층·메뉴·화면 전부 완료, BUILD=`20260810-1900`**
[QPS_DDL_INFRISK](../sql/QPS_DDL_INFRISK_2026-08-10.sql) ·
[QPS_DDL_INFRPT](../sql/QPS_DDL_INFRPT_2026-08-10.sql) ·
[QPS_DDL_INFPAT](../sql/QPS_DDL_INFPAT_2026-08-10.sql) 운영 DB 적용 완료.
첨부는 전부 **공통 첨부 재사용**(REF_GB = INFRISK / INFRPT / INFPAT / INFSTAFF).

***★설계 판단 3가지 — 원본이 문서 여러 개여도 우리 화면은 하나로 묶었다***
1. **감염종합보고 3종 → 한 서식.** 골격(기안서+내용+명단+첨부)이 같다. 종류를 바꾸면 안 쓰는 카드를 숨긴다.
   표를 3벌 만들면 기안서·명단·첨부를 세 번 만들게 된다.
2. **전담자 3종 → 한 사람 한 벌.** 임명장·자격경력·직무기술서는 <같은 사람의 서류>다. 탭으로 담고 인쇄만 나눈다.
3. **우선순위 기준표+집계표 → 한 화면 탭.** 집계표는 기준표를 점수순으로 세운 것뿐 — 같은 자료를 두 번 입력할 이유가 없다.

**우선순위 사정 도구 — 진행 상황**
[QPS_DDL_INFRISK_2026-08-10.sql](../sql/QPS_DDL_INFRISK_2026-08-10.sql) 적용 완료(머리·항목·기본표 3테이블).
기본 위험항목 **31개** 채록 검증(A손위생3 B삽입기구3 C시술1 D다제내성균5 E감염병6 F의료물품4 G환경2 H지역사회2 I직원5).
***★이 서식의 값은 자동계산이다*** — 원본은 사람이 곱셈을 손으로 한다.
`위험점수 = 발생가능성(1~5) × 심각성(1~5) × 준비·대처(1~4)`, 최대 100.
등급 1~24 낮음 / 25~49 중간 / 50~74 높음 / 75~100 매우 위험.
점수는 계산값이지만 **저장한다**(집계표 정렬 + 그때 기준의 스냅샷).
**남은 것 = 화면·5계층·메뉴.**

**감염종합보고 3종은 골격이 같다** — 「기안서 + 내용 + 참석자/서명 명단 + 첨부」.
개별로 3개 만들지 말고 **한 서식에 종류 구분**을 두는 쪽이 맞아 보인다(회의록과 같은 방식).

**미확인** — 결핵환자등 신고·보고서는 질병관리청 법정 서식(항목 100+·체크박스 위주)이다.
**이 화면에서 실제로 작성하는지, 아니면 질병보건통합관리시스템에 신고하고 사본만 보관하는지**에 따라
만들 범위가 완전히 달라진다. 사용자 확인 필요.

### [판정 2026-08-10] 보고서 폴더 — 사고 유형별 보고서 11종

원본 트리 : 환자안전사고보고서 · 욕창발생 보고서 · 직원안전 사고 보고서 · 직원 감염노출 사고 보고서 ·
감염성 질환 발생 보고서 · 유해물질 노출 보고서 · 보안안전 사고 보고서 · 환자 학대 및 폭력 발생 신고서 ·
근접오류 보고서 · 환자안전관리 라운딩 점검표 · 정신
(QPS 폴더 아래에도 FMEA · RCA · 환자안전관리 라운딩 점검표가 따로 있다.)

**캡처 2종 실측 결과 — 골격은 같고 「체크박스 세트」만 다르다.**

| | 환자안전사고보고서 | 욕창발생 보고서 |
|---|---|---|
| 공통 | 결재란(담당·팀장·부서장·이사장) · 환자정보 · 발생일/보고일 · 서술칸 | 같음 |
| 다름 | 오류구분·손상종류·오류등급(Level 0~4)·의식/활동상태·낙상 유발약물/유형/위험요인·투약오류·기타오류 | 발생원인·Braden 위험사정 점수·발생부위·크기/깊이/단계·예방간호·관리(드레싱) |
| 서술 | 2p : 사건경위·활력징후·신체손상정도·치료내용·문제원인·개선방안 | 1p 하단 : 관리·비고 |

***★그래서 감염종합보고처럼 「한 서식」으로 묶을 수는 없다.*** 저쪽은 항목이 거의 같았지만
여기는 **보고서마다 체크박스 묶음이 통째로 다르다.** 대신 지표 18종을 한 화면으로 처리한 방식이 그대로 먹힌다 —
**공통 골격은 화면 하나, 체크박스 세트는 데이터(코드표)로.** 우선순위 사정 도구의 기본항목표와 같은 구조다.

***★★더 중요한 발견 — 환자안전사고보고서는 이미 있는 「사고 보고」와 겹친다.***
오류구분(낙상·투약·기타) · 등급(Level 0~4) · 손상종류 · 발생장소 · 발생시간은
**TBL_QPS_INCIDENT 가 이미 받고 있는 항목**이다(낙상 화면에서 입력·검증까지 끝난 것).
욕창발생 보고서도 욕창 지표와 원천이 같다.
→ **새 화면을 만들 게 아니라, 기존 사고 자료에 상세 칸을 더하고 인쇄 서식을 붙이는 쪽이 맞다.**
그렇지 않으면 같은 사고를 지표용으로 한 번, 보고서용으로 또 한 번 입력하게 된다(원본이 그랬다).

**4종째 실측(직원 감염노출 사고 보고서)로 골격 확정.**
본인 정보(사번·부서/직위·직종·생년월일·경력·임신여부·B형간염 접종/항체) ·
노출대상 환자 정보 · 위험원인체(HBs Ag·HBc Ag·HBe Ag·Anti-HCV·HIV·VDRL·COVID-19) ·
사고 유형/경위(노출장소·발생부위·사고유형·노출경로) · 진료내역(소견·검사·처방·추후관리).
→ **역시 같은 골격**(결재란 + 대상정보 + 체크박스 묶음 + 서술칸), 묶음만 다르다.
★이것도 **이미 있는 「직원감염노출」 지표와 원천이 같다**(감염 폴더의 「직원감염노출사고분석보고서」와도 같은 사건).

***★★블록이 서식 사이에서 <돌려 쓰인다> — 6종 실측으로 확인(2026-08-10)***
| 공유 블록 | 쓰는 서식 |
|---|---|
| 육하원칙 사건개요(언제·누가·무엇을·어떻게·왜·어디서) | 직원안전 · 유해물질 노출 |
| 진료내역(소견 / 검사 / 처방[관찰·봉합·통증중재·항생제 처방·타병원진료·기타] / 추후관리) | 직원안전 · 직원 감염노출 · 유해물질 노출 |
| 의식상태(명료·졸음·혼미·반혼수·혼수) | 환자안전사고 · 감염성 질환 발생 |
| 활동상태(독립적·부분도움·항상 도움필요·침상안정) | 환자안전사고 · 감염성 질환 발생 |
| 환자관련 요인(어지러움·흥분·전신쇠약·마비·시력장애·체위성저혈압·평형장애·보행장애·수면장애·낙상경력) | 환자안전사고 · 감염성 질환 발생 |
| 직종(의사·간호사·간호조무사·요양보호사·조리사·의료기사·기타) | 직원안전 · 직원 감염노출 · 유해물질 노출 |
| 결재란(담당·팀장·부서장·이사장) | 전 서식 |

***→ 블록을 코드표로 빼면 11종이 블록 조합으로 만들어진다.*** 서식마다 화면을 짜는 게 아니라
**블록을 정의하고 서식은 「어떤 블록을 쓰는가」만 정한다.** 서식이 더 늘어도 코드 추가로 끝난다.

***[설계 확정] 보고서 폴더는 이렇게 간다***
1. **화면 하나 + 유형 구분**(RPT_GB). 골격(결재란·대상정보·서술칸)은 공통.
2. **체크박스 묶음은 데이터로** — 유형별 항목표(그룹·항목·중복선택 여부)를 코드표에 두고 화면이 그려준다.
   지표 18종을 화면 하나로 처리한 방식, 우선순위 사정 도구의 기본항목표와 같은 구조.
3. **사고 원천은 TBL_QPS_INCIDENT 를 재사용**한다. 새로 입력받지 않는다 —
   ***같은 사고를 지표용·보고서용으로 두 번 입력하게 만들면, 지표를 자동산출로 바꾼 의미가 없어진다.***

**실측 8종의 블록 조합** (가벼운 것부터)
| 서식 | 쓰는 블록 |
|---|---|
| 보안안전 사고 | 결재란 + 대상(부서·성명·직위) + 육하원칙 + 결과 + 사고 후 조치 — **체크박스 없음(최소 조합)** |
| 환자 학대·폭력 신고 | 결재란 + 환자정보 + **사건유형 4택** + 육하원칙 + 피해자 상담 + 처리결과 |
| 유해물질 노출 | 결재란 + 대상 + 직종 + 사고종류/노출정도/조치사항/Spill kit + 육하원칙 + 진료내역 |
| 직원안전 사고 | 결재란 + 대상 + 직종 + 육하원칙 + 사고유형 + 결과 + 진료내역 |
| 직원 감염노출 | 결재란 + 본인정보(사번·경력·임신·B형간염) + 노출대상 환자 + 위험원인체 + 노출경로 + 진료내역 |
| 감염성 질환 발생 | 결재란 + 환자정보 + 의식상태 + 활동및기능 + 보조기구 + 환자관련요인 + 서술4칸 |
| 욕창발생 | 결재란 + 환자정보 + 발생원인 + Braden점수 + 부위 + 크기/깊이/단계 + 예방간호 + 관리 |
| 환자안전사고 | 결재란 + 환자정보 + 오류구분/손상/등급/의식/활동/환자요인/낙상 3묶음/투약오류/기타오류 + 서술6칸 |

***★★[중요] 근접오류 보고서는 원본에도 없다*** — 트리에는 있으나 열면 **"준비중인 메뉴 입니다!"**
(2026-08-10 확인). ***만들 대상이 아니다.*** 다른 항목도 실제로 열어 보기 전에는 있다고 단정하지 말 것.

***★★[중요] 환자안전관리 라운딩 점검표 = 이미 만든 서식 3호다*** (2026-08-10 실물 대조).
구분 항목이 그대로 일치한다 — 의료기기 관리 · 의료용구 관리 · 응급 kit · 각실 관리(공통) · 공통 ·
화재 · 건축시설 · 공조설비 · 전기시설 · 교육 / 양호·불량 / 불량 내용 및 개선사항 / 전월복사.
**새로 만들 것이 없다.** (qpsRound.jsp 의 DEFAULTS 가 이 구성 그대로다.)

***★그 아래 못 보던 4종이 더 있다*** —
`환자안전관리 점검표 - 외래` · `- 병동` · `- 영양` · `직원간 폭행/성희롱 사건 보고서`.
→ **외래·병동·영양은 서식 3호에 「부서 구분」을 얹으면 된다.**
   오늘 감염관리에 넣은 FORM_GB 와 <같은 방식>이다(컬럼 하나 + 유니크 키에 포함 + 첨부 키에 반영).
   ***★원본도 이미 표지에 「부서」 드롭다운을 갖고 있다*** — 트리를 셋으로 나눈 건 <기본 항목 세트>가
   부서마다 다르기 때문이다. 즉 **부서 구분(FORM_GB 방식) + 부서별 기본항목표**(우선순위 사정 도구의
   TBL_QPS_INFRISK_DEF 와 같은 구조) 조합이면 셋을 한 화면으로 덮는다.
   ***★단, 주기가 다르다*** — 라운딩 점검표는 **월 1부**(1~12월 라디오), 점검표-외래는 **년·월·일 단위**다.
   키를 년월로 둘지 일자로 둘지 먼저 정해야 한다(섞으면 저장이 막힌다 — 오늘 계획서·라운딩에서 겪은 것과 같은 문제).
   **3부서 실측 완료(2026-08-10) — 틀은 완전히 같고 「구분 > 점검항목」 세트만 다르다.**
   틀 = 1p 표지(결재란 담당·팀장·부서장·이사장 / 부서 드롭다운 / 년·월·일 / 점검자)
      + 2p 표(구분 > 점검항목 > 점검내용 / 상태 양호·불량 / 조치내용(불량 시)) + 하단 **특이사항**.
   | 부서 | 구분 > 점검항목 |
   |---|---|
   | 외래 | 의료기기관리(영상의학실) · 의료용구관리(IV pole·물품관리) · 환경 및 시설관리(환경관리·시설관리) · 각실관리(외래전체·약국·주사실·영상의학실·소독실·구급차) |
   | 병동 | 의료기기관리(산소기 O2 tank·흡입기 suction기·환자감시장치 EKG Monitor) · 의료용구관리(휠체어/IV pole·BST 기계) · 응급kit(물품관리) · 병실관리(병실·화장실) · 시설관리(병실 전체 및 복도·계단) · 기타관리(폐기물 및 세탁관리·샤워실) |
   | 영양 | 보관 및 관리(식재료·냉장고·보존식) · 환경 및 시설관리(환경관리·시설관리) · 각실(영양과) |
   ★소화기 항목은 개수 입력칸(「( )개(투척소화기포함)」)이라 양호/불량이 아니다 — **항목 유형이 하나 더 있다**
     (체크형 / 수량입력형). 항목표에 유형 칸을 두어야 한다.
→ 직원간 폭행/성희롱은 보고서 폴더의 블록 조합에 추가(학대·폭력 신고서와 비슷한 조합일 가능성).

***★[확인 완료] 「정신」 폴더 = 정신과 운영 병원용 별도 세트 6종***
`환자안전사고보고서(정신)` · `환자안전사건 개선활동 보고서` · `격리강박 시행일지` ·
`격리·강박 기록지` ×2 · `방문객 안전사고보고서`.

***정신 버전 환자안전사고보고서는 일반 버전의 「변형」이다*** — 골격은 같고 항목 세트만 다르다.
| | 일반 | 정신 |
|---|---|---|
| 보고부서 | 병동·외래·물리/특수부서·기타 | 병동·외래·**행정부서**·기타 |
| 관련직원 | (없음 항목 없음) | **「없음」 추가** |
| 오류구분 | 낙상·투약·기타 | 낙상·투약·**자살자해**·기타 |
| 기타오류 | 검사관련·과정오류·결과오류·검사지연·학대및폭력·자살및자해·타해·진료재료오염·화상·시술관련·의료장비·시설관련·질식사고·위험물품소지·탈원 | **수혈부작용·자살및자해·타해·잘못된 부위 시술 및 수술·수술부위 감염·질식사고·폭력·위험물품소지·탈원** |
| 2p 서술 | 문제원인 / 개선방안 | **문제원인 분석 / 개선계획 수립** |

→ ***블록 조합 설계가 그대로 먹힌다.*** 서식을 새로 짜는 게 아니라 **유형을 하나 더 두고 항목 세트만 다르게** 준다.
   ★단, **정신과를 운영하지 않는 병원에는 이 세트가 필요 없다** — 병원별로 켜고 끌 수 있어야 한다.
   격리·강박 3종(시행일지·기록지 2종)은 정신과 고유라 별도 서식이 필요하다.
   방문객 안전사고보고서는 대상이 **환자가 아니라 방문객**이라 환자검색을 쓰지 않는다.

**정신 ▸ 환자안전사건 개선활동 보고서** (캡처 확인) — 서식 중 가장 단순하다. 1 Page.
결재란(담당·팀장·부서장·이사장) + **3열표**(분석 단계 / 항목질문 / 내용 조사 결과).
| 분석 단계 | 항목질문 |
|---|---|
| 환자안전사건 | 발생일시(날짜+시간 별도칸) · 발생장소(한 줄) · 문제 요약(서술) |
| 사건에 따른 개선활동 | 개선활동 요약(서술) · (하단 큰 서술칸 1개) |

→ 환자검색 없음, 체크항목 없음, **날짜/시간·한줄·서술** 세 유형만 쓴다.
   ***이 서식은 「보고서 골격 = 결재란 + (분석단계·항목질문·내용) 3열표」의 최소 형태다.***
   보고서 폴더 전체를 이 3열표 골격 + 항목 유형(체크·수량·한줄·서술·날짜) 조합으로 짜면
   ***정신·일반·부서별 변형이 전부 데이터로 흡수된다*** — 화면은 하나면 된다.

**정신 ▸ 격리강박 시행일지** (캡처 확인) — ***보고서가 아니라 「대장(臺帳)」이다.*** 연/월 단위 다행 그리드.
컬럼 : 번호·시행일자·구분(격리/강박)·지시자·작성자·등록번호·환자성명·성인구분·보험구분·
주치의/해제지시자·참여자·시작일·시작시간·종료일·종료시간·지침준수.
하단 집계 : **격리 N건 / 강박 N건 / 합계 N건**(자동). 보조기능 : 다음행으로 복사 · 선택행 삭제 ·
날짜시간보정 · 엑셀저장 · 엑셀불러오기 · 일괄저장.
→ ***감염병환자 월별 리스트(qpsInfPat)와 같은 형태.*** 그 화면을 본떠 만들면 된다.

**정신 ▸ 격리·강박 기록지 ×2 — 이름만 같고 실제로 다른 서식이다.**
- **(A) 4 Page 짜리 — 환자 1인 1건의 본 기록지**
  1p : 환자(등록번호·성명·나이·남/여·진단명) / **시행**(시행일시·지시자·수행자·참여자명) /
  **해제**(해제일시·지시자·수행자·참여자명) / **제한의 종류**(□격리 / □강박 → 억제대(□2·□3·□4·
  □5포인트이상·□기타)·□보호복·□보호조끼·□주사제 사용여부, ※중복선택 가능) /
  **격리·강박이 필요한 이유 체크 7개**(자살·자해 위험 / 폭력성 / 스스로 건강 훼손 / 병동환경 훼손 /
  지나친 자극 차단 / 환자가 강박 요구 / 환자가 격리 요구) / 구체적 상황(서술) /
  모니터링(서술) / 연장 이유 및 기록(서술).
  ★***「시행·해제·제한의 종류」 3칸 블록이 1p 안에서 3회 반복*** — 한 건에 연장·재시행이 여러 번 붙는다.
  2~4p : **격리·강박 모니터링 그리드**(시간 / 대·소변 / 음료 / 혈액순환(피부색) / V/S BP,P,BT,R /
  상태 및 INJECTION / 서명) — 3면 = 24시간 이상 기록용.
- **(B) 1 Page 짜리 — 모니터링만 있는 속지**
  헤더가 등록번호·성명·**성별·나이**로 다르고, 컬럼이 「상태 및 INJECTION**, ROM**」으로 **ROM이 추가**됐다.
  → 재활 목적 기록이 붙는 병원용 변형. **(A)의 2~4p와 같은 블록에 컬럼 하나 차이**뿐이다.

**정신 ▸ 방문객 안전사고보고서** (캡처 확인) — 결재란 4칸. ***환자검색을 쓰지 않는 유일한 서식.***
성함(직접입력) · **방문사유**(□보호자 □실습생 □기타) · 발생일시(날짜+시간) · 발생장소 /
**사고종류**(□시설 및 환경안전사고 / □폭언 □폭행 □성폭력(□성희롱 □성추행 □성폭행) / □기타) /
사고경위 · 사고처리 및 조치사항 · 발생원인 · 개선계획(서술 4칸) /
**사고결과**(□지속적인 치료가 필요 □치료 후 후유증 없이 치료됨 □특별한 이상 없음 □추후 관찰 필요 □기타) /
하단 "방문객 안전사고에 대해 위와 같이 보고합니다." + 년·월·일 + 작성자 + 서명칸.
→ 골격은 직원간 폭행/성희롱 사건 보고서와 사실상 동일. **대상만 직원→방문객.**

---

### ★「환자만족도 조사」폴더 — 폴더가 아니라 ***하나의 완결된 활동 사이클***

하위가 **6단계**로 나뉜다(각 단계가 다시 서식을 품는다) :
`안내문` → `조사 계획서` → `설문지` → `조사결과 보고서` → `지표분석 보고서` → `개선활동결과보고서`

***이건 QI 활동의 PDCA 그 자체다.*** 앞서 만든 **감염 종합보고(P/D/E 구분)** 와 같은 구조이고,
단계 수만 6개로 늘어난 것. → **하나의 화면 + 단계 구분(GB)** 으로 흡수 가능성이 높다.

- **만족도 조사안내** (캡처) — 내용이 거의 없다. 장식 테두리 + 제목 + **자유입력 박스 1개**. 게시용 안내문.
- **만족도 조사 계획서** (캡처) — 결재란 + 제출일 +
  주제 / 주제 선정 배경 / 조사팀 구성 / 조사계획(구분) / 조사기간(from~to) / 조사대상 /
  조사 인원 계획 / 조사 항목(서술) /
  **추진계획 및 활동일정** = □추진 계획(서술) + □활동 일정 **체크형 간트표**
  (행 9 : 조사 계획·설문지 작성·설문조사·설문지 통계분석·설문지 결과보고서 작성·
   개선활동 계획·개선활동 시행·개선활동 결과 분석 / 열 8 : 월)
  ★***이 간트표는 이미 구현한 「연간 활동계획서」와 같은 구조다*** — 행 정의만 바꾸면 재사용된다.

- **만족도 설문지** (캡처, 2 Page) — ***이 폴더의 핵심이자 유일하게 무거운 서식.***
  상단 안내문 문단(고정 문구) → 응답자 정보 → **5점 리커트 척도 문항표** → 기타의견.
  - 응답자 : 작성자(□환자본인 □보호자) / 성별(□남 □여) /
    연령대(□20대 □30대 □40대 □50대 □60대 □70대이상)
  - 척도 : **매우만족 5점 / 만족 4점 / 보통 3점 / 불만족 2점 / 매우불만족 1점** (모든 문항 공통)
    ★체크박스 옆 숫자는 1~5로 **왼쪽부터** 붙어 있으나 배점은 **5→1** 이다. 표시번호≠점수, 주의.
  - 영역 4개 / 문항 20개
    1. 전반적인 환경 시설 (5) — 병실내부 편의시설 · 안전시설 · 환기/냉난방 · 환자복 및 침구 교환 · 주차시설
    2. 직원의 친절성과 성의 (5) — 담당의사 · 담당간호사 · 방사선사 · 물리치료사 · 원무과직원
    3. 환자에 대한 관심과 서비스 (8) — 입원수속 절차 · 입원 대기시간 · 입원생활안내 및 낙상예방
       설명 · 치료 필요성·결과·주의사항 설명 · 의사의 질문 응답 · 투약 설명 · 식사(맛·양·질·제공시간) ·
       간병서비스
    4. 병원 의료에 대한 전반적인 만족도 (2) — 서비스 전반 만족 · 지인 권유 의향
  - 하단 **[ 기타의견 및 건의사항 ]** 서술칸

  ★***설계 요점 — 이 서식만 저장 단위가 다르다.***
  다른 서식은 「병원 1건」이지만 설문지는 ***응답자 1인 = 1건***이라 한 조사에 수십~수백 건이 쌓인다.
  → `TBL_QPS_SURVEY`(조사 회차) + `TBL_QPS_SURVEY_ANS`(응답 1건) + `TBL_QPS_SURVEY_ITEM`(문항별 점수)
  로 나눠야 하고, ***문항은 기본항목표(DEF)로 데이터화***한다(감염 우선순위 31항목과 같은 방식).
  그래야 다음 단계인 **조사결과 보고서 / 지표분석 보고서**가 이 표를 집계해서 자동으로 나온다.

- **만족도 조사결과 보고서** (캡처, **12 Page**) — ***이 폴더의 출력물 본체.
  사람이 채우는 서식이 아니라, 설문지 응답을 집계해 「자동으로 그려지는」 보고서다.***
  - **1p 표지** : `2026 년도` / `입원환자 의료서비스 만족도 조사 결과 보고서` (제목만)
  - **2p** 결재란 4칸 + [조사의 목적] [조사의 목표](서술) +
    **[조사개요]** 조사기간(from~to) · 조사대상 및 인원 · 조사방법 · 조사요원 · 통계방법 +
    **[만족지수 산출기준]** 표 —
    | 문항 | 매우만족 **1** | 만족 **2** | 보통 **3** | 불만족 **4** | 매우불만족 **5** |
    | 정의 | **5점** | **4점** | **3점** | **2점** | **1점** |
    ★***앞서 설문지에서 지적한 「표시번호 ≠ 배점」이 여기서 공식 산출기준으로 확인됐다.***
    보기 번호는 1→5, 배점은 5→1. **구현 시 반드시 뒤집어 계산**할 것.
  - **3p** [조사의 내용] `구분 | 관련 문항` 표 + [조사의 활용](서술) +
    **[만족도 조사 결과] Ⅰ.성별** — `구분(남성/여성/합계) | 빈도(명) | 비율(%)` 표 **＋ 막대그래프**
  - **4p** **Ⅱ.연령** (20·30·40·50·60·70대이상 + 합계) 표+그래프 /
    **Ⅲ.전반적인 환경 시설에 대한 만족도** 부터 ***문항별 집계 블록*** 시작
  - **4~9p** 문항별 집계 블록이 **20개 반복**. 한 블록 =
    `구분(매우만족~매우불만족) | 빈도(명) | 비율(%)` + 마지막 행 **`만족도`(가중평균)** ＋ 막대그래프.
    영역 : Ⅲ.환경 시설 → **Ⅳ.직원의 친절성과 성의** → **Ⅴ.환자에 대한 관심과 서비스** →
    **Ⅵ.전반적인 만족도** (설문지의 4개 영역과 1:1 대응)
  - **10~11p** 여백면 / **12p** `□ 의료서비스 만족도 조사 개선사항` — 3열 표(자유기재)

  ★***설계 요점***
  1. 사람이 입력하는 칸은 **2p(목적·목표·개요)·3p(조사의 내용·활용)·12p(개선사항)** 뿐.
     ***나머지 9면은 전부 설문 응답의 집계 결과다*** → 저장할 게 아니라 **조회 시 계산**해야 한다.
  2. 응답 0건이면 원본은 비율에 **`NAN`** 을 그대로 출력한다(캡처 확인). 우리는 **`-`** 로 표시할 것.
  3. 문항별 블록 20개 = **설문지 문항표(DEF)를 그대로 순회**하면 나온다. 화면에 20번 하드코딩 금지.
  4. 그래프는 가로 막대 5개. **Chart 라이브러리 없이 CSS 막대**로 충분하다(인쇄 안정성).

- **만족도 지표분석 보고서** (캡처, **10 Page**) — ★형제 서식 **`만족도 지표분석 보고서(점수)`** 가 하나 더 있음(미확인).
  ***조사결과 보고서와 집계 내용은 똑같다. 다른 건 그 집계를 「지표」 프레임에 얹었다는 것.***
  → ***이미 구현한 QPS 지표관리 서식(지표정의·분자·분모·목표·모니터링)과 같은 골격***이다.
  - **1p** 결재란 + 제출일 + **지표명** +
    **[지표정의]** 지표정의 / 분자정의 / 분모정의  ← **우리 지표 서식과 동일**
    **[목표]** `의료서비스만족도 ___ 이상`
    **[모니터링]** 지표관리자 · 모니터링 주기 · 자료수집 · 통계기법과 도구 · 기간(from~to)
    **[현황]** `년 | 평균` → 의료서비스만족도 평균점수 `00.0 %`
    **[지표 분석]** ***4개 영역 평균점수 요약표*** — `환경및시설 | 친절성 | 서비스 | 의료만족` 각 `00.0 %`
      ★***조사결과 보고서에는 없던 표.*** 지표분석 보고서만의 한 줄 요약이다.
    이어서 `1) 성별` · `2) 연령` 빈도/비율 표 + 그래프
  - **2~8p** 영역별 문항 집계 — `1.환경 및 시설` → `2.친절성` → `3.서비스` → `4.의료 만족도`.
    블록 모양은 조사결과 보고서와 **완전히 동일**(빈도/비율/만족도 + 가로막대).
    ★단 비율이 `NAN`이 아니라 **`0.0`** 으로 나온다 — 같은 데이터인데 서식마다 표기가 다르다.
  - **8p 하단** `개선 전략 및 실행` (세로 라벨 + 서술칸)
  - **9p** `결론 및 제언` / `증진활동 평가 및 추후 활동계획` (서술 2칸)
  - **10p** `□ 의료서비스 만족도 조사 개선사항` — **4열** `구 분 | 문제점 | 처리결과 | **사진**`
    ★***「사진」 컬럼 = 이미지 첨부가 필요한 유일한 표.*** 조사결과 보고서(12p)는 3열이라 사진이 없다.
    → 이미 만든 **qpsFileBox(공통 첨부)** 를 행 단위로 붙이거나, 행별 이미지 1장 업로드가 필요하다.

  ★***설계 결론 — 조사결과 보고서와 지표분석 보고서는 한 화면으로 합치지 말 것.***
  집계 로직(문항별 빈도·비율·만족도)은 **공용 함수 하나로 뽑아 쓰고**, 화면은 둘로 나눈다.
  같은 수치를 놓고 ①조사결과=단순 집계 나열 ②지표분석=지표 프레임+요약+개선전략 으로 목적이 다르고,
  결재라인도 따로 돌기 때문이다.

- ★★**만족도 지표분석 보고서(점수)** (캡처, 4 Page) — ***만족도 계산식의 「정답지」.***
  앞의 두 보고서는 결과만 보여주는데, ***이 서식은 중간 계산 과정을 그대로 인쇄한다.***
  헤더 : `문항 | 매우 만족(5) | 만족(4) | 보통(3) | 불만족(2) | 매우불만족(1) | 합계(점수/총점) | 백분율 | 평균`
  ★***헤더에 배점이 (5)…(1) 로 박혀 있다*** — 5→1 배점이 여기서 세 번째로 확인됨. 이제 확정.

  **문항 1개 = 3행 구조** (보기 5칸 각각에)
  | 행 | 내용 | 예 |
  |---|---|---|
  | 1행 | 응답 인원 | `0 명` |
  | 2행 | **인원 × 배점 = 점수** | `0 X 5 = 0` |
  | 3행 | 문항 내 해당 보기 비율 | `NAN %` |

  **합계(점수/총점) 칸** = 위에 **점수합**, 아래에 **총점**(분수 표기).
  **백분율 칸** = 위 `NAN %`, 아래 `NAN 점` — 두 값이 같이 나온다.
  **평균 칸** = 영역 단위로 세로 병합, 역시 `%` 와 `점` 두 값.
  맨 아래 행 **`평균(백분율)`** = 보기(5칸)별 평균 비율.
  **4p 맨 끝** : `의료 서비스 만족도 **전체 평균**   NAN %    NAN 점`

  ***→ 확정된 산식***
  ```
  점수합   = Σ( 보기별 인원 × 배점 )        // 배점 5,4,3,2,1
  총점     = 응답수 × 5                      // 만점 기준
  백분율(%) = 점수합 / 총점 × 100
  평균(점)  = 점수합 / 응답수                 // 5점 만점 환산
  영역 평균 = 그 영역 문항들의 위 값을 다시 평균
  전체 평균 = 전 문항 기준 최종 1개 값
  ```
  ★***「%」와 「점」은 같은 값의 두 표현이다*** (점 = % × 5 ÷ 100). 별도 입력값이 아니다.
  ★앞선 두 보고서의 `만족도` 칸 = 여기의 **평균(점)** 과 같은 값. **산식을 한 군데서만 구현**하면 된다.
  ★구성은 4개 영역 표 4개뿐 — **그래프·서술칸이 없다.** 순수 계산 검증용 출력물.

  → ***구현 판단 : 이 서식은 화면을 따로 만들 필요가 없다.***
  지표분석 보고서에 **「점수 상세 보기」 토글**로 붙이면 충분하다(같은 데이터, 표기만 상세).

- **개선활동결과보고서** (캡처, 4 Page) — ★형제 **4종** : `(원무)` `(원무2)` `(간호)` `(영양)`.
  한 장의 구성 : 결재란 4칸 +
  `부서명` / `주제` / `문제진술` / `현상파악 및 원인분석` / `개선 대책안` (서술) +
  세로라벨 `개선방안 적용 · Action · 개선지속` 안에 `*개선일시` +
  표 `유형 | 의료서비스만족도 문제점 | 개선활동 | **개선사진**`

  ★***4개 Page = 같은 서식의 반복이고, 「유형」 칸만 다르다.***
  | Page | 유형 |
  |---|---|
  | 1p | 병원 의료 관련 |
  | 2p | 시설 및 환경 |
  | 3p | 간병 서비스 관련 |
  | 4p | 간병 서비스 관련 (샘플 문구 있음: "검식과 환자 식사량등 파악하여 맛, 양, 질 높임") |

  → ***유형 = 설문지의 4개 영역과 대응.*** 만족도 조사에서 나온 문제 영역별로 한 장씩 쓴다.
  → 형제 `(원무)(원무2)(간호)(영양)` = ***부서별 버전***. **이미 쓰고 있는 FORM_GB(부서 구분) 패턴 그대로**다.
     별도 서식이 아니라 **한 화면 + 부서 구분**으로 흡수된다.
  → `개선사진` 컬럼 = 지표분석 보고서 10p에 이어 ***두 번째 이미지 첨부 지점***.

  ★**`(원무)` 버전 확인 결과 — 1 Page뿐이고 본판 1p와 완전히 같다.** 유형도 `병원 의료 관련`으로 동일.
  즉 ***부서 버전은 새 서식이 아니라, 본판에서 그 부서가 쓸 면만 떼어낸 것***이다.
  → **FORM_GB 흡수 확정.** 5종을 각각 만들 필요가 전혀 없다.
  ★**`(원무2)` 확인 — 유형이 `시설 및 환경`이다** (원무는 `병원 의료 관련`).
  ***즉 부서별 다건이 아니라 「부서 × 유형」 조합이다.*** 원무과가 두 영역에 대해 각각 한 장씩 쓴다.
  | 서식 | 부서 | 유형 |
  |---|---|---|
  | (원무) | 원무 | 병원 의료 관련 |
  | (원무2) | 원무 | 시설 및 환경 |
  | (간호) | 간호 | 간병 서비스 관련 |
  | (영양) | 영양 | ***비어 있음*** + 표 위에 **제목 입력줄 1개 추가** |

  ★**`(영양)`은 유형 칸이 비어 있다.** 대신 표 위에 자유 입력줄이 하나 더 있는데,
  본판 4p에서 "검식과 환자 식사량등 파악하여 맛, 양, 질 높임" 문구가 있던 바로 그 자리다.
  → ***유형은 코드값 고정이 아니다.*** 설문 4개 영역(환경·친절성·서비스·의료만족)에 딱 맞지 않는
     부서는 비워두고 자유 문구로 쓴다. **유형은 「선택 + 직접입력」 둘 다 허용**해야 한다.

  → 저장 키는 **`부서 + 유형`**. 부서 하나에 유형이 여러 개 붙을 수 있으므로
     `(HOSP_CD, 연도, 부서, 유형)` 단위로 잡으면 5종이 자연스럽게 들어간다.
     ***이름 뒤 숫자(원무2)를 연번으로 오해하면 키 설계가 틀어진다 — 숫자는 유형이 다르다는 표시일 뿐.***

---

### ✅「환자만족도 조사」폴더 조사 완료 — 실제 필요한 화면은 **6개**

| # | 화면 | 성격 | 재사용 |
|---|---|---|---|
| 1 | 조사안내 | 자유입력 1칸 | 거의 없음(최소 서식) |
| 2 | 조사 계획서 | 입력 + 체크형 간트 | ***연간 활동계획서 간트 재사용*** |
| 3 | 설문지 | **응답 1인 = 1건** | 신규(3단 테이블 필요) |
| 4 | 조사결과 보고서 | 집계 12면 | 집계함수 공용 |
| 5 | 지표분석 보고서 | 집계 + 지표 프레임 | ***QPS 지표 서식 골격 재사용*** + (점수)는 토글 |
| 6 | 개선활동결과보고서 | 서술 + 유형별 표 | ***FORM_GB 부서 구분 재사용*** |

***핵심은 3번 하나다.*** 설문 응답 저장구조(회차/응답/문항점수)만 제대로 잡으면
4·5번은 그 위에서 계산으로 나오고, 2·6번은 이미 만든 것을 재사용한다.
**계산 산식은 (점수) 서식에서 확정됐으므로 추측할 부분이 없다.**

---

### 「불만고충」폴더 — 만족도 조사와 ***같은 사이클, 더 짧은 버전***

하위 구성 : `처리대장`(**불만고충 처리계획서** · **불만고충 처리대장**) → `지표분석보고서` → `개선활동처리결과서`

- **불만고충 처리계획서** (캡처, 1 Page) — 결재란 4칸 + `2026 년도`
  `주제명` / `주제선정 배경` /
  ★**`팀구성`** — `구분 | 성명` 을 **4벌 가로 반복 × 3행 = 12칸**.
    구분 라벨 : 1열은 `팀장 · 간사 · 팀원`, 2~4열은 전부 `팀원`.
    → ***만족도 계획서엔 없던 표.*** 불만고충은 **팀을 꾸려서 다루는 활동**이라는 뜻.
  `문제개요 (현황분석 / 사전조사)` (서술) /
  **`자료수집`** — 조사대상 · 조사도구 및 방법 · **분석 및 통계도구(□막대그래프 □원그래프)** /
  `핵심지표/목표` (서술) /
  ★**`활동일정`** — 체크형 간트, **행 6 × 열 12개월**
    행 : `활동계획서 · 지표정의서 · 불만고충접수 · 현황분석 · 개선활동 · 지표분석보고`
    ★***만족도 계획서는 8열이었는데 여기는 12개월 전체다.*** 열 개수를 고정하면 안 된다.
  `제출일`

  → 간트는 ***연간 활동계획서 · 만족도 계획서와 세 번째로 반복***되는 구조.
     **행 정의와 열 개수를 파라미터로 받는 공용 간트 블록**을 만들어 세 곳에서 쓰는 게 맞다.

- **불만고충 처리대장** (캡처, 1 Page) — ***연 단위 대장 그리드.*** 제목 `2026 년 불만고충처리 대장`.
  컬럼 12개 :
  `번호 · 접수월 · 접수일 · 접수유형 · 민원인 · 처리기간 · 불만고충유형 · 불만고충내용 ·
   처리결과 · 회신날짜 · 회신방법 · **미회신사유**`
  데이터 없으면 `<No data to display>` 표시.

  → ***세 번째로 나온 「대장」 형태*** (감염병환자 월별 리스트 · 격리강박 시행일지 · 이것).
     **행 추가/삭제 + 그리드 저장**이라는 같은 뼈대를 공유한다 → ***공용 대장 컴포넌트로 뽑을 것.***
  ★`회신날짜`가 비면 `미회신사유`를 받는 구조 — **회신 여부가 이 대장의 관리 포인트**다.
  ★접수「월」과 접수「일」이 컬럼으로 분리돼 있다(한 날짜가 아님). 월별 집계를 위한 분리로 보이며,
    지표분석보고서에서 월별로 세는 근거가 된다.

- **불만고충 지표분석보고서** (캡처, 4 Page) — 형제 3종 : `지표분석보고서` · `- 전반기` · `- 후반기`.
  ***처리대장에서 전부 자동 집계되는 보고서다.*** 골격은 만족도 지표분석 보고서와 동일
  (지표정의·분자·분모 / 목표 / 모니터링 / 현황 / 지표분석 / 개선전략 / 결론).
  - **1p** 제목 `년 전반기 불만고충 지표 분석 보고서` + 결재란 + 제출일 · 지표명 +
    `[지표정의]` 지표정의/분자정의/분모정의 · `[목표]` ·
    `[모니터링]` 지표관리자 · 모니터링 주기 · 자료수집 · 통계적기법과 도구 · 기간
    **`[현황]`** `반기 | 전반기 | 후반기 | 평균` → **불만 및 고충처리률**
    **`[지표분석] (1)반기 표`** — 열 `1~6월 + 총계`, 행 3개
      `불만 및 고충건수(건)` / `불만 및 고충처리 처리건수` / `불만 및 고충처리 처리율(%)`
    + `TChart` 그래프 + `1) 불만 및 고충건수` 월별 표
  - **2p** `2) 불만고충유형` **유형별 건수** — 시설 및 환경 · 친절 · 간병사관련 · 식사 · 진료 · 처치 · 기타 · 합계
    `2) 불만고충유형` ***(번호 중복 — 원본 오타)*** **월 × 유형 교차표** (1~6월 × 7유형 + 합계)
    `4) 접수유형` — 고객의소리함 · 직접방문 · 전화 · 기타
    `5) 처리기간` — 1일~3일 · 4일~7일 · 7일 이후
  - **3p** `6) 회신방법` — 직접방문 · 전화 · 메일 · 기타 /
    `7) 미회신 사유` — 성함,연락처 미기재 · 전화,메일 주소오류 · 기타 /
    `개선 전략 및 실행` · `결론 및 제언` (서술)
  - **4p** `유형 | 불만고충 문제점 | 개선활동` 3열표 (★만족도 쪽과 달리 **사진 컬럼 없음**)

  ★***처리대장 ↔ 이 보고서의 1:1 대응 — 대장 컬럼 설계 이유가 여기서 전부 설명된다.***
  | 처리대장 컬럼 | 집계 항목 |
  |---|---|
  | 불만고충유형 | 2) 유형별 건수 · 월×유형 교차표 |
  | 접수유형 | 4) 접수유형 |
  | 처리기간 | 5) 처리기간 (구간으로 묶어 집계) |
  | 회신방법 | 6) 회신방법 |
  | 미회신사유 | 7) 미회신 사유 |
  | 접수월 | 모든 월별 집계의 축 |
  → ***대장의 유형·접수유형·회신방법·미회신사유는 자유입력이 아니라 「코드값」이어야 한다.***
    안 그러면 집계가 안 된다. **공통코드로 먼저 정의할 것.**
  ★`처리기간`은 대장엔 일수로 들어가고 보고서에서 **1~3 / 4~7 / 7일 이후 구간으로 묶인다.**
  ★**전반기(1~6월) / 후반기(7~12월)로 서식이 나뉜다** — 대장의 `접수월` 컬럼이 분리돼 있던 이유.
  ★번호가 `1, 2, 2, 4, 5` — ***원본에 번호 오타***(3번 없음, 2번 중복). 우리 구현에선 바로잡을 것.

- **지표분석보고서 - 전반기** (캡처, 5 Page) — 기본형(무제)의 **완성판**. 기간이 전반기로 고정돼 있다.
  제출일 `2026-06-30`, 모니터링 기간 `2026-01-01 ~ 2026-06-30`, 목표 `불만 및 고충처리 처리율 __ %`.

  ★★***기본형과 「코드값 목록」이 서로 다르다. 원본 자체가 서식마다 제각각이다.***
  | 항목 | 기본형(무제) | **- 전반기** |
  |---|---|---|
  | 현황 표 | 컬럼 `반기·전반기·후반기·평균` / 행 1개 | **`목표값달성여부` 컬럼 추가** / 행 3개(처리율·고충건수·처리건수) |
  | 불만고충유형 | 시설및환경·친절·**간병사관련**·식사·진료·**처치**·기타 | 시설및환경·친절·식사·**관리**·진료·간병사관련·기타 → ***「처치」가 빠지고 「관리」가 들어감*** |
  | 접수유형 | 고객의소리함·직접방문·전화·기타 | 직접방문·전화·고객의소리함·기타 (**순서 다름**) |
  | 회신방법 | 직접방문·전화·메일·기타 (4) | **구두**·직접방문·전화·E-mail·기타 (**5개, 「구두」 추가**) |
  | 항목 번호 | `1,2,2,4,5` (**오타**) | `1,2,3,4,5` (**정상**) |
  | 개선활동 표 | 4p 1면, `유형\|문제점\|개선활동` 3열 | 4~5p **2면**, `불만고충 문제점\|개선활동` **2열**(유형 없음) |

  ★***이건 우리가 결정해야 할 지점이다.*** 원본을 그대로 베끼면 서식마다 코드가 달라 집계가 깨진다.
  → **`- 전반기` 쪽을 정본으로 삼는다.** 번호가 정상이고, 목표값달성여부·현황 3행이 더 완전하며,
    회신방법에 「구두」가 있어 실제 운용에 맞는다.
  → 단 **불만고충유형의 「처치 ↔ 관리」는 병원마다 다를 수 있다.**
    ***코드 마스터를 화면에서 관리 가능하게*** 만들어야 한다(고정 배열 금지).
    앞서 만든 **감염 기본항목표(TBL_QPS_INFRISK_DEF)** 와 같은 방식.

- **지표분석보고서 - 후반기** (캡처, 5 Page) — ***전반기형과 완전히 동일. 월만 7~12월.***
  코드값도 전반기와 같다(식사·**관리**·간병사관련 / 회신방법 **구두 포함 5개**) → ***위 판단 확인됨.***
  기간 표시가 `0701- ~ 1231-` (연도 미입력 상태).
  ★***정리 : 기본형(무제)은 구버전, 전반기·후반기가 신버전이다.***
    → 우리는 **한 화면 + 반기 구분(전반기 1~6월 / 후반기 7~12월)** 으로 만들면 3종이 모두 커버된다.
      `- 전반기` / `- 후반기`를 각각 만들 필요 없음. **월 범위만 파라미터.**

- **불만고충 개선활동 처리결과 보고서** (캡처, 1 Page) —
  ★***좌측에 `1~14` 라디오. 한 서식에 건별로 최대 14건을 담는다.***
  즉 ***처리대장의 「개별 건」에 대응하는 상세 보고서***다. 대장이 목록, 이게 상세.
  - 결재란 4칸 + `보고날짜 | 부서명` / `접수날짜 | 개선날짜`
  - **`접수방법`** □직접방문 □전화 □고객 소리함 □기타( ) ← ***대장의 `접수유형`과 대응***
  - **`민원인`** □입원환자( ) □보호자( ) □내원객 □기타( ) ← ★대장엔 없던 구분. **내원객**이 있다.
  - `민원발생장소` / `불만고충내용` / `문제진술` / `현상파악 및 원인분석` / `개선 대책안` (서술)
  - `[개선방안 적용 · Action · 개선지속]` 표 — `유형 | 불만고충 문제점 | 개선활동`
  - `원인` / `조치 및 답변` / `재발방지대책` (서술)
  - **`고객통보`** □직접방문 □전화 □E-Mail □기타( ) ← ***대장의 `회신방법`과 대응***
  ★★***라디오 1~14 = 「한 화면에 여러 건」 UI 패턴.*** 앞서 나온 대장(그리드)과 달리
    **한 건을 한 면 가득 쓰고 번호로 넘겨 보는** 방식이다. 우리는 **목록+상세**로 풀면 자연스럽다.
    (14는 원본의 물리적 한계일 뿐 의미 있는 수가 아니다 — **건수 제한을 두지 말 것.**)

---

### ✅「불만고충」폴더 조사 완료 — 실제 필요한 화면은 **4개**

| # | 화면 | 성격 | 비고 |
|---|---|---|---|
| 1 | 처리계획서 | 입력 + 팀구성표 + **12개월 간트** | 공용 간트 블록(세 번째 등장) |
| 2 | 처리대장 | **연 단위 대장 그리드** | 공용 대장 컴포넌트(세 번째 등장) |
| 3 | 지표분석보고서 | 대장에서 **전량 자동집계** | **한 화면 + 반기 구분**으로 3종 커버 |
| 4 | 개선활동 처리결과 보고서 | 건별 상세 | 대장과 **목록↔상세** 관계 |

***이 폴더의 급소는 2번 처리대장이다.*** 대장의 코드값(유형·접수유형·회신방법·미회신사유)이
3번의 집계 축이고 4번의 체크항목과 대응하므로, ***공통코드를 먼저 확정***해야 나머지가 따라온다.

---

## ★★ 원본 프로그램 서식 조사 — **전부 완료** (2026-08-10)

`QPS공유`만 미확인이나, ***이는 서식 폴더가 아니라 공유 기능으로 보이며 우리 구현 범위 밖***이다
(위너넷은 이미 병원별 계정으로 분리 운영 — 별도 공유 장치가 필요 없음).

### 조사로 확정된 「전체 그림」

***신규로 설계할 것은 거의 없다. 이미 만든 4개 패턴의 반복이다.***

| 패턴 | 이미 구현된 것 | 재사용처 |
|---|---|---|
| **체크형 간트** | 연간 활동계획서 | 만족도 계획서 · 불만고충 처리계획서 **(총 3곳)** |
| **대장 그리드** | 감염병환자 월별 리스트 | 격리강박 시행일지 · 불만고충 처리대장 **(총 3곳)** |
| **지표 서식** | QPS 지표관리 | 만족도 지표분석 · 불만고충 지표분석 **(총 3곳)** |
| **구분(GB) 분기** | 감염 P/D/E · 부서 구분 | 개선활동결과 5종 · 반기 구분 · 정신 변형 **(다수)** |
| **기본항목표(DEF)** | 감염 우선순위 31항목 | 설문 문항 20개 · 불만고충 유형코드 |

### 남은 진짜 작업 (구현 착수 시 이 순서)

1. **공통코드 확정** — 불만고충 유형·접수유형·회신방법·미회신사유, 설문 문항 20개.
   ***이것부터.*** 집계 화면들이 전부 여기에 의존한다.
2. **설문 응답 3단 테이블** — 회차 / 응답(1인=1건) / 문항점수.
   만족도 폴더의 급소. 산식은 **(점수) 서식에서 이미 확정**됨.
3. **공용 블록 4개 추출** — 간트 · 대장 · 지표프레임 · 집계표(빈도/비율/만족도+막대).
4. 나머지는 위 블록의 조합.

### 판단이 필요한 항목 (사용자 확인 대기)

- **정신 폴더**(6종) — 정신과 운영 병원만 해당. ***켜고 끄는 장치 필요.***
- **불만고충 코드값 불일치** — 기본형 `처치` vs 전반기형 `관리`. **전반기형을 정본 제안.**
- **이미지 첨부 2곳** — 만족도 지표분석 10p `사진`, 개선활동결과 `개선사진`.
- ★***병원이 이 서식들을 실제로 다 쓰는가*** — 조사는 끝났으나 **실사용 여부는 미확인**.
  전부 만들면 과하고, 안 쓰는 서식은 메뉴만 늘린다. **착수 전 확인 권장.**

**착수 전 확인할 것** — 병원이 이 11종을 <실제로 다 쓰는지>. 원본에 있다고 다 쓰는 건 아니다
(근접오류가 그 예다). 쓰는 것부터 만들면 대부분이 사고보고 확장으로 끝날 수 있다.

### 감염 나머지 서식 — 원본 사양 채록 (2026-08-10 캡처, 다시 안 받아도 되게)

**① 감염병환자 월별 리스트** — 월 1부(작성일 = 년/월)
표 12행 : `no / 입원일 / 확인일 / 감염성질환 유형 / 등록번호 / 이름 / 성별 / 연령 / 원내발생 여부 / 조치결과`
하단 **발생자 통계** : 혈액매개주의 · 다제내성균 · 결핵(2칸) — 자유 입력.
[전월복사] 버튼 있음(라운딩과 같은 방식).
***★등록번호·이름·성별·연령은 TBL_IPWON_INFO 에서 [찾기]로 끌어오면 된다*** — 낙상 화면과 같은 환자검색 재사용.

**② 감염관리 전담자(담당)** — 3종. 사람 1명당 1세트(인사 성격).
- **임명장** : 소속 / 직위 / 성명 + 날짜. 장식 테두리(상장 양식) — 인쇄가 전부인 문서.
- **자격 및 경력** : 성명 / 직종 / 경력사항(글) + **교육이수 표 11행**(`교육 주관 / 과정명 / 교육일 / 교육기간`).
  ★원본에 PDF 탭이 따로 있다(수료증 파일 보관) → 우리는 **공통 첨부**로 대체.
- **직무기술서** : 입사일·성명·소속·직급·현부서배치일·현직무시작일·작성일자 +
  단위업무 5행 고정(`감염관리 계획수립 및 평가 / 감염관리 / 감염관리 교육 / 지표관리 / 직원감염관리`)
  × `주요 업무내용 / 업무 주기` + 하단 **자격요건 표**
  (학력수준 필수·권장·전문분야 / 전공학과 무관·1순위·2순위 / 면허 및 자격 필수·권장 / 경력요건 필수·권장).

**③ 감염종합보고 3종** — 골격이 같다.
- 감염관리 개선활동 **계획 수립** : 1p 기안서 / 2p 첨부(사진)
- 감염관리 개선활동 **수행** : 1p 기안서 / 2p 손위생 교육 결과 보고서 / 3p 참석자 명단(위원장·부위원장·간사·위원 다수) / 4p 첨부
- **손위생 교육 결과 보고서** : 1p 결재란 + `교육일시 / 교육자 / 교육대상자 / 주제` + 본문 / 2p 참여위원 서명(소속·성명 다수) / 3p 첨부
***★공통 골격 = 기안서 + 내용 + 참석자·서명 명단 + 첨부.*** 3개를 따로 만들지 말고
**한 서식에 종류 구분**을 두는 쪽이 맞다(회의록과 같은 방식) — 사용자 확인 대기.
★기안서 틀(문서분류·문서번호·기안일자·기안자·기안부서(위원장/실장)·협조부서·수신·참조·발송일자·제목
 + 담당/과장/부장/원장 결재란)은 **QPS 서식에도 재사용 가능** — 공용 조각으로 빼는 것을 검토할 것.

### QPS — 오늘 끝난 것
공통 파일첨부(서식 3종) · **자료실**(조직도·내규) · **QPS 담당자 권한**(자료실 한정) · QPS 화면 우측 여백 공통처리.
상세는 아래 「2026-08-10」 두 절. BUILD=`20260810-1200`,
DDL 2건(`QPS_DDL_FILE`·`QPS_DDL_MGR`)·코드시드 1건(`QPS_CODE_LIB`) **운영 DB 적용 완료**.

**★재개 시 첫 확인 — 권한별 동작.** 담당자를 한 명 지정한 뒤 **다른 계정으로 로그인**해 [파일 추가]가
사라지는지 봐야 한다. 위너넷 마스터는 항상 수정 가능이라 **그 계정만으로는 검증이 안 된다.**
(라운딩 실업로드 ✅ / 자료실 화면 ✅)

**QPS 다음 후보**
1. **보고서 폴더** — 캡처 수령·판정 완료(아래 절). **한 서식 + 유형 구분**으로 간다. 다음 작업은 여기.
2. **환자만족도 · 불만고충 폴더** — 아직 캡처 미수령. 수기 지표(MANUAL)의 원천 대장일 가능성.
2. **파일럿 병원 실사용** — 병원이 정해지면 여기부터.

미결(정하면 바로 되는 것): 자료실 권한을 서식에도 걸지 여부(현재 서식은 무제한) ·
결재 서명 이미지(보류) · FMEA·RCA(보류) ·
**기안서 틀을 공용 조각으로 뺄지**(감염종합보고에서 만든 것을 QPS 서식에도 재사용 가능).

### QPS 밖 — 같은 날 함께 진행한 것 (별도 문서)
요구사항 엑셀 156~199행 44건 + 원문 대조 점검. **QPS 와 무관하니 헷갈리지 말 것.**
→ [요구사항_156-199_처리_2026-08-10.md](요구사항_156-199_처리_2026-08-10.md)
- Q&A 오타 25건 · 명칭 변경(적정성평가 Q&A → **WinCheck 실무 Q&A**) · 월분석보고서 18건
- 원문 PDF(`요양병원 수가 실무교육자료 2022.10`)와 274건 전수 대조 → 실제 오류 18건 정정
- **미검증**: 월분석보고서는 로그인이 필요해 화면 확인을 못 했다(계산식은 요구사항 예시로 검산 완료).
  8월 이후 보고서의 **누적 줄**과 지표별 **점수 상향 목표** 문장을 화면에서 봐야 한다.
- **배포 필요**(사용자가 직접): `evalReport.jsp` · `qnaFloat.jsp` · `qnacd.jsp` · `common.css`,
  그리고 로그인 팝업 `LoginWinCT.jsp` + `images/winct/NLD.jpg`(wnn_consult).
  ★**팝업 시작일이 확인용 8/10 로 열려 있다 — 실제 안내는 8/11 부터라 되돌려야 한다.**
- Q&A 문구는 **DB(TBL_QNA_KB) 내용이라 배포 대상이 아니다**(이미 운영 반영).

---

## ▶ (이전) 2026-08-09 밤 23시 기준

**✅ 끝난 것.** ①"인증 제출 가능" 단계 전부 — 지표 18/18종(유형 4가지 실검증) · 결재선(확정취소·수치동결) ·
A4 인쇄 · 지표정의서 · 현황판·QPS 탭 메뉴 · 공통코드 11종 · 사용 안내.
②**서식 1~3호** — 회의록 v2(원본 5종 대조 + [지표 요약 넣기]) · 연간 활동계획서(원본 9장) · 라운딩 점검표(전월복사).
서식은 개별 방식 확정적(종당 반나절 ×3회 실측, 빌더 불필요).

**다음 후보 (위에서부터 권장)**

1. ~~**공통 파일첨부**~~ **— 완료(2026-08-10).** 서식 3종 부착 + **자료실** + **QPS 담당자 권한**까지.
   아래 「공통 파일첨부」·「자료실 + QPS 담당자 권한」 절. 남은 확인 = 화면에서 권한별 동작.
2. **잔여 폴더 캡처 확인** — 보고서·환자만족도·불만고충 폴더. 만족도·불만고충은 수기 지표(MANUAL)의
   원천 대장일 가능성 커서 캡처 한 번이면 커버 여부 판정 끝.
3. **파일럿 병원 실사용** — 병원이 정해지면 여기부터. 검증 범위는 전체지도 HTML 「일정」 절.
4. **[보류] 결재 서명 이미지** — 텍스트 전자서명 유지. 재개 시: 1회 등록(도장 스캔 기본) → 승인 시 자동 날인,
   ★승인 시점 이미지를 이력 행에 복사(스냅샷). 반나절~1일.
5. **[보류] FMEA·RCA** · **[제외 확정] 감염 폴더**(아래 [확정] 절).

**시작 전 확인 3가지**

- **커밋·푸시 확인** — 코드 분량은 22:23 커밋(40ac1f9)까지 들어갔다. **이 문서의 마지막 정리(이 블록 포함)가
  커밋·푸시돼 있어야 다른 PC 에서 이 내용을 본다.** `git status` 가 깨끗한지 + `git push` 됐는지 확인.
- **운영 배포 상태** — 20:39 WAR(현황판까지)은 배포·검증 완료. **21:42 WAR(서식 1·2호+다듬기)** 은 빌드만 됨 —
  배포 여부 확인. **그 이후 밤 작업분(회의록 v2·지표요약·서식 3호)은 WAR 재빌드 필요.**
  절차는 [배포 가이드](../배포_가이드.md). 배포 직후 화면이 통째로 죽어 보이면 **Ctrl+F5 먼저**(캐시 — [소동 아님] 절).
- **DB 는 이미 최신** — `docs/sql/QPS_*_2026-08-09.sql` **13개** 전부 운영 DB 적용 완료. 재실행 불필요.

**바꾸지 말아야 할 확정 사항** — 아래 「[확정]」 절들을 먼저 볼 것.
등급기준(낙상만 Level 2 이상, 나머지 전건) · 이름 표기(화면·소스 '기존 프로그램'/'이전 시스템', 문서 실명) ·
진행 방식(우리가 기본값을 정해 만들고 확인받는다) · 범위(감염 폴더 1차 제외) ·
회의록 계열은 서식 1호 하나(이름으로 구분, 종수 늘리지 않기).

---

# 2026-08-10 — 공통 파일첨부 (서식 3종 부착)

문서 「다음 후보 1순위」. **한 테이블로 모든 문서의 첨부를 담는다** — `REF_GB`(문서종류) + `REF_KEY`(문서키).

- **테이블 신설** `TBL_QPS_FILE` — [QPS_DDL_FILE_2026-08-10.sql](../sql/QPS_DDL_FILE_2026-08-10.sql), **운영 DB 적용 완료**(CREATE IF NOT EXISTS, 재실행 안전).
  `(HOSP_CD, REF_GB, REF_KEY, USE_YN)` 인덱스 + FILE_NM(원본명)·FILE_PATH(SFTP경로)·FILE_SIZE·SORT_NO·소프트삭제.
- **파일서버 재사용** — `SftpService.putFile()` **신설**(전송 전용, 반환=원격경로). 기존 `uploadFile()`은
  TBL_FILE_MST(공지·FAQ 전용)에 자동 저장하므로 QPS 첨부에 못 쓴다 → DB 저장은 QpsService가 TBL_QPS_FILE로.
  *[원칙] 기존 uploadFile 은 손대지 않았다(월보고서·공지가 쓴다). 저장 폴더 = `QPS/{병원}/{문서종류}/{문서키}/`.*
  다운로드는 **기존 `/sftp/download.do?filePath=` 재사용**(로컬우선+SFTP폴백+경로탈출차단, 새로 안 만듦).
- **5계층**: QpsFileDTO · selectQpsFileList/insertQpsFile/deleteQpsFile/selectQpsFileOne(매퍼) · 서비스 ·
  컨트롤러 `/qps/fileList.do`·`/qps/fileUpload.do`(멀티파트)·`/qps/fileDelete.do`(소프트).
  *멀티파트는 dispatcher-servlet.xml 의 전역 CommonsMultipartResolver 가 처리 — @MultipartConfig 불필요.*
- **★재사용 위젯** [inc/qpsFileBox.jsp](../../src/main/webapp/WEB-INF/jsp/main/inc/qpsFileBox.jsp) —
  `window.qpsFileBox({mount, refGb, ...})` → `api.setKey(문서키)`. 회의록·계획서·라운딩이 `<%@ include %>` 로 한 벌씩 끌어 쓴다.
  파일 추가(멀티)·목록(다운로드 링크)·삭제. **문서키 없으면 업로드 잠금**(어느 문서에 붙는지 정해져야).
  ***★함정(실제로 밟음) — 정적 include 조각의 page 지시자는 `pageEncoding` 만 남긴다.***
   *조각 파일도 톰캣이 따로 읽으므로 `pageEncoding` 이 없으면 부모가 UTF-8 이어도 **조각 안 한글만 전부 깨진다**
   (`ð…ï¾ë¶` 꼴). 반대로 `contentType`·`language` 는 넣으면 부모 지시자와 겹쳐 번역 에러.
   → 정답은 `<%@ page pageEncoding="UTF-8" %>` **한 줄만**. 처음에 통째로 뺐다가 3화면 다 깨져서 되돌렸다.
   기존 `com/head.jsp` 는 영문뿐이라 이 함정이 안 드러나 있었다. 조각 고친 뒤에는 **부모 JSP 의 번역 캐시**
   (`work/Catalina/localhost/ROOT/.../qps*_jsp.*`)를 지우거나 부모를 touch 해야 반영된다.*
- **부착 3종**: 회의록(키=회의록SEQ, 저장 후 열림) · 계획서(키=년도, 자연키) · 라운딩(키=년월, 자연키).
  각 화면 로드/저장 훅에서 `fileBox.setKey()` 호출.
- **✅검증 완료** — DB단(insert→list→소프트삭제) + **화면 실업로드(라운딩 점검표, 2026-08-10 사용자 확인)**.
  회의록은 문서 미작성이라 미확인이나 경로가 같다(키만 SEQ).
- BUILD=`20260810-0830`, WAR 재빌드 완료(08:30, 8/9 야간분+파일첨부 포함).

**화면 확인 순서(재개 시)**: `qps` → 회의록 → 저장 → [📎 첨부]에 파일 추가 → 목록·다운로드·삭제 확인.
계획서·라운딩은 년도/년월만 고르면 바로 첨부 가능(저장 불필요).

---

# 2026-08-10 — 자료실 + QPS 담당자 권한

**자료실** — 서식이 아니라 **보관물**(조직도·내규). 본문 입력이 없고 분류별 파일만 있다.

- 분류 = 공통코드 `QPS_LIB` 5종([QPS_CODE_LIB_2026-08-10.sql](../sql/QPS_CODE_LIB_2026-08-10.sql), 적용·한글 왕복 확인 완료)
  — 조직도·위원회 구성 / 규정·지침(내규) / 인증 관련 자료 / 교육자료 / 기타.
  ***★분류의 SUB_CODE 가 곧 첨부의 문서키(REF_KEY)다.*** 쓰기 시작한 뒤 코드값을 바꾸면 **그 분류의 파일이 미아**가 된다.
  이름만 바꿀 땐 SUB_CODE_NM, 없앨 땐 USE_YN='N'(행은 남긴다).
- 화면 [qpsLib.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsLib.jsp) + `main/qpsLib.do`, 사이드바 QPS 탭에 「자료실」.
  왼쪽 분류(건수 배지) · 오른쪽 첨부. 첨부는 서식과 **같은 위젯** 재사용(설계 의도대로 화면만 붙였다).
- 배지 갱신용 `/qps/fileCounts.do`(REF_KEY 별 GROUP BY 한 방) + 위젯에 `onChange` 콜백 추가.
- ✅**실업로드 확인 완료**(2026-08-10 사용자).

**권한 — 자료실만 담당자 제한** (요청: "QPS담당자만 수정 가능하게")

- 테이블 `TBL_QPS_MGR`([QPS_DDL_MGR_2026-08-10.sql](../sql/QPS_DDL_MGR_2026-08-10.sql), 적용 완료). (병원 × 사용자).
- **판정 3단**(`QpsController.canEditLib`): ①위너넷 계정=항상 가능(지원) → ②지정된 담당자 → ③**담당자가 0명이면
  병원관리자(MAIN_GU 1·2·3)가 대행** → ④그 외 불가. ***③이 핵심 — 없으면 신규 병원이 "아무도 못 고치는" 상태로 시작한다.***
  판정 실패(예외) 시에도 **막는 쪽**으로 떨어진다(규정 파일이 들어가는 곳).
- **왜 MAIN_GU 만으로 안 했나**: 3=병원관리자는 전산 담당이지 QPS 담당자가 아니다. 간호부 QPS 담당자가 4인 경우가 흔하다.
- 등급은 **쿠키(s_mainfg)를 믿지 않고** TBL_USER_MST 에서 직접 읽는다 — 쿠키는 화면에서 바꿀 수 있다.
- 명단 수정 권한은 별도(`canEditMgr`, 병원관리자·위너넷) — **담당자가 스스로 담당자를 늘리지 못하게**.
- ★**차단은 서버에서** — `fileUpload.do`/`fileDelete.do` 안에서 막는다. 화면의 버튼 숨김은 안내일 뿐.
  삭제는 fileSeq 만 오므로 `selectQpsFileOne` 에 REF_GB 를 추가해 "자료실 파일인지"를 서버가 판정한다.
- **적용 범위 = 자료실(LIBRARY) 뿐.** 서식 3종(회의록·계획서·라운딩)은 실무 입력자가 쓰므로 제한하지 않았다.
  서식에도 걸려면 같은 `canEditLib` 호출에 REF_GB 조건만 넓히면 된다.
- 화면: 「담당자 지정」 버튼(병원관리자·위너넷만 보임) → 병원 사용자 체크 → 저장(명단 통째 교체).
  권한 없으면 [파일 추가]·[삭제] 가 사라진다. 위젯에 `canEdit`/`setEditable(v, msg)` 추가.
  *권한은 서버 응답이 와야 정해지므로 위젯을 **잠근 채로 시작**해 늦게 푼다 — 반대로 하면 버튼이 깜빡 보였다 사라진다.*
- **안내 문구는 사유별로** — 서버가 `why`(WNN/MGR/ADMIN/NOMGR/NONE)를 같이 준다.
  위너넷·관리자대행은 파란 '알림', 진짜 보기전용만 주황 '경고'.
  *[버그·수정] 처음엔 위너넷 계정에 "보기 전용"이라 써 놓고 [파일 추가]는 살아 있었다(사용자 지적).
   원인은 위젯이 문구를 **덮기만 하고 되돌리지 않은 것** — 권한이 늦게 열려도 문구가 남았다.
   `syncEdit()` 이 canEdit 에 따라 **양방향**으로 문구를 세팅하게 고쳤다. 상태를 한 방향으로만 바꾸는 코드는 이렇게 어긋난다.*
- ✅검증: 신규 SQL 6종 DB 실행 확인(사용자목록·등급·건수·지정/해제 왕복, 테스트 행 원복), 엔드포인트 매핑 확인.
  BUILD=`20260810-1200`. **미검증=화면에서 권한별 동작**(아래).

**권한 화면 확인 순서**: 자료실 → [담당자 지정] → 한 명 체크·저장 → 그 사람 외에는 [파일 추가]가 사라지는지.
전원 해제하면 병원관리자에게 다시 열린다.

## 레이아웃 함정 2건 (2026-08-10)

- ***★`.dashboard-wrapper` 를 또 빠뜨렸다*** — 새 화면(qpsLib)에서 재발. 빼면 왼쪽 264px 이 사이드바에 가려지고
  좌우가 잘린다. **winn 화면을 새로 만들 때 첫 줄에 감싸는 것부터 하고 시작할 것.** (문서 기록만으로는 또 밟는다.)
- **우측이 접기 버튼(파란 원형)에 가림** — 화면마다 고치지 않고 `asset/css/common.css` 에 **공통 한 줄**로 처리:
  `.dashboard-wrapper div[id^="qps"] { padding-right:56px !important; }` — QPS 9개 화면 일괄, 다른 winn 화면 무영향.
  각 화면이 ID 선택자로 자기 padding 을 갖고 있어 `!important` 가 필요하다.

---

# 2026-08-08 — 시작일 (아래부터는 날짜순 기록)

## 오늘 한 일 (한 줄씩)

1. **SUNWOO DB 역설계** — saynice.co.kr MSSQL `SUNWOO`. 서식 203종 실측, 지표 18종 확정, 낙상 산식 복원.
2. **DDL 6테이블 + 지표 18종 시드** — `docs/sql/QPS_DDL_2026-08-08.sql`. 운영 WNN DB(114.108.153.178) 적용 완료.
3. **공용 화면 1개** (`qpsFall.do?indi=코드`) — 지표코드로 분기. 패키지 `egovframework.wnn_medcost.qps`.
4. **지표 유형 3종 구현·검증** (실데이터, 화면=독립검산 일치):
   - INCIDENT(낙상) — 0.33‰/0.67‰/1.00‰ SUNWOO 실측과 일치
   - PATVAL(욕창) — 환자평가표 직결, 입력 없이 집계(6월 22건→1.93‰)
   - MONITOR(손위생) — 코드 완성, 실입력 테스트만 남음
5. **분모 자동산출** — 입퇴원 자료로 재원일수 계산(중복 스냅샷 제거). 검산 일치.
6. **환자 연계** — 입원환자 입력검색 + [찾기] 팝업. 주민번호 비노출(성별·나이만 파생).
7. **지표 산식 전량 확정** — 지표분석보고서 캡처에서 분자·분모 확보. **격리·강박도 관찰형(MONITOR)** 발견.
8. **10지표 가동** — 낙상·욕창·손위생·격리·강박·환자안전·투약·학대폭력·자살자해·직원안전.
9. **메뉴 게이트** — `qps` 타이핑으로 토글(sessionStorage — 브라우저 닫으면 꺼짐), 위너넷 이중게이트.

## 지금 위치 (정직하게)

- **지표 자동집계 골격 = 견고·검증 완료.** 시작 단계지만 이 한 갈래는 실운영에 가깝다.
- **QPS 모듈 전체로는 시작 단계.** 203서식 중 지표 계열(18종)만. 결재·인쇄·서술형 서식은 미착수.
- 완료 기준 3갈래: ①지표 자동집계(≈80%) ②인증 제출 가능(결재+인쇄 필요, ≈40~50%) ③SUNWOO 전량 대체(서식빌더 필요, 별도 프로젝트).

## ~~★월요일 위너넷 협의 안건~~ — **2026-08-09 방침 변경으로 대부분 무효**

<sub>※ 위너넷은 분석·설계에 참여하기 어렵고 협의는 "할지 말지"를 정하는 자리다(2026-08-09 사용자).
→ **결정을 기다리지 않고 우리가 기본값을 정해 만들고 확인받는다.** 아래 안건 중 1·2·3 은 그렇게 처리했다
(완료 목표=인증 제출 가능 / 결재선=4단계로 만들되 단계 수 설정 가능 / 정의서 문구=표준안 선입력 후 병원이 편집).
남은 것은 **4(첫 적용 병원)** 와 **5(서술형 서식 범위)** 뿐이다. 자세한 것은 「[확정] 진행 방식」 절.</sub>

<details><summary>원래 안건 5가지 (기록용)</summary>

1. **완료 목표를 어디로?** — 권고: ②인증 제출 가능(지표 10종 + 결재 + 인쇄)으로 1차 완성 후 실심사.
2. **결재선** — SUNWOO는 담당·팀장·부서장·이사장 **4단계**. 위너넷도 4단계로 갈지, 간소화할지.
3. **지표별 목표값·정의문구** — 정의서 분자·분모·포함/제외기준은 **병원이 채우는 항목**. 병원이 편집하는 화면을 만들지, 위너넷이 대신 세팅할지.
4. **대상 병원·병원유형** — 재활/정신(격리·강박 등)에 따라 지표 집합이 다름. 첫 적용 병원 확정.
5. **서술형 서식(계획서·회의록·FMEA·RCA) 범위** — 203종 중 다수. 서식빌더로 갈지, 우선순위 몇 종만 개별로 갈지.

</details>

## ~~재개 시 첫 작업 (손위생부터)~~ — **2026-08-09 전부 완료**

<sub>※ 아래는 8/8 밤에 적어 둔 계획. 손위생·격리·강박 검증, 결재선, 지표정의서 모두 8/9 에 끝났다.
다음 할 일은 문서 맨 위 「▶ 다음은 여기부터」를 볼 것.</sub>

1. ~~손위생 실입력 테스트~~ → 완료(1분기 84.3%, 화면=검산 일치)
2. ~~격리·강박 확인~~ → 완료(85.0% / 60.0%)
3. ~~결재선 / 지표정의서 편집 화면~~ → 둘 다 완료

---

# 2026-08-09 — 손위생(MONITOR) 마무리

## 한 일

1. **집계 로직 검증(DB 실행)** — 가짜 병원 `99999998` 에 관찰 5행을 넣고 매퍼 SQL 4개를 그대로 실행,
   손검산과 일치 확인 후 **테스트행 전량 삭제**(현재 `TBL_QPS_MONITOR` 0행).
   - 월별: 1월 46/60 · 2월 45/50 · 3월 27/30
   - **1분기 118/140 = 84.3%** ← 월별 수행률 평균(85.6%)과 다르다. **분기 = 분자합÷분모합** 이 맞다는 확인.
   - `USE_YN='N'`(삭제행) 제외 · 직군/병동/순간 3축 분류집계 정상.
2. **지표 마스터 정의 채움** — [QPS_MST_MONITOR_2026-08-09.sql](../sql/QPS_MST_MONITOR_2026-08-09.sql),
   운영 DB 적용 완료. 손위생·격리·강박의 정의문·분자·분모·자료원·조사방법·담당, `DECIMALS=1`(수행률은 소수 1자리).
   ※ 분자·분모 문구는 병원이 고치는 칸이라 **표준 산식 시딩**이며 병원 전용행이 생기면 그쪽이 이긴다.
3. **화면 — 관찰형 라벨 정정**: 낙상 라벨이 그대로 나와 손위생인데 `분모(재원일수)`·`발생률` 로 읽혔다.
   → `labelsOf()` 로 유형별 라벨(분자(수행건수)/분모(관찰건수)/수행률), 분기표 헤더도 동적,
   차트 막대는 수행건수 대신 **관찰건수**(수행률과 같은 말 반복 방지), 흐름 안내·분류표 설명·정의박스(목표값 표시) 정리.
4. **입력 가드 신설** — 수행건수 > 관찰건수면 수행률이 100%를 넘어 분기합까지 오염된다.
   화면(JS)과 **서버(QpsController.monitorSave)** 양쪽에서 차단, 관찰건수 0 도 거부.
5. **★직원안전사고(STAFFSAFE) 분모 경로 없음 — 발견·수정**: 마스터 `DENOM_GB='STAFF'` 인데
   분모 탭이 `censusGb:'INDAYS'` 고정이라 **입력해도 다른 칸에 저장 → 지표가 영원히 '-'**.
   → 분모 탭이 지표의 `DENOM_GB` 를 따르게(제목·라벨·탭명·저장구분 전부), 직원수는 자동계산 버튼 숨김.
   이를 위해 `qfReload` 순서를 **indiLoad → censusLoad → incidLoad** 로 바꿨다(마스터를 먼저 알아야 한다).
   같은 문제를 가진 지표: `STAFFSAFE`, `INFEXP`(직원 감염노출).

## 사고보고형(INCIDENT) 5종 일반화 (2026-08-09)

관찰형에서 한 것과 같은 문제가 사고형에도 있었다 — 화면이 **낙상 전용으로 하드코딩**돼 있어
투약오류를 열어도 `낙상 사고 등록 / 낙상유형: 침대·휠체어`가 나왔다.

- **[확정] 등급 기준(사용자 2026-08-09)**: **낙상만 Level 2 이상, 나머지 5종은 전건.**
  마스터 `MIN_LEVEL` NULL = 전건(매퍼의 `<if>` 가 꺼진다). 바꾸려면 이 칸에 숫자만 넣으면 되고 코드는 안 고친다.
- **마스터 정의문 채움** — [QPS_MST_INCIDENT_2026-08-09.sql](../sql/QPS_MST_INCIDENT_2026-08-09.sql), 운영 DB 적용 완료.
  PTSAFE·MEDICATION·ABUSE·SUICIDE·STAFFSAFE 의 정의문·자료원·조사방법·담당(분자·분모 문구는 이미 채록분 유지).
- **화면(`INCID_UI` 표)** — 지표코드별로 탭명·카드제목·하위유형 라벨과 선택지를 갈아 끼운다.
  낙상 침대/휠체어 · 환자안전 낙상·투약·자살자해·수혈… · 투약 처방/조제/투여/모니터링 ·
  학대폭력 신체적/언어적·정서적/성적/방임/환자간/보호자 · 자살자해 자해/자살시도/자살 ·
  직원안전 주사침자상/감염노출/낙상/근골격계/폭행·폭언/화학물질.
  카드 제목 옆 안내도 등급정책을 따라간다 — 낙상만 "Level 2 이상만", 나머지는 "보고된 전건".
- **직원안전사고는 대상이 환자가 아니다** → 라벨을 `대상 직원(성명·사번)` 으로 바꾸고 **입원환자 [찾기] 버튼과
  입력검색을 끈다**(안 끄면 직원 이름으로 엉뚱한 입원환자 후보가 뜬다).
- 하위유형 목록을 갈아 끼울 때 **수정 중이던 값은 보존**한다(목록에 없으면 그 값을 항목으로 덧붙임) —
  안 그러면 옛 자료를 열었을 때 유형이 조용히 지워진다.

## 수기입력형(MANUAL) 신설 — 지표 6종 개방 (2026-08-09)

**막고 있던 것**: `NUMER_SRC='MANUAL'` 6종은 서비스에 분기가 아예 없어, 열면 사고 테이블을 뒤져 **0건**이 나왔다.
원천이 위너넷 안에 없는 지표들이다(대장·설문을 병원이 옮겨 적어야 한다).

- **테이블 신설** `TBL_QPS_MANUAL` — [QPS_DDL_MANUAL_2026-08-09.sql](../sql/QPS_DDL_MANUAL_2026-08-09.sql), 운영 DB 적용 완료.
  `(HOSP_CD, INDI_CD, IN_YEAR, VAL_GB)` UNIQUE + M01~M12. `VAL_GB`= NUMER(분자)/DENOM(분모).
  *[함정] UNIQUE 키가 없으면 저장(ON DUPLICATE KEY)이 행을 계속 쌓고 조회가 옛 행을 집는다 — 차등제에서 겪은 그 사고.*
- **분모가 두 갈래다**: 마스터 `DENOM_GB` 가 있으면(신체보호대=INDAYS) **기존 재원일수 마스터를 그대로 쓰고**,
  없으면(TAT·재택복귀·불만고충·만족도) 분모도 수기 DENOM 행을 쓴다.
  *[함정] `calcIndicator` 가 빈 denomGb 를 'INDAYS' 로 메우고 있었다 — MANUAL 은 그러면 안 되어 예외 처리했다.*
- **5계층**: QpsManualDTO · selectManual/saveManual(매퍼) · 서비스 · `/qps/manualGet.do`·`/qps/manualSave.do` ·
  화면 **탭5 [✍ 월별 입력]**(분자·분모 2줄 그리드, 분모가 재원일수인 지표는 분모 줄을 감추고 안내).
- **✅검증(운영 DB 실행, 가짜 병원 99999998 → 삭제 완료)**:
  UNIQUE 키 재저장 시 1행 유지(90/80 로 갱신) · 검체TAT 1월 90.0% · 2월 80.0% · **1분기 170/200 = 85.0%** ·
  신체보호대 12/3,000×1000 = **4.00‰**(수기분자 + census분모 교차 확인).
- **사이드바 6종 개방** — 신체보호대·영상TAT·검체TAT·재택복귀·불만고충·환자만족도. **가동 지표 10 → 16종.**
- 남은 2종: 요로감염(평가표 UTI 항목으로 PATVAL 전환) · 직원감염노출(정의문구).

## ✅ 지표 18종 완주 (2026-08-09) — 요로감염·직원감염노출 + 욕창 분자 버그

- **요로감염(UTI) = PATVAL 연결.** 단 **욕창과 셈법이 다르다** — 평가표의 `UTI` 는 발생일 컬럼이 없고
  '현재 요로감염 있음'을 뜻하는 **유병 플래그**라 매달 평가표마다 다시 켜진다(실측 2,228행 = **725명**,
  한 환자 최대 10회 이상). 그대로 세면 발생률이 아니라 **유병 연인원**이 된다.
  → `selectMonthlyNumerPatvalUti` = **환자별 최초 평가표(MIN(DOC_DT))의 달**에 1건.
  전년도에 이미 켜져 있던 환자는 올해 분자에서 빠진다(= '신규'의 정의). 원천 구분은 `INCID_GB='UTI'`.
- **직원 감염노출(INFEXP)** — 정의문·자료원만 채우면 끝(사고형 + STAFF 분모, 경로는 이미 있었다).
- **★버그 — 욕창 분자가 부풀어 있었다(8/8 '검증 완료' 값이 틀렸다)**:
  `COUNT(*)` 로 세는데 **같은 평가표가 두 번 적재된 행이 실재한다**(PAT_ID·ULCER_DT·DOC_DT·CHUNGSEQ 까지 동일).
  전 병원 962행 → 실제 **779건**. 경도 2026-06 **22건 → 19건**, 지표값 **1.93‰ → 1.67‰**.
  → `COUNT(DISTINCT PAT_ID, ULCER_DT)` + `ULCER_DT REGEXP '^[0-9]{8}$'`(머리글 오염행 차단)로 수정.
  *[교훈] 평가표 원천은 '한 사건 = 한 행'이 아니다. 업로드가 재실행되면 같은 행이 또 들어온다.*
- **마스터 정의문 18/18 완료**(빈 곳 0). 사이드바 18종 전부 개방.
- **[함정] 평가표의 환자키는 `PAT_ID` 인데 값이 주민번호 13자리다** — 집계·중복제거에만 쓰고
  화면·응답으로 절대 내보내지 말 것(사고보고 화면의 JUMINNO 처리와 같은 원칙).

## 메뉴 정리 (2026-08-09) — 지표를 사이드바에서 화면으로

**문제**: 지표 18종을 사이드바에 평면으로 늘어놓으니 메뉴가 화면을 다 먹고, 앞으로 붙을
지표정의서·분석보고서·서식이 들어갈 자리가 없었다.

- **[신설] 지표 현황 화면** `/main/qpsIndex.do` ([qpsIndex.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsIndex.jsp))
  — 18종을 **영역별 카드**로 보여주고 클릭하면 그 지표로 간다. 카드에 **입력 자료 유무**를 함께 찍는다
  (입력됨 N / 자동집계 / 입력 전) — 담당자가 가장 먼저 알고 싶은 게 "어디가 비었나"다.
  값(율)까지 계산하면 지표마다 집계 SQL 이 돌아 무거워지므로 **건수만** 센다(서브쿼리 3개).
- **[신설] 영역 컬럼** `AREA_NM/AREA_NO` — [QPS_MST_AREA_2026-08-09.sql](../sql/QPS_MST_AREA_2026-08-09.sql), 운영 적용 완료.
  환자안전 7 · 감염관리 3 · 인권·행동제한 2 · 진료지원·서비스 5 · 직원안전 1.
- **사이드바 축소** — 지표 18줄 → **4줄**(지표 현황 / 지표정의서 / 지표분석보고서 / 서식).
  뒤 셋은 화면이 없어 지표 현황으로 보낸다(없는 주소로 두면 404, 링크를 빼면 로드맵이 안 보인다).
- **[신설] 상단 QPS 탭** — 적정성평가와 별개 업무라 탭을 따로 뒀다. 누르면 좌측이 QPS 전용으로 바뀐다.
  *[함정] `menu-qps` 는 `menu-section` 이 아니라 `.menu-section` 일괄 hide 로 안 사라진다 → 따로 처리.*
  *[함정·중요] top.jsp 는 **마지막 선택 탭을 localStorage 로 복원**하며 자동 click 을 일으킨다.
   그대로 두면 새 세션에서 `qps` 를 치지 않았는데도 QPS 메뉴가 펼쳐진다(게이트가 새는 길).
   → sidebar 가 `window.qpsMenuOn()` 을 노출하고, 탭 핸들러가 이를 재확인해 아니면 전체메뉴로 되돌린다.*
- **기존 동작은 그대로 유지** — 게이트(`qps` 타이핑 + 위너넷), `qpsFall.do?indi=코드` 주소, 즐겨찾기 모두 무변경.
  지표 바로가기가 다시 필요하면 사이드바에 `<li>` 한 줄씩 되살리면 된다.
- 타일즈는 `.main/*` 와일드카드라 **설정 파일 수정 없이** `.main/qpsIndex` 가 잡힌다(확인).

## 결재선 (2026-08-09) — 2단계 완료

**결재(決裁)=승인 절차다. 요금 결제(決濟)가 아니다.** 인증 심사에 제출하려면 지표분석보고서에
결재가 찍혀 있어야 한다. 이전 시스템은 담당·팀장·부서장·이사장 4단계였다.

- **[신설] 테이블 2개 + 컬럼 1개** — [QPS_DDL_APPROVAL_2026-08-09.sql](../sql/QPS_DDL_APPROVAL_2026-08-09.sql), 운영 적용 완료.
  `TBL_QPS_APPR_LINE`(결재선 설정, `HOSP_CD='*'` 기본 4단계) · `TBL_QPS_APPR`(이력) ·
  `TBL_QPS_REPORT.CUR_STEP`(어디까지 승인됐는지).
- **★단계 수를 코드에 박지 않는다**(진행 방식 원칙 그대로). 병원 행이 있으면 그 병원 결재선을 쓰고,
  없으면 공통 4단계. 화면 [결재선 설정]에서 `담당, 승인` 처럼 적으면 2단계가 된다 — **코드 수정 없음.**
  *[함정] 단계를 줄일 때 뒤 행이 남으면 결재가 영원히 안 끝난다 → 저장은 DELETE 후 재삽입(통째 교체).*
- **상태 전이**: DRAFT →(상신) SUBMIT →(단계별 승인) … → 마지막 단계 승인 시 **CONFIRM**.
  반려는 REJECT + `CUR_STEP=0`(처음부터 다시), 아무도 승인 전이면 **상신 회수**(CANCEL)로 DRAFT 복귀.
  *승인 요청에 화면이 보낸 `stepNo` 와 서버의 `CUR_STEP+1` 을 대조한다 — 두 사람이 동시에 눌러도 순서가 안 꼬인다.*
- **★서술 잠금(중요)**: `saveReport` 가 `STATUS=VALUES(STATUS)` 로 상태를 덮어써서 **서술을 저장할 때마다
  결재 상태가 DRAFT 로 되돌아가는** 문제가 있었다 → 해당 구문 제거 + 컨트롤러에서 SUBMIT·CONFIRM 이면 저장 거부
  (화면도 저장 버튼을 잠근다). 결재자가 본 내용과 최종본이 달라지면 안 된다.
- **권한에 대하여(솔직히)**: 시스템에 병원 내 직위·역할 정보가 없다(로그인 = `s_userid`).
  그래서 v1 은 **'누가 언제 결재했는지를 남기는 전자서명'** 이다 — 종이 결재를 그대로 옮긴 것.
  결재자 지정·대결·알림은 사용자 마스터에 직위가 생긴 뒤에 붙인다. 이름은 `s_usernm` 쿠키로 찍는다.
- **✅검증(운영 DB, 가짜 병원 → 삭제 완료)**: 기본 4단계 상속 · 2단계 승인 시 SUBMIT 유지 ·
  3단계 반려 시 REJECT+0 복귀 · 재상신 후 4단계 승인 시 **CONFIRM** · 이력 9행 순서대로 ·
  병원 전용 2단계 결재선을 넣으면 그쪽이 이김.
- 화면: 지표분석 탭 하단 **[결재] 카드** — 상태 배지, 단계별 서명칸(이름·일시), 상신/회수/승인/반려, 이력표.
  반려는 사유 입력이 필수다(다음 사람이 무엇을 고칠지 알아야 한다). [결재선 설정]은 위너넷에만 보인다.

## 인쇄(A4) (2026-08-09) — 제출용 서식

지표분석 탭 [🖨 인쇄(A4)] → **2장짜리 제출 서식**. 1장 = 결재란·지표정의·월별·분기·추이, 2장 = 분석·개선계획·개선활동·작성일.

- **결재란이 인쇄물에 그대로 찍힌다** — 결재선 단계만큼 칸이 생기고 승인 이력에서 이름·날짜를 채운다.
  미승인 단계는 빈칸(종이 결재와 같은 모양).
- *[함정] 인쇄 영역을 화면 컨테이너 안에 두면 안 된다* — print CSS 로 앱 껍데기를 숨길 때 같이 사라진다.
  자식만 `visibility:visible` 로 되살리는 방식은 **조상의 `display:none` 을 못 이긴다**.
  → `document.body` 직속 `#qfPrintHost` 를 만들어 그 안에 그린다.
- *[함정] 차트(ECharts)는 canvas 라 숨긴 영역에서 다시 못 그린다* → 화면 차트를 `getDataURL()` 로
  이미지로 떠서 넣는다(pixelRatio 2, 흰 배경).
- 수치는 **화면이 이미 계산한 값(lastCalc)** 을 그대로 쓴다 — 인쇄 때 다시 계산하지 않으므로
  화면과 인쇄물이 어긋날 수 없다.
- `@page A4 세로 · 여백 12mm`, 표·분석박스는 `page-break-inside:avoid` 로 잘리지 않게.
- 관찰형은 관찰이 없는 달의 분자를 '-' 로 찍는 규칙이 인쇄물에도 적용된다(화면과 동일).
- **[함정] 종이 맨 위의 '위너넷 분석·평가'는 우리 인쇄물이 아니다** — 브라우저가 붙이는 **머리글(page header)**
  이고 내용은 `document.title` 이다. CSS 로 못 지운다. → 인쇄하는 동안만 제목을 보고서 이름
  (`손위생 수행률 지표분석보고서_경도요양병원(예천)_2026년 1/4 분기`)으로 바꿔 끼우고 `afterprint` 에 되돌린다.
  **덤: 'PDF로 저장' 시 이 제목이 파일명 기본값**이 된다. 날짜·주소 줄까지 없애려면 인쇄 창의
  [추가 설정] → [머리글 및 바닥글] 체크를 끄면 된다(확인창에 안내 문구를 넣어 뒀다).
- **[함정] 병원명은 배지 글자를 긁으면 안 된다** — 화면 로드 시점 값이라 병원을 바꾸고 새로고침 없이
  인쇄하면 **숫자는 선택 병원, 이름은 옛 병원**이 된다(2026-08-09 실제 발생). → `indiCalc` 응답에
  `hosp` 를 실어 보내 배지·인쇄물이 모두 '그 수치를 계산한 병원'을 쓰게 했다. 인쇄 전 확인창에도 병원명을 띄운다.
- **[함정] `_alertBox/_confirmBox` 의 `icon` 은 글자를 그대로 찍는다** — `'warning'` 을 넘기면
  아이콘 자리에 **'warning' 이라는 단어**가 나온다. 이모지(⚠️❌❓🖨)로 넘겨야 한다(14곳 정리).

## 지표정의서 편집 화면 (2026-08-09)

정의서는 **산식이 인쇄된 문서가 아니라 병원이 채우는 빈 양식**이다(산식채록 §0.1).
이 화면이 곧 그 서식을 대체한다. `/main/qpsDef.do` ([qpsDef.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsDef.jsp))

- **마스터에 양식 칸 10개 보강** — [QPS_DDL_INDIDEF_2026-08-09.sql](../sql/QPS_DDL_INDIDEF_2026-08-09.sql), 운영 적용 완료.
  주관부서·선정배경·포함기준·제외기준·이전값·목표값근거·보고주기·보고범위·성과공유·비고.
- **★병원별 격리(핵심)**: 저장은 **항상 그 병원 행**에만 한다. 공통('*')은 절대 안 바뀐다.
  아직 안 고친 병원은 공통값을 보고(배지 `공통 기본값`), 저장하면 `우리 병원 정의서`가 된다.
  [공통값으로 되돌리기] = 병원 행 삭제.
  *[함정] INSERT 때 산식 컬럼(NUMER_SRC·INCID_GB·DENOM_GB·MULTIPLIER·MIN_LEVEL·UNIT·DECIMALS)을
   공통 행에서 **복사**해 와야 한다 — 안 그러면 병원 행이 생기는 순간 그 병원 지표 집계가 통째로 죽는다.*
- **산식은 화면에서 못 고친다** — 문구를 다듬다 집계 방식이 바뀌면 지표가 조용히 틀어진다.
  산식·주기·해당영역은 읽기 전용으로 보여주고 "위너넷 관리" 라고 명시.
- **인쇄(A4 정의서 1장)** — 빈 결재란(결재선 단계만큼) + 21행 양식. 지표분석보고서와 같은 별도 창 방식.
- **✅검증(운영 DB, 가짜 병원 → 삭제 완료)**: 저장 전 공통값(own=N) → 저장 후 병원값(own=Y, 산식 복사 확인:
  MONITOR/×100/%) → **공통 행은 그대로** → 되돌리기 후 다시 공통값. 마스터 총 18행 유지(오염 없음).
- 사이드바 [지표정의서] 링크 활성화(준비 중 딱지 제거).

## 서식 1호: 위원회 회의록 (2026-08-09 저녁) — 서술형 203종의 첫 실물

`/main/qpsMinutes.do` ([qpsMinutes.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsMinutes.jsp)) — 사이드바 [회의록 (서식 1호)].
서식빌더로 갈지 개별로 갈지 **결정의 근거**가 되는 파일럿 — 개별 1종 = 반나절이 실측됐다.

- 구성: 좌 목록(연도별) + 우 작성 폼(회의일·회의명·장소·참석자·안건·논의·결정·차기일정) + **A4 인쇄**.
- 테이블 [QPS_DDL_MINUTES_2026-08-09.sql](../sql/QPS_DDL_MINUTES_2026-08-09.sql) — 운영 적용 완료. 삭제는 소프트(USE_YN).
- **전자결재 없음(v1)** — 인쇄물에 결재란(빈칸)만, 지표정의서와 같은 방식. 필요해지면 그때 확장.
- ✅CRUD 운영 DB 검증(가짜 병원 → 삭제): 등록·연도필터·수정·소프트삭제 정상.
- **2호 이후 판단 기준**: 개별 방식 = 서식당 반나절 × 필요 종수 vs 빌더 = 10~15일 선투자.
  우선순위 서식이 10종 미만이면 개별이 싸다.

## 서식 1호(회의록) v2 — 원본 「QPS위원회 회의록」 실물 3장 대조 보강 (2026-08-09 밤)

- **추가된 칸**: 회의 구분(정기/임시 라디오 → 인쇄 ☑/☐) · 인원 · 첨부자료(텍스트) ·
  **참석자 명단**(`직책: 이름` 한 줄씩 — 인쇄에서 원본 3쪽처럼 **2열 표**로 조립, 직책 구성이
  병원마다 달라 고정 칸으로 안 만든다) · 특이사항. DDL: [QPS_DDL_MINUTES2](../sql/QPS_DDL_MINUTES2_2026-08-09.sql), 운영 적용.
- 인쇄 제목·배치를 원본대로: 상단 우측 결재란 4단(담당~이사장 = **결재선 기본값과 동일** — apprLine 그대로),
  제목 「QPS위원회 회의록」, 일시/회의구분·장소/인원 2열 행.
- **★규정입안회의록·첫 회의록·임시회의록 등은 별도 서식이 아니다** — 원본 시스템도 같은 틀에 문서
  이름만 바꿔 쓴다(실물 3종·14장 대조 확인). 우리는 **회의명으로 구분**한다. 서식 종수를 늘릴 필요 없음.
  (첫 회의록 본문 안의 구분/성명 표는 그 문서에만 타이핑한 내용물이지 서식 필드가 아니다 —
   위원 명단은 우리 참석자 명단 칸이 담당.)
- **원본 5쪽에서 발견 — 문서마다 첨부(사진)·첨부(파일, 최대 14개) 페이지가 있다**: 이전 시스템은
  문서 단위 파일첨부를 지원. → **공통 파일첨부**(TBL_QPS_FILE: REF_GB+REF_KEY, 월보고서 PDF 가 쓰는
  파일서버 재사용)를 만들면 회의록·계획서 첨부 + 조직도·내규 자료실까지 한 번에 해결된다. **다음 작업 후보 1순위.**

## 서식 3호: 라운딩 점검표 (2026-08-09 밤) — 원본 2장 채록

`/main/qpsRound.do` ([qpsRound.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsRound.jsp)) — 사이드바 [라운딩 점검표 (서식 3호)].
**월 1부**(병원+년월 UNIQUE). 구분(의료기기·의료용구·응급kit·각실·공통·화재·건축·공조·전기·교육) ×
점검항목/내용 × 평가(양호/불량 — **재클릭으로 해제=미점검**, 불량 행은 배경색) × 불량내용·개선사항.

- **[⧉ 전월 복사]가 핵심**(원본 [전월복사] 이식) — 전월 항목·내용을 가져오고 **평가·불량내용은 리셋**.
  서버 API 없이 화면이 전월 roundGet 을 불러 채운다.
- DB [QPS_DDL_ROUND_2026-08-09.sql](../sql/QPS_DDL_ROUND_2026-08-09.sql) 운영 적용. 저장=항목 통째 교체(계획서 방식).
- 인쇄: A4 1장, 년월·점검자 머리, 구분은 같은 값 반복 시 첫 행만 표기, 평가 ✔.
- 원본 2쪽(사진 첨부)은 공통 파일첨부 과제로.

## 회의록 [지표 요약 넣기] (2026-08-09 밤) — 원본 「1월(인증후)」 실물에서 발견한 기능

원본 회의록 본문에 **지표 8종 수치표 + 사고형태·분류 건수표**가 박혀 있고 좌측에 [지표조회] 버튼이 있다
— 월례 회의에서 지표 결과를 보고하는 핵심 패턴. 옮겨 붙였다:

- `/qps/indiSummary.do` — 기간(분기)의 **18종 전부**를 calcIndicator 로 돌려 {지표명, 율, 분자/분모} 반환.
  ★무거운 호출(지표당 산출 1회) — **버튼 클릭 때만** 부른다. 한 지표 실패해도 나머지는 나감.
- 화면: 회의내용 아래 [📊 지표 요약 넣기] — **회의일이 속한 분기**를 자동 판정, 본문 끝에
  `· 낙상 : 0.33‰ (1/3000)` 형식 텍스트 블록 붙임(지우거나 고칠 수 있는 일반 텍스트).
- 원본과의 차이(우위): 저쪽은 손입력 값을 불러오고, **우리는 산출값**이라 회의록과 지표화면이 어긋날 수 없다.
- v2 후보: 사고형태(낙상/투약/기타 건수)·사고분류(근접오류=LV1/위해=LV2~4/적신호=LV5) 표,
  인쇄에서 텍스트 대신 표 렌더.

## 서식 2호: 연간 활동계획서 (2026-08-09 밤) — 원본 캡처 9장 채록

`/main/qpsPlan.do` ([qpsPlan.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsPlan.jsp)) — 사이드바 [연간 활동계획서 (서식 2호)].
사용자가 준 이전 시스템 실물 캡처 9장을 채록해 **연 1부(병원+년도 UNIQUE)** 문서로 구현.

- 섹션 6개: 개요(6항목, 항목명 편집 가능 — 원본 3번의 추진체계 흐름도는 v1 에서 글로 적는다) /
  주제선정(6기준×10점, **총점 자동합산**) / 주요업무 사업계획 / 활동 평가지표 /
  **연간 활동표(월 1~12 체크, 원본의 고정 행 24개를 새 문서 기본 틀로 프리필)** / 예산안(총예산 자동).
- DB: [QPS_DDL_PLAN_2026-08-09.sql](../sql/QPS_DDL_PLAN_2026-08-09.sql) — 운영 적용 완료.
  **항목 테이블 한 벌(TBL_QPS_PLAN_ITEM)로 전 섹션**을 담는다(SECT_CD + C1~C4·S1~S6·M01~M12·AMT 겸용).
- **저장 = 항목행 통째 교체**(items JSON → Jackson 파싱, 200행 단위 분할 삽입). 섹션별 부분수정 API 없음(단순 우선).
  *[관용구] upsert 후 seq 확보 = `ON DUPLICATE KEY UPDATE … PLAN_SEQ=LAST_INSERT_ID(PLAN_SEQ)`* — ✅운영 검증
  (재저장 시 같은 seq·행 1개 유지·제출일 갱신, 섹션별 조회·통째 교체 정상, 가짜 병원 → 삭제).
- 인쇄: 표지(제목·년도·**작성/검토/승인 3단** 결재란·제출일) + 섹션 6개 A4 여러 장(월칸 ●).
- v1 제외: 원본의 점수 참고사항표(빈 표) · 첨부(사진) 2장 — 필요해지면 추가.

## [소동 아님] WAR 교체 직후 QPS 화면 전체 사망처럼 보임 (2026-08-09 저녁)

환자안전사고 화면이 낙상 문구 그대로 + 탭·년도 전부 무반응, 콘솔 `prdOf is not defined`.
**코드 버그가 아니라 브라우저 캐시** — 교체 전 페이지 조각을 섞어 실행한 것. Ctrl+F5 로 즉시 정상.
판별법: 콘솔 오류 줄번호·내용이 현재 소스와 구조적으로 안 맞으면(호이스팅되는 함수의 ReferenceError 등) 캐시다.
배포 가이드 §7 함정에 등재.

## 다듬기 3건 (2026-08-09 저녁)

- **★동결값을 화면 표에도** — 확정(동결)된 기간이 인쇄물만 동결값이라 원천이 바뀌면
  **화면과 인쇄물이 서로 다른 숫자**를 보일 수 있었다. → 병합 로직을 공용 헬퍼(frozenMap/mergedMonths/
  mergedRollup)로 빼고, 결재 상태 로드 때 `applyFrozenToView()` 가 월별·분기표·차트를 동결값으로 다시
  그린다(기간을 바꾸면 실시간값으로 되돌아감). 인쇄도 같은 헬퍼를 쓴다 — 이제 어긋날 수 없다.
- **TAT 축별 표를 인쇄물에도** — 정규/응급 상세가 있으면 Ⅲ 표 아래 「정규·응급별 집계」가 붙는다
  (인쇄 함수를 비동기로 감싸 상세를 받아온 뒤 본문 생성. 실패해도 축 표 없이 인쇄는 진행).
- **매뉴얼에 현황판 반영** — [사용 안내] 결재 절에 [지표분석보고서] 안내 추가.

## 지표분석보고서 현황판 (2026-08-09) — '준비 중' 메뉴 하나 개통

`/main/qpsRpt.do` ([qpsRpt.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsRpt.jsp)) — 사이드바 [지표분석보고서].
선택 기간(분기·반기)의 **18종 전부**를 서술 작성 여부 · 결재 상태(작성 전/작성중/결재중/반려/최종승인) ·
진행(n/m 단계) · 최종수정과 함께 표로. 상단에 요약 타일 5개(전체/최종승인/결재중/반려/작성중·작성 전).
QPS 담당자는 "어느 보고서가 안 됐나", 결재자는 "내가 승인할 문서가 있나"를 여기서 본다.

- 행 클릭 → 그 지표 분석 탭으로 이동. **기간 딥링크** `qpsFall.do?indi=X&prd=Q2` 신설 —
  *[함정] 주소 숨김이 `location.search` 를 지우므로 쿼리를 JS 로 읽지 않고 서버가 모델(`data-prd`)로 내려준다.*
- 조회는 지표당 한 줄 보장(정의서 병원 행 중복 제거 — selectIndiList 와 같은 패턴).
- 기본 기간 = 오늘이 속한 분기. 보고서 행이 없으면 '작성 전'(회색) — 빈 기간도 18줄이 다 보인다.
- ✅SQL 운영 실행 확인: 2026Q3 = 18줄 전부 '작성 전' / 2026Q1 = 손위생·격리 DRAFT 반영.

## 사용자 매뉴얼 (2026-08-09) — 앱 안의 [사용 안내] 화면

**문서 파일이 아니라 화면**으로 만들었다([qpsHelp.jsp](../../src/main/webapp/WEB-INF/jsp/main/qpsHelp.jsp),
`/main/qpsHelp.do`, 사이드바 [사용 안내]) — 파일로 두면 병원에 전달이 안 되고, JSP 는 수정 = 파일 교체다.
캡처는 넣지 않았다(화면이 바뀔 때마다 낡는다 — 글·표로만).

구성 8절: ①시작하기(병원 배지 경고) ②지표 현황(배지 4종 뜻) ③자료 입력 — **유형 4갈래별 절차**
④지표분석('-'는 자료 없음) ⑤결재(동작 5종 표 + 동결) ⑥인쇄(PDF 파일명·머리글 끄기)
⑦지표정의서(병원별 격리) ⑧**문제 해결 6문답**(전부 '-'로 나옴 → 분모 없음 / 자료 안 보임 → 병원·년도 확인 /
저장 잠김 → 결재 상태 / 승인 차례 어긋남 → 새로고침 / 목록에 항목 없음 → 공통코드 요청 / 인쇄 머리글).

## 공통코드화 (2026-08-09) — 하드코딩 selectbox 11종 → TBL_CODE

**병원 요구("유형에 OO 도 필요")가 올 때마다 JSP 배포하던 것**을, 기준정보 화면의 행 추가로 끝나게 했다.

- **시드** [QPS_CODE_SEED_2026-08-09.sql](../sql/QPS_CODE_SEED_2026-08-09.sql) — 운영 적재 완료(11종 65행).
  `CODE_GB='Q'`(기존 관례가 한 글자 그룹, Q 미사용 확인) · 세트 = QPS_LEVEL·PLACE·DAMAGE·JOB·MOMENT +
  QPS_SUB_{FALL·PTSAFE·MEDICATION·ABUSE·SUICIDE·STAFFSAFE}.
- **★SUB_CODE = 저장값 그대로** — 위해등급만 LV1~5 코드, 나머지는 한글 라벨. 기존 저장 자료
  (SUBTYPE_CD 등)가 라벨을 담고 있어 **코드-라벨 이원화를 안 한다**(하면 옛 자료가 전부 '미상'이 된다).
- **★폴백이 원칙**: 화면은 `/qps/codeList.do` 로 코드를 받아 selectbox 를 다시 그리되,
  **코드가 비어 있거나 조회가 실패하면 기존 하드코딩 목록으로 그대로 동작**한다 — 코드 미적재가 장애가 되지 않게.
  초기 로드 순서: 코드 → indiLoad(유형 목록이 코드를 본다). 값 보존 재구성(수정 중 값이 목록에 없으면 덧붙임).
- 적용 selectbox: 위해등급·발생장소·손상유형(사고 폼) · 직군·순간(관찰 폼) · 사고유형(지표별 QPS_SUB_*).
- 이제 **항목 추가 = TBL_CODE_DTL 행 추가**(기준정보 공통코드 화면). 배포 불필요.

## TAT 정규·응급 축 분리 (2026-08-09) — 지표 완주의 마지막 꼬리

- **설계**: `TBL_QPS_MANUAL` 에 `AXIS_CD` 추가([QPS_DDL_MANUAL_AXIS_2026-08-09.sql](../sql/QPS_DDL_MANUAL_AXIS_2026-08-09.sql), 운영 적용 완료).
  **지표 산출은 총계 행(AXIS_CD='')만 읽는다 — 집계 로직 무변경.** 정규/응급은 상세(선택) 행.
  *[함정] UNIQUE 키에 AXIS_CD 를 안 넣으면 정규 행 upsert 가 총계 행을 덮는다 — 키 재생성 필수.*
- **화면**: [월별 입력] 탭에 `└ 정규·분자/분모 · └ 응급·분자/분모` 상세 행 4줄(선택 입력, 안 적어도 지표는 나옴).
  분석 탭의 분류 카드 자리를 **축별 집계표**(분기·연간, 축별 율)로 재활용 — 상세가 없으면 안내문만.
  대상은 `MANUAL_AXES` 표(TATIMG·TATLAB)로 관리 — 축이 필요한 수기 지표가 늘면 여기에 한 줄.
- **✅검증(운영 DB, 가짜 병원 → 삭제)**: 총계·정규·응급 6행 독립 저장 · 재저장 시 행 수 불변 ·
  산출용 조회는 총계만 · 축별 1분기 정규 137/155=88.4% · 응급 35/45=77.8% · 총계 85.0%.

## 결재 후속 (2026-08-09 오전 실사용 피드백 2건)

- **[확정 취소] 신설** — "이사장까지 하면 취소가 안 되나요"(사용자). CONFIRM 뒤 되돌릴 길이 없었다.
  → `REOPEN`: 최종승인 상태에서만, **사유 필수**, DRAFT 로 복귀 + 동결 해제 + 이력에 '확정취소'로 남는다.
- **결재란 이름 깨짐**(`%uB9C8%uC2A4%uD130`) — 로그인 화면이 쿠키를 비표준 `escape()` 로 저장하는데
  서버가 그대로 썼다. → 서버 `cookieDecode()`(%uXXXX + %XX/UTF-8 모두 처리)로 저장 시 복원,
  화면 `decNm()` 으로 **이미 저장된 옛 이력도** 표시 시 복원. `%XX 는 바이트 단위라 모아서 UTF-8 로 풀어야 한다.`
- **QPS 탭 = QPS 전용 사이드바** — 항목 나열이 아니라 **`#sbAllMenu` 의 QPS 외 전부**를 숨긴다
  (2026-08-09 지시 "QPS만 넣고 다 제외" — 나열 방식이면 메뉴가 새로 생길 때마다 QPS 탭에 샌다).
  *[함정1] '지금 보이는 것만' `qps-tab-hid` 표시 후 숨기고, 다른 탭에서 그것만 복원한다 —
  권한 게이트로 숨겨져 있던 관리자 메뉴를 무조건 show 로 되살리면 일반 계정에도 떠 버린다.*
  *[함정2] `.menu-section` 은 표시 없이 그냥 숨긴다 — 각 탭 핸들러가 계약구분으로 다시 정하므로
  우리가 복원하면 계약 필터와 싸운다.* (부수 수정: 종전엔 QPS 탭이 숨긴 evalreport·simulation 을
  다른 탭이 복원하지 않아 사라진 채로 남았다)
  **✅운영에서 확인(2026-08-09)** — QPS 탭 전환·복원 정상.
- **사이드바 강조가 '서식(준비 중)'에 붙던 버그** — 지표 현황을 열었는데 마지막 메뉴가 강조됐다.
  준비 중 링크 둘이 임시로 지표 현황과 **같은 주소**인데, 활성 판정의 `exactMatched` 검사가
  정확일치 대입 **뒤에** 있어 같은 주소의 뒤 링크가 계속 덮어썼다 → 검사 순서 교정(sidebar.jsp).
  **✅운영에서 확인(2026-08-09)** — 지표 현황 강조 정상.
- **서명칸이 지난 회차를 보여주던 버그** — 확정취소 후 재상신했더니 "1단계 차례"인데 서명 4개가
  이미 차 있었다. 이력에는 지난 회차 승인도 남아 있는데 그대로 그린 탓.
  → `signMapOf()`: 이력을 시간순으로 훑으며 **승인이 아닌 동작(상신·반려·회수·확정취소)이 나오면 비운다**
  = 현재 회차 승인만 남는다. 화면·인쇄 공통. 머리글에 `1. 담당 ✔ / ▶` 진행 표시도 추가.
  **✅운영에서 확인(2026-08-09, 격리지침 재상신)** — 서명칸 비워짐 정상.
- **사인 처리 방식(v1)**: 도장·서명 이미지가 아니라 **로그인 사용자명 + 승인 일시**가 결재란에 찍히는
  텍스트 전자서명이다. 시스템에 직위·서명 이미지가 없기 때문(결재선 절의 '권한에 대하여' 참조).
  도장 이미지가 필요하면 사용자 마스터에 서명 이미지 컬럼을 붙이는 확장으로 간다(미착수).

## 기간 확정(스냅샷) (2026-08-09) — 최종승인 시 수치 동결

**왜**: 지표를 매번 실시간 산출하므로, 결재까지 끝낸 보고서라도 나중에 원천(사고·관찰·평가표)이
고쳐지면 **제출한 종이와 화면 값이 어긋난다** — 인증에서 그 자체가 지적사항이다.

- **동결**: 결재가 CONFIRM 되는 순간 `TBL_QPS_STAT` 에 그 기간 합계 1행 + 소속 월행을 저장(`freezeStat`).
  8/8에 테이블만 만들어 두고 안 쓰던 것을 이제 쓴다.
- **해제**: 반려(REJECT)·상신회수(CANCEL)로 결재가 풀리면 동결도 지운다(`unfreezeStat`) —
  확정 안 된 값이 확정본처럼 남으면 안 된다.
- **표시**: `apprGet` 응답에 `frozen`/`stat` 이 실려 온다. 결재 카드에 "수치 확정(동결)" 안내,
  **인쇄물은 동결값으로 찍고** 바닥에 "최종승인 시점에 확정된 값" 문구가 붙는다.
  동결값이 없는 기간(다른 분기·연간)은 실시간값 그대로.
- 동결 실패가 결재를 되돌리진 않는다(try-catch) — 결재가 본체, 동결은 부가 장치.
- **✅검증(운영 DB, 가짜 병원 → 삭제)**: 동결 4행(Q1+월3) 저장 → 기간 조회 정확 →
  동결값 vs 실시간값 분리 확인 → 해제 시 0행.
- ※화면(qfMonRate 등 지표분석 탭)은 계속 실시간이다 — 동결값 적용은 **인쇄물만**.
  화면까지 동결값으로 바꾸면 "왜 입력했는데 안 변하지"가 되므로 일부러 그렇게 뒀다.

## [확정 2026-08-09] 범위 — 감염 폴더는 QPS 1차에서 제외

**감염관리는 별개 업무다**(인증 별도 장, 담당 = 감염관리실) — QPS 1차 범위에 넣지 않는다(사용자 확정).
원본 감염 폴더 10항목 대조 결과, 절반은 이미 커버라 당장 막히는 것도 없다:

| 원본 감염 폴더 | 판정 |
|---|---|
| 손위생수행률 · 요로감염 · 직원감염노출 | **이미 있음** (QPS 지표 3종) |
| 감염관리위원회 회의록 | **서식 1호 재사용** (회의명만 다르게) |
| 감염관리계획서 · 감염라운딩 | 서식 2·3호와 같은 구조일 가능성 — 확장 시 구분값 얹는 정도 |
| 우선순위 사정 도구 · 감염병환자 · 감염종합보고 · 전담자 | 감염 고유 — 미커버(확장 시 개별 서식 2~3일) |

**재개 시점** = 파일럿 병원이 돌아본 뒤 감염 요구가 나올 때. "감염도 해달라" 재요청 시 이 표부터 볼 것.
같은 방식으로 **보고서·환자만족도·불만고충 폴더**는 캡처 확인만 남음(수기 지표 원천 대장일 가능성 — 커버 여부 즉시 판정 가능).

## [확정 2026-08-09] 진행 방식 — 우리가 리드한다

**위너넷은 분석·설계에 참여하기 어렵고, 협의는 "할지 말지"를 정하는 자리다**(사용자 2026-08-09).
→ 일정을 협의 결과에 걸지 않는다. **기본값은 우리가 정해 만들고, 돌아가는 화면으로 확인받아 그때그때 고친다.**

- 산식·등급기준·정의문구는 **표준값으로 먼저 넣는다**(빈칸으로 두면 아무도 안 채운다). 병원이 고치면 그 값이 이긴다.
- 결재선은 4단계로 만들되 **단계 수를 설정값으로** 빼 둔다 → "2단계로 줄여 달라"가 와도 코드를 안 고친다.
- 지표는 전부 열고 **병원별로 끄는** 방식으로 간다(병원유형별 집합을 미리 합의하지 않는다).
- 심사 일정이 없으면 **①지표 완주 ②결재·인쇄 붙은 시연본 ③파일럿 1곳 실사용** 을 자체 마일스톤으로 쓴다.
- → 전체지도 HTML 의 '협의 안건' 섹션을 **'진행 방식'** 으로 교체했다(결정 대기 구간 제거).

## [확정 2026-08-09] 이름 표기 규칙 — 화면·소스는 '기존 프로그램', 문서는 실명 유지

- **화면(DB 정의문)**: 욕창 정의문에 있던 `(… 명칭·단위·상수는 SUNWOO 정의서 실물로 확정)` 문구 삭제.
  병원 사용자에게 타사 프로그램 이름이 보이면 안 된다. 마스터 전 행 재확인 — 남은 곳 없음.
- **소스 주석**(JSP·자바·매퍼XML 14곳): `SUNWOO` → **`기존 프로그램`**. 주석은 브라우저로 안 나가지만
  배포물에 섞여 나가므로 표기를 통일했다. **참조 자체는 지우지 않는다 — 산식의 근거라 맥락이 필요하다**
  (사용자 지시 2026-08-09: "기존 프로그램으로 유지, 맥락을 위해서").
- **개발 문서**(`docs/proposals/QPS_*`, `docs/sql/*`)는 **실명 그대로 둔다.** 배포물이 아니고,
  `SUNWOO` 는 역설계 대상 **MSSQL DB 이름**(saynice.co.kr)이라 바꾸면 다시 접속·대조할 수가 없다.
  `T_PATIENTCNT`·`T_RPT` 같은 원본 테이블명도 같은 이유로 보존.
- 정리하면: **밖으로 나가는 것(화면)에는 안 쓰고, 배포물(소스)에는 중립 표기, 우리 자료(문서)에는 실명.**

## 배포 (오늘 변경분)

- `qpsFall.jsp` — **파일 교체만으로 반영**(라벨·분모구분·JS 가드). 운영 서버는 아직 08-08 빌드다.
- `QpsController.java`(서버 가드) — **WAR 재빌드 필요.** 없어도 화면 가드가 먼저 막으므로 테스트에는 지장 없음.
- DB(마스터 UPDATE) — **적용 완료**, 재기동 불필요.

## ✅ 손위생 실입력 검증 완료 (2026-08-09, 화면 = 사전 검산 100% 일치)

관찰 4건(1월 40/34·20/12, 2월 50/45, 3월 30/27) 입력 후 화면 값이 DB 검산과 전부 일치:
월별 76.7 / 90.0 / 90.0 % · **1분기 118/140 = 84.3%**(월별 평균 85.6% 아님 — 분자합÷분모합 확인) ·
상반기·연간 84.3% · 직군 간호사 87.8%·의사 60%·간호조무사 90% · 병동 A 76.7%·B 90% · 순간 4행 정상.
**→ MONITOR 유형 확정.** 격리·강박은 같은 코드경로라 화면만 한 번 열어보면 된다.

후속 표시 보정(같은 날): ①분류표 수행률이 `90%`/`87.8%` 로 섞여 찍히던 것 → 소수자리 고정(fmtRate).
②관찰이 없는 달·분기는 분자 0 이 '수행 0건'으로 읽혀 **분모와 함께 '-'** 로(사고형은 0 유지 — 사고 0건은 사실).

## ✅ 격리·강박 확인 완료 (2026-08-09)

같은 MONITOR 경로라 **지표 분리**를 중심으로 DB 실행 검증(가짜 병원 99999998, 손위생 행과 섞어 넣고 확인 후 삭제):
`INDI_CD` 로 정확히 갈림 — 격리 1분기 17/20 = **85.0%**, 강박 3/5 = **60.0%**, 손위생 행은 어느 쪽에도 안 섞임.
사이드바 링크(`?indi=ISOLATION` / `SECLUSION`)·마스터 정의(수행건수 ÷ 전체 시행 건수 ×100) 모두 정상.

**★고친 것 — '순간(moment)'은 손위생 전용이다**: 격리·강박 화면에도 WHO 5 moments 칸이 떠 있어
늘 빈 값으로 저장되고 **분류표에 `미상` 축 하나가 통째로 생겼다**. → `USE_MOMENT`(=INDI_CD가 HANDWASH)로
입력칸·목록열·분류표 축을 함께 감춤. 분모 명칭도 갈랐다 — **손위생=관찰건수 / 격리·강박=시행건수**
(마스터 DENOM_DESC '전체 시행 건수'와 일치). 입력 라벨·목록 머리글·차트 막대·경고문·검증 메시지에 일괄 반영.

## 손위생 테스트 순서 (화면에서)

1. **상단 [병원검색]으로 대상 병원부터 고른다** — 위너넷(w1234567) 상태로 입력하면 위너넷 계정에 쌓인다
   (08-08 재원일수 때 실제로 겪은 사고). 화면 머리 배지가 그 병원인지 확인.
2. [관찰 입력] 탭에서 관찰일·관찰건수·수행건수(+직군·병동·순간) 로 3~4건 등록.
3. [지표분석] 탭 — 월별 수행률, **분기 = 분자합÷분모합**, 직군·병동·순간 분류표 확인.
4. 격리·강박도 같은 화면·같은 방식이라 한 번씩만 확인하면 된다.

## 산출물·배포 상태

- 코드: `src/main/java/egovframework/wnn_medcost/qps/**`, `Qps_SQL.xml`, `qpsFall.jsp`, `sidebar.jsp`
- WAR: `target/wnn_medcost-1.0.0.war` (2026-08-08 22:59) — 운영 반영 시 업로드+재기동
- DDL: 운영 WNN DB 적용 완료 / 마스터 18종 시드 완료
- 문서: `QPS_위너넷구현_분석` · `QPS_서식목록_SUNWOO_실측` · `QPS_지표정의서_산식채록` · `QPS_낙상파일럿_구현노트` · `QPS_업무_전체지도(HTML)` · (이 문서)
- 공유 아티팩트(전체지도): https://claude.ai/code/artifact/74a73ed8-cc87-408e-93ec-981ea3b7d501

## 함정 메모 (재개 시 시간 아끼려면)

- 톰캣: JSP는 즉시반영, **클래스·매퍼XML은 web.xml touch 로 리로드 강제** + BUILD 표식으로 확인 후 증상 해석.
- AJAX는 **dataType:'json'** 필수(+서버 produces). 빠뜨리면 조용히 0건.
- 마스터에 컬럼 추가하면 **읽는 SELECT 절도 같이** 고칠 것(등급필터 사라진 사고).
- `TBL_PATVAL_MST.USE_YN`은 전 행 'N' — 필터 걸면 전멸.
- `TBL_IPWON_INFO`는 입원건이 월 스냅샷마다 반복(중복제거 필수).
