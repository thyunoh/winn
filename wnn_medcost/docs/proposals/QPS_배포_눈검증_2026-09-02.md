# QPS — 운영 배포 + 눈 검증 안내 (2026-09-02)

> 08-15 안내([QPS_배포_눈검증_2026-08-15.md](QPS_배포_눈검증_2026-08-15.md))와 같은 꼴. **이번 = 08-19 이후 밀린 것 전부**(마지막 build 표식 `20260818-INDIDIST` 이후).
> 일반 절차는 [배포_가이드](../배포_가이드.md) 그대로 — 실서비스는 `webapps/wnn_medcost-1.0.0`(ROOT 아님), 재기동은 `shutdown.sh`+`startup.sh`.
> DB 는 **전부 적용 완료**라 스크립트 실행은 없다. 배포 뒤 눈 검증 ⏱15분.

## 1. 무엇이 밀려 있나 (08-19 이후, QPS 만)

| 갈래 | 파일 | 배포 방법 |
|---|---|---|
| **자바 4** | `QpsController`(BUILD=`20260902-DELPHI`, 공휴일·공통코드·체크묶음 엔드포인트) · `QpsMapper` · `QpsService` · `QpsServiceImpl` | ⛔**WAR 재빌드 + 재기동** |
| **매퍼 1** | `Qps_SQL.xml`(EXCL_YN·CELL_TXTS·공휴일·공통코드·체크묶음·병동/부서 목록) | ⛔ 위와 같이(기동 시 로드) |
| **JSP 8** | `sidebar`(메뉴 재배열·검색·관리 메뉴 3) · `qpsChk`(편의기능·SEL·병동) · `qpsChkForm` · `qpsChkUse` · `qpsSafeRpt` · **신규** `qpsCode` · `qpsHoliday` · `qpsRptDef` | WAR 에 포함 |
| **DB** | ✅**전부 적용 완료**(09-02 스크립트 10건 — [인수인계 §3](QPS_인수인계_2026-09-02.md)) | ***다시 돌리지 말 것*** |

### 반영 확인 — 한 줄

```bash
curl -s -X POST https://<운영주소>/qps/codeList.do -H 'Cookie: s_hospid=<병원>; s_wnn_yn=Y' | grep -o '"build":"[^"]*"'
```

**`20260902-DELPHI`** 가 나와야 한다. 옛 값이면 재기동이 안 된 것(파일만 바꾸면 매퍼·컨트롤러가 안 바뀐다).
⚠기동 로그에 `Qps_SQL.xml` 파싱 오류가 없어야 한다 — 09-02 밤 `<>` 사고를 한 번 겪었고 고쳤다(XML 파서로 확인 끝).

## 2. 눈 검증 — 순서대로 (위너넷 계정으로)

| # | 어디 | 무엇을 보나 | 틀리면 이렇게 보인다 |
|---|---|---|---|
| 1 | 사이드바 QPS | 맨 위 **🔍 메뉴 검색** 칸. 「공휴」 치면 관리(설정) ▸ 기준코드 ▸ 공휴일 관리만 남고 그룹이 펼쳐진다. 지우면 원래대로 | 검색 칸 없음 = 옛 sidebar. 검색해도 안 줄어듦 = 스크립트 오류(콘솔) |
| 2 | 사이드바 구조 | 계획·위원회 → 지표·분석 → 환자안전 활동 → QI → 환자만족도·불만고충 → 보고서·서식 → 점검표 작성 → **담당자별 업무**(부서 15) → **관리(설정)** → 자료·안내 | 옛 순서(부서별 점검표가 따로) = 옛 sidebar |
| 3 | 관리(설정) ▸ 기준코드 ▸ **공통코드** | 왼쪽 묶음 31개(이름·쓰는 수/전체), 오른쪽 세부코드. 묶음 목록·표가 각각 안쪽 스크롤. 이름 하나 고쳐 [저장] → 새로고침해도 남음 | 「불러오지 못했습니다」 = `qcodeList.do` 없음(재기동 안 됨) |
| 4 | 〃 ▸ **공휴일 관리** | 2026 고르면 공휴일 목록(추석·개천절·한글날…). 하나 추가·삭제 | 빈 목록 = DDL 미적용(아님 — 09-02 적용 확인됨) 또는 엔드포인트 없음 |
| 5 | 〃 ▸ **보고서 체크 묶음** | 왼쪽 「공유 묶음」+유형 76. PTSAFE 고르면 카드 ~15(직종·손상종류·발생장소…). 공유 카드에 「⚠ 공유 — n개 유형」 경고. 항목 하나 「사용」 끄고 저장 → 보고서 화면 그 유형의 「구분」 카드에서 사라짐 | 「불러오지 못했습니다」 = `rdefList.do` 없음 |
| 6 | 점검표 작성 · 아무 일 단위 서식 | 값칸 **더블클릭** 빈→O→X→빈. 날짜 머리 더블클릭 = 세로줄. [☑ 전체 O] 는 빈 칸만. 「토·일·공휴일 제외」 켜면 공휴일 열이 빨간 점선 + 건너뜀 | 더블클릭에 반응 없음 = 옛 qpsChk. 공휴일이 안 빨감 = 공휴일 조회 실패 |
| 7 | 점검표 · 사인 행 | 사인 행 머리(「점검자 사인」) 더블클릭 → 빈 사인칸에 로그인 이름 | 이름 대신 경고 = 쿠키 `s_usernm` 없음 |
| 8 | 점검표 · **병동** | 병동 칸 클릭 → 그 병원 작성분의 병동 목록(datalist, 처음엔 비어 있을 수 있음). 저장된 점검표 앞 **병동 필터**(병동 있는 서식만). 간호 서식을 병동 없이 [저장] → 「병동 없이 저장할까요?」 한 번 | 필터 셀렉트가 늘 보임/안 보임 오작동 = ckDocFill 미반영 |
| 9 | 점검표 · **선택 칸** | FAC047 개인영상정보 관리대장 → 「구분」 열이 드롭다운(이용/제공/열람/파기). NUR022 마약류 저장시설 점검부 → 「점검내용」 2단 머리 밑 유/무 3열. 인쇄 미리보기에 고른 글자만 | 글자 입력칸 = 시드 미적용(아님) 또는 옛 JSP |
| 10 | 점검표 · 인사고과 평가표(ADM019, 원무·총무) | 10행 × 5열 칸마다 **고정 글** + 아래 입력칸, 한 줄에 O 하나만(다른 O 지워짐), 열 머리 더블클릭 = 그 열로 채움 | 글이 없음 = CELL_TXTS 미반영 |
| 11 | 서식 관리(qpsChkForm) | 입력 종류에 **「선택」**, 고르면 「선택지」 칸이 보임. ITEM_COL 서식엔 「셀 고정문」 칸 + 「행 배타(EXCL_YN)」 체크 | 종류가 O/X·글자·숫자뿐 = 옛 JSP |
| 12 | 우리 병원 사용 서식(qpsChkUse) | 서식을 옮기면 띠에 **[지금 저장]** 단추. 다른 탭에서 켜고 작성 화면으로 돌아오면 서식 목록이 **F5 없이** 갱신 | 단추 없음 = 옛 JSP |
| 13 | 보고서·서식(qpsSafeRpt) | 유형 PTSAFE 「구분」 카드에 묶음 ~15개(투약오류·낙상유형…). 부서 칸 클릭 → QPS 부서 이름 목록. 유형 셀렉트에 HARASS 하나(STAFFVIO 없음) | 묶음이 4~5개뿐 = CHK2 시드 미적용(아님) |
| 14 | 병원 계정으로 | 관리(설정) 그룹의 위너넷 항목이 안 보이고, 공통코드·공휴일·체크 묶음 화면은 **보기만**(저장 단추 없음). 주소를 직접 쳐도 `FAIL — 위너넷 담당자만` | 저장 단추가 보임 = data-wnn 게이트 깨짐 |

## 3. 끝나면

- [남은할일](QPS_남은할일_2026-08-15.md) A1·A2 를 ✅ 로. 남는 것은 병원 답변 3 + 회의 사안 4([인수인계 §5](QPS_인수인계_2026-09-02.md)).
- 첫 실사용 병동이 병동 이름을 적기 시작하면 병동 목록·필터가 살아난다 — 「3병동/3 병동」이 갈리면 그때 정리(작성분 UPDATE 한 줄).
