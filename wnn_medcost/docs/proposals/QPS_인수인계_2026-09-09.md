# QPS 인수인계 — 2026-09-09 (인사 등록 · 담당자별 사인·도장)

> 어제 문서 [QPS_인수인계_2026-09-08.md](QPS_인수인계_2026-09-08.md)(일괄 작성·출력·사인 + 결재란·근무표)에 이어지는 하루다.
> 오늘 한 일은 **하나** — 「근무표 사람 고르기 + 사인 칸 도장」의 뿌리인 **직원 명부**를 만들었다. 배경은 어제 문서 §12·§13, 이 문서만으로 이어서 할 수 있다.
>
> **읽는 차례** : §1 상태 → §2 왜 이 표인가(되돌리기 전에 꼭) → §3 파일별 → §5 배포 → §7 후속 3건(면허 가져오기·동명이인 도장·사인 칸 이름) → §6 함정 → §8 남은 일.
> **오늘 끝난 것** : 인사 등록 화면 · 근무표·점검표 연결 · 후속 3건까지 **로컬 재기동 뒤 실왕복 완료**. 남은 것은 **커밋 4파일 · 운영 배포**뿐이다.

## 1. 지금 상태 한 장

| 항목 | 상태 |
|---|---|
| DDL 2개 | ✅ 사용자 실행(로컬·운영 같은 DB) — [SIGNER](../sql/qps/ddl/QPS_DDL_SIGNER_2026-09-09.sql) · [EMP](../sql/qps/ddl/QPS_DDL_EMP_2026-09-09.sql) |
| 자바·매퍼 | ✅ **로컬 재빌드·재기동 완료**(오전분 + 후속 3건) · ⛔**운영 WAR 재빌드+재기동 필요** (`BUILD="20260909-SIGNER"`) |
| JSP | 신규 `qpsSigner.jsp` · 수정 `qpsChk.jsp` `qpsDuty.jsp` `qpsApprAuth.jsp` `sidebar.jsp` — tmp1 배포본 복사 완료 |
| 검증 | 매퍼 XML **319문**·중복 0 · javac · JSP 문법·태그·**id 중복**·미정의·Swal 0 · 시뮬 **459** · **재기동 뒤 실왕복 2벌**(오전 20항목 · 후속 3건 API 15항목 + 화면) · 내장 브라우저 시연(등록→사인→근무표→일괄 사인→인쇄 도장) |
| 시험 자료 | 전부 삭제(가짜병원 99999998 — 직원·근무표·문서·값 잔여 0). 실자료 병원(11282991)은 **읽기만** |
| 커밋 | ✅오전분 + 후속 3건 **커밋됨**(`eae4462`) · ⛔**미커밋 4파일**(§5) — 사용자 몫 |

**사용자 지시(순서대로)** : 「담당자별 사인(작성해서) 및 도장 관리 필요함」 → 「사인은 작성해서 저장 기능」 → 「진행하고 있는 것 하고 해줘」 → 「**인사등록도 필요함**」 → 「TBL_QPS_SIGN 이 사원정보로 가능할듯 확인해줘」 → 「사인 시연 부탁」 → 「입력시 부서 선택하면 그리드 부서 자동 따라오게」 → 「두 개 상단 설명 간결하게」 → 「**남은일 해줘**」(§7 후속 3건) → 「재빌드하고 재기동했음」(실왕복).

## 2. 왜 이렇게 만들었나 (설계 — 되돌리기 전에 읽을 것)

- **문제** : 도장은 로그인 계정 본인만([🖋 내 도장], 결재란용) 등록할 수 있었다. 점검표 사인 칸에 이름이 오르는 **간호사·조무사는 계정이 없다** → 그 사람 사인을 붙일 길이 없고, 근무표 사람 콤보도 비었다.
- **답** : ***새 표 없이 `TBL_QPS_SIGN` 을 직원 표로 넓혔다.*** 이 표가 이미 「병원+USER_ID = 사람마다 한 줄」이라 **근무표·사인 칸·결재란이 한 사람을 가리킨다.** 직원 표를 따로 두면 사람이 두 표에 갈린다(이 저장소가 여러 번 겪은 함정 — 「같은 것을 두 군데서 하지 않는다」).
- **기존 `TBL_HOSPEMP_MST` 와 대조**(사용자 물음, DB 로 확인) : 그 표는 차등제 **면허 인력 신고**(면허종류·면허번호·입사·퇴사·주당근무시간, 키 = 면허번호+입사일). 면허자만 다루고 사번·직책이 없어 목적이 다르다 — **합치지 않았다.** 뒤에 「면허등록에서 가져오기」 다리는 놓을 수 있다.
- **키 규칙** : 계정 있으면 USER_ID = 계정(ACCT_YN=Y, 결재란 도장과 **같은 줄**) · 없으면 서버가 `P`+UUID 12자리(ACCT_YN=N). **만든 줄의 USER_ID 는 바꾸지 않는다**(결재 기록·근무표가 그 키를 가리킨다) — 계정 잇기는 새 사람일 때만 보인다.
- **퇴직자** = RETIRE_DT 가 오늘 이전. 근무표 콤보(`selectQpsSignerNames`)에서 빠지고 인사 화면은 「퇴사자 포함」을 켜야 보인다. ★**사인 칸 도장 찾기(`selectQpsSignsByNames`)는 그대로** — 옛 종이의 도장이 사라지면 안 된다. 삭제(USE_YN=N)와 퇴사는 다르다(화면 삭제 확인창이 「퇴사자는 빼지 말고 퇴사일을」이라 말한다).
- **개인정보 최소화** : 주민번호·연락처·주소는 받지 않는다(HRCARD 와 같은 원칙). 칸 = 이름·사번·직종·직책/직급·부서·입사·퇴사·차례·비고.
- **결재란은 그대로 본인만** — 이 화면은 결재를 대신 찍는 곳이 아니다. 계정을 이어 두면 그 사람 도장이 사인 칸과 결재란에 같이 쓰인다.
- **그림 저장 경로는 하나** — `signerImgSave.do` 도 `saveQpsSign`(내 도장과 같은 upsert)을 부른다.

## 3. 바뀐 것 — 파일별

### DDL (둘 다 실행됨, 더하기만 하는 ALTER — 옛 WAR 무해)
- `QPS_DDL_SIGNER_2026-09-09.sql` : JOB_NM·DEPT_CD·ACCT_YN(기본 Y)·SORT_NO
- `QPS_DDL_EMP_2026-09-09.sql` : EMP_NO·POS_NM·JOIN_DT·RETIRE_DT(CHAR 8)·REMARK

### 매퍼 `Qps_SQL.xml` (오전 7문 + 후속 2문 · 고친 것 1문 = **319문**)
- 오전 : `selectQpsSigners`(hospCd·deptCd·**withRetire**, 그림 포함, `retired` 계산 열) · `selectQpsSignerNames`(근무표 콤보, 그림 없이, 퇴직자 제외) · `selectQpsSignerOne` · `insertQpsSigner`(ON DUPLICATE KEY … USE_YN='Y' — 지운 사람을 같은 계정으로 다시 넣으면 살아난다) · `updateQpsSigner` · `clearQpsSignImg`(그림만) · `deleteQpsSigner`(USE_YN=N).
- 후속(§7) : `selectHospEmpCandidates`(면허등록 후보, 이름으로 묶음) · `selectQpsSignerPicks`(사인 칸 이름 고르기) · **`selectQpsSignsByNames` 차례 바꿈**(부서·재직 우선).
- ⚠`<=` 는 `&lt;=` 로 적었다(매퍼 규칙). PowerShell `[xml]` 로 정형성 확인함.

### 자바
- `QpsMapper` 9 · `QpsService` · `QpsServiceImpl` : `saveQpsSigner(hospCd, userId, acctUserId, userNm, jobNm, deptCd, sortNo, **hr**(Map), regUser)` — 날짜는 `hrDate`(숫자만 남겨 8자리, 아니면 예외), 퇴사<입사 예외, 길이 검사. `clearQpsSignImg` · `deleteQpsSigner` · **`selectQpsSignsByNames(…, deptCd)`**(시그니처 바뀜) · `selectHospEmpCandidates` · `selectQpsSignerPicks`.
- `QpsController` : 화면 `main/qpsSigner.do` · 엔드포인트 **6** `signerList / signerSave / signerImgSave / signerDel.do`(`what=img|row`) **+ `signerEmpList` / `signerPicks.do`**. 권한 = `canEditLib`(위너넷 `s_wnn_yn=Y` · QPS 담당자 · 담당자 없으면 병원관리자 1·2·3) — 단 `signerPicks` 는 **보기 전용이라 안 좁힌다**. 본인 그림은 본인이. `dutyGet.do` 가 `signers` 를, `signNames.do` 가 `deptCd` 를 받는다. **`BUILD = "20260909-SIGNER"`**.

### 화면
- **`qpsSigner.jsp`(신규)** — 메뉴 관리(설정) ▸ **인사 등록 · 사인·도장**(병원도 보임). 부서 필터 · 찾기(이름·사번·직종·직책) · 퇴사자 포함 · [+ 사람 추가] 폼 · **[📋 여러 명 붙여넣기]** · 표(사인 미리보기·계정 배지·퇴직 배지) · 사인 그리기 창 · 그림 내리기 · 삭제. 동명이인은 경고만(막지 않음). 조회 순번 가드.
  - 붙여넣기 = 엑셀 열(이름·사번·직종·직책·부서·입사일) 복사 → 탭/쉼표 · 머리줄 건너뜀 · 부서는 이름/코드 · 못 읽는 줄은 「건너뜀」 표시 · 한 줄 실패해도 계속(결과 토스트+실패 목록). SUNWOO 사원등록의 「엑셀 불러오기」 갈음(파일 업로드 없음).
  - 사인 창 = qpsChk [🖋 내 도장] 창과 같은 캔버스 규칙(devicePixelRatio 로 화면·그리는 크기 분리, 스캔 그림은 캔버스 폭에 맞춰 축소, 1MB 상한).
  - ★사용자 지적 반영 : **폼 부서를 고르면 표 부서 필터가 따라오고, 저장 뒤엔 저장한 사람 부서로** 맞춘다(안 그러면 「등록했는데 안 보인다」). 상단 설명 5줄 → 2줄.
  - 후속(§7) : **[👩‍⚕️ 면허등록에서 가져오기]** 띠(`sgEmpBox`) 신설.
- `qpsChk.jsp` — 후속(§7) : 사인 칸 `<datalist>`(`ckSignPickSync`, `renderGrid` 끝에서) · 도장 찾기에 부서를 실어 보내고 **먼저 온 것을 지킨다** · 부서가 바뀌면 받아 둔 도장을 버린다(`SIGN_DEPT`) · 부서 판정 `ckFormDept()`.
- `qpsDuty.jsp` — 사람 콤보를 `<optgroup>` 두 묶음(인사 등록 직원 ✎ / 명단에 없는 계정 직원)으로, 이름·직종은 명단에서 먼저 채움, 툴바 「인사 등록 →」.
- `qpsApprAuth.jsp` — 상단 설명 4줄 → 2줄(사용자 「두 개 상단 설명 간결하게」).
- `sidebar.jsp` — 메뉴 한 줄.

### 시뮬·문서
- `docs/tools/sim/sim_qpsSigner.js`(신규 **61검사** — 인사 칸·퇴사자·붙여넣기·면허 가져오기·부서 따라오기·**id 중복**) · `sim_qpsChk_signpick.js`(신규 **11**) · `sim_qpsChk_stamp.js`(+4, 부서·재직 우선) · `sim_qpsDuty.js`(+5, 콤보 optgroup·명단 채움) → 전체 **459** 통과.
- scratch `chk_duty_jsp.js` 에 **id 중복 검사**를 넣었다(§6-8 의 결함을 브라우저 전에 잡는다).
- 어제 문서 §13 에 짧은 요약, `.claude/CLAUDE.md` ▶이어서 6 「후속 ③」.

## 4. 검증 — 무엇을 어떻게 봤나 (오전분)

> 후속 3건(§7)의 검증·실왕복은 **§7 끝**에 따로 적었다.

- **재기동 뒤 실왕복 20항목**(scratch `signer_rt.ps1`, PowerShell 로 UTF-8 바이트 본문 — ⚠git-bash curl 은 한글이 깨진다) : 등록 2명(P+12자리, 사번·직책·입사·비고 되읽기, `2025.07.15` → `20250715`) · 거절 3(깨진 날짜·퇴사<입사·이름 없음) · 계정 없는 사람 사인 저장 → 목록 hasimg · `signNames` 로 찾힘 · 근무표 콤보 둘 다 · 퇴사 처리 → 기본 목록·콤보에서 빠짐, 포함하면 retired=Y, **도장은 여전히 찾힘** · 그림 내리기·줄 삭제·없는 사람 FAIL · 권한 없는 계정 저장 FAIL(목록은 보임).
- **내장 브라우저 시연**(사용자 「사인 시연 부탁」) : 인사 등록 [+ 사람 추가] 홍길동 → 사인 창 자동 열림 → 마우스로 긋고 저장 → 미리보기 / 근무표 9월에 홍길동 DDDDDOO 저장 / 점검표 「혈당측정기 점검대장」(NUR053) 9월 문서 → [✍ 일괄 사인] 그날 근무자 → **1건 22칸**(휴무일 빈 칸) / [☑ 사인 칸 도장] + 인쇄 가로채기(`QPS_BULK_CB`) → 사인 행에 **도장 22개**.
- 쿠키 심기 : `s_hospid=99999998; s_userid=admin; s_wnn_yn=Y` 를 `document.cookie` 로 넣고 `.do` 를 다시 연다(`s_wnn_yn=Y` 가 고치기 권한).

## 5. 배포 — 무엇을 해야 하나

1. ✅**커밋 — 사용자가 `eae4462` 로 마쳤다**(자바 4 · 매퍼 1 · JSP 5 · DDL 2 · 시뮬 4 · 문서 3 = 19파일).
   ⛔**그 뒤 미커밋 4파일** : `qpsSigner.jsp`(id 중복 고침 — §7 끝) · `sim_qpsSigner.js`(id 검사) · `.claude/CLAUDE.md` · 이 문서.
2. **운영 DB** — DDL 2개는 이미 돌았다(로컬과 운영이 같은 DB). 다시 돌리면 「Duplicate column」 오류가 날 뿐 자료는 안 바뀐다.
3. **운영 WAR 재빌드 + 재기동**([배포 가이드](../배포_가이드.md) — `webapps/wnn_medcost-1.0.0`, `shutdown.sh`+`startup.sh`). 반영 확인 = `codeList.do` 응답 `build` 가 **`20260909-SIGNER`**.
   JSP 만 먼저 올려도 안 깨진다(`signerList.do` 404 → 화면이 「서버에 연결하지 못했습니다」 · 사인 칸 목록·면허 띠는 조용히 종전대로).
4. 눈 검증(운영, ⏱5분) : 관리(설정) ▸ **인사 등록 · 사인·도장** 열림 → [👩‍⚕️ 면허등록에서 가져오기]에 그 병원 인력 신고 명단이 뜸 → 한 명 가져와 사인 저장 → 근무표 사람 콤보에 보임 → 점검표 사인 칸을 누르면 그 이름이 목록으로 뜸. 병원 계정(담당자 아님)으로 열면 보기만.

## 6. 함정 — 오늘 새로 얻은 것

1. **새 화면 시뮬 본문 추출** : `</style>` 뒤부터 뽑으면 **style 앞에 있는 래퍼 div 가 잘려** `gel('qpsSigner')` 가 null 로 죽는다 → 인라인 `<script>` 앞까지 통째로 잡고 style·지시자·주석만 뗀다(`sim_qpsSigner.js` 머리).
2. **시뮬 `until` 이 영영 안 풀리면 프로세스가 멎는다** — 픽스처의 점 날짜 `2026.4.1` 은 6자리라 「건너뜀」이 되어 기대 3건이 안 찼다. 픽스처는 서버 규칙(8자리)과 같게.
3. **`ckSave({quiet:true})` 는 문서 목록을 다시 안 읽는다**(설계) — 시연에서 그 상태로 일괄 사인을 돌리니 방금 저장한 문서를 못 찾았다. 화면 [저장] 단추는 목록을 갱신하므로 실사용 무해. 자동화할 땐 `ckBase()` 를 한 번 더.
4. **`ckPrint()` 를 직접 부르면 도장이 안 찍힌다** — 도장 조회(`ckSignsLoad`) 뒤 인쇄하는 것은 `ckPrintGo()`. 툴바 단추가 부르는 함수를 쓸 것.
5. **한글 파라미터 실왕복은 PowerShell** `Invoke-WebRequest -Body ([Text.Encoding]::UTF8.GetBytes(...))` 로(응답도 `RawContentStream` 을 UTF-8 로 읽는다). git-bash curl 은 CP949 로 깨진다(08-08 함정 재확인).
6. `RunSql` 콘솔은 한글이 깨져 보이지만 저장은 정상 — 확인은 HTTP 응답(JSON)으로.
7. 내장 브라우저 스크린샷이 **한 프레임 늦게** 오는 때가 있다(패널이 열렸는데 안 보임) — JS 로 `offsetHeight` 를 같이 본다.
8. ★★**한 화면에 같은 id 를 두면 조용히 죽는다** — `getElementById` 가 앞엣것을 집는다. 「단추를 눌러도 아무 일 없음」의 흔한 원인이고, **display:block 을 엉뚱한 입력칸에 붙여 놓고도 오류가 안 난다.**
   판별법 = `offsetHeight` 가 0 인데 `display` 는 block 이면 **부모가 숨겨진 것**이다 → `parentElement` 를 거슬러 올라가면 범인이 나온다. 이제 검사 도구·시뮬이 id 중복을 센다.
9. 브라우저 콘솔에서 화면 함수를 시험할 때 **IIFE 안 변수(EMP·LIST 등)는 밖에서 못 고친다** — `EMP=…` 는 전역을 새로 만들 뿐이라 화면은 안 바뀐다(테스트가 거짓으로 통과한다). 화면이 부르는 함수(`sgEmpOpen`)로 태울 것.

## 7. 후속 3건 — 「남은일 해줘」로 이어서 마쳤다 (같은 날)

§6 끝에 후보로 적어 둔 셋을 그대로 만들었다. **자바·매퍼가 바뀌었으므로 ⛔재빌드+재기동**(`BUILD` 를 **`20260909-SIGNER`** 로 올렸다 — `codeList.do` 응답 `build` 로 확인).

### ⑯ 면허등록에서 가져오기 (매퍼 1문 · 엔드포인트 1 · JSP)
- 인사 등록 툴바 **[👩‍⚕️ 면허등록에서 가져오기]** → 초록 띠. 차등제 **인력 신고표(`TBL_HOSPEMP_MST`)** 의 이름·직종(면허 종류 이름)·입사일·퇴사일을 후보로 보여 주고, 고른 사람을 인사 등록에 넣는다.
- 매퍼 `selectHospEmpCandidates` — **이름으로 묶는다**(한 사람이 면허를 둘 이상 신고할 수 있다) · 면허 종류는 공통코드(`Z`/`LIC_TYPE`)로 이름을 붙인다 · 이미 인사 등록에 있는 이름은 `already='Y'`.
- 엔드포인트 `signerEmpList.do`(권한 `canEditLib`). ★**등록은 화면이 줄마다 `signerSave.do` 를 부른다** — 붙여넣기와 같은 길이라 저장 규칙(날짜 검사·P키 만들기)이 한 곳에만 있다.
- 화면 : 전부 고르기 · **부서 함께 지정**(저 표의 `DEPT_CODE` 는 진료과 코드라 QPS 부서와 체계가 다르다 — 그래서 가져오며 고른다) · 「퇴사일 지난 사람도」 · 이미 있는 이름은 회색·체크 불가 · 한 줄 실패해도 나머지는 들어간다 · 끝나면 그 부서로 표를 맞춘다.
- ★**두 표를 합치지 않는다** — 저쪽은 「면허 신고」(키 = 면허번호+입사일), 이쪽은 「사람 명부」다. **베껴 오기만** 하고 뒤에 저쪽이 바뀌어도 따라가지 않는다(따라가면 사인이 딸려 흔들린다). 비고에 「면허등록에서」를 남긴다.
- ⚠**`20991231`(무기한) 관용값** — 실자료(11282991, 68명)의 `TE_DT` 가 대부분 이 값이다. 그대로 가져오면 인사 카드에 2099년이 남으므로 **2090년 이후는 퇴사일로 안 가져온다**(미리보기도 빈 칸).

### ⑰ 동명이인 도장 — 부서·재직 우선 (매퍼 · 자바 · JSP)
- 종전엔 같은 이름이 둘이면 **USER_ID 순 앞 사람** 도장이 붙었다(임의). ⇒ `selectQpsSignsByNames` 에 `deptCd` 를 받아 **①그 문서 부서 ②재직자 ③USER_ID** 차례로 준다.
- 화면 `ckSignsLoad` 가 **부서를 함께 보내고**(서식 부서, 공통이면 화면 부서 — `ckFormDept()`), 받은 목록에서 **먼저 온 것을 지킨다**(전에는 뒤엣것이 덮었다 — 차례를 매겨도 뜻이 없어진다).
- ★**부서가 바뀌면 받아 둔 도장을 버린다**(`SIGN_DEPT`) — 같은 이름이라도 부서가 다르면 다른 사람일 수 있다.
- 완전한 해결(사람 키로 매치)은 사인 칸에 이름만 남는 한 불가능하다 — ⑱ 이 그 실수를 줄인다.

### ⑱ 점검표 사인 칸에서 이름 고르기 (매퍼 1문 · 엔드포인트 1 · JSP)
- 사인 칸은 자유 글자다 — 손으로 치면 「홍길동」·「홍 길동」처럼 갈려 **도장이 안 붙는다**. ⇒ 인사 등록 명단을 **`<datalist>`** 로 붙여 고를 수 있게 했다(`signerPicks.do` · `selectQpsSignerPicks`).
- ★**입력을 막지 않는다** — 그냥 `input` 이라 명단에 없는 사람도 적을 수 있다(막으면 급할 때 못 쓴다). 부서로 좁히고(그 부서 + 부서 없는 사람) 퇴직자는 뺀다. 사인 있는 사람은 ✎ 로 보인다.
- 붙이는 자리 = `renderGrid` 끝(`ckSignPickSync`) — 격자 코드를 안 건드리고 뒤에서 속성만 붙인다. 사인 칸이 없는 서식은 묻지도 않고, 명단이 비거나 **옛 서버면 조용히 종전대로** 자유 입력.

**검증(후속 3건)** : 매퍼 XML **319문**·중복 0 · javac exit 0 · JSP 문법·태그·**id 중복**·미정의·Swal 0 · 새 SQL 3문을 재기동 전에 DB 로 직접 태워 봄 · 시뮬 `sim_qpsSigner` **61** + `sim_qpsChk_stamp` **18**(+4) + 새 `sim_qpsChk_signpick` **11** → 전 시뮬 **459** 통과.

**✅재기동 뒤 실왕복(같은 날) — API 15항목 + 화면** :
- `build = 20260909-SIGNER` 확인.
- **이름 고르기** : NURSE 로 물으면 「김간호·공통씨」(그 부서 + 부서 없는 사람, 퇴직자 제외) · LAB 이면 「이검사·공통씨」 · 부서를 안 주면 재직자 3명 · 직종·사인 유무가 함께 온다.
- **동명이인 도장** : 같은 이름 「한이름」을 셋(LAB 재직 · NURSE 재직 · NURSE 퇴사) 만들고 도장 3장을 올린 뒤 — NURSE 문서면 첫 줄이 **NURSE 재직자**, LAB 문서면 **LAB**, 부서를 안 주면 **퇴사자가 맨 뒤**. 세 줄 다 도장이 실려 온다.
- **면허등록 가져오기** : 가짜병원은 빈 목록 · 실자료 병원(11282991)은 **68명**(이름으로 묶여 중복 0, 첫 사람 「윤병녕 / 의사 / 20250331」) · 담당자 아닌 계정은 FAIL(이름 고르기는 보기 전용이라 OK).
- **화면** : 인사 등록에서 면허 띠가 열리고 실자료 68명이 표로(무기한 2099 는 퇴사일 칸에 안 보인다) · 점검표 NUR053 을 열면 사인 칸 30개에 목록이 붙고 항목이 「김간호 / 한이름 ✎ / 공통씨」(LAB 사람은 빠진다) · 사인 칸에 이름을 넣고 인쇄하면 **도장 있는 사람은 그림, 없는 사람은 이름 글자**.
- 시험 자료 7건 삭제(잔여 0) · 실자료 병원에는 **아무것도 쓰지 않았다**(읽기만).

⚠★**여기서 결함 하나를 잡았다 — id 중복** : 면허 띠에 `sgEmp` 를 붙였는데 **위 폼의 사번 칸이 이미 `sgEmp`** 였다. `getElementById` 가 앞엣것(입력칸)을 집어 **띠가 안 열리고**(그 입력칸에 `display:block` 이 붙었다) 화면은 조용했다.
  ⇒ 띠를 `sgEmpBox` 로 바꿨다. ★**시뮬도 놓쳤다** — `$('sgEmp').style.display === 'block'` 이 그 입력칸을 보고 통과했다.
  ⇒ **검사 도구(`chk_duty_jsp.js`)와 시뮬에 「같은 화면 안 id 중복」 검사를 넣었다.** 앞으로 이런 것은 브라우저 전에 걸린다.

## 8. 격자 안 「직원 이름 칸」 — 「qps 이어서」로 §9 후보 첫째를 마쳤다 (같은 날)

⑲ **점검표 격자의 직원 이름 열에도 인사 등록 명단을 붙였다**(JSP 2 · 시드 1 · **DDL·자바·매퍼 0 → 재빌드 불필요**).

- **왜** : 어제는 사인 칸(`SIGN_NO=900`)에만 붙였는데, 격자 안에도 **직원 이름을 적는 열**이 있다(직원 교육 현황표 성명 · 검진 명부 이름 · 서명대장 성함…). 손으로 치면 이름이 갈려 나중에 사람으로 셀 수 없다.
- **어떻게** : ***새 표·새 칸을 만들지 않았다*** — 입력 종류 `INPUT_GB` 에 값 하나(**`NAME`**)를 더했다(2026-09-02 `SEL` 을 더한 전례와 같은 결).
  서버는 INPUT_GB 를 그대로 흘려보내고 **CHECK 일 때만** O/X 로 맞추므로(`ChkNorm`) **자바·매퍼가 그대로**다. 화면이 그 열에 `.nmpick` 를 붙이고 사인 칸과 **같은 datalist**(`signerPicks.do`)를 단다.
- ★★**「이름」이라는 낱말로 자동 판정하면 안 된다**(조사로 실증) — 같은 「이름」이 **HLT002 는 직원, LAB002 는 환자**다. 「성명」도 마찬가지(NUR011 은 환자, ADM014 는 직원, **인사기록카드 HRCARD 는 가족**).
  ⇒ **서식이 열마다 켠다.** 공통 시드로 **직원 명부 15열만** 켜고([QPS_SEED_NAMECOL_2026-09-09.sql](../sql/qps/seed/QPS_SEED_NAMECOL_2026-09-09.sql), ✅사용자 실행) 나머지는 병원이 서식 관리에서 켠다.
  확인자·점검자 같은 **서명류 열은 일부러 안 켰다** — 서식마다 우리 직원인지 외부 업체인지 갈린다.
- **손댄 곳** : `qpsChk.jsp`(`isNameGb`·`ltxtCls` 신설 · `noxCls` 에 NAME · `ckSignPickSync` 가 사인 칸 + `.nmpick` 둘 다) · `qpsChkForm.jsp`(입력 종류 콤보 **「직원 이름」** · 붙여넣기 파서 · LIST 안내문).
- **글자 칸(TEXT)과 똑같이 동작한다** — 15열이 전부 TEXT 였으므로 켜도 값·정렬·O/X 규칙이 안 바뀐다. 되돌리기는 시드 맨 아래 UPDATE 한 줄.
- **검증** : JSP 문법·태그·id 중복·미정의·Swal 0 · 시뮬 `sim_qpsChk_signpick` **21**(11→21) → 전 시뮬 **470** ·
  **실왕복**(가짜병원) : API = HLT002 의 NAME 열 1개(이름) · **LAB002 는 0개** · ADM014 성명 1개 · 부서별 명단(HEALTH 한보건 / NURSE 김간호) ·
  화면 = HLT002 격자 270칸 중 **이름 열 30칸에만** 목록이 붙고 항목이 「한보건 / 보건관리자」 · **환자 대장(LAB002)은 0칸**(핵심 안전장치) ·
  이름 두 칸에 값을 넣어 **저장→재조회에 그대로**(명단에 없는 이름도 직접 칠 수 있다). 시험 문서·직원 삭제(잔여 0).
- ⚠**같이 고친 회귀** : `sim_qpsChk_sel.js` 가 `noxCls` 만 꺼내 돌리는데 그 함수가 새 `isNameGb` 를 부르게 되어 **통째로 죽었다**(전 시뮬에서 「못 돈 파일 1」로 잡힘).
  한 줄 정의를 꺼내는 `fn1()` 을 더해 함께 태운다 — ★**여러 줄 패턴으로 한 줄 정의를 잡으면 뒤 함수까지 삼킨다**(이 저장소의 오랜 함정).

⑳ **보고서(safeRpt) 반복행에도 직원 이름 열**(사용자 「safeRpt 이름 칸도 진행해줘」) — DDL 1 · 매퍼 2문 · JSP 1 · 시드 1 · **자바 0**. ⛔**WAR 재빌드+재기동 필요**(매퍼).

- **왜 새 칸이 필요했나** : 점검표는 항목마다 한 줄이라 그 줄의 `INPUT_GB` 로 됐는데, 보고서 반복행은 **열 이름이 `SUB_COLS` 쉼표 한 줄**이라 열마다 표시를 달 자리가 없다.
  ⇒ **이름 열 번호 목록**을 담는 칸 `NAME_COLS` 를 두 표에 하나씩([QPS_DDL_SRPT_NAMECOL_2026-09-09.sql](../sql/qps/ddl/QPS_DDL_SRPT_NAMECOL_2026-09-09.sql), 더하기만 하는 ALTER).
  ⛔열 이름에 표식을 박는 방법(「담당의사#」)은 버렸다 — 화면·인쇄에서 벗겨내야 하고 병원이 그 규약을 알아야 한다.
- **화면**(qpsSafeRpt.jsp) : `nameColSet`·`namesOfBox` 신설 · `subDefs` 가 `names` 를 함께 만들고 `rowHtml(nCol, v, names)` 가 그 칸에만 `list="srNmList"` ·
  `srNamePickSync` 가 점검표와 **같은 엔드포인트**(`signerPicks.do`)로 명단을 받는다. ★보고서는 부서가 자유 글자(DEPT_NM)라 **부서로 좁히지 않는다**(재직자 전부).
  ★`.rowset` 에 `data-names` 를 적어 두어 **[＋ 행 추가]로 뒤에 생긴 행에도** 붙는다 · 옛 문서 구제 벌(no=0)도 열 정의를 물려받는다.
- **시드**([QPS_SEED_SRPT_NAMECOL_2026-09-09.sql](../sql/qps/seed/QPS_SEED_SRPT_NAMECOL_2026-09-09.sql)) — 켠 것은 **3유형 7열**뿐 :
  혈액 반납(BLOODRTN 벌1) 간호사·의사 4열 · 의무기록 완결도(MRCOMPL 벌1) 담당의사 2열 · 보건 업무일지(HLTDIARY 벌4) 상담 근로자.
  ⛔**안 켠 것과 그 이유가 이 시드의 핵심** : HRCARD 벌3 「성명」=**가족** · SWVOLCF 「성명」=**외부 자원봉사자** · HARASS 「성명」=당사자지만 **민감한 서식이라 명단을 펼치지 않는다** · MRCOMPL 벌2 「확인」=사람인지 불분명.
- **검증** : 매퍼 id 중복 0·태그 짝 OK · JSP 문법·태그·id 중복·미정의·Swal 0 · 새 시뮬 `sim_qpsSafeRpt_namecol.js` **14** → 전 시뮬 **484**.
  ✅**실왕복 완료**(DDL·시드 실행 뒤, 가짜병원) : `safeRptBase.do` 가 `namecols=5,6,7,8` 을 내려준다 ·
  혈액 반납 신청서 화면에서 표에 `data-names="5,6,7,8"` 이 붙고 **1~4열은 그냥 칸, 5~8열에만 목록**(박의사·김간호) ·
  **[＋ 행 추가]로 생긴 줄도 5~8열만** 붙는다 · 값을 넣어 **저장→재조회에 그대로**(「적혈구 / B-001 / 김간호 / 박의사」가 제자리).
  시험 보고서·직원 삭제(잔여 0) — 이때 **09-08 검증 때 소프트 삭제로 남아 있던 가짜병원 보고서 4건**(PTSAFE 2·BEDSORE·EDURPT)도 함께 완전 삭제했다.
- ⚠**겪은 것** : 사용자가 **시드를 DDL 보다 먼저** 돌려 `Unknown column 'NAME_COLS'`(1054)가 났다. 자료는 하나도 안 바뀌므로 DDL 을 돌린 뒤 시드를 그대로 다시 실행하면 된다.
  ⇒ 시드 파일 **맨 첫 줄에 실행 순서를 크게** 못 박았다(파일 안쪽 주석은 놓치기 쉽다).

## 9. 남은 일

1. ✅**DDL·시드 실행 + 로컬 재빌드·재기동 완료**(사용자) — 재기동 뒤 **다시 확인했다**(`build=20260909-SIGNER`) :
   점검표 HLT002 NAME 열 1개 · **환자 대장 LAB002 는 0개** · 보고서 BLOODRTN `namecols=5,6,7,8` · **인사기록카드 가족사항은 빈 값** ·
   화면도 그대로(점검표 270칸 중 이름 열 **30칸만** · 보고서 표 **5~8열만**, 머리글 = 수(책임)간호사·담당간호사·담당의사·혈액은행담당자). 시험 직원 삭제(잔여 0).
2. ⛔**운영 WAR 재빌드 + 재기동**(아직) — 어제분(§5-3)과 오늘 매퍼 2문(⑳)이 함께 나간다. 눈 검증은 §5-4(5분) + 혈액 반납 신청서에서 간호사 칸 목록.
   ⚠**§8-⑲(점검표 직원 이름 칸)은 JSP 뿐이라 재빌드가 필요 없다** — 시드는 이미 반영됐다(공통 서식 15열).
   ⚠**⑳은 재기동 전까지 화면에 안 나타난다**(매퍼가 새 칸을 읽어야 한다). 그동안은 그냥 글자 칸이라 깨지지 않는다.
3. 결재란 **단계 이름을 부서마다** 다르게 할지 — 사용자 「확인 후에」(2026-09-08). 지금은 병원 하나의 결재선을 전 부서가 함께 쓴다.
4. 요청이 오면 할 후보 :
   - 근무표 사인 매치를 **이름이 아니라 USER_ID 로** — 사인 칸에 이름만 남는 지금 구조에서는 값 자리를 바꿔야 한다(큰 일). §7-⑰⑱·§8 이 실수를 줄여 두었으므로 급하지 않다.
   - 면허등록이 바뀌었을 때 **다시 가져오기**(지금은 이미 있는 이름을 건너뛴다 — 직종·입사일 갱신은 안 한다. 일부러 그렇게 두었다 : 인사 명단이 저쪽을 따라 흔들리면 안 된다).
