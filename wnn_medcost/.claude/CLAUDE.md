# Project Memory

## ★알림·확인 메시지 표준 = ui-message.js (상시 방침 — 사용자 반복 지적 끝에 2026-08-21 확정)
- **새 알림·확인은 무조건 [asset/js/ui-message.js](src/main/webapp/asset/js/ui-message.js)** 의
  `_alertBox(msg,{icon,okText,okColor})` / `_confirmBox({msg,icon,okText,okColor,onOk})` / `_toast` —
  konet 물류와 같은 **흰 카드(라운드16)+이모지 아이콘+넓은 색 버튼**. 페이지에 `<script>` 한 줄이면 끝(CSS 자동 주입).
- ⛔**messageBox(파란 머리띠 모달)·Swal 기본값을 새 화면에 쓰지 말 것** — 기존 화면 잔존분은 그대로 두되 신규 금지.
  사용자가 「기존 스타일」이라 말해도 그 뜻은 **ui-message** 다(2026-08-21 joinReq 에서 messageBox 로 오해했다 재작업).
- **입력칸 있는 확인창**(사유 입력 등)은 ui-message 에 없다 → [joinReq.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinReq.jsp) 의
  **`_jrAsk`**(ui-message 의 .cfm-* 클래스를 빌려 같은 모양 + 입력칸·인라인 검증) 패턴을 복사.
- ⚠**함정 2** : ①화면 IIFE 에 같은 이름의 지역 _alertBox 를 만들면 전역이 가려져 옛 스타일이 나온다 — 지역은 위임만
  ②z-index 높은 오버레이(계약정보 창 12500 등)가 있으면 .cfm-backdrop(10000)이 뒤에 깔린다 — 그 화면에서 13000 으로.

## 운영 배포 — [docs/배포_가이드.md](docs/배포_가이드.md)

매일 하는 작업이라 절차를 문서로 고정했다. **"WAR 올리고 재기동하는 방법"을 묻거나 배포를 진행할 때는 이 문서를 먼저 볼 것.**
요약: ①JSP·정적파일만 고쳤으면 **파일 교체로 끝**(재빌드·재기동 불필요) ②자바·매퍼XML·설정이 섞이면 **WAR 재빌드 + 재기동**
③DB 변경은 적용 즉시 반영. 실서비스는 `webapps/wnn_medcost-1.0.0`(ROOT 아님), 재기동은 `/usr/share/tomcat9/bin/shutdown.sh`+`startup.sh`
(**`systemctl restart tomcat9` 금지** — 유령 인스턴스를 건드린다).

## ★★[2026-09-02] SUNWOO 델파이 **원본 소스 확보** — 캡처 판독을 소스로 대조·보강

- 소스 = `D:\sunwoo\sunwoo`(.pas 1,531 · .dfm 1,486). ***색인은 `sunwoo2026.txt` 의 `t_unit`/`t_menu` insert*** —
  서식 이름 grep → `TfrmXXX_Chart_NNN` 유닛. 찾아 읽는 법·함정은 [소스 대조 §6](docs/proposals/QPS_델파이소스대조_2026-09-02.md).
- **구조** : SUNWOO 는 서식 엔진이 없다 — 폼 1,373개가 종이 픽셀 복제, 부모 `ParentChartMulti` 가 K/V(`t_patient_chart_m/d`)로
  저장. **우리 엔진이 설계로는 앞선다.** 소스로 보강할 것은 「모양」이 아니라 **「동작」**(사용자 편의기능).
- ★**보류 6종 판정 3건이 바뀌었다** : **XR23≠XR08**(XR08 엔 성명 칸이 없다 — 갈음 불가, #44 물음이 「개별 화면」으로) ·
  **w42 는 SUNWOO 도 기기 마스터 없음**(p2 = 자유입력 438칸, #45 는 이식 전제가 아님) · **RN25 약품 목록 = `t_list` 병원별
  등록**(RN25_SPLIT 의 LIST+CARRY_YN 이 원본과 맞음). w15 는 셀 고정문 외에 **행 배타 체크**(SUNWOO 173종 패턴)가 더 있다.
  확인목록 #43·#44·#45 에 소스 판정을 달아 두었다.
- ✅**편의기능 5종 이식**(`qpsChk.jsp` 「편의 기능」 절, **JSP만·재빌드 불필요**) — 사용 빈도로 골랐다(전체 마킹 300종 ·
  O/X 토글·Enter 복사·일괄 서명 각 61종 · 빈 행 잠금 47종) : 더블클릭 **빈→O→X**(칸/세로줄/가로줄) · [전체 O]/[전체 지움] ·
  **Enter 오른쪽 복사 / Ctrl+Enter 아래 복사** · 사인 머리 더블클릭 = 일괄 서명(`s_usernm` 쿠키) · 이름칸 빈 행 **흐리게만**(잠금은
  사용자 요청으로 뺌 — 새 서식이 통째로 회색이라) · 토·일 제외.
  ★두 원칙은 SUNWOO 와 다르게 지켰다 — **①적힌 값은 안 덮는다(빈 칸만) ②대상은 `.ltxt` 아닌 값칸뿐**(글자 칸에 Enter 복사를
  허용하면 대장 이름이 사유 칸으로 번진다). 리스너는 `#ckGridWrap` 위임 한 번, 열 머리에 `data-col` 추가.
- ✅**2차 이식(같은 날 오후, 「그대로가 아니라 개선해서」)** — **WAR 재빌드 + DDL** 묶음. DDL [QPS_DDL_CHK_EXCL_HOL_2026-09-02.sql](docs/sql/qps/ddl/QPS_DDL_CHK_EXCL_HOL_2026-09-02.sql)(사용자가 09-02 실행함).
  ① **행 배타 체크** = 서식 옵션 `EXCL_YN`(ITEM_COL 만, 서식 관리 체크칸 「한 줄에 O 는 하나만」) — O 를 찍으면 그 줄의 다른 O 삭제(X 는 안 건드림 — O 끼리만),
    열 머리 더블클릭 = 그 열로 채우기, 전체 O·가로줄 토글·Enter 오른쪽 복사는 꺼짐. SUNWOO 175종 폼 ↔ 우리 ITEM_COL **11종**에 시드로 켬(compare.tsv 유닛 대조).
  ② **공휴일** = `TBL_QPS_HOLIDAY`(전 병원 공용) + 2025~2027 씨앗(✅웹 대조 완료 — 제헌절·현충일 대체는 규정상 제외) + `holidayList/Save/Del.do`(등록·삭제 위너넷만) +
    **메뉴 QPS ▸ 공통 ▸ 기준코드 ▸ 공휴일 관리**(`main/qpsHoliday.do` · qpsHoliday.jsp, 사용자 지시로 작성 화면 패널 대신 메뉴 화면 — 툴바엔 「공휴일 관리 →」 바로가기만, 위너넷만 보임) ·
    날짜 머리 `.hol` 빨간 점선(이름 title) · 「토·일·공휴일 제외」. **옛 서버(엔드포인트 없음)면 공휴일 없음으로 조용히 넘어간다** — JSP 가 먼저 배포돼도 안 깨짐.
  ③ **주차 날짜 자동 채움** = 주차(N) 격자 890행이 비면 월~일 범위(1주=1일부터, 5주=달 끝까지) 자동, [주차 날짜 채움]으로 다시 쓰기. JSP 만.
  검증 = javac(WAR lib 클래스패스, exit 0) · 가짜 DOM **15경우 49검사** · 함수검사(정의 107/호출 45) · 문법 · 구조. ⛔**Eclipse 빌드+재기동 뒤** 눈 검증(캡처).
- ⛔**일부러 안 옮긴 것** : **온습도 랜덤 채움**(55종 — 측정값을 지어내는 기능, 인증 자료 위해) · 근무표 서명(B4 DUTY 뒤). [소스 대조 §5](docs/proposals/QPS_델파이소스대조_2026-09-02.md).
- 검증 = jsdom 가짜 DOM **12경우**(배포 JSP 블록 그대로) · 함수검사 정의 94/호출 32 · 인라인 JS 문법 · 구조검사(Node 대체).
  ⚠**이 PC 의 `python` 은 Windows 스토어 스텁** — `JSP구조검사.py` 가 「Python」만 찍고 끝난다. 같은 검사를 Node 로 대신했다.
  ⚠**DFM 한글은 `#51032#47308` 이스케이프** — grep 전에 디코드. ⚠PS7 `Select-String` 은 `$matches` 가 비어 있다 — `.Matches[0].Groups`.
  ✅브라우저 검증 완료(사용자 크롬 캡처 — 전체 O · 토·일 제외가 9월 5·6/12·13/19·20/26·27 만 비움). 이름 빈 줄 **잠금은 사용자 요청으로 뺌**(흐리게만).
- ★**시드 385종 → dfm 자동 대조 끝**(같은 날) : 서식 173 · 항목 1,759 를 델파이 원본 글자와 맞춤 — **158종 전부 확인, 14종 55항목만 사람이 볼 것**(일치 97%).
  보고서 [QPS_시드_dfm_대조_2026-09-02.md](docs/proposals/QPS_시드_dfm_대조_2026-09-02.md) §5 가 판독(A 캡처가 다른 변형 2종 · B 병원 자료를 항목으로 박은 의심 2종 · C/D 표기 차이).
  ★**A·B 4종 판정 끝(같은 날)** : NUR003 = WARD_Chart_107 과 글자 그대로(오타 2) · **RNL019 는 지어 넣은 15항목이었다** → 원본 KIDNEY_Chart_003 14항목으로 교체 ·
  ADM003 = Employee_Chart_020 과 전부 일치(손댈 것 없음) · FAC032 는 원본이 17줄+빈 줄이라 병원이 적은 5줄을 공통에서 뺌.
  시드 3파일 고침 + 운영 보정 [QPS_SEED_FIX_DFM_2026-09-02.sql](docs/sql/qps/seed/QPS_SEED_FIX_DFM_2026-09-02.sql)(⛔**운영 실행 필요**, 작성분 확인 SELECT 먼저).
  ⚠교훈 : **이름 매칭이 못 찾는 폼이 있다**(창 제목 ≠ 화면 제목) — 「하나도 안 맞음」이 나오면 dfm 입력칸 기본글자 전체를 grep 해 본다(RNL019 처럼 지어 넣은 것과 갈린다).
  ⇒ 도구에 **전수 폴백**을 넣었다(하나라도 안 맞으면 dfm 1,367개 전부 대조, SCORE=「전수」). D 묶음 표기 차이 9건도 원본대로 보정(같은 SQL §4).
  **최종 : 172종 중 169종 전부 확인 · 남은 것 = 묶음 이름 표현 차이(FAC028·PHA009)와 NUR066 「D-set·Can」(원본에 없음, 병원 확인)뿐.**
  운영 보정 SQL 은 사용자가 09-02 실행했다. 공휴일 씨앗도 웹 대조 완료(제헌절·현충일 대체는 규정상 제외).
- ✅**보류 서식 w15 인사고과 평가표 등록(같은 날 밤)** = 마지막 조각 **셀 고정문 `TBL_QPS_CHK_ITEM.CELL_TXTS`**(열마다 쉼표, ITEM_COL 전용, 비면 종전과 같음) +
  오전의 `EXCL_YN`. 50칸 문구는 Employee_Chart_011.dfm 에서 직접 뽑아 `ADM019` 로 시드. [DDL+시드](docs/sql/qps/ddl/QPS_DDL_CHK_CELLTXT_2026-09-02.sql) ⛔운영 실행 필요.
  손댄 곳 = 매퍼 2쿼리 · 서식 관리 「셀 고정문」 칸(ITEM_COL 만 보임) · 작성 화면 격자(글 + 아래 입력칸, 글 더블클릭도 토글). 자바 변경 없음(항목 맵 그대로 흐름). 재기동은 필요.
  ⇒ 보류 6종 중 코드로 풀 수 있는 것은 끝. 남은 XR23·RN08~10·MR13 은 화면 개발 사안, RN25·w42 는 병원 답.
- ✅**QPS 메뉴 재배열**(같은 날 밤, 사용자 지시 「업무별로 직관적으로 — 관리(설정) · 담당자별 업무(부서별 업무 순서)」) — sidebar.jsp `#qps-sub` 를 통째로 다시 짰다 :
  계획·위원회 → 지표·분석 → 환자안전 활동 → QI → 환자만족도·불만고충 → 보고서·서식 → 점검표 작성 → **담당자별 업무**(부서 15 × 점검표→서식→위원회→기록) → **관리(설정)** → 자료·안내.
  ★옛 링크 69개 전부 유지(자동 대조), 같은 화면이 두 자리에 걸릴 땐 `?d=부서` 를 붙여 강조 중복을 막음. 부서 위원회는 부서 묶음으로, 감염 그룹은 「담당자별 업무 ▸ 감염관리」로.
  ★`qpsDeptMenu` 스크립트는 이제 줄을 다시 그리지 않고 **서식 없는 부서의 점검표 줄만** 감춘다. 위너넷 게이트 id(qpsChkUseMenu…)는 그대로.
- ✅**공통코드(QPS) 관리 화면**(같은 날 밤, 사용자 지시 「스크립트로 생성한 공통코드를 관리하게」) — 메뉴 QPS ▸ 관리(설정) ▸ 기준코드 ▸ **공통코드**
  (`main/qpsCode.do` · qpsCode.jsp). 대상 = TBL_CODE_DTL 'Q' 묶음 `QPS_%` 31개. 왼쪽 묶음(이름·쓰는 수/전체) · 오른쪽 세부코드(이름·차례·사용).
  ★**코드값은 못 바꾼다**(작성분의 키) · 지움 = USE_YN 'N'(옛 문서 보존) · upsert 는 시드와 같은 JOB_SEQ 1 방식(기존 공통코드 화면의 JOB_SEQ 증가 방식과 다름 — 섞지 말 것).
  엔드포인트 `qcodeList/qcodeSave/qcodeGrpSave.do`(고치기는 위너넷만). ⚠`/qps/codeList.do` 는 **이미 있는** 화면용 코드 조회라 처음에 같은 이름을 붙였다가
  javac 는 통과하고 Spring 기동 때 죽을 뻔했다 — **새 매핑은 grep 으로 중복부터 본다.** 묶음 이름이 없던 21개는 [QPS_DDL_CODE_MST_NAMES_2026-09-02.sql](docs/sql/qps/ddl/QPS_DDL_CODE_MST_NAMES_2026-09-02.sql)로 채움(⛔운영 실행 필요).
  기존 「공통코드」(base/commcd) 화면은 MST 가 있는 묶음만·병원엔 숨김이라 그대로 두고, QPS 것은 이 화면이 맡는다.
- ✅**QPS 메뉴 검색**(같은 날 밤, 사용자 요청) — 사이드바 `#qps-sub` 맨 위 검색 칸(`#qpsMenuQ`). 글자가 든 잎만 남기고 그룹 자동 펼침, 지우면 펼침까지 원래대로,
  Enter = 첫 항목, Esc = 지움. ★**원래 감춰 둔 줄(위너넷 전용 display:none · 부서별 그룹 hidden)은 검색해도 안 꺼낸다**(첫 검색 때 상태 기록). 문서 위임(사이드바 두 벌 화면 대비).
  검증 = 가짜 DOM 11검사. JSP 만.
- ⚠**작성 화면 부서 셀렉트는 「사용자별 담당 부서」로 걸러진다** — 등록이 있으면 그 부서만(+공통). 「원무·총무가 없다」는 그 계정에 담당 부서가 잡힌 것 →
  QPS ▸ 점검표 ▸ 사용자별 담당 부서(위너넷)에서 풀거나 더한다. 병원이 「우리 병원 사용 서식」을 정해 뒀으면 새 서식(ADM019 등)은 거기서 켜야 보인다.
  ★★**새 서식을 시드할 때는 `TBL_QPS_CHK_USE` 기본 세트('*')에도 넣는 INSERT 를 같이 쓴다**(CELLTXT DDL 끝 참조) — 안 넣으면 부서별 양식·서식 관리엔 보이는데
  **작성 화면에만 안 나온다**(09-02 밤 ADM019 로 겪음). 병원 전용 세트가 있는 병원은 병원이 [우리 병원 사용 서식]에서 켠다.
  실측(09-02) : 세트는 `'*'`(314종)와 `99999998`(위너넷 마스터 병원, 311종) 둘 — 둘 다 ADM001~018 은 있고 새 서식은 없다. 새 서식은 두 곳에 다 넣어야 마스터 계정에서 보인다.
  ★DB 직접 확인 : 이 PC 에 mysql 클라이언트가 없다 → scratch 에 `mysql2` 를 깔아 **SELECT 만** 한다(local 프로필 = 운영 WNN DB, context-datasource.xml). 쓰기는 사용자가 SQL 로.
  ✅「F5 눌러야 하네요」 → 작성 화면이 탭 복귀/창 포커스 때 **서식 목록만** 조용히 다시 받는다(`ckFormsRefresh`, 3초 묶음). ⚠`ckBase` 를 그대로 부르면 새 문서의 격자를 비우므로 따로 만들었다.
  ✅「옮기고 상단 저장 버튼을 실행해야 하네요」 → 우리 병원 사용 서식의 상태 띠(「아직 저장 전입니다」)에 **[지금 저장]** 단추를 같이 둔다(qpsChkUse.jsp paintState).
- **safeRpt 79 유형도 dfm 대조**(같은 날 저녁, `DFM_SET=rpt`) — [QPS_safeRpt_dfm_대조_2026-09-02.md](docs/proposals/QPS_safeRpt_dfm_대조_2026-09-02.md).
  78/79 가 원본 폼과 이름으로 이어지고 41종은 라벨까지 전부 확인. 나머지 「시드에만」은 날짜 칸(SUNWOO 는 피커라 글자 없음)·정형문구·**고정 23칸 엔진에 맞춘 의역**이라 고칠 것 없음.
  ★확인 후보 하나 — **MDDISP 의료기기 폐기 확인서**: 원본(Employee_Chart_033)은 식약처 서식(업소명·허가번호·품목명·폐기량…), 우리 것은 병원 내부용 단순판. 병원확인 목록 #46.
  도구 = [docs/tools/dfm대조/](docs/tools/dfm대조/README.md) 5단계(색인→시드 추출→이름 매칭→항목 대조→보고서). 함정 둘 — **dfm 문자열 목록의 닫는 `)` 는 마지막 줄 끝에 붙는다**(단독 `)` 만 찾으면 객체를 삼킴) ·
  **이름 매칭만으로 유닛을 고르면 틀린다**(변형 폼·재활용 유닛) → 후보 10개를 항목 대조해 일치 최대 dfm 채택(25종 바로잡힘). 시간칸(07:00~)은 런타임 생성이라 대조 밖.
- ⚠**로컬 8080 로그인 페이지는 깨져 있다** — `LoginWinCT.jsp` 가 `/js/winct/*`·`/css/winct/*`(wnn_consult 자원)를 절대경로로 불러 `LoadingUI` 미정의로 로그인 단추가 죽는다.
  로컬 로그인은 **wnn_consult(9090) 로그인 페이지**에서 한다 — 쿠키(`s_hospid`…)가 `localhost` 공유라 8080 도 열린다. medcost 안에 같은 파일이 `js/winmc/`·`css/winmc/` 로 있으니 고치려면 경로 7줄(운영에도 나감).
- ⚠**앱 내장 브라우저는 별도 세션·기본 「숨김」** — 사용자가 「로그인했음」이라 해도 본인 크롬일 수 있다. 서버 접속 로그의 `loginChk` 로 어디서 로그인했는지 가른다(이번에 4번 헛돌았다). Claude in Chrome 확장은 이 PC 계정에 안 붙는다.
  ⛔남은 것 = 브라우저 눈 검증은 **사용자 크롬 캡처**로 받는다(빠르고 확실).

## 진행 중 — QPS(질향상·환자안전) 이식 · [docs/proposals/QPS_인수인계_2026-08-14.md](docs/proposals/QPS_인수인계_2026-08-14.md)

기존 프로그램(SUNWOO)의 QPS 업무를 WinCheck+ 로 옮기는 일. **가장 큰 진행 과제다.**
***"QPS 이어서" 라고만 해도 위 인수인계 문서를 먼저 읽고 착수할 것*** — 배경 재설명 불필요.
***★[2026-08-15 갱신] 다음 재개 = [QPS_남은할일_2026-08-15.md](docs/proposals/QPS_남은할일_2026-08-15.md) 부터*** —
자료 대기(C)는 전부 소진, 남은 것은 A(운영 WAR 사흘치 재배포·눈 검증·병원확인 답변)와 B(회의 사안)뿐.
(~~08-14 마감 지시 「사진첨부 구현」~~ ✅완료 — §5-4 완료 블록.)
(앞 문서 [08-13](docs/proposals/이력/QPS_인수인계_2026-08-13.md) 은 **SUNWOO 조작 절차·예약 번호 표·로컬 반영 절차**,
[08-11](docs/proposals/이력/QPS_인수인계_2026-08-11.md) 은 **저장 엔드포인트 함정·로컬 DB 도구**용으로 계속 쓴다.)
- **1차 완료** : QPS · QI · 감염관리 (+ 만족도 · 불만고충 · 보고서 폴더) · 다듬기 3건.
- **2차 진행** : 간호/병동 · 약국 · 영양 · 시설 → ***전부 「점검표 엔진」 하나로 받는다.***
  서식이 코드가 아니라 **데이터**다(`TBL_QPS_CHK_*`). 화면은 둘 — `qpsChkForm`(서식 관리, **위너넷 전용**) · `qpsChk`(작성).
- **[2026-08-13] 간호/병동 완주** — 점검표 **누계 177종** 적재 완료. 남은 것은 **감염·진단검사 등 나머지 모듈 캡처**.
- **[2026-08-14] `safeRpt` 확장** — 반복행 표·서명란·정형문구를 더했다(사고·고장 보고 계열을 유형 추가로 흡수하려는 것).
  설계 [QPS_safeRpt_반복행서명_설계_2026-08-14.md](docs/proposals/설계/QPS_safeRpt_반복행서명_설계_2026-08-14.md).
  **DDL 적용 · 로컬배포 완료**(`build 20260814-SRPROW`). ⚠**화면 검증만 남았다** — 08-14 인수인계 §5·§6ⓐ.
  로컬 환경 실측(워크스페이스 `D:\egv` · 로그인 `localhost:8080/user/main.do`)은 08-14 §5-1.
- **[2026-08-14 오후] 진단검사(임상병리) 완주 + 「주기 복합」 조각 신설** — 판독 39종·재캡처 12건,
  엔진 조각 구현, 시드 **28종 운영 DB 적재**(`LAB001~LAB028`).
  [판독](docs/proposals/판독/QPS_서식판독_진단검사_2026-08-14.md) · [주기 복합 설계](docs/proposals/설계/QPS_점검표엔진_주기복합_설계_2026-08-14.md) ·
  [시드](docs/sql/qps/seed/QPS_CHK_SEED_LAB_2026-08-14.sql).
  - **주기 복합** = 매일(1~31일)/매주(1~5주)/매월(한 칸) 격자가 한 장에 세로 3벌. 근거 10종.
    ***`TBL_QPS_CHK_FORM.GRP_PRD` 한 칸***(`'매일>D,매주>N,매월>1'`) — 묶음 이름은 이미 항목의 `GRP_NM` 에 있다.
    비면 기존 서식 177종 그대로. **값 자리 무변경**(행번호가 곧 묶음을 가른다).
  - ⚠**`ROW_BLKS` 로 하려다 바꿨다** — 그건 ***LIST 전용***이고 행번호 `블록×1000` 규칙이 딸려 있다.
    ***설계를 코드로 옮기기 전에 그 칸이 실제로 무엇인지 확인할 것.***
  - ⚠**자바+매퍼 변경 → WAR 재빌드 + 재기동 필수.** 반영 확인 `build`=**`20260814-GRPPRD`**.
  - ✅**검증** — 배포된 JSP 의 인라인 JS 를 **Node 로 태우고 운영 DB 의 실제 서식을 물려** 칸 수·저장값 복원 확인.
    가짜병원 `99999998` 로 **한글 왕복**까지 보고 테스트 행은 지웠다. 서식 관리 **미리보기**도 같이 맞췄다.
    ⛔남은 것은 **브라우저로 눈으로 보는 것** — 특히 **인쇄**(표가 여러 벌이라 처음 겪는 모양).
    ★로그인(비밀번호 입력)은 내가 하지 않으므로 **사용자가 열어 줘야 한다.**
  - ⚠***서식 이름으로 갈래를 가를 수 없다*** — 「일상/일일」로 갈리는 줄 알았으나 반증 2건이 나왔다.
  - ⚠**시드는 39종 중 28종만**이다. 뺀 11종(대조 대기 4·조각 대기 3·불필요 4)은 시드 머리말에 이유가 있다.
  - **[2026-08-14 저녁] 대조 5건 완료**(판독 §4-3) — 혈액 대장 2종은 간호와 **같은 종이**라
    `LAB008` 을 뺐고(보정 [DELTA](docs/sql/qps/seed/QPS_CHK_SEED_LAB_DELTA_2026-08-14.sql), DB 반영 완료),
    낙상은 **별건**이라 `LAB029` 로 등록했다(현재 LAB 28종). 화면 검증은 인쇄까지 사용자 확인 완료.
    ⚠**부서 소속 어긋남 발견** — `NUR018`·`NUR029` 는 임상병리 서식인데 간호 소속. 부서 공유 서식을
    COMMON 으로 합칠지는 **위너넷 결정 사안** → [병원확인목록](docs/proposals/QPS_병원확인목록_2026-08-13.md) #35~37.
    ***대조는 이름이 아니라 항목 문자열로 한다*** — 같은 이름·다른 판(냉장고 온도표)과
    다른 이름·같은 판(불출대장↔입출고대장)이 둘 다 실재한다.
- **[2026-08-14 밤] 보건관리자·원무총무 모듈 캡처 완료** — 보건관리자 33종(+여러 쪽 32장, **판독까지 끝** :
  [판독](docs/proposals/판독/QPS_서식판독_보건관리자_2026-08-14.md) — safeRpt 쪽 19종이 주류) ·
  원무총무 59종(+여러 쪽 29장, **판독까지 끝** : [판독](docs/proposals/판독/QPS_서식판독_원무총무_2026-08-14.md)).
  캡처 `D:\워너넷자료\caps\HB_·WM_2026-08-14\`. ⚠원무총무 **중복 3종 등록 금지**(낙상=LAB029·AED=NUR014·UPS=FAC004) ·
  **제외 4종**(내규·조직도=자료실, 사원등록·의료기기목록=마스터 화면) · 재캡처 2장(`w20_p2·p3`·`w49_p2`).
  ★**「여러 쪽 판별」을 캡처 절차에 넣었다**(사용자 지적) — 전체화면 캡처의 페이지 탭 띠를 콘택트시트로
  이어 붙여 한 번에 본다. 진단검사 39종은 재점검 결과 전부 1쪽(빠진 페이지 없음).
- ★★**[2026-08-14 심야] SUNWOO 모듈 18개 전체 캡처·판독 완료** — 마지막 11개(관리자~사회복지)까지.
  [기타모듈 판독](docs/proposals/판독/QPS_서식판독_기타모듈_2026-08-14.md)(요약표·타부서 대조 목록·캡처 절차서 통짜).
  기능 화면 4(관리자⚠위너넷 중복·규정집·DUTY·인증=범위 결정 사안) · 보고서=공용 11종 일치 ·
  서식 신규 : 인공신장 42·의무기록 30·방사선사 25·물리재활 6·사회복지 45항목·진료 3.
  캡처 = `D:\워너넷자료\caps\` **14개 폴더** — ✅**다른 PC 로 복사 완료**(2026-08-14, 인수인계 §5-3.
  다른 PC 에서는 복사해 둔 위치로 경로만 바꿔 읽으면 된다).
  「직원 교육 결과 보고서」 공용판 **9곳째** 확정. ⚠사용자 요청 2건 기록됨(인수인계 §5-3) —
  ~~QPS JSP 그룹핑~~(✅끝+**28화면 전수 렌더 검증** — `jsp/main/qpsmgr/` 28개 + 컨트롤러 29곳,
  `build=20260814-QPSDIR`. ★검증법: `qpsScreen` 가드는 **s_hospid 일반 쿠키**라 가짜병원 쿠키로
  `GET /main/qpsXxx.do` 하면 로그인 없이 렌더 전 체인이 돈다. 운영 반영은 **WAR 재빌드+재기동**) ·
  **관리자 모듈 위너넷 중복 확인**(대기).
- 확장 2건 설계 완료 : [사진첨부·연간격자 설계](docs/proposals/설계/QPS_사진첨부_연간격자_설계_2026-08-14.md) —
  ★연간 격자는 **새 조각 불필요**(항목×월 = 기존 `ITEM_MONTH` · 혈당측정기만 서버 한 줄). 재캡처 2장도 완료.
- ✅**[2026-08-14 심야·집 PC] 사진첨부 구현 완료** — DDL(`PHOTO_YN`+`TBL_QPS_SAFERPT_FILE`) **운영 적용**,
  매퍼 3쿼리·자바 4개(`safeRptPhotoUpload/Delete.do`)·qpsSafeRpt.jsp(2×2 칸·개인정보 안내·인쇄).
  `build=`**`20260814-SRPHOTO`**. ⚠`<img>` 직결 불가(`download.do` attachment 강제) → **fetch→blob→objectURL**.
  이어서 **라벨 오버라이드 `LBL_JSON`**(FORM 한 칸, 값 `'-'`=칸 숨김 — 교육·상담 계열이 사고 라벨을 제 이름으로)
  DDL 운영 적용 + 화면·인쇄 적용, **`EDURPT` 공용 유형 시드 운영 적재**(9곳 확인 공용판, PHOTO_YN='Y').
  ⛔남은 것 : Eclipse 빌드·재기동 후 EDURPT 로 화면 왕복 검증(저장→사진 업로드→복원→인쇄 2×2).
- ✅**[이어서] 연간(월12) 격자 완료** — `chkFormSave` 한 줄(날짜 격자 `PRD_KIND='M'`→`PRD_GB='Y'` 연 문서, 자바) +
  시드 6종 **운영 적재**(`HLT001`·`ADM001~004`·`LAB030`, 부서 코드 HEALTH·ADMIN 신설).
  실제 qpsChk 격자 함수에 적재값 물려 검증(혈당측정기 = 연 문서·12월×용액A/B 24칸).
  w23/w25 같은 주제 2벌 → 병원확인목록 **#38·#39**. 상세 = 인수인계 §5-4 완료 블록 2.
- ✅**[이어서] 보건관리자(HB) 시드 완주** — LIST 10종(`HLT002~011`, h20 부서블록·h22 구간띠 = `ROW_BLKS`) +
  safeRpt 유형 4종(`CONSULT`(재검자 6형제+상담일지 4종 = **한 판**)·`CONSULTB`·`CONSULTF`·`HLTRPT`,
  LBL_JSON 라벨·상담일지 PHOTO_YN='Y') **운영 적재**. ⛔보류 6종(보고서 5종=집계표 화면검증 대기 ·
  h17 안내문 · h19 업무일지 · h29 3단머리)은 시드 머리말. 상세 = 인수인계 §5-4 완료 블록 3.
- ✅**[이어서] 원무총무(WM) 시드 완주** — 회의록 위원회 코드 3(`QPS_FORM_GB` W·C·P — ★**엔진 변경 0**,
  회의록 표에 정기/임시·간사·의안·의결·참석서명이 이미 있다) + 점검표 8종(`ADM005~012`) +
  safeRpt 유형 17종 + 체크묶음 6유형 **운영 적재**(safeRpt 누계 **31종**).
  ⛔등록 금지 3(w36=LAB029·w53=NUR014·w59=FAC004) · 제외 4 · 보류 8(「항목×날짜아닌 고정열」 셀타입 4갈래).
- ✅★★**[검증 완료] 로컬 8080 재기동 후 사진첨부 전 경로 실증** (`build=20260814-SRPHOTO`) —
  화면 렌더·EDURPT 라벨·**한글 왕복**·**사진 업로드(한글 파일명 보존)**·**같은 칸 교체(UPSERT)**·
  다운로드 바이트 일치·그림 아닌 파일 거부·시드 서식 9종 조회 **전부 통과**. 가짜병원 테스트 자료는 삭제.
  ⛔남은 것 = **눈으로 보기**(사진 2×2 배치·인쇄물·연간 격자 12칸). 상세 = 인수인계 §5-4 완료 블록 4·검증표.
- ✅**[이어서] 인공신장(RN) 시드** — `RNL001~025` 운영 적재(부서 `RENAL` 신설). EQUIP_DAY 5·ITEM_DAY 8
  (주기복합 4)·DAY_ITEM 6(PRD_SUB 시간대)·ITEM_MONTH 5·LIST 1. ★**등록 금지 7종을 「항목 문자열」 DB 대조로
  확정**(RN38=LINEN·40=NUR014·41=NUR025·26=PHA025~028·27=PHA024·33=NUR011·34=NUR012).
  ⚠**RN24 는 이름이 비슷해도 NUR008 과 항목이 달라 별건** — ***대조는 이름이 아니라 항목으로.***
  보류 10종(검사성적서 3·RN16 장비체크=WM w43~45 와 같은 계열·RN31 행별 유효기간 등)은 시드 머리말.
- ✅**[이어서] 의무기록(MR)·방사선사(XR) 시드** — 부서 `MEDREC`·`RADIO` 신설.
  MR = 점검표 11(`MRC001~011`) + safeRpt 15(**법정 양식 3종** 포함 — 의료법 별지 제9호의3·개인정보보호법 2) ·
  XR = 점검표 21(`RAD001~021`, **TLD 관리대장이 `ITEM_COL` 첫 실사용**). safeRpt 누계 **46종**.
  ★**같은 판이어도 모델명이 서식에 박혀 있으면 따로 넣는다**(XR04↔12 CR/DR · XR14↔16) — 묶으면 장비를 못 가른다.
  ⛔제외/보류 : MR29 껍데기·MR30·XR25=`EDURPT` 중복 · MR13 기능화면 · MR14 통계표 2벌 ·
  XR07(점검표엔 사진칸 없음) · XR18 엑셀그리드 · **XR23 문서 키가 「사람 1명」**(키 설계에 없다).
- ★★★**[이정표 2026-08-14 심야] SUNWOO 18개 모듈 서식 시드 전량 완료** — 마지막 3모듈
  (물리재활 `REH001~005`(3벌 중복 5계열→한 벌씩) · 진료 2(DR01=`DUTYDOC` 중복 제외) ·
  사회복지 `SOC001~009`+safeRpt 15) 적재. **누계 = 점검표 300종 · safeRpt 유형 61종 · 부서 13개.**
  ⚠사회복지 **설문 7종은 만족도 조사 엔진(TBL_QPS_SURVEY) 시드로** 가야 한다(점검표 아님 — 보류).
  ⚠타 병원 실명(온사랑·예성요양) 잔존 문구는 시드에서 뺐다. 오연결 2건은 **문서 제목 기준** 등록.
  남은 백로그 6건(설문 시드·「항목×고정열」 조각(근거 6종)·보고서 5종 검증·반복행 여러 벌·기능화면 범위·
  눈 검증+WAR 재배포)은 인수인계 머리 이정표 절.
- ✅**[이어서] 메뉴 반영** — 점검표·safeRpt 는 메뉴 작업 0(셀렉트가 DB). **회의록 위원회만 3곳 하드코딩**
  (qpsMinutes 셀렉트·sidebar 메뉴·`putFormGb` 허용 목록 — 빠지면 조용히 Q). 운영위 W·중독연구소 C·
  인사위 **H** 반영. ★**'P' 충돌 실사고 잡음** — 약사위원회가 같은 화면에서 P 를 먼저 쓴다 → H 로 정정.
  ***새 위원회 = 화면·사이드바·허용 목록 3곳을 함께 본다***(코드 테이블은 정본이 아니다).
- ✅**[이어서] 보류분 재판정 4종 등록** — 새 조각 없이 기존 축으로 담겼다([RETRY 시드](docs/sql/qps/seed/QPS_SEED_RETRY_2026-08-14.sql)) :
  w12 인력연간계획서=`ITEM_COL`(직종 25행×수치 6열) · w20/21 직원교육현황표=**`LIST`**(행=직원이 곧
  LIST 의 정의) · w29 위탁업체 평가표=`ITEM_COL`(업체 12열) · RN16=`LIST`. **「항목×고정열」 새 조각은
  불필요했다** — 남는 보류 2종(w15 셀 고정문·MR13 기능화면)만 진짜 조각 사안.
  ★**설문 7종 판정 확정** — `TBL_QPS_SRV_DEF` 가 **설문지 축 없는 고정 20문항**(DB 실측)이라 시드 불가.
  설문지 축 확장은 병원확인목록 **#40**(쓰는지부터 확인).
- ✅**[이어서] 보건관리자 보고서 5종 등록**([HBRPT 시드](docs/sql/qps/seed/QPS_SEED_HBRPT_2026-08-14.sql)) —
  보류 사유를 다시 세니 **표 1개짜리가 3종**(h03·09·11 = SUB_COLS 그대로), h01·h02 는 열 접두어 합침.
  집계표가 내용 문단 **앞**에 오는 배치 차이는 병원확인 **#41**. **safeRpt 유형 누계 66종.**
  ⇒ **에이전트가 할 수 있는 백로그는 소진** — 남은 것 : w15·MR13 조각(회의) · #40·#41 답변 ·
  눈 검증 · 운영 WAR 재배포(사용자 몫).
- ✅**[2026-08-15] 담당자 위주 메뉴 개편**(사용자 요청 「직관적으로·담당자 위주」) + 남은 일 총정리
  [QPS_남은할일_2026-08-15.md](docs/proposals/QPS_남은할일_2026-08-15.md) :
  사이드바 **「부서별 점검표」 15부서**(`qpsChk.do?dept=코드` 프리셀렉트) · 「보고서·서식」 **계열 6링크**
  (`qpsSafeRpt.do?gb=유형`) · 유형 셀렉트 70종 **optgroup 계열 묶음**.
  ★**SORT 대역 = 계열** 이 시드 규약이 됐다(1~19 사고·20대 보건·31~ 인사총무·51~ 의무기록·
  71~ 영양·73~ 사회복지·91~ 검진) — **새 safeRpt 유형은 대역 안 SORT 로 등록**하면 화면 무수정.
  selectQpsCodes 에 sort 추가(매퍼) — sort 없는 옛 서버면 평평한 셀렉트로 폴백(무해).
  ✅**로컬 실서버 검증 완료**(재기동된 8080) — 프리셀렉트·사이드바 렌더·부서 걸러짐(공용 COMMON 포함이
  정상) 전부 통과. ★SUNWOO 가 집 PC 에도 있다(`D:\위너넷\HIB70`).
- ✅**[2026-08-15 낮] 약국 실물 캡처로 의약품 계열 5유형 적재** — 사용자가 SUNWOO 약국 모듈을 열어 줌.
  [약국보충 판독](docs/proposals/판독/QPS_서식판독_약국보충_2026-08-15.md) · [DRUG 시드](docs/sql/qps/seed/QPS_SEED_DRUG_2026-08-15.sql).
  확인#1 = **다른 판**(→ DRUGRTN 간호판 + DRUGRTNP 약국판) · RN29 = 약국판과 **같은 판**(→ DRUGBRK 공용) ·
  RN28 ↔ 약국 평가서 = **별건**(→ DRUGADR·DRUGADRE) · RN25 ↔ PHA020 = 다른 판(대조만 해소).
  **SORT 대역 10~19 「의약품 · 혈액」 신설**(BLOODRTN 26→11 이동, BANDS 에 밴드 추가).
  ★**작은 글씨 재판독 = SUNWOO [폰트 확대] 후 재캡처**(사용자 제보 — 크롭 확대보다 정확, 끝나면 ▼ 원복).
  이어서 사용자가 **간호/병동 모듈로 전환**해 줘 [274]도 캡처·`DRUGREQ` 등록 — 의약품 계열 7종 완성,
  safeRpt **76종**. ★모듈 전환 = SmartChart **프로세스가 새로 뜬다**(PID 바뀜·제목 빈 채 수십 초) —
  프로세스 재탐색 후 제목 찰 때까지 대기. 잔여 : 원외 법정 4쪽(별지 77호의2, 캡처 4장 확보·등록 여부
  병원확인) · ⚠창 최소화 등 SUNWOO 밖 조작은 사용자가 거부 — 창 안 클릭만 쓴다.
  이어 **원무총무 모듈**로 재캡처 2장도 완료 — w20 2·3쪽(24~69)·w49 2쪽(기기 11~20) **연속 확정**,
  시드 영향은 **ADM012 EQUIP_CNT 10→20** 보정뿐(시드·운영 DB). ★**캡처 실패 원인 확정** :
  문서 로딩 중 첫 페이지 탭 클릭은 조용히 무시된다 — **렌더 완료 후 다시 누르고, 탭 띠를 작게 캡처해
  눌린 탭을 확인한 뒤** 본 캡처를 뜬다. 이어 **보건관리자 모듈**에서 h19 [폰트 확대] 재캡처로
  HLTDIARY 저해상 3곳도 확정 — **G5 12번만 정정**(「중량물 들어올리는작업 특별조치」, 시드·운영 DB),
  13번·G6 3번은 그대로. ⇒ ★★**자료 대기(C) 전부 소진** — 남은 것 = A(사용자 몫)·B(회의 사안)뿐.
  ⚠qpsChk 부서 프리셀렉트는 **FORM.formid 와 대조**해 3번째 chkBase 를 태운다 — 셀렉트는 목록이
  다시 그려질 때 첫 서식을 저절로 보여줘 값 비교로는 어긋남이 안 잡힌다.
  자바+매퍼 → **WAR 재빌드 필수**, `build=20260815-DEPTMENU`. Node 로 4시나리오+묶음 70종 검증.
- ✅**[2026-08-15 저녁] 보류분 재판정 2차 — 4종 추가**([RETRY2 시드](docs/sql/qps/seed/QPS_SEED_RETRY2_2026-08-15.sql),
  점검표 **309종**) : w43·44·45 기기 예방점검표(`ADM016~018`)=**ITEM_COL**(`COL_NMS` 의 `묶음>열` 2단 머리로
  w45 6열 수용·`HEAD_NMS` 8칸이 기기정보 수용) · RN31 응급키트(`RNL027`)=**ITEM_DAY + DESC_NM「수량」
  + PRE_COLS「유효기간」**. ★두 보류 사유가 **코드와 어긋나 있었다** — ①`INPUT_GB` 는 정렬만 바꾼다
  (셀 타입은 등록 제약이 아니다) ②`sideOk()` 는 ITEM_DAY 를 **포함**하고 앞/뒤 열은 **행마다** 저장된다.
  ⇒ ***보류 목록은 주기적으로 다시 볼 것*** — 재판정 두 번으로 8종이 조각 없이 들어갔다.
  검증 = 배포 JSP 함수를 Node 로 태우고 운영 DB 값 물림(화면 왕복은 다음 재기동 때).
- ✅**[이어서] 남은 보류 6종 최종 판정** — [QPS_보류서식_최종판정_2026-08-15.md](docs/proposals/QPS_보류서식_최종판정_2026-08-15.md).
  걸림돌을 **판독 인상이 아니라 코드로** 확인해 사유 3건을 또 정정했다 :
  **RN08~10 = 성적서라서가 아니라 인적 머리 21칸**(`HEAD_MAX`=8, 화면 주석이 「법정·의뢰 서식은 개별 화면」이라
  이미 못 박아 둠) · **w15·RN10 = 셀 타입이 아니라 「셀마다 다른 고정 글자」**(조각 설계 §3 = `CELL_TXTS`
  칸 하나 + `RO_COLS`, 새 표·새 축 없음. 다만 실제로 푸는 건 w15 하나라 **만들 값어치가 결정 사안**) ·
  **XR23 = 키가 아니라 종이 모양 + XR08 데이터 중복**. RN25 는 **쪼개면 바로 등록**된다.
  ⇒ **병원 한 줄 답변이면 갈리는 3건을 확인목록 #43·#44·#45 로 신설**(총 45건).
  ***서식 이식은 이 6종을 빼고 완료로 본다.***
- ✅**[이어서] 데이터 점검 스크립트 신설**([QPS_AUDIT.sql](docs/sql/qps/QPS_AUDIT.sql) — 조회 전용,
  시드 넣은 뒤 돌린다) — ⛔**결함 1건 발견·수정** : ***부서 코드 15개 중 감염관리·진료는 점검표가 0종***인데
  어제 만든 「부서별 점검표」 메뉴에 링크를 걸어 둬 **누르면 빈 화면**이었다 ⇒ 두 링크 제거(사유 주석).
  ★***부서별 메뉴는 「서식이 있는 부서만」 건다*** — 서식이 생기면 스크립트 ①이 알려 준다.
  나머지(항목 0개 8종=EQUIP_DAY 범례 · ITEM_COL 열 없음 1종=`COL_SRC='D'`)는 **정상**,
  미등록 부서·사진칸 초과·SORT 중복/대역밖·JSON 깨짐·빈 체크묶음·코드 없는 FORM **전부 0건**.
- ✅**[이어서] 배포·눈검증 안내 한 장**([QPS_배포_눈검증_2026-08-15.md](docs/proposals/QPS_배포_눈검증_2026-08-15.md)) —
  A1(배포)이 A2(눈 검증)를 막는 임계 경로라 둘을 묶었다. 밀린 것 = **자바 4·매퍼 1·JSP 3**(DB 는 적용 완료),
  반영 확인은 `codeList.do` 의 `build` 한 줄. 체크리스트 숫자는 **전부 운영 DB 실측**으로 맞췄다
  (⚠초안에 「혈당측정기=HLT001」로 잘못 적었다가 대조에서 잡음 — **LAB030**(진단검사)이다).
- ✅**[이어서] 배포 전 코드 재검토 — 잠재 결함 2건 수정**(`build=`**`20260815-ROWSAFE`**) :
  ①★**옛 단벌 문서가 벌(SUBS) 추가 뒤 사라진다** — 벌 없던 시절 행은 `ROW_NO 1~999`(벌 0)인데
  화면은 벌 1~9 만 그려 **안 보이고, 저장하면 지워진다**(삭제 후 재삽입). ⇒ `withLegacySet()` 신설
  (벌 0 에 값이 있으면 「이전 자료」 벌을 앞에 붙임). ⚠**화면과 인쇄가 같은 함수를 써야 한다** —
  인쇄는 `splitRowVals(collectRows())` 로 다시 가르므로 따로 두면 종이에서만 빠진다.
  ②**행 999 상한**(safeRpt·점검표 둘 다) — `벌×1000+행` 규칙이라 1000행째가 옆 벌/블록을 덮어쓴다.
  ★지금은 QPS 가 병원에 **안 열려 있어**(사이드바 display:none + 위너넷·개발자 플래그) 피해 0 —
  **정식 오픈 뒤를 위한 안전장치**다. Node 로 6경우 검증(구제·무변경·빌린 열·값 제자리).
  ③**전월복사가 ITEM_DAY·ITEM_MONTH 의 앞 열을 안 가져왔다** — `carryVals` 가 EQUIP_DAY·LIST 만
  다뤘는데 「행 이름의 연장」인 앞 열은 다른 축에도 있다(**근거 7종** : 응급키트 유효기간·구급차 수량·
  월별비치의약품 비치량 4·연간계획서 점검업체). 원본 SUNWOO 에도 「전월 가져오기」 단추가 있다.
  ⇒ 세 축으로 확장(앞 열 1000대만 · ITEM_COL 은 근거 0 이라 제외). 행 890 은 앞 열에 값을 안 써
  필터에 안 걸린다(확인함). **같은 배포에 합류.**
- ✅**[2026-08-15 저녁] 병원 답변 대기분을 미리 만들어 둠** — #43(RN25 분할)이 「예」면
  [RN25_SPLIT 시드](docs/sql/qps/seed/QPS_SEED_RN25_SPLIT_2026-08-15.sql) **실행 한 번**으로 끝난다
  (⛔머리말 정지 표시 — DRUGRTN 때와 같은 방식). `RNL028` 목록(LIST + 블록 5 = 경구·주사·수액·기타·설하정,
  **CARRY_YN 켜서 전월복사로 딸려 오게**) + `RNL029` 점검표(ITEM_DAY 3항목 + 사인 행 + 자유행 표).
  ⚠약품 프리셋은 안 넣는다(병동마다 다르다) · GRP_NM 비움(넣으면 원본에 없는 2단 머리가 생긴다).
  #43·#44 는 **답만 오면 그날 끝난다** — 대응표는 [최종판정 §5-1](docs/proposals/QPS_보류서식_최종판정_2026-08-15.md).
- ✅**[이어서] 눈 검증 6종을 미리 확인** — 로컬 8080 이 내려가 있어도 **화면이 쓰는 계산 함수를
  꺼내 운영 DB 값을 물리면** 칸 수는 확인된다(RAD022 사진 5·PHA024 12·ADM018 2단 6열·
  ADM012 기기 20·RNL027 옆칸 2/25행·LAB030 연 문서 — **6종 전부 일치**).
  이어서 **safeRpt 6유형도 같은 방식으로 확인**(HRCARD 9벌·HLTDIARY 4벌/6묶음 50·DRUGADRE 3벌/7묶음 34·
  RPTBHEP 2벌·BLOODRTN 1벌·EDURPT 사진+라벨 14칸 숨김 — 전부 일치). **낮에 고친 「옛 자료 구제」가
  실제 5개 유형에서 도는 것도 확인**했다.
  이어 **메뉴 묶음을 76종으로 재확인**(DRUGREQ 추가 뒤 다시 봄 — 8묶음 전부 담김·잔여 0)하고
  **인쇄 구조**도 확인했다(사진 4·3장→2줄 2칸씩·0장→안 넣음 · 반복행은 **값 있는 벌만** 종이에).
  ⇒ 사용자가 눈으로 볼 것은 **「브라우저가 실제로 그리는 모습」**뿐(체크리스트 20→**10분**,
  대조표는 배포·눈검증 §2-1).
- ✅**[2026-08-15 밤] 재기동된 8080 으로 오늘 고친 3건 실왕복 검증**(`build=20260815-ROWSAFE`) :
  **전월복사** RNL027 7월→8월 = **유효기간 3건만 오고 점검값 0건**(규칙대로) ·
  **옛 단벌 구제** = HRCARD 문서에 벌 번호 없는 행을 **DB 로 직접 심어** 재현 → 그릴 벌 9→10
  (「이전 자료」가 앞에 붙고 열은 옛 단벌 정의에서 빌림) · ★**재저장 후에도 살아남음**
  (구제 전이면 이 지점에서 사라졌다) · 메뉴 13부서·프리셀렉트 정상. 가짜병원 자료 삭제(잔여 0).
  ⚠렌더 페이지의 `dept=` 14회 중 하나는 **사이드바 문의 폼 `action`**(Spring 이 현재 URL 을 넣는다) —
  링크는 13개가 맞다. 그 폼은 ajax 전용이라 무해.
- ✅**[이어서] 배포 전 스모크 — 385종 전부 열어 봄** : 점검표 **309종**(`chkBase`) · safeRpt **76종**
  (`safeRptBase`) · 부서 링크 13개 — **문제 0건**(500·빈 서식·엉뚱한 서식·열 빈 벌 전부 없음).
  ⚠**사유 정정** : 서식 0종 부서를 눌러도 「빈 화면」이 아니라 ***공통(MSDS 2종)만 뜬다***
  (화면이 「그 부서 + COMMON」을 보여주므로). 담당자가 **「우리 부서 건 이것뿐인가」로 잘못 읽는 것**이
  문제라 링크를 뺀 것이다 — 결론은 같지만 주석·감사 스크립트의 이유를 실측대로 고쳤다.
- ✅**[이어서] 권한·병원 간 격리 확인**(처음 본 영역) — 병원 계정의 **서식 관리 화면 직접 접근**은
  작성 화면으로 돌려보내고 **`chkFormSave` API 직접 호출도 서버가 막는다** · 다른 병원이 `hospCd` 를
  **위조해도** 남의 문서를 못 읽고 못 지운다. ★구조 = `hospCd(request,p)` 가 **파라미터를
  위너넷일 때만** 받고 그 밖엔 로그인 쿠키를 쓴다(위너넷은 여러 병원을 지원하므로 맞는 설계).
  ⚠작은 흠 : 남의 문서 **삭제 시도에 응답이 `OK`** 로 온다(실제로는 안 지워짐) — 동작은 안전, 응답만 오해 소지.
- ✅**[2026-08-15 밤] 쓰기 편하게 — 탭 · 글자 크기**(사용자 요청) :
  **qpsSafeRpt** = 카드 7장을 `data-tab` 으로 묶어 **기본·본문·사진첨부·전체** 4탭 ·
  **qpsChk** = **점검표·사진(n)·전체**(***사진칸 있는 서식에서만*** — 격자는 본체라 못 가른다).
  ★**「전체」 탭을 남긴다**(쭉 훑던 방식을 뺏지 않는다) · 고른 탭은 localStorage.
  ⚠***카드를 숨기는 주체가 둘***(유형별 display:none · 탭)이라 섞이면 「있는데 안 보인다」가 된다 ⇒
  **탭은 `data-off` 로만 숨긴다**(유형이 숨긴 카드는 탭을 켜도 숨은 채). 유형마다 숨는 카드가 달라
  **띠는 그때그때 다시 센다**(빈 탭을 안 만든다).
  ★**글자 크기 `가－/가＋/↺` = `zoom`** — CSS 가 px 라 뿌리 font-size 로는 격자가 안 커진다.
  0.8~1.6배, 칸 너비까지 같이 커져 **SUNWOO 의 [폰트 확대/축소] 와 같은 느낌**. 인쇄는 새 창이라 무관.
  ※Q&A 화면(qnacd)은 안쪽이 `em` 이라 **뿌리 font-size 방식** — 그 방식은 QPS 에 못 쓴다.
  검증 = 배포 JSP 함수를 가짜 DOM 으로 10경우 + 실서버 렌더.
  ⚠**JSP 를 고치면 첫 요청이 재컴파일로 20초 넘는다** — 타임아웃을 오류로 오해하지 말 것.
- ✅**[2026-08-15 밤] 탭·글자 크기를 긴 화면 13개로 넓힘**(사용자 지적 「계획서도 스크롤이 길다」) —
  qpsPlan·QI계획서/보고서·RCA·FMEA·불만고충 2·감염종합보고·만족도 2·서식관리 + 기존 3.
  ★★***고정으로 달지 않는다*** — 전부 펼친 높이를 재서 **한 화면을 넘칠 때만** 탭이 나온다
  (「화면 반도 안 차는데 탭이 떴다」는 지적). 창 크기·글자 크기가 바뀌면 **다시 잰다**.
  ★**탭을 잘게 나누지 않는다** — 카드가 많으면 이웃끼리 묶어 **최대 4개**(7장이면 2·2·2·1, 이름은 첫 제목+「외」).
  ★**「전체 보기」는 탭이 아니라 보기 방식** — 오른쪽 끝 토글(`☰ 전체 보기 ↔ ▤ 나눠 보기`), 선택은 기억된다.
  ⚠⚠**여러 JSP 를 스크립트로 한꺼번에 고칠 때 두 번 크게 데였다** :
  ①삽입 자리를 정규식으로 잡다가 ***여는 태그 한복판을 잘라*** 9개 파일이 한꺼번에 깨졌다
  (`<div class="qr-title">` → `v class=…`) ⇒ **줄 단위로 div 개수를 세어** 넣을 것.
  ②JS 주석에 `--` + `%>` 를 남겨 ***JSP 가 거기서 주석을 닫았다*** — 눈으로 안 보인다.
  ⇒ **검사 도구 3종을 `docs/tools/` 에 상비**했다 : `JSP구조검사.py`(잘린 태그·div/script/주석 짝) ·
  `JSP함수검사.js`(호출만 있고 정의 없는 함수) · `링크검사.py`(문서 링크). ***대수술 뒤에는 반드시 돌린다.***
  ⚠★**「높이를 언제 재는가」가 진짜 함정이었다** — 카드 속은 **AJAX 로 나중에** 채우는데
  높이는 **부팅 직후**에 쟀다. 긴 문서인데도 「한 화면에 들어간다」로 잘못 보고 **탭이 영영 안 나온다.**
  ⇒ `MutationObserver` 로 **내용이 채워지면 다시 잰다**(12화면. qpsChk 는 자료 로드 뒤 이미 부른다).
  ⚠**탭 띠 자신이 바뀐 것은 무시해야 한다** — 안 그러면 재기→그리기→재기 로 **서로 부른다**.
  검증 = 가짜 DOM 5경우(빈 문서 탭 없음 · 내용 차면 뒤늦게 나옴 · 루프 없음 · 한 묶음만 보임 · 전체 보기).
- ✅★★**[2026-08-16] 점검표 인쇄가 A4 를 넘던 것 — 309종 전수 실측으로 찾아 한 줄로 고침**
  (`build` 무관 · **JSP 파일 교체만**). 눈 검증 항목이던 「격자가 종이 폭을 넘는지」를 브라우저로 직접 쟀다.
  ★**재는 법** : `window.open` 을 가로채 인쇄물을 **감춘 iframe** 으로 받고(⚠`print()`·`focus()` 도
  같이 막아야 멈추지 않는다), 폭을 **1055px**(A4 가로 297mm − 여백 9mm×2)로 두고 표의 `scrollWidth` 를 잰다.
  ⚠**본문 폭을 안 맞추면 전부 6% 넘침으로 나온다**(처음에 29/29 로 나와 이상해서 잡았다).
  - **결과** : 고치기 전 **122종 초과**(31일 격자 대부분 16~21% · 최악 ADM014 182%·NUR024 70%) → 고친 뒤 **0종**.
  - **원인** : 인쇄는 ***화면 표를 복사***해 쓰는데(따로 그리면 화면과 종이가 갈리므로 일부러 그렇다),
    화면 날짜 칸의 `min-width:32px`(눌러야 하니 화면엔 필요)가 **종이까지 따라왔다**. 31×39px=1209 > 1055.
  - **고침** : PRINT_CSS 에 `table.gr th, table.gr td{ min-width:0 !important; }` 한 줄.
    그러면 `width:100%` 가 남은 폭을 고르게 나눠 날짜 칸이 **약 6mm**(원본 종이와 비슷)로 앉는다.
    ⚠***화면 쪽 min-width 는 건드리지 말 것*** — 종이에서만 푼다.
  - ⚠**끊기 설정(`SPLIT_N`)은 이 문제의 답이 아니다** — 15종만 쓰고, 쓰는 NUR024 조차 하루가 3교대라
    조각 하나가 1797px 였다. 끊기는 **원본 종이가 나뉘어 있을 때** 쓰는 것이지 폭 맞추기 수단이 아니다.
  - **[후속 2026-08-17] 「날짜 칸 3.6~4.9mm 로 좁은 23종」도 해소** — 사용자가 SPLIT_N 을 지시했으나
    **캡처 대조로 원본이 안 끊는 것을 확인**(XR02·w26·RN31 셋 다 1 Page·31일 한 줄, 항목 칸을 접는다)
    → 원본 방식대로 `ckPrintFitDays()` 신설 : 날짜 칸(`data-day` ≥8개)에 **최소 5mm**, ***넘치면 되돌림***.
    ✅**검증 완결(08-17)** — 브라우저 실측 **28종**(좁던 23 + 넘침 심했던 5) : 좁던 23종 전부
    **5.2~6.9mm**(NUR024 5.2 는 D/E/N 3칸이라 정상) · 28종 **넘침 0** · 되돌림 걸린 서식 없음.
    ⚠일률 「첫 칸 26%」는 실험으로 기각 — PHA022 는 좋아지고 RNL027 은 나빠진다(앞 열 있는 서식).
    ★브라우저 측정 함정 2 : **창이 숨겨져 있으면** 서식 전환이 8~28초(렌더 억제 — 서버는 3ms) ·
    측정 루프의 `sel.value=id` 는 **목록이 다시 그려지면 풀린다** — 걸어 놓고 값 확인 후 재시도.
- ✅**[2026-08-15 밤] 사용자별 담당 부서**(사용자 요청 「사용자마다 프로그램 사용등록」 → 부서 매핑으로 확정) —
  새 표 `TBL_QPS_USER_DEPT`(DDL 운영 적용) · 화면 `qpsUserDept.do` · `chkBase` 가 담당 부서로 거른다.
  ★★***규칙은 하나 — 「등록이 없으면 전 부서」***. 부서 0개로 저장 = 전 부서 복귀(해제 기능 불필요).
  ⚠***계정 종류로 가르지 않는다***(사용자 지시) — 처음엔 「관리자는 언제나 전 부서」 예외를 두었는데
  **표에는 전 부서라 적혀 있는데 지정은 되는 모순**이 생겼다. 예외를 지우니 화면·서버가 맞았다.
  ⚠기존 `TBL_USERAUTH_MST` 와 **섞지 않는다**(그쪽=무엇을 할 수 있나 / 이쪽=어느 부서 것을 보나).
  ★★***이 화면은 「덜어내기」가 일의 대부분이었다*** — 복사·여러 명 일괄·부서 일부고르기를 한 자리에
  넣었다가 사용자 지적으로 계속 걷어냈다. 최종 = **주 기능(한 사람 고쳐 저장) + 편의 기능(복사 하나)**.
  ***원칙 : 자주 쓰는 것을 먼저, 편의는 그 뒤에. 같은 것을 두 군데서 고르게 하지 않는다.***
  안내문도 **모르면 틀리는 것만** 남기고, 「공통은 누구에게나」는 **그 체크칸에** 적었다.
  ⚠**Swal 창은 `<body>` 바로 밑**이라 `#화면id` 안에 CSS 를 두면 안 먹는다(작게 손볼 때 겪음).
  ⚠⚠**편집 중 함수 정의가 통째로 잘려도 화면은 조용히 죽는다**(`udCopyMsg` 실제 사고 — 「제목은 바뀌는데
  아래는 안 바뀜」으로만 보였다) ⇒ ***호출만 있고 정의 없는 함수*** 점검을 JSP 대수술 뒤에 돌릴 것.
  검증 : 필터 309→35종 · **주소로 남의 부서 지정해도 무시** · 미등록 전 부서 · 되돌리기 · 더하기/덮어쓰기 ·
  관리자 지정 309→24종. `build=20260815-USERDEPT`(자바+매퍼 → WAR 재빌드).
- ✅**[2026-08-15 밤] 사용자별 담당 부서**(사용자 요청 「사용자마다 프로그램 사용등록」 → 부서 매핑으로 확정) —
  새 표 `TBL_QPS_USER_DEPT`(DDL 운영 적용) · 화면 `qpsUserDept.do` · `chkBase` 가 담당 부서로 거른다.
  ★★***등록이 없으면 「전 부서」*** — 막는 장치가 아니라 **좁혀 주는 장치**다(등록 안 했다고 못 보게 하면
  도입하는 날 업무가 멈춘다). **부서 0개로 저장 = 전 부서 복귀**라 해제 기능이 따로 없다. 위너넷은 항상 전 부서.
  ⚠기존 `TBL_USERAUTH_MST` 와 **섞지 않는다**(그쪽=무엇을 할 수 있나 / 이쪽=어느 부서 것을 보나).
  화면 = **선택하면 지금 담당이 체크되어 보이고**(여러 명이면 공통분만+"서로 다릅니다" 경고) ·
  **다른 사람에서 가져오기**(새 직원) · **여러 명 일괄** · **더하기/덮어쓰기** 토글.
  ⚠**Swal 창은 `<body>` 바로 밑에 붙는다** — `#화면id` 안에 CSS 를 두면 안 먹는다(작게 손볼 때 겪음).
  검증 : 필터 309→35종 · **주소로 남의 부서 지정해도 무시** · 미등록 전 부서 · 되돌리기 · 더하기/덮어쓰기.
  ⚠자바+매퍼 → WAR 재빌드, `build=20260815-USERDEPT`.
- ⚠⚠**CLI 로 고친 파일은 이클립스가 모른다** — **F5(새로고침)** 해야 자동배포가 돈다.
  (JSP 를 고쳤는데 화면이 그대로면 이것부터 의심할 것. 메모리에도 있는 함정이다.)
  ★**JSP 함수 추출 함정** : `function f(){ … }` **한 줄 정의**를 여러 줄 패턴(`\n  }`)으로 잡으면
  ***뒤 함수들까지 통째로 삼킨다***(docGrpCnt 에서 실제로 겪음 — 브레이스 균형은 맞아 안 드러난다).
  ⇒ **한 줄 정의를 먼저** 시도하고, 추출본을 `new Function` 으로 **한 번 파싱해 보고** 쓴다.
- ✅**[2026-08-15] 반복행 표 「여러 벌」 조각 완성** — 정의는 신설 `TBL_QPS_SAFERPT_SUB`(RPT_GB×SUB_NO 1~9),
  **값은 기존 ROW 표에 `ROW_NO=벌×1000+행`**(LIST 블록 규칙과 같은 발상 — 단벌 유형·옛 문서 무변경.
  SUB 벌이 있으면 FORM 단벌을 이긴다). DDL 운영 적용 · 매퍼 1쿼리 · 화면(벌마다 이름 띠+행추가)·인쇄
  (빈 벌 생략) · `build=20260815-SRSUBS`. 순수 함수는 Node 로 검증(단벌 호환·9벌 분배·우선권).
  [SUBS 시드](docs/sql/qps/seed/QPS_SEED_SUBS_2026-08-15.sql) 운영 적재 : **`HRCARD` 인사기록카드 9벌+사진**
  (⚠주민번호 칸은 민감정보 최소화로 뺌 — 병원확인 **#42**) · `MRCOMPL` 완결도 2벌 ·
  RPTBHEP/RPTCHK **집계표 원형 복원**(#41 의 「합침」 해소). safeRpt **68종**.
  ⛔h19 업무일지만 계속 보류(체크 ~50문항 전문이 판독에 없다 — 캡처 재판독 필요).
  ⚠자바+매퍼 변경 → WAR 재빌드+재기동 필요. 화면 왕복 검증은 재기동 뒤.
- ✅**[이어서] 점검표 사진칸 완성** — `TBL_QPS_CHK_FORM.PHOTO_NMS`(칸 이름 목록=수·라벨, 최대 12) +
  `TBL_QPS_CHK_FILE`(문서×칸, 재업로드=교체). safeRpt 사진과 판박이(blob 표시·인쇄 대기 포함).
  [CHKPHOTO 시드](docs/sql/qps/seed/QPS_SEED_CHKPHOTO_2026-08-15.sql) : **`RAD022` 납가운 관리대장 등록**(XR07 보류 해소 —
  LIST+평가기준 정형문+사진 5칸) · **`PHA024` 봉인스티커 월별 12칸**(RN27 p2 흡수).
  ⇒ ⓐ급 백로그 전부 소진.
- ✅**[이어서] h19 보건관리 업무일지 등록**(`HLTDIARY`) — 보류 사유(문항 전문 없음)를 **캡처 직접 재판독**으로
  풀었다(h19.png 를 구역별로 잘라 읽음 — System.Drawing Crop → Read). 체크 6그룹 **50항목**(DEF) +
  통계 소표 3벌·상담 반복행 = **SUBS 4벌** + LBL_JSON. safeRpt **69종**.
  ⚠긴 문항 3곳은 저해상 판독이라 실물 대조 표시를 시드에 남김(구분5 12·13번·구분6 3번).
  ★캡처 재판독 기법 : 전체화면 PNG 를 **구역별 Crop 후 Read** 하면 작은 문항도 읽힌다 — 남은 재판독류
  (w20_p2 등)에 같은 방법. ⇒ 서식 보류는 **XR23(개인 키)·w15(셀 고정문)·MR13(기능화면) 3건**뿐.
- ✅**[이어서] 실서버 왕복 검증 + 혈액 반납/폐기 신청서 등록** — 재기동된 8080(`build=20260815-SRSUBS`)에서
  **반복행 여러 벌**(HLTDIARY 4벌 분배·한글·조도 부기)·**점검표 사진**(RAD022 문서→업로드→복원) 왕복 통과,
  테스트 자료 삭제(⚠08-13 devtest 잔재 2건도 발견·정리). 이어서 `BLOODRTN` 등록(safeRpt **70종**) —
  간호 [201]·진단검사 11·RN37 **3부서 공용 확인 서식**. RN37.png 재판독으로 주의문 전문 확보,
  ★**서명 4칸이 별도 서명란이 아니라 표의 열**임을 확인(반복행 8열로 폈다).
  DRUGRTN(의약품 반납) 확인 2건은 여전히 **약국 캡처 대조 몫**(이 PC 에 약국 캡처 없음).
- ★★**SUNWOO 캡처는 이 PC 에서도 된다**(2026-08-14 실측으로 정정 — 「그 노트북에서만」이 아니다).
  computer-use MCP 없이 **PowerShell `CopyFromScreen` + `user32 mouse_event`** 로 몬다.
  ***★진위는 `MainWindowTitle`(=열린 문서 이름)로 본다*** — 이미지를 안 읽고도 무엇이 열렸는지 안다.
  어긋남의 원인 3가지(창 이동 · **트리 「보고서」 폴더 펼침 = 18칸 밀림** · 닫기 뒤 트리 스크롤)와
  좌표 실측값은 판독 문서 §캡처 방법. 세션 시작 때 **환경부터 확인.**
- **왜 그렇게 만들었는지 · 확정사항 · 함정**은 [QPS_세션요약_2026-08-08.md](docs/proposals/이력/QPS_세션요약_2026-08-08.md).
  ***설계를 바꾸기 전에 그 문서의 [확정] 절들을 먼저 볼 것*** (사용자가 이미 정한 것을 되돌리지 않도록).
- ⚠**DB 는 08-14 분까지 운영 적용 완료** — `docs/sql/QPS_*` 시드를 다시 돌리지 말 것(병원이 고친 서식이 되돌아간다).
  ~~예외 1건 — DRUGRTN 시드~~(✅2026-08-15 확인 해소·**운영 적재 완료** — 다시 돌리지 말 것에 합류).
- ⚠**Eclipse 서버가 도는 중에 명령줄 `mvn compile` 금지** — 자동배포와 겹치면 배포 폴더의 클래스가
  통째로 비고, 8080 은 옛 클래스가 남아 **재기동 전까지 티가 안 난다.** 복구는 **Servers ▸ Clean...**

- ✅**[2026-08-17] 관리자 모듈 29건 대조 완료**(사용자 요청 「관리자 부분 정리할 것 없나」) —
  [대조 문서](docs/proposals/QPS_관리자모듈_대조_2026-08-17.md). ***새로 만들 것 0건*** :
  **이미 있음 14**(병원·사용자·권한·서식관리·결재라인·병동·도움말·문서목록·적정성평가 ·
  규정집=`qpsLib` 갈음 · 조직/팀=`qpsUserDept`+`TBL_QPS_MGR` · 위원회=회의록 13종) ·
  **서식 흡수 2**(당직의료인=`DUTYDOC` · 의료기기목록=점검표가 기기 행을 문서마다 적는다) ·
  **SUNWOO 내부용 8**(설정·유틸·Form Tech·서식자동화·설치관리·메뉴관리 2 등).
  ⇒ **남은 결정거리 5건**(DUTY · 인증 · 접속 이력 · 서명관리 · 공휴일)만 B4 에 남긴다.
  ⚠**위원회는 마스터 화면이 없다** — 새 위원회 = 코드 3곳 수정(qpsMinutes·사이드바·putFormGb).
  신설이 잦아지면 화면 값어치 판단 사안. ★**캡처는 더 필요 없다**(AD·RG·DT·CT 이미 확보).
- ✅**[2026-08-17] 삭제 응답의 오해 소지 해소**(08-15 격리 점검의 「작은 흠」) —
  `safeRptDelete`·`chkDelete` 가 **매퍼가 돌려주는 건수를 버리고 무조건 `OK`** 를 주고 있었다
  (WHERE 에 병원코드가 있어 **동작은 원래도 안전**했다). 0건이면 「삭제할 문서가 없습니다」로 답한다.
  `deleteChkDoc` 반환형 `void`→`int`(서비스·인터페이스 동반).
  ✅**실서버 검증**(`build=20260817-DELGUARD`) : 없는 문서·**남의 병원 문서** → `FAIL`(DB 그대로 `USE_YN='Y'`) ·
  주인 삭제 → `OK`(`USE_YN='N'`). 가짜병원 시험 자료 삭제(잔여 0).
  ⚠★**0 건의 뜻은 「없거나 남의 것」이지 「이미 지운 것」이 아니다** — 소프트 삭제라 두 번째 호출도
  **행이 걸려 1** 이 온다(MySQL 은 값이 안 바뀌어도 matched 를 돌려준다). 두 번 눌러도 `OK` 인 편이
  안전하므로 동작은 그대로 두고 **문구만** 실측에 맞췄다(처음에 「이미 삭제되었거나」로 썼다가 정정).
  ⚠**CLI 로 고친 자바는 이클립스가 모른다** — 이번에도 F5 없이 빌드해 **클래스가 10시간 전 것**이었다
  (소스 08:49 · 클래스 어제 22:58). ***소스와 클래스 시각을 견주면 한눈에 잡힌다.***
  ⚠**메이븐이 도는 동안 8080 이 500** 을 냈다(클래스 교체 중) — 끝나면 저절로 돌아온다. 오류로 오해 말 것.

## 예정 개발 (회의 후 착수)

### [예정 · 2026-07-14 회의 후 착수] 컨설팅 문서 전산화 (의뢰서·견적서·홍보물·안내문)
- **개요**: 종이·팩스로 오가던 컨설팅 문서 5종을 WinCheck+ 안으로 전산화. **1단계=의뢰서+견적서**(핵심, 수기 접수·견적 제거), 2=홍보물 편집CMS, 3=프로그램안내→도움말/온보딩·연간안내문→공지.
- **계획·DB설계·민감정보 원칙 전부**: [docs/proposals/문서전산화_개발계획.md](docs/proposals/문서전산화_개발계획.md) — 제안 원문(시각) docs/proposals/위너넷_문서전산화_제안_2026-07-12.html · 공유 아티팩트 https://claude.ai/code/artifact/211932ce-00e8-476b-b591-6af1bff156b7 · 원자료 `D:\워너넷자료\제안내용\`(PDF 6종).
- **이어서 작업 시(다른 PC 포함)**: 위 계획 md 읽고 **회의에서 확정된 범위대로 1단계부터** 구현 — 배경 재설명 불필요. 신규 테이블 `TBL_CONSULT_REQ/MANAGER/AGREE`·`TBL_QUOTE/QUOTE_ITEM`·`TBL_PROMO_CONTENT`. 민감정보(프로그램PW·심평원 인증서 암호) 저장최소화+AES-256+마스킹+감사로그. 요양기관기호·병원명 로그인 자동채움, 목표점수·등급 `TBL_GRADE_MST` 연계.

## 장애/수정 이력

### [대기 · DB 적용 필요] 마감업로드 — 평가표 배치 삭제가 **다른 배치 환자까지 소실** (2026-08-24)
- **증상**: 같은 달 환자평가표(.L01)를 나눠 올린 뒤(예: 67건+70건) 한 배치를 **삭제하면 다른 배치
  건수도 사라짐**. 삭제 후 재업로드 흐름에서 재현(더행복요양병원 202607). 병원처럼 삭제 없이
  올리기만 하면 "정상 공존"으로 보인다(목록 2행 + 환자행 병합).
- **원인** — 평가표 업로드는 **UPSERT** 라 겹치는 환자 행의 `CHUNGSEQ`(배치키)가 마지막 업로드로
  넘어간다(2026-07-30 수정이 이를 보장). 그런데 `SP_DELETE_MAGAM_CLAIMNO2` 는
  `DELETE FROM TBL_PATVAL_MST WHERE CHUNGSEQ=그 배치` — **그 배치가 「현재 소유한」 행 전부**를
  지우므로 다른 파일에서 온 환자까지 함께 사라진다.
  실측(31288286/202607): 목록 6배치(70·67·83·4·7·70건)인데 행 소유는 83/4/4/70 — 앞 배치들은
  소유 0(화면 「자료없음」 배지의 정체), 7건 배치도 3행을 마지막 배치에 뺏김.
- **조치**([SP_DELETE_MAGAM_CLAIMNO2_2026-08-24.sql](docs/sql/proc/SP_DELETE_MAGAM_CLAIMNO2_2026-08-24.sql) ·
  백업 [BACKUP_…_20260824.sql](docs/sql/proc/BACKUP_SP_DELETE_MAGAM_CLAIMNO2_20260824.sql)) —
  평가표(9) 분기만: **같은 달에 다른 평가표 배치가 남아 있으면 DELETE 대신 남은 최신 배치로
  CHUNGSEQ 이관**, 마지막 배치 삭제일 때만 실제 DELETE(그 달 평가표 비우기 = 기존 의미 유지).
  청구(8)·입원현황(Z)·목록/이력 삭제는 그대로.
- ★**평가표의 배치는 「업로드 이력」일 뿐** — 환자행은 달 단위 병합 스냅샷이다. 배치별 원래 구성은
  저장하지 않으므로 "그 파일에 있던 환자만 지우기"는 불가능하고, 이관이 유일하게 정합적인 답이다.
- **적용 완료**(2026-08-24 14:24:56, 사용자 실행 — 문법·정의 실측 확인).
- **[확정 2026-08-24] 평가표 소유이동 = 정식 사양**(병원 담당자와 협의) — 인위적으로 지우지 않는 한:
  나중에 올린 파일이 <최종건>이 되고, 겹치는 환자는 이전 배치에서 제외되어 잔여만 남는다
  (예: 70건 뒤 67건 → 67은 최종건·70배치엔 3건 / 70건을 다시 올리면 70이 최종건·67배치는 0건).
  이 동작을 바꾸지 말 것. 삭제 버튼은 이관 방식(위 수정)으로 데이터를 지키면 된다.
- **★「자료없음」 배지는 삭제가 아니다** — 업로드(UPSERT)만으로는 행이 안 지워진다. 나중에 올린 파일이
  겹치는 환자를 <소유>해 앞 배치가 자료없음으로 보일 뿐(70⊃67 관계라 「70을 나중에 돌리면 67이 죽은 듯」
  보인다). 실측: 총 70행(67명) 그대로 보존. 진짜 소실은 <구 SP의 삭제 버튼> 경로뿐이었다.
- ⚠**별건 발견 — 마스터 세션 업로드가 w1234567 밑으로 들어감**: 화면 머리엔 「더행복 [마스터님]」인데
  14:2x 업로드들의 HOSP_CD='w1234567'(위너넷 자체 기호). 파일명 기호(31288286)와 무관하게 세션
  s_hospid 로 저장된다 — 병원 전환이 안 된 채 올리면 <병원에 반영 안 되고> 위너넷 밑에 쌓인다.
  → 업로드 시 파일명 기호≠세션 기호 경고/차단 가드 필요(미착수, 사용자 판단 대기).
  w1234567 밑 202607 평가표 70행은 테스트 잔재 — 정리 대상.
- **[확정] 「자료없음」 배지 규칙(2026-08-24)** — 평가표(9)는 소유 0 이어도 <같은 달에 같은 건수 배치가
  또 있을 때만>(같은 파일 중복 재업로드) 배지를 띄운다. 흡수(포함, 예: 67⊂70) 케이스는 숨김.
  구현 = **화면(JS) 계산** — 응답에 그 달 배치가 전부 오므로 평가표(9) 중 같은 건수 2개 이상이면 dup.
  ⚠처음엔 목록 SQL 에 dup_cnt_yn 컬럼을 넣었다가 **공유 resultMap 함정**에 걸렸다 — 그 resultMap 을
  쓰는 다른 조회가 "컬럼 없음"으로 깨져 화면 전체가 빈 목록이 됐다(사용자 신고). 컬럼·resultMap 원복.
  ★같은 실수로 원복 중 `END AS data_cnt` 줄까지 지웠다가 복구 — awk 범위 삭제 뒤 반드시 이웃 줄 확인.
  청구서(8)·입원현황(Z)의 배지 판정은 종전 그대로(data_cnt=0).

### [완료 · 로컬] 가입신청 프로세스 개편 — 신청은 최소 입력, 나머지는 승인 후 (2026-08-24)
- **요청**: 「신규가입신청서 프로세스 변경 — (해당 내용만 입력하고) 관리자 승인 후, 병원이 로그인하면
  가입 화면을 띄워 **모두 정상 입력 + 동의서 제출** 시 메뉴 활성화」. 신청 단계 입력 =
  **요양기관기호·요양기관명·신청자(로그인 계정)** 뿐(사용자 확정). 승인 화면(joinReq.jsp)은 **변경 없음**
  (「병원에서 등록하면 다 보여주어야 할 내용이라서」 — 빈 칸이 보이는 게 정상이다).
- **① 신청 화면 축소** — wnn_consult [join_apply.jsp](../wnn_consult/src/main/webapp/WEB-INF/jsp/login/join_apply.jsp) (1,095→868줄):
  탭 3개(의뢰서·원격접속동의·개인정보동의) 폐기 → 한 장. 남긴 칸 = 병원명·요양기관기호(확인)·신청자 계정.
  전화·주소·대표자·전산프로그램·인증서암호·담당자표·목표점수·**동의서 2종·대표자 도장** 전부 승인 후로.
  ⚠**fnJoinApply 의 초기화 6개(jaLoadAgree·jaSealClear·jaPcUseSync·jaConactSync·jaMgrSync·jaSignSync)를
  같이 빼야 한다** — 없어진 요소를 만져 ReferenceError 로 ***스크립트가 통째로 죽는다***(실제로 터졌다).
- **② 신청 서버 검증 완화** — wnn_consult `JoinController.joinReqSaveAct` : 필수 14→**5**
  (hospCd·hospNm·email·mbrNm·mbrTel), 담당자 2행·필수동의·도장 필수 검사 제거(도장 해시 처리는 값 있을 때만).
  ⚠`TBL_JOIN_REQ` 의 NOT NULL 은 REQ_NO·REQ_STAT·DOC_YN·HOSP_CD·EMAIL 뿐 — DB 변경 불필요(실측).
- **③ 승인 후 화면 확장** — wnn_medcost [joinDocs.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinDocs.jsp) (774→1,010줄):
  「신청 내용」 표에 전산프로그램(MASTER)·인증서암호·환자평가표일·목표점수·희망서비스·PC사용 추가
  (보기 모드에서도 빈 칸은 **빨간 「미입력」**), **담당자 카드**(4구분, 총관리자·심사과 필수)·
  **동의 내역 카드**(마스터 기준, 전문 펼침 + 체크; 이미 동의한 건은 잠금)·**대표자 도장 업로드** 신설.
  바닥의 `jdLack` 안내가 서버 `lackOf` 와 **같은 이름·같은 순서**로 남은 항목을 적는다.
- **④ 저장·제출 서버** — wnn_medcost `JoinController`/`JoinServiceImpl`/`Join_SQL.xml` :
  · `updateMyJoinReq` 확장(옮겨 온 컬럼 + 도장은 `CASE WHEN` 으로 **빈 값이면 기존 유지**) ·
    담당자는 `deleteJoinMgrAll` 후 재삽입(**진짜 삭제** — soft 로 남기면 MGR_SEQ 가 쌓인다) ·
    동의는 `insertJoinAgree`(ON DUPLICATE) — 전부 한 트랜잭션.
  · `joinDocsSave` 는 **느슨하게**(중간 저장 허용, 병원명만) · 도장 해시는 서버가 재계산.
  · **`joinDocsSubmit` 이 최종 게이트** — `lackOf()`(11항목) + 필수 동의(ESS_YN) 검사. 여기를 통과해야
    REQ_STAT 40·DOC_YN Y 가 되고 사이드바 게이트(`selDocGate`)가 풀린다. ⇒ **selDocGate 는 손댈 필요 없었다**
    (조건이 DOC_YN='N' 이라, 제출 검증을 강화한 것으로 「모두 입력+동의서」가 자동 보장).
  · `selAgreeMstList` 신설(joinDocsInfo 가 `agreeMst` 로 내려줌) — 신청 때 동의를 안 받으므로
    TBL_JOIN_AGREE 가 빈 채로 넘어온다. 마스터가 있어야 화면이 무엇에 동의받을지 안다.
- **⑤ 계약정보 창(별건 요청)** — [joinReq.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinReq.jsp) `jrcOpen` :
  탭(08-21) 폐기 → 적정성평가·진료비분석 카드를 **한 화면에 세로 나열**(내력: 좌우→탭→나열).
  `jrcTab()`·탭 점 로직 제거. 저장·체크 동작은 그대로.
- **후속(같은 날 저녁, joinDocs 다듬기 연속 요청)** :
  ①입력 표 3등분 → **4등분**(라벨+입력 4쌍) · 대표자↔FAX 자리 교체 · select 도 input 규칙을 못 받아
  납작하던 것 교정(`select.jd-in` 신설). ②**주소검색 = 카카오(다음) 우편번호 API**(join_apply 와 동일).
  ③**희망 서비스 필수** — 화면(jdLackSync)·서버(joinDocsSubmit) 둘 다. ★**승인이 만든 계약이 있으면
  자동 체크** : `selContGbList`(TBL_HOSPCONT_MST 의 CONACT_GB) → 화면 `jdConactEff` 가
  신청서 값이 비면 계약 구분으로 채움. 서버 검사도 같은 기준(신청값 없어도 계약 있으면 통과).
  ④**담당자 표 두 개씩**(총관리자·간호과 / 심사과·전산담당 — 12칸 두 줄). ⑤**동의 탭 통합** —
  서식2·3 을 한 탭에서 **좌우 2등분**(`.jd-agr2col`, 1100px 미만은 세로), 탭은 2개가 됨.
  jdLackSync 의 ● 매핑도 서식3 → f2 로.
- ⚠**사고·복구(밤)** : joinDocs.jsp 편집 중 마커 grep 실패(빈 값)로 awk 가 1~806행을 날렸다.
  **/tmp 조각(오늘 편집 원문 전부)과 아침 백업(joinDocs.bak.jsp)으로 전 편집을 재적용**해 복원 —
  보존된 꼬리와의 diff 가 tail-전용 편집 11건과 정확히 일치함을 확인 후 설치.
  ★교훈: **awk 줄삭제 전 `[ -n "" ]` 가드 필수**(빈 grep → NR<0 → 전부 삭제). 중간 파일은 지우지 말 것(이번 복구의 전부였다).
- **후속(밤) — 오류·메시지 정비** : ①저장 오류 원인 = `ASQ_DAY varchar(2)` 길이 초과(Data too long, 로그 실측)
  → 전 입력칸에 컬럼 길이 maxlength·숫자칸 제한·PC시작일 하이픈 제거, 서버는 길이·형식 오류를 구분 안내.
  ②PDF 만들기·제출이 미완성/파일없음일 때 <조용히 무반응>이던 것 → 남은 항목 경고.
  ③**알림·확인 전부 표준 ui-message.js 로**(상시 방침 — joinReq 승인 화면과 동일). Swal 은 폴백만.
  ④Swal 폴백 규격 통일(width 380·아이콘 48px — 메모리 규정), 담당자 표 이메일13%·전화8.5%,
  단계표시 알약을 제목 선상 좌측으로, 전체 동의(좌측 카드 제목 선상), 수정버튼 탭 옆, 로그인 비밀번호 ●표시.
- **후속(심야)** : ①저장 검사 = <항목별 메시지+포커스>(중간 저장 불가, 사용자 확정) ②도장 SEAL_IMG
  이중 인코딩 교정(update 도 FROM_BASE64 — 신청 INSERT 와 동일) + 손상 도장 onerror 감지→미등록 처리
  ③**selMyJoinReq 에 OCS_USER_PW 누락** — "PW 입력해도 초기화"의 원인(저장은 됐고 조회가 빈 값→재저장이 덮음)
  ④동의 체크 = 보기 잠금·<수정 모드 활성>(해제 저장 시 철회 N — upsert 가 덮음, 체크상태는 재그림 때 보존)
  ⑤도장 미등록 전용 안내(PDF·제출) ⑥도장 미리보기 72px ⑦**PDF 우측 잘림 = jdDocBox 가 zoom(1.1) 영역 안**
  → 밖으로 이동 + html2canvas scrollX/Y:0. ⚠이 밤에 마커 빗나감 사고 2회 더(ess 삭제·괄호 2개 유실) —
  <교체 전 앵커 검증 echo>와 <추출 JS 문법검사>가 아니었으면 못 잡았다. 규칙: 편집 후 반드시 jjs 검사.
- **후속(같은 날 오후) — joinDocs 화면을 원래 신규가입 모양으로** :
  ①**3개 탭**(컨설팅 의뢰서 · 원격접속·DB접근 동의 · 개인정보 수집·이용 동의) — 동의는 `FORM_NO` 로 나눠
  전문을 펼치고, 빠진 항목이 있는 탭에 **●** 표시(join_apply 와 같은 규칙). 대표자 도장은 서식2 자리.
  ②**필수표시 원복** — 수정 모드에 노란 바탕(`.jd-must`) + 라벨 `*` + 「노란색 바탕은 반드시 작성…」 안내.
  ③**입력 표 한 줄 3등분**(라벨+입력 3쌍 6칸, 주소·비고만 colspan).
  ④**신청자 내용 표시** — 신청 때 받은 신청자·로그인 계정(성명·직위·연락처·이메일·비고)을 읽기전용 그룹으로.
  ⑤**「승인 시 등록 안 된 내용 저장도」** — 병원이 채운 전산프로그램 정보를 `updateContOcsFromMy` 로
  **계약(TBL_HOSPCONT_MST)의 빈 OCS_COMPANY/ID/PW 에만** 채운다(관리자가 넣은 값은 CASE 가 지킴).
  병원마스터(주소·연락처·FAX)는 기존 `updateHospFromMy` 가 이미 반영.
  ⑥제출 서버검사에 **담당자(총관리자·심사과) 성명·전화·이메일** 추가 — 화면 jdLackSync 와 같은 기준.
  ⚠**함정 — `#jdAgree` 셀렉터**: 동의가 탭 3곳(`jdAgrF1~3`)으로 흩어지면서 옛 `#jdAgree` 를 보던
  `jdAgrData` 가 **조용히 0건**을 보냈다 → `#joinDocs .jd-agr` 로. 동의내역 표를 다시 만들면 이걸 되짚을 것.
- **검증**: 두 프로젝트 mvn 컴파일 통과 · joinReq/joinDocs/join_apply 인라인 JS 문법검사 통과 ·
  신청 화면 브라우저 실측(탭 0·필수 7칸·미입력 안내 6항목). ⚠**joinDocs 화면은 로그인이 필요해
  브라우저 실측을 못 했다** — 승인된 병원 계정으로 열어 저장→제출→메뉴 열림을 확인해야 한다.
- **배포(로컬만)**: tmp0(wnn_consult)·tmp1(wnn_medcost) 에 JSP·클래스·매퍼XML 복사 + web.xml touch.
  운영은 **WAR 재빌드 필요**(클래스·매퍼 변경 — JSP 교체만으로 안 된다). 커밋 안 함.

### [완료] 신규병원 가입신청 — **개발 중 숨김 열쇠 = Ctrl+Alt+R** (2026-08-24)
- 「신규병원 가입신청」은 아직 정식 오픈 전이라 **기본 숨김**이다(2026-08-19 도입). **Ctrl+Alt+R** 로 켜고 끈다.
  ***메뉴·링크가 안 보인다고 지워진 것으로 오해하지 말 것.***
  · 로그인 화면(wnn_consult) [LoginWinCT.jsp](../wnn_consult/src/main/webapp/WEB-INF/jsp/login/LoginWinCT.jsp) — 로그인 상자의 링크
  · 사이드바(wnn_medcost) [sidebar.jsp](src/main/webapp/WEB-INF/tiles/main/sidebar.jsp) `#adminJoinReqMenu` — 관리자 메뉴
- **저장키 = `sessionStorage['joinReqDev']`** — 두 화면이 **같은 키**를 본다. 운영은 두 앱이 같은 호스트라
  로그인 화면에서 한 번 켜면 사이드바에도 켜져 있다(로컬은 9080·8080 이라 origin 이 달라 각각 켜야 한다).
  브라우저를 닫으면 자동으로 꺼진다.
- **열쇠가 바뀐 내력**: `req` 타이핑 → `rg` 타이핑 → `Ctrl+R` → **`Ctrl+Alt+R`**(확정).
  ★`Ctrl+R`(새로고침)·`Ctrl+Shift+R`(강력 새로고침)은 **브라우저가 쓰는 키라 피했다** — 가로채면
  그 화면에서 새로고침을 잃는다. 특히 sidebar.jsp 는 **전 화면**에 붙어 앱 전체가 영향을 받는다.
  Ctrl+Alt+R 은 크롬·엣지에서 비어 있고, 파이어폭스 리더모드만 겹쳐 `preventDefault` 로 막는다.
- **★한/영 상태와 무관하게 먹어야 한다** — 한글 IME 에서는 `e.key` 가 `'r'` 이 아니라 `'ㄱ'` 으로 올 수 있어
  물리 키 코드 `e.code === 'KeyR'` 를 **함께** 본다. 조건에서 이걸 빼면 「한글일 때만 안 먹는다」가 된다.
- **정식 오픈 시**: 양쪽의 `display:none` 과 `joinReqDev` 스크립트만 지우면 된다.
- ※사이드바의 **QPS 개발자 플래그(`qpsDev` / `qps` 타이핑)** 는 **별개**다 — 같이 바꾸지 말 것.

### [완료] 장기입원 환자분율(14) — **181일 이상 0명인데 현황값 100%** (2026-08-24, 운영 적용 완료)
- **요청**(월분석보고서 검수): 「181일 이상 장기입원 환자가 **0명일 경우 수치가 100%** 로 확인되는데
  **0%로 수정**하는 것이 적절할 것 같습니다(5점 구간 % 기준이 20% 미만이기에). 예) 평택삼성요양병원」
- **원인** — 「산출값 0.00 이면 만점 처리」 규칙에 **14 가 08 과 함께 묶여** 있었다:
  `IF l_calcvalue = 0.00 AND in_cate_gory IN ('08','14') THEN … SET l_calcvalue = 100.00;`
  08(DUR 점검율)은 **높을수록 우수**라 만점=100% 가 말이 되지만, 14(장기입원)는 **낮을수록 우수**
  (5점 = 0.00~19.99%)라 **0% 가 맞다**. 100% 는 오히려 **최저 구간(1점 = 80~100%)** 의 값이라 오해를 부른다.
  ★**점수는 원래도 만점이었다** — 같은 블록이 `l_std_score = l_max_score`·`l_weightval = l_stdweight` 를
  함께 넣어 5점·가중치 5.00 을 줬다. **틀린 것은 화면에 보이는 현황값(CAL_VAL) 하나뿐.**
- ⚠⚠★★**같은 로직이 두 곳에 복제**돼 있다(2026-08-17 약사건과 같은 함정) — **둘 다 적용해야 한다**:
  · `SP_EVALUATION_INDICATORS_REGISTER` — 자료생성 시 `TBL_PAT_INDI.CAL_VAL` 에 **저장**
  · `EVALUATION_INDICATORS_VALUE`(FUNCTION) — 누적(`select_Hosp_Indi` L5026)·`SP_DASH_INDICATORS` 에서 **재계산**
  (`SP_EVALUATION_INDICATORS_CREATE2` 는 분모=전체·분자=181일이상 을 **넘겨주기만** 한다 — 손댈 것 없음)
- **조치 파일**(전부 `('08','14')` → `('08')` **한 줄**, 나머지는 운영 원본 그대로):
  · [FN_EVALUATION_INDICATORS_VALUE_2026-08-24.sql](docs/sql/proc/FN_EVALUATION_INDICATORS_VALUE_2026-08-24.sql) — **적용 완료 08-24 08:53**
    · 백업 [BACKUP_…_20260824.sql](docs/sql/proc/BACKUP_FN_EVALUATION_INDICATORS_VALUE_20260824.sql)
  · [SP_EVALUATION_INDICATORS_REGISTER_2026-08-24.sql](docs/sql/proc/SP_EVALUATION_INDICATORS_REGISTER_2026-08-24.sql) — **적용 완료 08-24 08:28**
    · 백업 [BACKUP_…_20260824.sql](docs/sql/proc/BACKUP_SP_EVALUATION_INDICATORS_REGISTER_20260824.sql)
  · 과거 자료 보정 [FIX_LONGADM14_CALVAL100_2026-08-24.sql](docs/sql/evalrpt/FIX_LONGADM14_CALVAL100_2026-08-24.sql) — **실행 완료**
    — **26건 · 10개 병원 · 202407~202612**(평택삼성 202509~202512·202606~202608 7건 포함)의 `CAL_VAL` 100.00 → 0.00.
    ⚠**백업 테이블은 만들지 않았다**(UPDATE 만 실행) — 되돌리려면
      `UPDATE TBL_PAT_INDI SET CAL_VAL=100.00 WHERE CATE_CD='14' AND UPD_USER='fix20260824';`
      (보정 전 값이 26건 모두 100.00 으로 같아 표식만으로 복구된다. 단 UPD_USER 원값은 못 살린다.)
    **점수 컬럼은 건드리지 않는다**
    (26건 전부 이미 `S_SCORE=5 · WEIGVAL=STDWEIG · MAX_VAL=5` 임을 실측 확인).
  ★**프로시저·함수를 먼저** 적용할 것 — 안 그러면 자료 재생성 때 100 으로 되돌아간다(2026-08-17 약사건 재현).
- **회귀 없음 근거**: `TBL_WEVALUE_MST` 의 14/5점 구간이 **0.00~19.99**(2024·2025·2026 판 모두 동일)이라
  0.00 도 일반 경로(구간 조회)에서 그대로 **STD_SCORE 5 · WEIGVAL `TRUNC(5/5*5)`=5.00** 을 받는다.
- **검증(운영 실측 2026-08-24)** :
  · 함수 직접 호출 — `EVALUATION_INDICATORS_VALUE('1','14','202612',14,0)` = **0.00**(전 100.00),
    `('2',…)` = **5.00**(가중치 만점 유지), `('1','08',…)` = **100.00**(08 은 종전 그대로 — 안 건드림).
  · 대상 26건 : `CAL_VAL=100` **잔여 0건** · `0.00` 26건 · `UPD_USER='fix20260824'` 26건 ·
    점수 4컬럼(`S_SCORE`·`STDWEIG`·`WEIGVAL`·`MAX_VAL`) **26건 전부 5/5.00/5.00/5** 회귀 없음.
  · `fix20260824` 로 바뀐 행은 **전부 CATE_CD='14'** — 다른 지표 오염 없음.
  · 정상 산출 행(14 중 분자>0) **770건 불변**(가중치 합 2,455.80, 손댄 행 0).
  · 누적 경로도 확인 — 평택삼성 2026 평가기간(7~8월) 분모 8·분자 0 → **현황값 0.00% · 가중치 5.00**.
- **JSP 수정 없음**: [evalReport.jsp](src/main/webapp/WEB-INF/jsp/main/evalReport.jsp) 는 `cal_val` 을 그대로 찍고,
  누적은 함수로 재계산하므로 DB 만 고치면 보고서·적정성평가 화면이 함께 0% 로 바뀐다.
- **배포**: DB 프로시저/함수 + 데이터 보정만 — 앱 변경·재기동 불필요.
- **함께 확인한 것(별건, 손대지 않음)**: 같은 블록에서 **08(DUR 점검율)** 은 분모>0·분자=0 이어도
  100%·만점이 된다. 「점검 대상이 있는데 하나도 안 했다」와 「대상이 없다」가 구분되지 않는다.
  이번 요청 범위 밖이라 **그대로 뒀다** — 담당자 확인 필요.

### [완료] 가입신청 → **별도 계약정보 입력창** (hospcd.jsp 연동 폐기) (2026-08-21)
- **요청**: 「계약정보입력 시 hospcd.jsp 연동하지 말고 **별도 계약정보입력창**(입력 시 전산프로그램 정보도
  끌고 오게), 적정성평가·진료비분석 **두 가지 계약정보**」 — 08-20 의 「계약관리로 화면 이동」 방식을 대체한다.
- **조치**([joinReq.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinReq.jsp) 단독, **서버 무변경**) :
  `jrcOpen(hospCd, reqNo)` 오버레이 창 신설. [계약정보 입력]·목록 [계약입력] 단추가 이 창을 연다
  (더 이상 `/user/hospcd.do?hospCd=` 로 안 간다 — 화면이 안 바뀌어 보던 신청을 잃지 않는다).
  - **두 계약을 한 창에** : 구분(CONACT_GB)마다 카드 하나 — 체크로 켜고 끈다. 구분 목록은
    `/base/commList.do`(공통코드) 가 정본, 못 받으면 1·2 기본. **적정성평가(2)를 앞에** 정렬.
    처음 체크 = 기존 계약 있으면 켬 / 없으면 **신청서의 희망 서비스**(A=둘 다)를 따른다.
  - **전산프로그램 정보 끌어오기** : `joinReqInfo.do` 의 OCS_COMPANY/ID/PW 를 카드에 미리 채운다
    (기존 계약 값이 있으면 그게 우선). [신청서 값 넣기] 단추로 언제든 다시 덮을 수 있다.
    심평원 인증서암호는 **계약에 저장 안 하므로 상단 안내에만**(참고용) 보여 준다.
  - **재사용 서버** : `hospCdList.do`(UUID·가입일 — 계약 저장에 HOSP_UUID 필수) · `hospContList.do`
    (기존 계약, 구분별 **최신 건**이 수정 대상) · `hospContInsert/Update/Delete.do` · `commList.do`.
    ⚠응답이 JSON 아닌 `"OK"` 텍스트 — ajax `dataType:'text'` 필수(json 이면 정상 저장도 오류로 보인다).
  - **날짜 규약** : 화면 input[type=date](yyyy-mm-dd) ↔ DB yyyymmdd. 승인일자·중지일자는
    시작·종료일을 따라가되 **손으로 고친 칸(data-man)은 안 덮는다**(이 두 값이 로그인 사용기간 판정).
  - ★**기간을 바꾼 수정은 「새 행 INSERT → 옛 행 소프트삭제」 순서** — 반대로 하면 INSERT 실패 시
    계약이 통째로 사라진다. 기간 그대로면 기존 `hospContUpdate.do`(내리고 새로넣기) 한 번.
  - ⚠**Swal z-index 함정** : 창 오버레이가 12500(자주쓰는메뉴 12000 위)이라 Swal 기본 1060 이
    **뒤에 깔린다** → `body .swal2-container{ z-index:13000 !important }` 를 같이 넣었다.
    저장 확인창이 안 보이면 이것부터 볼 것.
- **hospcd.jsp 는 안 건드렸다** — 08-20 에 넣은 `?hospCd=` 자동선택·[◀ 가입신청으로] 블록은 남아 있지만
  이제 아무도 그 파라미터를 안 보내 **잠자는 코드**다(메뉴로 여는 계약관리는 종전 그대로).
- **검증**: jsdom 으로 배포 JSP 를 그대로 태워 **48케이스 전부 통과**(신규 2건 INSERT·희망별 체크·
  기존값 로드·수정/기간변경 경로·병원없음 안내·삭제 키 4종·날짜 검증·수동일자 보존) +
  구조검사(div/script/주석 짝·잘린 태그 0)·함수검사(정의 없는 호출 0). 로컬 8080 이 꺼져 있어
  브라우저 눈 검증은 다음 기동 때(Eclipse tmp1 배포본에는 복사해 둠).
- **[다듬기 2026-08-21] 화면 요소 3가지 제외(사용자 표시 지시)** : ①상단 「신청서에서 가져온 값」 안내 박스
  ②[신청서 값 넣기] 단추·MASTER 힌트(값은 여전히 **자동으로** 채워진다 — jrcPullPgm 함수는 남겨 둠, 단추만 되넣으면 부활)
  ③계약 담당 「이메일」 줄 — ⚠칸만 뺐고 **저장 때는 기존 계약의 conEmail 을 그대로 물려** 수정해도 지워지지 않는다.
  jsdom 53케이스 재검증 통과.
- **[확정 2026-08-21 저녁] 메시지 = ui-message.js(코네트 스타일 흰 카드)** — 같은 날 두 번 바뀌어 확정 :
  ①「기존하고 틀림」 → messageBox·small-swal 로 맞춤 ②「sejong_vsweb(코네트) 있는 내용으로」 →
  **ui-message.js 위임으로 재확정**(현재). ★joinReq 는 맨 위에서 /asset/js/ui-message.js 를 싣고 있었는데
  ***IIFE 지역 _alertBox/_confirmBox 가 같은 이름으로 전역을 가리고 있었다*** — 지역 함수는 이제
  `window._alertBox(h)` / `window._confirmBox({msg,onOk})` 위임만 한다(개행→<br> 변환 포함).
  ui-message 없는 곳(wnn_consult 복사 시)은 Swal→alert 폴백. **저장·삭제 성공 알림은 없다**(「마지막
  결과확인 없애기」 — 실패 때만 알림, 결과는 창 재렌더의 「기존 계약」 배지로 보인다).
  ⚠z-index 함정 : ui-message 의 .cfm-backdrop 은 **10000** 이라 계약정보 입력창(12500) 뒤에 깔린다 →
  jrcMask style 이 `#confirmBackdrop` 을 13000 으로 올리고 폭 480px(날짜 줄바꿈 방지)도 이 화면에서만 덮는다.
  ★[이어서] 승인·반려·가입취소·반려취소·승인취소 **다섯 큰 창도 같은 모양으로**(「전부 다 이 스타일」) —
  ui-message 에는 입력칸이 없어 화면 전용 `_jrAsk`(제목+본문+선택 입력칸+빨강/teal 단추)를 신설,
  **ui-message 가 주입하는 .cfm-* CSS 클래스를 빌려 써** 모양이 완전히 같다(공용 파일 무수정).
  빈 사유는 서버로 안 가고 인라인 오류. 실서버 렌더 실측 : 승인취소=빨강+입력칸+검증 ·
  반려취소/승인=teal(jr-ok-teal) · Swal 미사용 · z 13000.
  jsdom 59케이스 통과. ⚠하네스 함정 2 : jrList 는 jQuery ready(비동기)라 테스트에서 직접 부른다 ·
  h3.js 끝 process.exit 뒤에 테스트를 붙이면 조용히 안 돈다.
- **[다듬기 2026-08-21 밤] 계약 두 구분을 탭으로 분리**(「두 개 탭을 분리」) — 좌우 두 카드 → 탭 띠
  (적정성평가/진료비경영분석) + 한 장씩. 창 폭 1180→820px. ★숨은 탭의 입력칸도 DOM 에 있어
  **저장은 종전대로 두 구분을 함께** 본다(jrcSave 무변경). 탭 단추의 점 = 그 구분 체크 상태(초록=등록 켜짐).
  처음 탭 = 보던 탭 > 기존 계약 있는 구분 > 체크된 구분 > 첫 구분. 승인창 「승인신청을 완료하고
  계약정보를 입력하세요」 · 반려/가입취소/승인취소 문구도 같은 날 한두 줄로 간결화(사용자 문구 그대로).
  jsdom 65케이스(탭 6건 추가) 통과.
- **배포**: JSP 1개 — **파일 교체만**(WAR 재빌드 불필요).

### [완료] 신규병원 가입신청 → 계약관리 바로가기 (승인 병원 자동 선택) (2026-08-20)
- **요청**: 「신규병원 가입신청에서 **승인된 병원을 고르면 계약정보 입력 화면**(hospcd.jsp)이 열리게」 —
  종전에는 승인 완료 안내에 *"계약은 [계약관리] 화면에서 등록합니다"* 라고 **글로만** 적혀 있어,
  사용자가 메뉴에서 계약관리를 다시 찾아 그 병원을 검색해야 했다.
- **조치 ①**([joinReq.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinReq.jsp)) : 상세 패널에 **[계약정보 입력]** 단추 신설.
  **승인건(reqStat 30·40)에서만** 보인다 — 승인 전에는 병원 자체가 아직 없다(승인 때 병원·사용자·회원이 생긴다).
  누르면 `/user/hospcd.do?**hospCd**=<요양기관기호>` 로 **같은 창** 이동(메뉴로 여는 화면과 같아 뒤로가기로 복귀).
  `CUR_HOSP` 에 상세조회(`joinReqInfo`)의 `hospCd` 를 담아 쓴다. 승인완료 안내 문구도 단추를 가리키게 고쳤다.
- **조치 ②**([hospcd.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/hospcd.jsp) 맨 아래 「가입신청에서 넘어온 병원 자동 선택」) :
  `?hospCd=` 가 있으면 ⓐ**조회칸(`#findData`)에 값을 넣고 `findField()` 로 내부 조건(`findValues`)까지 맞춘다**
  → `window.onload` 의 `find_Check()` 가 그 조건으로 목록을 만든다 ⓑ`draw.dt` 에서 그 줄을 찾아 **`click` 을 실제로 일으킨다.**
  - ★**값만 채우면 안 된다** — 아래 3패널(계약정보 `hcontLoad` · 사용자정보 · 이메일정보 `he_setHosp`)이
    전부 **상단 그리드 행 click 에 매달려** 있다. 그래서 반드시 클릭을 발생시켜야 계약 입력 준비가 끝난다.
  - `draw.dt` 는 document 에 위임하고 **`e.target.id === 'tableName'`** 로 상단 그리드만 본다
    (아래 패널 그리드도 draw 를 쏜다). 찾으면 즉시 unbind, 못 찾으면 5회까지만 시도하고 조용히 그만둔다.
  - **서버는 손대지 않았다** — `UserController.hospcd` 는 그대로고 파라미터는 화면에서만 읽는다.
    `selHospCdList` 의 `findData` 는 `HOSP_CD LIKE` 라 8자리 기호로 그대로 걸린다.
- **조치 ③ 계약 등록 모달 자동 열기 + 돌아오기**(2026-08-20 추가 요청) :
  - **계약 등록 모달은 그 기관에 「바로 묶어서」 연다** (2026-08-20 재요청 *「해당 선택기관으로 바로 연결해야함,
    지금은 찾아서 함」*). ***그리드에서 줄을 찾아 클릭하는 것에 기대지 않는다*** —
    `/user/hospCdList.do` 에 **`hospCd`(정확일치)** 로 그 기관 **한 건만** 받아
    `hctmpedit_Data`(`hospCd_one`·`hospUuid_one`·`joinDt_one`)를 직접 채우고 `hc_modal_Open('I')` 를 부른다.
    ⚠**계약 저장에는 `HOSP_UUID` 가 필요**해서 이 한 건 조회는 없앨 수 없다(기호만으로는 못 넣는다).
    ⚠**한 번만** 연다(`opened` 플래그) — 닫았는데 또 뜨면 손을 못 쓴다.
    계약구분·시작일(가입일)·종료일 등 기본값은 2026-08-19 에 넣어 둔 `hc_modal_Open` 의 'I' 분기가 채운다.
    그리드 줄 선택(`pick`)은 **모달을 닫은 뒤 아래 패널이 그 기관 것으로 보이게** 하는 용도로만 남았다.
  - **[◀ 가입신청으로]** 단추를 띄운다(가입신청에서 넘어왔을 때만 — 계약관리는 메뉴로도 여는 공용 화면).
    ⚠**z-index 는 모달보다 낮게(1000)** — 높이면 자동으로 열린 계약 모달 위로 단추가 떠서 모달을 가린다.
  - 앞뒤로 **`reqNo` 를 함께 들고 다닌다** : joinReq → `?hospCd=..&reqNo=..` → 돌아올 때 `?reqNo=..` →
    joinReq 가 목록 조회 뒤(0.5초) `jrInfo(reqNo)` 로 **보던 신청을 다시 펼친다**(승인·반려 뒤 재조회와 같은 방식).
- **조치 ④ 「안 된다」의 실제 원인 두 가지**(2026-08-20 재보고 *「해당병원 못찾아가고 다시 돌아오는 기능 없음」*) :
  1. **단추가 아예 안 보였다** — 승인건(30·40)에서만 보이게 했는데 시험 자료가 **접수(10)** 였다.
     ⇒ ***단추는 늘 보이고, 승인 전에는 눌리지 않게***(회색 + 툴팁 «승인해야 병원이 만들어집니다»).
     감춰 두면 "그런 기능이 없다" 로 읽힌다. **없는 것과 못 쓰는 것은 다르게 보여야 한다.**
  2. **모달이 빈 계약구분으로 뜰 수 있었다** — 계약구분 드롭다운은 `window.onload` 의 `comm_Check()` 가
     `/base/commList.do` 로 채우는데, 그 전(DOM ready)에 모달을 열고 있었다.
     ⇒ **window load 이후 + 옵션이 실제로 들어온 뒤**(0.2초 간격 최대 4초) 연다.
- **조치 ⑤ 목록에 「계약」 칸 신설**(2026-08-20 *「버튼 변경했는데 바뀐 게 없음」*) —
  단추가 **상세 패널 안에만** 있어서 ***목록만 보면 아무 변화가 없어 보였다.***
  ⇒ 목록 오른쪽에 **계약** 칸을 더해 **승인건(30·40)에만 [계약입력]** 단추를 둔다(줄 클릭=상세 열기와
  겹치지 않게 `event.stopPropagation()`). 칸 수가 9→10 이라 `<colgroup>`·`<th>`·빈 목록 `colspan` 을 함께 고쳤다.
  ⚠**칸을 더/뺄 때 이 셋을 같이** 고치지 않으면 표가 어긋난다.
- **★★[진짜 원인 · 2026-08-20] 「눌러도 아무 일 없음」 = 주소의 물음표 뒤가 통째로 지워진다**
  tiles 템플릿 [main.jsp](src/main/webapp/WEB-INF/tiles/main/main.jsp) 가 **`<head>` 에서**
  `history.replaceState({...}, '', '/user/dashboard.do')` 로 ***주소를 숨긴다*** — 이때 **쿼리스트링이 사라진다.**
  `<head>` 라 화면 본문 스크립트보다 **먼저** 돌아서, 본문에서 `location.search` 를 읽으면 **언제나 빈 값**이다.
  ⇒ ***이 앱에서 화면끼리 값을 주소로 넘길 때는 반드시***
  `location.search` **+** `sessionStorage.getItem('_realPath')`(main.jsp 가 지우기 전에 담아 둔 원래 경로)를
  **함께** 읽어야 한다. 사이드바 `qpsdev` 스위치가 같은 함정을 먼저 겪고 같은 방법을 쓴다(sidebar.jsp).
  hospcd.jsp(hospCd·reqNo) · joinReq.jsp(reqNo) 양쪽 모두 이 방식으로 고쳤다.
- **✅검증**(2026-08-20, 로컬 8080 브라우저 실측) : `/user/hospcd.do?hospCd=11223344&reqNo=2` →
  조회칸 자동 입력 · **계약 등록 모달 자동 열림(「입력 모드입니다」)** · `hospCd_one`/`hospUuid_one` 채워짐 ·
  기본값(계약구분 2·시작 2025-05-01=가입일·종료 2099-12-31·사용 Y) · 그리드 그 줄 선택 ·
  [◀ 가입신청으로] → `/join/joinReq.do?reqNo=2` 로 돌아와 **그 신청 상세가 다시 펼쳐짐**.
- **★로컬 확인 경로**(2026-08-20 실측) : Eclipse 배포본은
  `D:\egv\.metadata\.plugins\org.eclipse.wst.server.core\**tmp1**\wtpwebapps\wnn_medcost\`
  (tmp0=wnn_consult · tmp2=sejong_web · tmp4=Sejong_APP). `target/wnn_medcost-1.0.0/` 은 **낡은 사본**이라
  여기 고쳐도 화면에 안 나온다. CLI 로 `src/` 만 고치면 Eclipse 가 모르므로 **위 경로에 복사**하거나 F5(Refresh).
- **조치 ⑦ 상태 필터 기본값 = 「전체 상태」**(2026-08-20 요청 *「선택 바꾸지 않는 한 전체상태로」*) —
  종전 기본이 **접수(10)** 로 박혀 있어서 ***승인하는 순간 그 줄이 목록에서 사라졌다***
  (「없어졌다 / 안 된다」로 읽히던 원인 중 하나). `<option value="" selected>전체 상태</option>` 로 옮겼다.
- **조치 ⑧ 사용자 「신청서 제출」 화면**([joinDocs.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/joinDocs.jsp), 2026-08-20) —
  ①**주소·상세주소를 한 줄로**(각각 `<tr>` 이던 것을 4칸 한 줄로) ②**글자 축소·확대 신설**
  (`jdZoom` · `#jdZoomBox` · localStorage `wnnJoinDocsZoom` · 기본 1.1 — joinReq 와 같은 규칙).
  ⚠**줌 범위는 본문(`#jdZoomBox`)만** — 머리줄(제목·단추)은 밖에 둔다.
  ③**이름을 「신청서 제출」 → 「동의서 제출」로**(2026-08-20 요청) — 화면 제목 · 단계 ③ ·
  제출 카드 제목 · 제출 확인창, 그리고 **사이드바 안내 배너**(sidebar.jsp `동의서를 제출하셔야…` ·
  `동의서 제출하러 가기 →` · 잠긴 메뉴 tooltip)까지 함께.
  상태값 40 의 이름이 이미 '동의서제출'(joinReq 상태 콤보)이라 그쪽과도 맞는다.
  **PDF 쪽 이름도 같이**(2026-08-20 «신청서 PDF도 동의서로») : 단추 `동의서 PDF 만들기` · `동의서 미리보기` ·
  **파일명 `동의서_<기호>_<신청번호>.pdf`**(종전 `가입신청서_…`) · 인쇄창 title · 제출완료 안내문.
  ⚠***문서 1장째의 제목(「위너넷 적정성 컨설팅 의뢰서 / 신규병원 가입신청서」)은 그대로 뒀다*** —
  그건 **서식 자체의 이름**이다(2·3장이 동의서). 그걸 바꾸는 건 서식 문구 변경이라 별도 확인이 필요하다.
  ④**「대표자 도장·담당자」 줄 높이 축소**(2026-08-20 «위아래 조금 축소») —
  ***그 줄의 높이는 담당자 글줄이 아니라 도장 그림이 정한다*** ⇒ `jdSealTag(120→96)`
  (+ 담당자 줄 `padding 2→1px`·`line-height 1.45`). 줄 높이 실측 ≈150 → **123px**. 인쇄용 도장 크기는 무관.
  ⚠***이 두 가지를 hospcd.jsp 에 했다가 되돌렸다***(2026-08-20 «hospcd.jsp 아님») —
  「사용자 가입신청 화면」은 **joinDocs.do**(병원이 승인 후 신청서를 제출하는 화면)다. 헷갈리지 말 것.
- **조치 ⑥ 기본 글자 크기 한 단계 크게**(2026-08-20 요청) — 두 화면 모두 **1.0 → 1.1** 로 시작.
  · 관리자 : joinReq.jsp `JR_Z_DEF` · 병원(사용자) : **wnn_consult** `login/join_apply.jsp` `JA_Z_DEF`
  · [↺ 처음 크기로] 도 1.0 이 아니라 이 값으로 돌아간다. **조절은 그 상수 한 줄.**
  · 저장된 개인 설정(localStorage `wnnJoinReqZoom`·`wnnJoinZoom`)이 있으면 **그게 우선**이라,
    이미 한 번이라도 가－/가＋ 를 누른 PC 는 그 값이 그대로 뜬다(↺ 를 눌러야 새 기본으로 온다).
- **배포**: JSP 2개만 — **WAR 재빌드 불필요**(파일 교체).
  ⚠글자 크기 건은 **wnn_consult 의 join_apply.jsp 도 함께** 올려야 한다(다른 프로젝트·다른 WAR).

### [완료] 월보고서 검수 4건 — DUR 글씨체·최고구간 문구·HbA1c 정의·결론 삭제 (2026-08-17, 검수 박혜련)
- **①DUR(ana_08) 글씨체가 그 지표만 다르다**: `.er-ana` 본문은 **700**인데 DUR 고정문만
  `font-weight:400` 을 지정 — 맑은 고딕은 400·700 이 **다른 글꼴 파일**이라 서체가 달라 보였다.
  JSP 고정문을 700 으로 정정(브라우저 실측 `B:900·SPAN:700·SPAN:700` = ana_09 와 동일).
  ⚠**처음에 「남들이 800이겠지」로 넘겨짚어 800으로 바꿨다가 더 달라졌다** — 반드시
  `getComputedStyle` 로 재고 고칠 것. 저장본 3행은 DB 정정(백업 `TBL_EVAL_REPORT_TEXT_BAK_20260817*`).
- **②편집기가 심는 font-family/size 차단**: `stripFontStyle()` 신설(collectTexts 저장 시 글꼴·크기만
  제거, 색·굵기는 유지). 기존 오염 8행은 텍스트 동일 확인 후 DB 정리.
- **③승인본 서식 정규화 = 선택 C**(사용자 확정): 「승인이어도 DUR 글씨체 바뀌게」 요청은
  재생성 방식(ALWAYS_AUTO)이 **대전제(승인본 불가침) 위배**라 — 고정문 속 `가중치 N점` 이
  현재값으로 재계산돼 확정 숫자가 바뀔 수 있다 — 보고 후 C 로 확정 :
  **`normalizeWeight()`** = 저장본 HTML 을 **글자 그대로** 쓰되 그릴 때 **700 미만 굵기 지정만** 제거
  (400·normal 제거 / 700·800 유지 — 담당자가 넣은 「굵게」는 뜻이 있어 남긴다).
  승인본 실검증(31287051/202608): 렌더 텍스트 = DB 저장본 **완전 일치** + 굵기 정상.
- **④「현재 최고 구간으로 추가 개선 여지 없음(유지)」 삭제**: 누적 만점 지표의 점수상향목표 줄 제거.
- **⑤HbA1c 지표정의**: 「당뇨병 상병 환자 중」→「당뇨 환자 중」. ⚠정의는 **JSP+운영DB 템플릿 동시**
  수정([DEF13 SQL](docs/sql/evalrpt/EVALRPT_TPL_DEF13_2026-08-17.sql) 운영 적용·확인 완료,
  시드·08-11 SYNC 파일도 동기화). def_13 은 병원별 저장본 없음 확인 → 전 병원 즉시 반영.
  분모 라벨('당뇨병 상병 환자')·개선방향(dir_13)은 요청 범위 밖이라 유지.
- **⑥「결론」 박스 통째 삭제**(concl·concl_note): 병원과 무관한 고정 문구라 기관마다 고칠 게 많다는
  지적. 화면 블록 제거 = 전 병원 적용(JS 는 data-key 를 훑으므로 코드 수정 불요).
  **TBL_EVAL_REPORT_TEXT 의 concl 행은 안 지운다** — 되살리면 옛 편집본이 그대로 돌아온다.
  ⚠승인 때 굳은 **기존 PDF 에는 결론이 남아 있다** — 재승인해야 새로 생성됨.
- **⑯★구조영역 목표를 「현재 구간에서 1단계만」으로 제한**(검수 : 「구조영역을 모두 다 5점구간으로 적용해서
  제공하고 있으나 현재 표준화 구간에서 1단계만 올릴 수 있는 값으로 수정 … 구조영역 30점 만점으로
  진행하지는 못하는데 기본 세팅값이 30점 만점이라 수정이 필요합니다」):
  Ⅳ 로드맵 시나리오(`renderSec5`)의 종전 규칙은 **최우선 2개=+2구간 · 나머지=+1구간(상한 5)** 이라
  구조영역도 **5점 구간(=30점 만점)** 까지 끌어올려 ***현실에서 도달 못 하는 목표***를 제시했다
  (구조영역은 **인력 충원**이라야 오르므로 단기간에 여러 구간을 못 뛴다).
  ⇒ `cate_fg==='10'` 이면 **`min(5, s+1)`**, 진료영역은 종전대로. **한 줄**이면 되는 이유 =
  이 tz 가 시나리오 표·상승분·**개선 후 예상 종합점수**에 모두 쓰이므로 아래 숫자가 함께 맞는다.
  ★**다른 곳은 손댈 필요가 없었다**(확인함) : `goalUp`(Ⅲ 점수상향목표)은 `GOAL_VERB` 에 05·06·09~14 만 있어
  ***구조영역을 아예 다루지 않는다*** · `simStep`(총평 시나리오)은 이미 `s+1` · 구조영역 「개선 여지」는
  **다음 구간 경계**로 계산(=1단계) · `simNeed`·`simReduceList` 는 **호출되지 않는 죽은 코드**.
  ⚠종전에 함께 문제였던 「구조영역 (30)」 합산 줄은 ⑦에서 이미 삭제됐다.
  ✅검증 : 배포된 JSP 에서 **규칙식을 꺼내 Node 로 전 경우 계산** — 구조영역은 순위와 무관하게
  1→2·2→3·3→4·4→5·5→5(**1단계 초과 0건**), 진료영역 1·2위는 +2 유지. 실화면(31286003/8월)에서도
  구조영역 「의사 1인당 환자수」 3→**4구간**, 상승분 합계 11.1 = 화면 표시 `+11.1`(예상 84.5·2등급) 일치.
- **⑦「참고 : 현재 vs 목표 점수 비교」 표(er-cmpBody) 삭제**: 구조영역을 30점 만점까지 오를 수 있는
  것처럼 보여 **현실과 어긋난다**는 지적(실제로 만점에 못 닿는 기관이 많아 문의·문제 소지).
  로드맵의 시나리오 표·「개선 후 예상 종합점수」 줄은 유지. ⚠HTML 표와 **renderSec5() 끝의
  채움 코드를 함께** 지워야 한다 — `el()` 이 null 이라 한쪽만 지우면 renderSec5 가 죽어
  captureAuto 까지 안 돌고 **저장 필터가 통째로 깨진다**(되살릴 때도 두 곳 함께).
- **⑧총평 P4(sum_p4) = 고정 한 문장으로 교체**: 「총평에 처방률이 0.0%로 나오면 안 된다」 지적.
  종전엔 기본 문장 뒤에 전월 실측 처방률 문형(N1)·항정 리스크 문형(P4)이 동적으로 붙어
  「7월 처방률은 0.0%로 표준화 5점 구간…」 수치가 노출됐다 → 검수 확정문
  「…예상값을 기준으로 산출한 결과이므로 청구자료 및 최종 평가 결과에 따라 종합점수는 일부 변동될
  수 있음.」 하나로 고정(수치·구간·상승/하락 예측·꼬리문 전부 제거. 원형은 git 이력에).
  `_psyPrev` 계산부는 잔존(무해). 운영 DB 의 sum_p4 저장본 3건에 옛 수치 문구 없음 확인 —
  자동 문구라 전 병원 즉시 반영. 저장본 없는 병원(31288481/8월)으로 문장 완전 일치 실측.
- **⑨★승인취소 후 편집한 총평이 사라지던 버그**(「수정했던 내용은 유지되어야」 — 실사고 신고):
  `savedKeys`(저장본 표시)가 **loadSavedTexts(조회 시점)에만** 채워지고 doSave 성공 때는 안 채워졌다.
  ⇒ 「편집→저장→승인→승인취소」 하면 승인취소의 `renderGoalSummary()` 재렌더가 총평(sum_p1~5)을
  <저장본 없음>으로 보고 **자동 문구로 덮음**. 더 나쁘게는 그 상태로 재저장하면 덮인 문구=AUTO 라
  저장 제외 → 서버 DELETE 후 재INSERT 라 **DB 행까지 영구 소실**. **고침** = doSave 성공 콜백에서
  `payload.texts` 의 sectKey 를 savedKeys 에 **병합**(초기화 금지 — applyTpls 의 전사 TPL 마킹이 날아감).
  검증 = 가짜병원 99999998 로 편집→저장→승인→승인취소→편집켜기 전 과정 왕복, 문구 보존 확인 후
  시험 보고서 삭제(MST·TEXT·HST 잔여 0).
- **⑩★편집 화면 줄바꿈과 변환본 간격이 어긋나던 것**(「엔터로 맞춘 간격이 변환하면 뒤죽박죽」):
  ***글줄 폭이 서로 달랐다.*** 편집 중 보이는 것은 원본 `.er-page`(880px − 좌우 50px = **본문 780px**),
  변환·PDF·인쇄에 쓰이는 것은 A4 복제본 `.er-autopage`(210mm − 좌우 15mm = **180mm ≈ 680px**).
  **100px(약 7~8글자) 차이**라 같은 문장이 다른 자리에서 꺾이고, 화면에 맞춰 넣은 수동 엔터가
  종이에서 엉뚱한 곳에 떨어졌다. ⇒ `.er-page` 를 **`width:210mm; padding:46px 15mm`** 로 바꿔
  본문 폭을 **180mm 로 일치**(`#evalReport *{box-sizing:border-box}` 확인).
  ★**종이 결과물은 안 바뀐다** — 인쇄·PDF 는 예전에도 복제본(180mm)을 썼다. 화면만 종이에 맞춘 것.
  분할이 꺼진 폴백 인쇄(`:not(.er-paged)`)는 `width:100%` 로 덮여 무관.
  함께 고침 : **타이핑 중 재분할** — 엔터를 쳐도 `erPaginate` 가 안 돌아 「N장 시작」 표지가 낡은
  자리에 남았고 사용자는 그 **틀린 경계**에 맞춰 간격을 조정했다 ⇒ input 에서 0.6초 디바운스 재분할
  (원본 DOM 을 안 건드려 커서가 안 튄다 — 복제본만 다시 만든다).
  ✅**브라우저 실측 완료**(창을 띄운 뒤) : 본문 폭 **681px = 681px** · 편집영역 **54문단 전부 높이
  일치(불일치 0)** = 줄바꿈 자리가 같다 · 표지 안내 박스도 폭 680·**줄 수 3,4,5,3,3 동일**·가로 넘침 없음 ·
  재분할은 input 뒤 실제로 돌고(호출 확인) **원본 노드는 그대로**(커서 안전), 복제본에만 반영.
  시험 자료 정리 : 자동보관 초안(IndexedDB `wnnEvalReportDraft`) 삭제 · DB 저장본 오염 0.
  ⚠**브라우저 측정 함정 재확인** : ***창이 숨겨져 있으면 렌더가 억제돼 `innerWidth=0`*** 이 되고
  `max-width:100%` 요소가 몇 px 로 접혀 **측정값이 전부 거짓**이 된다(이번에도 폭이 36px 로 나왔다).
  ***재기 전에 `innerWidth` 를 먼저 확인할 것.***
  ⚠검증 중 알아낸 것 : 표지 불릿의 실제 줄 수는 **3·4·5·3·3** 이다(위 2026-07-23 항목의 「3·3·4·3·3」은
  부정확한 기록). 고정 `<br>` 뒤에서도 문장이 길어 자연 줄바꿈이 더 일어난다.
- **⑪Ⅴ 의무기록 안내문 — 「한글·워드 가능」인데 탐색기에 .hwp 가 안 보인다는 신고**:
  ***기능 결함이 아니라 안내문이 두 갈래를 뭉쳐 쓴 탓이다.*** 한글·워드는 **복사→붙여넣기(Ctrl+V)** 길,
  탐색기는 **PDF·이미지 잘라오기** 길이다. 잘라오기는 파일을 캔버스에 그려 오려내므로
  **브라우저가 .hwp 를 그릴 수 없어** 파일로는 못 받는다(accept 에 .hwp 를 넣지 않는다 —
  넣으면 골라도 안 열려 더 헷갈린다). ⇒ ①툴바 힌트를 **①한글·워드 / ②PDF·이미지 두 갈래로** 갈라
  적고 「.hwp 는 복사·붙여넣기 또는 PDF 로 저장」 안내 추가 ②**버튼 이름에 종류를 박음**
  (`📂 탐색기 열기` → **`📂 PDF·이미지 열기`**, tooltip 도) ③빈 영역 안내문도 같은 말로 통일.
  ⚠***안내문이 세 군데(툴바 힌트·빈 영역·버튼 tooltip)다 — 고칠 때 함께 볼 것.***
  ✅**한글·워드 둘 다 실제로 된다** — 각 프로그램이 클립보드에 싣는 형태로 붙여넣기를 태워 확인 :
  워드(`MsoNormal`·`mso-` 스타일·pt 고정폭) = **맑은 고딕 10pt·색·표 정상** ·
  한글(`HStyle0`·hwpTable) = **함초롬바탕 10pt·파란색·표 정상**. 붙여넣기 처리는 **클립보드를
  가로채지 않고 브라우저 기본 동작**을 쓰므로 ***출처를 가리지 않는다***(그래서 둘 다 된다).
  ⚠★***문구를 두 번 고쳤다*** — 「.hwp 는 탐색기로 열 수 없다」만 적었더니 ***「한글은 안 되는 것」으로
  읽혀 재문의***(「그럼 HWP 도 되는 것 아닌가요」)가 왔다. ⇒ ***「한글·워드 둘 다 붙여넣기는 된다」를
  먼저 말하고, 못 되는 것은 「파일로 여는 것」뿐임을 뒤에 붙였다.***
  ***안 되는 것만 적으면 「그 프로그램 자체가 안 된다」로 읽힌다 — 되는 길을 먼저 적을 것.***
- **⑬[⑪ 검증 중 발견] 붙인 표의 글꼴·크기가 저장 때 지워지던 것**(오늘 ②가 만든 부작용):
  `stripFontStyle`(편집기가 심는 글꼴·크기 제거)이 **모든 편집영역에 걸려** Ⅴ 의무기록에도 적용됐다.
  그 영역은 ***사용자가 한글·워드에서 붙여넣은 문서 자체***라 글꼴·크기가 곧 내용인데,
  저장하면 `font-family`·`font-size` 가 **2곳→0곳**으로 사라져 「붙인 모습과 저장 후 모습이 달라짐」이 된다.
  ⇒ `collectTexts` 에서 **`mr_body` 만 필터를 태우지 않는다**(captureAuto 가 mr_body 를 빼는 것과 같은 이유).
  ✅**실서버 왕복** : 워드 표 붙여넣기 → 저장 → 재조회 = **DB 에 글꼴·크기 2곳 그대로**,
  화면 복원값 「맑은 고딕 / 13.33px(=10pt) / rgb(192,0,0)」·표·굵게 전부 살아 있고 인쇄 장에도 실림.
  가짜병원 99999998 시험 자료 삭제(MST·TEXT·HST·초안 잔여 0).
  ⚠**색·표 폭·테두리는 원래 안 지운다** — 처음 측정에서 「색·폭도 사라짐」으로 봤던 것은
  ***제 정규식 실수***였다(브라우저가 재직렬화하며 `#C00000`→`rgb(192,0,0)`, `420.0pt`→`420pt` 로 바꾼다).
  ***저장 필터를 점검할 때는 문자열이 아니라 파싱된 값으로 볼 것.***
- **⑫[⑩의 잔여 원인] 편집 도구가 A4 장 계산을 흔들던 것**: `.er-mrbar`·`#er-mrPh` 는 **인쇄에 안 찍히는데**
  A4 복제본에는 복제됐다. 평소 `display:none`(높이 0)이라 안 보였지만 ***편집을 켜면 보이게 되어***
  (er-editmode 규칙) **133px 를 차지** → 편집 중 「N장 시작」 표지가 실제 인쇄 경계보다 위에 찍히고
  사용자는 그 **틀린 경계**에 맞춰 간격을 조정했다(⑪에서 안내문을 늘리며 오차가 커져 발견).
  ⇒ `erCollectUnits` 에서 **`.er-noprint` 요소를 장 계산에서 제외**. 앞으로 er-noprint 를 붙이는
  요소는 자동으로 걸린다. ✅**실측** : 복제본에 툴바·안내문 없음 · 편집 모드와 보기 모드의
  **1~9장 경계 완전 동일**(10장 차이는 설계된 것 — 내용 없는 의무기록 장은 편집 중에만 자리로 보여준다).
- **⑭Ⅴ 의무기록 — 붙인 그림·표 「위치 옮기기」 신설**(「이미지 전체를 이동은 안 되나요」):
  조절바에 크기·삭제·새 장은 있는데 **옮기는 수단이 없었다** ⇒ **⬆ 위로 · ⬇ 아래로** 추가(`erImgMove`).
  ★***자유 배치(절대 위치)는 일부러 안 만든다*** — `erPaginate` 가 **흐름 높이**로 A4 장을 나누므로
  떠 있는 그림은 장 경계·인쇄가 어긋나고 글과 겹친다. 그래서 '이동' = **같은 층(er-mrBody 직계) 순서 바꾸기**.
  ⚠**기준점은 「고른 덩이 밖의 가장 가까운 블록」이다** — 처음엔 위=맨앞의 앞 / 아래=맨뒤의 뒤 로 찾았는데
  ***띄어 고른 경우***(A·B 고르고 표 건너 C 도 고름)에는 맨뒤가 이미 마지막이라 **아무 일도 안 났다**
  (「전체선택 후」 실측으로 잡음) ⇒ 아래로 = **맨앞 뒤쪽** 첫 비선택 블록 뒤로 / 위로 = **맨뒤 앞쪽**
  첫 비선택 블록 앞으로 덩이를 **모아** 옮긴다(양쪽 다 진행됨).
  ✅실측 : `ABTC`(T=표) 전체선택 → 아래로 **TABC** → 위로 **ABCT** · 단일 C 위로 3회 `ABTC→ABCT→ACBT→CABT`
  → 맨 위에서 멈춤(무의미 스냅샷 회수) · ↩ 실행취소 복원 · 여러 장 함께 이동 · 그림이 p·div 로 싸여도
  직계 조상 블록을 옮김.
- **⑭-2 [이어서] 마우스로 끌어 옮기기 + 좌우 배치**(「Ctrl+클릭 전체 선택 후 마우스 이동하면 안 됨」·「좌우 위아래」):
  ⬆⬇ 단추만으로는 여러 칸 옮길 때 여러 번 눌러야 한다 ⇒ **그림을 잡아서 끌어 놓기**(`_mrBindImgDrag`) +
  **◀▶ 좌우 배치**(`erImgAlign` — 왼쪽↔가운데↔오른쪽 3단).
  · 끌기 : 놓일 자리에 **파란 선**(`.er-mrdrop`)을 그려 미리 보여 준다. 놓는 자리는 **블록 사이**(자유 좌표 아님).
    ⚠**브라우저 기본 그림 끌기(native drag)는 끈다** — 한 장만 옮겨지고 엉뚱한 곳에 떨어져 우리 규칙과 충돌.
    ⚠**클릭과 끌기를 5px 로 가른다**(안 가르면 선택이 안 된다) · **손잡이(.er-hnd)에서 시작한 끌기는 크기 조절**이라 제외 ·
    Ctrl/Shift 누른 mousedown 은 **더하기 선택**이라 끌기로 안 본다 · 제자리에 놓으면 무의미 스냅샷 회수.
    ★**여러 장 선택 상태에서 그중 하나를 끌면 전체가 함께** 간다(요청의 요지).
  · 좌우 : 흐름 문서라 '좌우 이동' = **정렬**이다. 상태를 **`data-align` 속성**에 적어
    `mr_body` innerHTML 에 함께 저장 → **재조회·인쇄에도 남는다**(CSS 가 data-align 을 보고 여백을 준다).
  ✅실측 : `ABC` → C 를 A 위로 끌기 **CAB**(표시선 위치 = A 상단 ±2px) · A·B 두 장 선택 후 B 를 C 아래로 끌기
  **CAB**(함께 이동) · 좌우 가운데(208px)→오른쪽(406px)→가운데→왼쪽(9px)·끝에서 멈춤 ·
  **저장 후 DB 에 `data-align="right"`·`"left"` 보존** · ↩ 실행취소 동작. 가짜병원 자료 삭제(잔여 0).
- **⑭-3 [이어서] 조절바 고정 + 좌우 세부 이동**(「툴바는 고정으로 그리고 이동을 조금 세부화 — 지금은 기본이동이 큼」):
  · **조절바 z-index 수정** : 「툴바가 안 보인다」는 신고의 원인은 자리가 아니라 **겹침**이었다 —
    화면 아래·위에는 ***사이트 고정 요소가 이미 있다*** : **`#todayAsqBar` z-index 9999**(전체폭 띠 36px) ·
    `#wqaFab` **12000**(오른쪽 아래) · `#top-navbar` **1030** · 「자주 쓰는 메뉴」 `#wnnFavBar`.
    조절바가 **1400** 이라 **그 아래에 깔려 안 보였다**(요소는 정상 렌더 중이었다) ⇒ **12100** 으로 올렸다.
    ★***판별법 = `elementFromPoint` 로 「그 자리에서 무엇이 잡히는가」를 본다*** —
    rect 는 정상인데 클릭이 딴 요소로 잡히면 **덮인 것**이다(`asq-bar-scroll` 이 잡혀 원인이 나왔다).
  · ⚠⚠★**자리는 결국 「원래대로」 — 고른 그림 바로 위**(사용자 최종 확인 「원래가 그랬음」).
    「툴바는 고정으로」를 **화면 한 자리에 못 박는 것**으로 잘못 읽어 ***화면 아래 가운데 → 화면 맨 위***로
    두 번 옮겼는데, 둘 다 ***그림에서 멀어 눈이 왕복***했고 맨 위는 「자주 쓰는 메뉴」 띠와도 겹쳤다
    (「선택영역 바로 위에 있어야 하는데 맨 위에 위치」). ⇒ `_mrPlaceImgBar` 가 **그림 위 8px** 에 붙인다.
    ***「고정」 요구가 나와도 원래 배치를 버리기 전에 무엇이 불편했는지부터 확인할 것*** — 실제 불만은
    **가려짐**이었고 그건 z-index 한 줄이었다.
    함께 넣은 안전장치 2 : ①단추가 늘어 바가 **600px 넘게 넓어져** 좁은 그림에서 화면 밖으로 나가므로
    **좌우를 화면 안으로 당긴다** ②**위에 자리가 없으면**(그림이 상단 메뉴에 가까움) **그림 아래로 피한다**
    — 안 그러면 메뉴 밑에 깔려 또 「없어졌다」가 된다.
    ✅실측 : 넓은 그림(60%)·좁은 그림(25%) 모두 **간격 8px·화면 안·가려짐 0** ·
    그림을 화면 맨 위(top 70)로 올리면 바가 **그림 아래(170)로 피함**(메뉴 아래끝 63 침범 안 함).
  · ⚠★**천장은 상단 메뉴만이 아니다 — 붙여넣기 툴바(`.er-mrbar`)도 천장이다**(「겹쳐보여서 간격 유지」).
    그림이 툴바 바로 밑에 있으면 「그림 위」가 곧 툴바 자리라 ***조절바가 단추들을 덮었다.***
    ⇒ 천장 = `max(메뉴 아래끝, 화면에 보이는 툴바 아래끝)`. **자리 정하기 우선순위** :
    ①**그림 바로 위 8px**(기본) → ②천장과 겹치면 **천장~그림 사이 가운데**(양쪽 간격) → ③사이가 좁으면 **그림 아래**.
    ⚠***②를 기본으로 두면 안 된다*** — 여유가 많을 때 바가 그림에서 **95px 나 떨어져** 「바로 위」 요구를 깬다
    (한 번 그렇게 만들어 실측으로 잡았다). ①→②→③ 순서가 핵심이다.
    ✅실측 3경우 : 여유 충분 = 바 262 / 그림 307(**바로 위 8px**) · 좁음(툴바 106·그림 123, 사이 17px) =
    **그림 아래 330**(툴바·그림 겹침 0) · 화면 밖 방지 clamp 도 천장을 지킨다.
  · ★**툴바와 붙여넣기 영역 사이를 편집 모드에서만 58px 띄웠다**(「1번 간격 띄우고 2번 위로」) —
    그림이 툴바에 붙어 있으면 「그림 위」에 자리가 없어 조절바가 **그림 아래(화면 맨 아래)로 밀려났다.**
    여기를 띄우니 조절바가 **제자리(그림 위)로 올라온다**. ⚠**보기·인쇄에서는 툴바가 `display:none` 이라
    이 여백도 함께 사라진다** — 종이에 빈 줄이 생기지 않는다(실측 : 보기 모드 여백 8px 로 복귀).
    ✅실측 : 툴바 349 · 사이 67px · 조절바 371~408(**그림 위**, 위 22px·아래 9px, 겹침 0).
  · ★**가로는 그림이 아니라 붙여넣기 영역 가운데에 맞춘다**(「이동시 툴바도 같이 움직임」) —
    그림 가운데에 맞추면 ◀▶ 로 밀 때·크기를 바꿀 때마다 ***조절바가 따라 흔들려 방금 누른 단추가
    커서 밑에서 달아났다.*** 영역 가운데는 고정이라 단추 자리가 안 변한다.
    ※세로는 그대로 그림을 따라간다(무엇을 고치는지 보여야 한다).
    ✅실측 : 그림이 500→580 으로 이동하고 크기까지 바뀌는 동안 바는 **318/351 그대로**.
  · ⚠**측정 착오 기록** : 「인쇄 장에 의무기록 그림이 없다」로 보였던 것은 ***제 선택자 착오***였다 —
    `erCollectUnits` 는 `er-mrBody` 의 **자식을 각각 유닛으로** 복제하므로 클론에서는 `.er-mrbody` 로
    감싸이지 않고 **`.er-autobody` 직계**로 들어간다. `#…autopage .er-mrbody img` 로 찾으면 0 이 나온다.
  · ★**`⬆ 위로 / ⬇ 아래로` 단추 삭제**(「위아래 기능 자체 삭제」) — ***그림을 잡아서 끌면 같은 일을 하고***
    (놓을 자리에 파란 선이 뜬다) 단추까지 두니 띠만 길었다. 조절바 최종 **10개** :
    `➖ % ➕ · ↺ 원래대로 · ◀ 좌우눈금 ▶ · ⤓ 새 장에서 · 🗑 · ✕`.
    ⚠`erImgMove()` 함수는 **남겨 뒀다** — 단추만 되넣으면 부활한다(끌기와 같은 규칙을 쓴다).
    ⚠**끌기 시험 함정** : 그림을 방금 만들어 넣고 바로 끌면 ***아직 로드 전이라 높이가 0*** 이어서
    끈 거리가 5px 미만 → 「끌기가 안 된다」로 보인다(실제로 한 번 오판했다).
    ***`img.complete`·높이를 확인한 뒤 끌 것.*** 로드 후 재시험 = `AB → BA` 정상.
  · ★**조절바에 색을 넣어 본문과 구분**(「다른 내용하고 구분이 안 되서 살짝 색깔 구분」) —
    흰 바탕+옅은 테두리라 ***붙인 서류(흰 종이) 위에서 그 일부처럼 보였다.***
    ⇒ 바탕 `--er-navytint`(#f2f6fc) · 테두리 **2px** `--er-navy2`(#2a5298) · 그림자 진하게.
    단추는 흰 바탕이라 띠 안에서 도리어 또렷해진다.
    ⚠**1.5px 로 했다가 브라우저가 1px 로 반올림**해(배율 1) 구분이 약했다 — **2px** 로 확정(실측).
  · **①번 여백 58 → 46px**(「간격 살짝 좁혀주고」) — ⚠**하한선에 가깝다** : 붙여넣기 영역의 padding 8 +
    테두리 1 이 더해져 실제 여유 **55px** 이고, 조절바가 「그림 위」로 가려면 **37+8+6 = 51px** 이 필요하다.
    ***더 좁히면 바가 그림 아래로 밀려난다.*** ✅실측 : 여백 46 · 사이 55 · **그림 위 유지**(위 6·아래 9, 겹침 0).
  · **안내문 2줄로 축약 + 단추를 붙였다**(「문구 정리해서 줄이고 버튼 조금 위로 겹쳐서」) —
    띠 높이 **133 → 87px**, 문구·단추 사이 **2px**. `row-gap:2px`(좌우 gap 8 은 유지 — 단추끼리 붙지 않게) ·
    안내문 `line-height:1.45`·`flex:1 1 100%`. ⚠**한 줄에 맞추려면 실제 폭을 재야 한다** —
    눈대중으로 줄였다가 3줄이 되어 두 번 더 깎았다(`scrollWidth` 대신 **숨은 probe span 으로 조각별 폭 측정**
    → 첫 줄 708px vs 쓸 수 있는 654px = 54px 초과를 확인하고 정확히 잘랐다).
  · **좌우를 3단 정렬 → 0~100 눈금 세부 이동으로** : 정렬(왼쪽/가운데/오른쪽)은 ***한 번에 너무 크게 튀었다***
    ⇒ 한 번 = **5눈금**, **Shift+클릭 = 1눈금**(➖➕ 와 같은 규칙) + **눈금 라벨 클릭 = 숫자 직접 입력**.
    상태는 **`data-ox`**(0=왼쪽 끝·50=가운데·100=오른쪽 끝)로 저장 — 옛 `data-align` 문서도 읽는다(호환).
    ⚠★***여백은 「남는 공간(100−그림폭)」에 비례해야 한다*** — `margin-left:ox%` 로 바로 주면
    **큰 그림이 종이 밖으로 밀려 잘린다.** 그래서 `ox/100 × (100−폭%)` 로 환산해 인라인으로 준다
    (CSS 만으로는 낼 수 없어 JS 가 계산 · `_mrApplyOx`).
    ⚠★***크기를 바꾸면 여백도 다시 계산***해야 한다 — **네 곳 모두**(➖➕ · 숫자 입력 · ↺ 원래대로 · 손잡이 끌기)에
    연결했다. 빠뜨리면 오른쪽에 둔 그림이 **크게 만든 순간 종이를 벗어난다.**
    ✅실측 : 5눈금≈19px · Shift 1눈금≈4px · **오른쪽 끝에 두고 40%→70%→100% 로 키워도 우측 넘침 0** ·
    ↺ 원래대로 복원 정확.
- **⑭-4 [이어서] 배율 단추 중복 제거**(「2번 밑에 있음 · 1번은 중복됨(삭제필요)」):
  위 툴바의 **'영역 전체 배율' `➖ 100% ➕`** 와 아래 조절바의 **'그림 크기' `➖ 72% ➕`** 가
  ***생김새가 똑같아 무엇을 누르는지 알 수 없었다.*** 기능은 실제로 달랐지만(전체 CSS zoom vs 그림 폭)
  ⇒ **위쪽(영역 배율)을 삭제.**
  ★**지울 수 있었던 근거** : 그 배율은 값을 `#er-mrBody` 의 `style.zoom` 에만 두고 `collectTexts` 는
  innerHTML 만 저장하므로 ***저장되지 않는다*** → 열 때마다 100% 로 시작하는 **임시 보기**였다
  (남는 상태가 없어 문서가 달라지지 않는다). ***UI 를 지우기 전에 「그 값이 저장되는가」를 먼저 볼 것.***
  ⚠**Ctrl+휠 배율도 함께 없앴다** — 단추만 지우면 휠로는 여전히 걸려 **표시도 되돌릴 수단도 없는**
  배율이 우연히 잡힌다(그게 더 나쁘다). 표 크기는 `↔ 폭맞춤` 이 맡는다.
  함수(`erMrZoomStep`·`erMrZoomInput`·`erMrZoomReset`·`_erMrApplyZoom`)는 **남겨 뒀다** — 단추만 되넣으면 부활.
  ⚠잘라오기 창의 별개 배율(`er-cropZoomLbl`)은 **건드리지 않았다**(파일 미리보기 확대용).
  ✅실측 : 위 툴바 = `↔ 폭맞춤 · ↩ 실행취소 · 📂 PDF·이미지 열기` 3개만 · Ctrl+휠로 zoom 안 변함 ·
  아래 조절바 12단추 정상(80%→75%).
- **⑭-5 [이어서] 「폭맞춤」 정체 확인 → 남기고 결함 2건 수정**(「폭맞춤 기능은 무엇인가요 의미없으면 제외」):
  ***의미는 있다*** — 한글·워드 표는 **고정폭(pt)** 이라 붙이면 오른쪽이 빈다(실측 : 260pt 표 **347px** →
  본문 폭 **662px**). 이름이 '폭맞춤' 뿐이어서 무엇의 폭인지 몰랐던 것 ⇒ **`↔ 표 폭맞춤`** 으로 고쳤다.
  확인하다 **결함 2건**을 찾아 함께 고쳤다 :
  ①⚠★***저장이 안 되고 있었다*** — 상태를 `#er-mrBody` 의 **클래스**로만 두었는데 `collectTexts` 는
  innerHTML 만 저장한다 ⇒ 넓힌 뒤 저장하고 **다시 열면 표가 또 좁아졌다**(지운 배율 단추와 **같은 뿌리**).
  ⇒ **표마다 인라인 `width:100%`** 를 적고 원래 폭은 **`data-w0t`** 에 보관(꺼면 복원).
  ②⚠**그림까지 늘리던 규칙**(`.er-mrfit img{width:100%}`)을 **뺐다** — 표를 맞추려 눌렀는데 폭 없이
  붙인 그림이 통째로 커졌다(인라인 폭이 있는 그림은 원래도 영향 없었다). 그림은 조절바가 맡는다.
  ★**켜짐 판정은 「표에 남은 `data-w0t`」로** 한다 — 클래스는 새로 열면 사라지므로 클래스로 보면
  「단추는 꺼졌는데 표는 넓은」 어긋남이 난다.
  ✅실측 : 토글 347↔662 왕복(원래 폭 `260pt` 복원) · **저장→재조회 후에도 662 유지·단추 켜짐** ·
  **인쇄 장에도 반영(680)** · 그림 폭 변화 0. 가짜병원 자료 삭제(잔여 0).
  ⇒ ★***UI 를 지울지 판단할 때 「그 상태가 저장되는가」를 먼저 보라*** — 이번 세션에서 같은 뿌리의
  결함이 **두 번**(영역 배율·표 폭맞춤) 나왔다. 저장 안 되면 그건 기능이 아니라 임시 보기다.
- **⑮[⑪ 후속] 안내문 중복 제거** — 툴바 힌트와 빈 영역 안내문(`#er-mrPh`)이 **같은 말**이라 두 번 보였다
  (「중복표시 아래표시는 삭제」) ⇒ 아래 안내문 삭제. `erMrToggleSec` 이 `if(ph)` 로 방어하므로
  요소만 다시 넣으면 되살아난다(JS·CSS 무수정). ⇒ 안내문은 이제 **툴바 힌트·버튼 tooltip 두 곳**.
- ⚠⚠**새 함정 — 주석에 꺾쇠 태그를 쓰면 `JSP구조검사.py` 가 헛경보를 낸다.**
  JS 주석에 `p·div` 를 **꺾쇠째** 적었더니 div 짝이 117/116 으로 나와 구조가 깨진 줄 알았다
  (실제 HTML 은 정상 — 검사기가 raw 문자열을 세기 때문). ***주석에서는 꺾쇠를 떼고 적을 것.***
  ★판별법 : `git show HEAD:…` 와 **열림·닫힘 개수를 견주고**, diff 에서 `</?div` 줄만 뽑아 보면
  어느 줄이 카운트를 흔들었는지 바로 나온다.
- **배포**: JSP만 — 파일 교체(WAR 재빌드 불필요). DB 는 적용 완료.
- ⚠로컬 검증 함정 : 이클립스 자동배포 폴더는 `C:\Users\HYUN\eclipse-workspace\.metadata\...\tmp1\wtpwebapps\wnn_medcost`
  — CLI 수정 후 F5 안 하면 **옛 JSP 가 그려진다**(이번에도 겪음). evalReport 는 URL 파라미터가
  main.jsp 의 replaceState 로 지워지므로 **sessionStorage(erOpenHospCd/erOpenYm) 원샷**으로 열 것.

### [완료] 적정성평가 — 차등제 **미신고 분기인데 약사 재직일수율에 1.1점** (2026-08-17)
- **증상**: 병원이 4분기 차등제를 신고하지 않았는데 「약사 재직일수율」만 **결과 1.1점**
  (분모 92 · 분자 0 · 현황값 0%). 같은 구조영역인 01~03(의사·간호사·간호인력 1인당 환자수)은 0점으로 정상.
- **원인** — `SP_INDICATORS_STRUCTURE_ZONE` : ***약사(04)만 분모를 신고값이 아니라 날짜 계산식***으로 만든다
  (4분기 = 6/15~9/14 **92일**, 신고 여부와 무관하게 항상). 분자(`PHAM_DAYS`)는 신고가 없으면
  `SELECT ... INTO` 가 **아무 것도 못 넣어 0** 이 된다 ⇒ 0/92 = **0% → 표준화 1구간 → 5.5÷5 = 1.1점.**
  01~03 은 분모가 신고값(의사수 등)이라 미신고 시 분모 0 → **미산정 0점**이 되어 티가 안 났다.
  ★***`SELECT ... INTO` 는 행이 없으면 변수를 그대로 둔다*** — 「신고 없음」과 「신고했으나 0」을 구별할 수 없다.
- **조치**([SP_INDICATORS_STRUCTURE_ZONE_2026-08-17.sql](docs/sql/proc/SP_INDICATORS_STRUCTURE_ZONE_2026-08-17.sql),
  **운영 적용 완료**) : 루프 안에서 **그 분기 신고 행을 먼저 센다**(`v_has`). 0 이면 값들을 초기화하고
  ***약사 분모도 0*** → 다른 구조영역과 같이 미산정. 원본 백업 =
  [BACKUP_…_20260817.sql](docs/sql/proc/BACKUP_SP_INDICATORS_STRUCTURE_ZONE_20260817.sql)(그대로 실행하면 원복).
  ★**신고했는데 약사재직일수가 0** 인 달은 **그대로 1.1점** — 그건 「약사가 없다」는 사실값이라 최저 구간이 맞다.
- **검증**: 11283122 로 실측 — 미신고 12월 재계산 → **01~04 모두 0/0/0점**(교정됨) ·
  신고된 8월 재계산 → **92/92 · 5.5점 · 5구간 그대로**(회귀 없음) · 01~03 값도 불변.
- ⚠⚠★★**같은 결함이 프로시저 4개에 복제돼 있다** — 처음에 `SP_INDICATORS_STRUCTURE_ZONE`(차등제 저장용)만
  고쳤는데, ***교정한 값이 곧 1.1점으로 되돌아왔다***(`UPD_USER=admin`). 원인은
  ***「월 자료생성」이 다른 프로시저(`SP_EVALUATION_INDICATORS_CREATE2`)를 쓰기 때문***이다.
  `PHAM_DAYS` 를 다루는 프로시저 = `CREATE` · `CREATE2` · `CREATE3` · `STRUCTURE_ZONE` **4개**.
  ***지표 계산을 고칠 때는 「그 값을 쓰는 화면이 어느 프로시저를 부르는지」를 먼저 확인할 것.***
  (Magam_SQL.xml L1294 = CREATE2 가 실사용. CREATE·CREATE3 은 호출되지 않는 옛 판이라 손대지 않았다.)
- **★사용자 지시로 최소 수정으로 다시 만들었다**(「약사만 오류이니까」) — 처음 판은 미신고 시
  `pat_count`·`doc_count` 등까지 0 으로 초기화했는데, **01~03 은 분모가 신고값이라 저절로 0** 이 되므로
  불필요했다. ⇒ 손대는 것은 **약사 블록 한 곳**(신고 행 수 `v_has` 를 세고, 0 이면 분모·분자 0)뿐.
  ※`CREATE2` 는 `str_month = end_month = jobyymm` 이라 **월 루프가 한 번만** 돈다(값 이월 걱정 없음 — 확인함).
- **적용 상태**(2026-08-17 기준) :
  · `SP_INDICATORS_STRUCTURE_ZONE` — **운영 적용 완료**(최소 수정본).
  · `SP_EVALUATION_INDICATORS_CREATE2` — **파일만 전달**(사용자가 직접 실행). ⚠***이걸 적용해야 자료생성에서
    다시 안 생긴다.*** 임시 이름(`ZZ_SYNTAX_TEST_CREATE2`)으로 **문법 검증 통과** 후 임시본 삭제까지 확인했다.
    파일 = [SP_EVALUATION_INDICATORS_CREATE2_2026-08-17.sql](docs/sql/proc/SP_EVALUATION_INDICATORS_CREATE2_2026-08-17.sql) ·
    백업 = [BACKUP_…_20260817.sql](docs/sql/proc/BACKUP_SP_EVALUATION_INDICATORS_CREATE2_20260817.sql).
- **과거 자료 보정 — 실행 완료**([FIX_PHAM_MISREPORT_2026-08-17.sql](docs/sql/evalrpt/FIX_PHAM_MISREPORT_2026-08-17.sql)) :
  **182건 · 66개 병원 · 202401~202612**(잘못 부여된 점수 합 200.20)을 분모·분자·점수·구간 **모두 0** 으로.
  ★변경 전 원본은 **`TBL_PAT_INDI_BAK_PHAM_20260817`** 에 182건 백업(되돌릴 수 있다).
  검증 : 잔여 0건 · **「신고했으나 약사 0일」 6건은 손대지 않음**(1.1점 유지 — 사실값) ·
  정상 산출 행 1,138건(점수합 6,109.40) 불변 · `UPD_USER='fix20260817'` 로 바뀐 것은 **04 가 182건**뿐
  (01~03 각 1건은 검증용 재계산 1개월분).
  ⚠**182건 중 1건(11283122/202612)은 보정 직후 `admin` 의 자료 재생성으로 1.1 로 되돌아갔다** —
  CREATE2 미적용 상태였기 때문. ***CREATE2 적용 후 그 달을 다시 생성하면 0 이 된다.***
- **배포**: DB 프로시저만 — 앱 변경·재기동 불필요(적용 즉시 반영).

### [완료] 월보고서 장 나눔 — 그룹 라벨만 앞 장에 남는 '고아 라벨' (2026-08-11)
- **증상**: 4장 끝에 **`과정지표` 라벨만** 덩그러니 남고 첫 지표(유치도뇨관)는 5장으로. 편집 중 뜨는
  **`N장 시작` 표지도 라벨이 아니라 그 아래 지표에** 붙어 장 경계가 어긋나 보였다.
- **원인**: 헤더류(`keep` = 그룹라벨·소제목·섹션제목)를 다음 장으로 넘기는 조건이
  ①`남은 공간 < 130px` 이거나 ②`장 전체가 헤더뿐`일 때뿐이었다. **앞에 다른 지표가 실린 장의
  꼬리에 라벨이 붙은 경우**는 어느 쪽도 아니어서 라벨만 남았다(여백은 130px 넘게 남았지만
  지표 한 개가 들어갈 만큼은 아니었다).
- **조치**([evalReport.jsp](src/main/webapp/WEB-INF/jsp/main/evalReport.jsp) 장 배치 루프):
  본문이 넘칠 때 **`curUnits` 꼬리에 연속으로 매달린 `keep` 유닛들을 본문과 함께** 다음 장으로 옮긴다.
  헤더뿐이던 장은 종전처럼 지운다. `_erPageStarts` 에 **옮긴 헤더의 첫 노드**를 장 시작으로 기록하므로
  `N장 시작` 표지도 라벨 위로 올라간다.
- **함께 수정 — `N장 시작` 알약이 제목을 가렸다**: `_erGuideSync` 가 `r.top-9` 로 놓아 알약이 블록에
  절반쯤 걸쳤다(‘■ 유치도뇨관이 있는 환자분율’ 글자가 가려짐) → **블록 위쪽 여백**(`r.top - 알약높이 - 2`)으로.
- **주의**: 2026-07-23 [확정] "페이지 하단 빈 여백은 유지"(그룹·박스 통짜 보호 우선)와 충돌하지 않는다 —
  이 건은 여백 문제가 아니라 **라벨과 내용이 갈라진 것**을 붙이는 수정이다.
- **배포**: JSP만 — 파일 교체.

### [완료] 월보고서 점수 상향 목표 — 당월 기준 → **평가기간 누적 기준** (2026-08-11)
- **요청**(검수 박혜련, `D:\위너넷\수정사항.xlsx` 2건): "점수 상향 목표는 해당월이 아니라 **누적값에서의 상향값**으로".
  적정성평가 점수는 평가기간(7월~당월) 합산으로 정해지므로 목표도 누적 분모·분자로 따져야 한다.
- **조치**([evalReport.jsp](src/main/webapp/WEB-INF/jsp/main/evalReport.jsp) `goalUp`/`cumOf`):
  누적 자료(`_cum`)가 있으면 **분모·분자·현재 가중치·표준화점수를 전부 누적값으로** 놓고 계산.
  누적 개념이 다른 **인력(01~03)·약사(04)·항정(07)·DUR(08)** 과 7월 보고서(누적=당월)는 종전대로 당월.
- **여력 상한 안내 신설**: "8월 욕창 개선대상자 17명 중 현재 4명이 개선되어 있어 추가 개선 가능한 대상자는
  최대 13명임. 13명 전원 추가 개선 시 누적 개선 대상자는 5명→18명(56.25%)으로 상승하여 표준화 4점 구간…"
  — **당월 여력(분모−분자)을 넘는 구간은 나열에서 뺀다**(이번 달에 못 닿는 목표를 적지 않는다).
  감소형(05·10·14)은 '이미 발생한 건'이라 여력 개념이 달라 안내를 붙이지 않는다.
- **[함정] `act` 를 여력 문장에 그대로 쓰면 안 된다** — '추가 개선' 이라 "추가 개선되어 있어 추가 추가 개선"
  이 된다. `GOAL_VERB` 에 `did`(개선/실시/적정범위에 해당)·`more` 를 따로 뒀다.
- **당월 만점 지표도 목표를 낸다**: ADL 당월 12.0/12(5점)인데 누적 9.6/12(4점) → 종전엔 '추가 개선 여지
  없음(유지)' 로 끝났다. **목표 표시 여부(`fullGoal`)를 누적 기준으로 판정**(헤더 점수·색·개선방향은 당월 유지).
- **검증**: 검수표 3건 숫자를 실제 함수로 재현 확인(욕창개선 5명→3점·10명→4점, ADL 5명→5점(+2.4), 배뇨 1명→5점(+0.6)).
- **[확정·완료 2026-08-11] 점수 기준을 통째로 누적으로 전환**(사용자 지시 "검수표대로"):
  - **창구는 4개 함수뿐** — `gotOf`(획득 가중치) · `sOf`(표준화구간) · `calValOf`/`calDispOf`(현황값).
    누적이 없는 지표·7월 보고서는 `cumOf` 가 null 이라 자동으로 당월값이 쓰인다. **새 화면도 이 함수만 쓸 것.**
  - 적용처: Ⅲ 지표 헤더 점수·만점판정·개선방향 노출 · Ⅱ 상세표(현황값/구간/획득/부족·소계·종합) ·
    `computeScores`(표지 예상 종합점수·등급·Ⅰ 카드·부족점수·비교표) · `topGaps`(우선순위·최우선 태그) ·
    `simStep`/`simNeed`/`simRoomLower`/`roomRiskTxt`(총평·권고 시나리오).
  - **분석문 첫 줄의 빨강 강조만 당월 기준(`fullM`)** — 그 문장은 '당월 실적' 서술이라 누적으로 판정하면
    당월 만점인 달도 빨갛게 나온다.
  - **전월 대비 비교도 전월 <누적>으로**: `aPrevCum`(7월~전월) 신설. 누적 대상 지표는 전월 누적 가중치,
    인력·약사·항정·DUR 은 전월 단월 가중치로 합산 — 당월과 같은 잣대라야 증감이 의미를 갖는다.
    (안 고치면 '전월 단월 합' 과 '당월 누적' 을 비교해 매달 큰 폭 상승으로 잘못 읽힌다.)
  - **[함정] `select_Hosp_Indi` 의 `dtortot`·`ntortot` 은 이름과 반대**(01~04 제외) — 전월 누적 map 을
    담을 때도 `dtor:ntortot / ntor:dtortot` 로 뒤집어 담아야 한다.
- **[후속] 카드와 핵심 진단이 서로 다른 점수를 말했다**(2026-08-11): 카드 73.4점(4등급)인데 핵심 진단은
  "현재 종합점수 75.2점은 3등급 구간…". **핵심 진단(`diag_core`)·각주(`diag_note`)는 편집영역이라
  저장본이 있으면 자동 문구를 안 만든다** → 저장 당시 점수가 문장에 박제된다(실제 보고서 28).
  → **문장 속 종합점수가 지금 값과 다르면 다시 만든다**(`_staleNum` 헬퍼). 점수는 산출된 사실값이라
  옛 저장본이 덮으면 안 된다(2026-07-14 목표점수 건과 같은 원칙).
  **판정 규칙 — 그 수치를 <말하고 있을 때만> 개입한다**:
  ① 문장에 `종합점수 NN점`(각주는 `+NN점`) 표현이 **없으면 손대지 않는다** — 점수를 뺀 채 다듬은 문장 보존
  ② 있는데 지금 값과 다르면 → 자동 문구로 교체  ③ 있고 값도 같으면 → 다듬은 문장 유지.
  0.05 이내는 같은 값으로 본다(소수 첫째 자리 표기 흔들림). 5가지 사례로 동작 확인.
  **★승인(APPROVED)본은 손대지 않는다** — 확정본이므로 수치가 달라져도 문장 그대로.
  달라진 사실은 기존 `_erApproveDiff` 배너가 위너넷에게 알린다.
  **[함정] `approved` 확정 시점** — `loadSavedTexts` 는 `renderGoalSummary()` 를 부른 <뒤>에
  `setStatus()` 로 approved 를 세팅한다. 그래서 **renderGoalSummary 앞에서 `approved` 를 먼저 세팅**해야
  승인본이 안 덮인다(안 그러면 판정이 직전 보고서의 승인 상태를 보고 돈다).
  **승인취소 시 즉시 반영** — `_erDoApproveCancel` 이 `setStatus('DRAFT')` 만 하고 재렌더를 안 해
  화면을 나갔다 들어와야 갱신됐다 → `renderGoalSummary()` 를 그 자리에서 호출.
  알림 문구는 **실제로 문장이 바뀐 경우에만** '갱신했습니다'라고 말한다(안 바뀌었는데 그러면 거짓 안내).
  ※지표 정의·개선방향·점수 상향 목표는 저장본이 없어 **승인 여부와 무관하게 열 때마다 최신**이다.
  승인취소가 필요한 건 **핵심 진단 문구**와 **승인 시점에 굳은 PDF**(재승인해야 새로 생성)뿐.
- **배포**: JSP만 — 파일 교체. **병원에 나가는 종합점수·등급이 달라지므로 안내 필요**(당월값 → 누적값).

### [완료] 자주 쓰는 메뉴 바 — 끌어서 옮기기 (2026-08-11)
- **증상**: 상단 고정 바(#wnnFavBar, 위너넷 전용)가 월간보고서 **편집 툴바(글꼴·크기)를 가려** 쓰기 불편.
- **조치**: [sidebar.jsp](src/main/webapp/WEB-INF/tiles/main/sidebar.jsp) — 제목표(★ 자주 쓰는 메뉴)를
  잡고 끌면 이동(`sbFavDragBind`), 자리는 `localStorage.wnnFavBarPos`(PC별). **두 번 누르면 처음 자리**로.
  화면 밖으로 못 나가게 clamp(창 크기 변경·접기/펼치기 때도 재보정).
- **[함정] 접힘(.fold) CSS 가 `right:14px`** 를 주므로, 옮긴 뒤에는 **inline `right:auto`** 를 함께 둬야
  접었다 펴도 자리가 우측으로 튀지 않는다(`sbFavPosSet` 이 항상 같이 설정).
- **[후속·중요] 자리가 안 남고 메뉴도 안 쌓였다 → 저장소가 원인**(2026-08-11 재보고):
  둘 다 `localStorage` 에만 저장했는데 **브라우저가 사이트 데이터를 막아 두면 조용히 실패**한다
  (try/catch 로 삼켜져 아무 표시도 안 났다 — JSESSIONID 미전송 건과 같은 뿌리).
  → **`sbStoreSet/Get/Del` 저장 계층 신설** = localStorage 에 쓰고 **되읽어 확인**, 안 되면
  **쿠키로 대체 저장**(1년, encodeURIComponent). 메뉴·접힘·자리 3가지를 모두 이 계층으로 통일.
- **[후속] 클릭 적립을 document 위임으로**: 종전엔 `sbFavInit` 이 도는 순간의 `#sbAllMenu` 링크에만
  리스너를 달아, **QPS 탭 전환 등으로 나중에 생기거나 그 밖에 있는 메뉴는 눌러도 안 쌓였다.**
  이제 `.nav-left-sidebar, #sbAllMenu` 안의 `href^="/"` 링크면 무엇이든 잡는다.
- **[함정] 자리 복원 때 clamp 를 즉시 걸면 안 된다** — 바 안의 `<style>` 이 아직 안 먹어 `offsetWidth` 가
  실제와 달라 엉뚱한 자리로 당겨진다. 저장값대로 놓고 **다음 프레임(rAF)에 보정**한다.
- **[함정·진짜 원인] 자리 복원은 반드시 `sbFavRender()` <뒤>에** (2026-08-11 3차):
  저장값은 멀쩡한데(`{"l":1198,"t":14}` 확인) 계속 원위치로 보였다. clamp 가 **바의 폭**으로 오른쪽 한계를
  계산하는데, 메뉴를 채우기 전에는 바가 '제목+단추' 뿐이라 폭이 150px 남짓 → 한계가 실제보다 훨씬
  왼쪽으로 잡혀 1198 이 1050 근처로 당겨졌다. **메뉴를 먼저 그려 폭을 확정한 뒤 자리를 잡는다.**
- **[개선] 붙여 둔 쪽으로 기억**: 바가 화면 오른쪽 절반에 있으면 **오른쪽 여백(`r`)** 으로, 왼쪽이면
  **왼쪽 좌표(`l`)** 로 저장. 왼쪽 좌표로만 기억하면 창 폭이 줄었을 때(예: DevTools 열기) clamp 에
  당겨져 또 '원위치처럼' 보인다. 우측 고정은 접기/펼치기·창 크기 변화에도 손대지 않는다.
- **[개선] 저장 시점을 여러 겹으로**: 끄는 도중(150ms 디바운스) · 놓을 때 · 화면을 떠나기 직전.
  마우스를 **iframe(PDF 미리보기 등) 위에서 놓으면 document 의 mouseup 이 안 와** up() 이 안 돌았다.
- **[진짜 원인·확정] 바가 <2개> 생기고 있었다** (2026-08-11 4차, 콘솔로 `#wnnFavBar` 개수 2 확인):
  먼저 그려진 바에 메뉴가 채워지고 저장 자리로 이동한 뒤, **뒤에 생긴 빈 바가 CSS 기본 자리
  (top:66px/right:14px)에 겹쳐 덮어** — 사용자 눈에는 "메뉴가 보였다가 사라지고 자리도 원위치".
  `sbFavInit` 이 두 번 도는 경로가 있다는 뜻(호출부는 1곳인데 재실행되는 상황이 있다).
  → **바를 만들기 전에 기존 `#wnnFavBar` 를 제거**하고, **클릭 리스너는 `window._sbFavClickBound`
  로 1회만** 등록(두 번 등록되면 한 번 눌러도 사용횟수가 2씩 오른다).
- **[안전장치] 정돈 로직이 빈 결과로 덮어쓰지 못하게** — 한 번이라도 비면 쌓아 둔 메뉴가 통째로 날아간다.
- **[설정] 칸 수 `SB_FAV_MAX` = 4** (2026-08-11 사용자 요청, 종전 5). 줄이면 다음 접속 때
  정돈 로직이 등록순 앞 4개만 남기고 자동 정리한다.
- **배포**: JSP만 — 파일 교체로 반영.

### [완료] 월보고서 지표정의 "미변경" 신고 — DB 템플릿이 JSP 수정을 덮고 있었다 (2026-08-11)
- **증상**: 검수 피드백(2026-08-07)대로 지표 정의 문구를 2026-08-10 오전 JSP(evalReport.jsp TPL_DEF)에
  수정·커밋했는데 화면은 옛 문구 그대로 → "미변경/미반영" 재신고.
- **원인**: 화면은 **TBL_EVAL_REPORT_TPL(DB)이 JSP 기본값을 덮는다**. def_06 때(2026-07-23)와 같은 함정 —
  **정의 문구를 고칠 땐 JSP + 운영DB 템플릿을 같이 고쳐야 한다.**
- **조치 ①정의**: [EVALRPT_TPL_DEF_SYNC_2026-08-11.sql](docs/sql/evalrpt/EVALRPT_TPL_DEF_SYNC_2026-08-11.sql) —
  def_01~15 전부를 JSP 확정본으로 일괄 UPDATE(운영 적용·확인 완료). 검수 원칙 = **지표 정의에
  기관별 분석 결과(현재 점수·표준화 구간·개선 여지) 금지** — "값이 낮을수록 우수(N구간)" 류 문구 제거.
- **조치 ②개선방향**: [EVALRPT_TPL_DIR_SYNC_2026-08-11.sql](docs/sql/evalrpt/EVALRPT_TPL_DIR_SYNC_2026-08-11.sql) —
  dir_11·14·15 새 문구, dir_09·13 신규행, **dir_12(ADL) USE_YN='N'**(JSP 에서 내렸는데 DB 행이 살아
  계속 출력되고 있었다). 개선 방향에는 **관리해야 할 내용만** — 구간·점수·필요인원은 목표 줄이 맡는다.
- **조치 ③점수 상향 목표 서술**(evalReport.jsp `goalUp`): 결과만 축약하지 말라는 지시에 따라
  `(분모 N명, 분자 a명→b명, x%→y%) 시 표준화 s점→t점 구간 진입 → 가중치 +d점(g→g'점)` 형태로 보강.
  장기입원(14)은 분모·분자가 함께 빠지므로 `(분자/분모 …)` 형식 유지.
- **조치 ④지표명**(`NM_FIX`): DB 지표명이 줄임말이고 괄호가 지워져 나온다 — `일상생활수행능력ADL`·
  `장기입원181일 이상`. 보고서에서만 `일상생활수행능력(ADL) 개선환자분율`·`장기입원 환자분율` 로 교정.
  종전에는 Ⅲ장·Ⅱ표에만 걸려 Ⅰ-2 도표·총평·변경내역 배너에 옛 이름이 남았다 → `indiNm()` 을 그곳들에도 적용.
- **조치 ⑤장기입원 시뮬레이션**: 의료중도 이상 전환은 **분모·분자에서 함께 빠진다**. `goalUp` 은 2026-08-10 에
  `both` 로 이미 고쳤고, 이번에 **`simReduceList`(전 구간 나열형)도 같은 `reqCnt()` 로 교체**했다
  (35명 중 22명 기준 종전 2·9·16명 → 3·14·19명). ※`simReduceList`·`fiveHint` 는 현재 **호출되지 않는다**
  (8/10 목표 두 줄 통합 때 빠짐) — 되살릴 때 계산이 틀리지 않도록 맞춰만 둔 것.
- **조치 ⑥구간 표기**: `30~34미만명`·`75~85미만%` → **`30~34명 미만`·`75~85% 미만`**(단위를 '미만' 앞으로).
  2026-07-23 에 절단상한을 실제 경계로 바꾸며 '미만'을 붙였는데 단위가 뒤에 남아 오타처럼 읽혔다.
  고친 곳 3군데 — `zoneRange`(Ⅲ 분석문·Ⅱ표) · `zoneListText`(Ⅳ 나열) · `structRiskTxt`(구조 하락 경고).
  다른 표기(`N명 미만`·`N% 이상`)는 이미 이 형식이었다. assessment.jsp 구간표는 범위형이라 무관.
- **조치 ⑦누적 실적 줄**(`cumAna`/`.er-cum`): "7~8월 누적 평가대상자 …" 는 **편집 없이 자동 산출되는 고정
  문장**이라 당월 문장과 구분되게 **앞에 `*` + 파랑(--er-navy2) + 굵게**. 인쇄는 print-color-adjust:exact 라 색 유지.
- **시드**([TBL_EVAL_REPORT_TPL_seed.sql](docs/sql/evalrpt/TBL_EVAL_REPORT_TPL_seed.sql))도 같은 내용으로 맞춤.
  단 시드는 `INSERT IGNORE` 라 **기존 행을 안 고친다** — 운영 문구 변경은 반드시 위 SYNC 스크립트로.
- **잔여 주의**: 이미 저장된 병원별 보고서의 편집본은 설계상 옛 문구 유지 — 검수 확인은 **새로 생성한
  보고서**로 할 것(기존 월 보고서에 옛 문구가 남았으면 그 월을 다시 생성).
- **배포**: 정의·개선방향은 **DB만**(배포·재기동 불필요). 점수 상향 목표 서술은 **JSP 파일 교체**면 반영
  (WAR 재빌드 불필요).

### [완료] 메일 발송 네이버 전환 + TLS 핸드셰이크 실패 (2026-08-07)
- **한 일**: 발송계정을 구글(587/STARTTLS) → **네이버(465/SSL)** 로 전환. 서버는 **WAR 밖 외부 설정파일** 방식으로 정리 — `/home/winner/conf/mail.properties`(권한 600) + `/usr/share/tomcat9/bin/setenv.sh` 의 `-Dwnn.mail.config=...`. 이후 계정 교체는 **이 파일 3줄(user·password·from) 고치고 재기동**이면 끝, WAR 재빌드 불필요.
- **★핵심 함정 — `mail.smtp.ssl.protocols` 를 반드시 명시해야 한다**: 안 주면 발송이 통째로 실패하고 화면엔 `Could not connect to SMTP host: smtp.naver.com, port: 465` 만 뜬다. 진짜 원인은 `SSLHandshakeException: No appropriate protocol`. `javax.mail 1.5.0`(2013) 이 SSL 소켓에 켤 프로토콜을 구버전(SSLv3/TLSv1) 위주로 지정하는데 **Java 11 이 java.security 의 jdk.tls.disabledAlgorithms 로 그걸 막아** 쓸 프로토콜이 하나도 안 남는다. → [MailUtil.java](src/main/java/egovframework/util/MailUtil.java) 에 `mail.smtp.ssl.protocols`(기본 TLSv1.2, 설정파일로 덮어쓰기 가능) 추가, 465·587 양쪽 적용.
  - **판별법**: 서버에서 `openssl s_client -connect smtp.naver.com:465` 는 붙는데 앱만 안 붙으면 십중팔구 이것. (cacerts·방화벽·IPv6 를 먼저 의심했지만 전부 정상이었다)
  - **진짜 원인이 안 보이는 이유**: [MagamController.java](src/main/java/egovframework/wnn_medcost/magam/web/MagamController.java) 의 catch 가 `ex.getMessage()` 만 화면에 돌려주고 **스택트레이스를 로그에 안 찍는다** → catalina.out 에 아무것도 없다. 원인 추적은 서버에서 JavaMail 로 직접 재현(`java -cp <WEB-INF/lib jar> Test.java`, `session.setDebug(true)`)해야 나온다. **서버에 JDK 가 있어 단일파일 실행이 된다.**
- **[함정] setenv.sh 에 `>>` 로 옵션 추가 금지**: 파일 끝에 개행이 없어 앞줄에 그대로 붙는다. 실제로 `-Dspring.profiles.active=prod` 가 `prodJAVA_OPTS=-Xms1024m` 이 되어 **prod 프로파일이 깨진 채로 기동**됐다. 반드시 `cat -A` 로 줄 끝(`$`)을 확인하고 편집할 것.
- **[함정] 서비스 인스턴스가 어느 쪽인지**: 톰캣이 2개 떠 있다. 실사용은 **`catalina.base=/usr/share/tomcat9`**(443·8080 보유, root 실행, `bin/shutdown.sh`+`startup.sh` 로 재기동). `/var/lib/tomcat9` 쪽은 빈 ROOT 만 있는 유령 — **`systemctl restart tomcat9` 를 쓰면 엉뚱한 걸 건드린다.**
- **[함정] webapps 에 같은 앱이 2벌**: `ROOT` 와 `wnn_medcost-1.0.0` 둘 다 클래스를 갖고 있고 **실제 서비스는 `wnn_medcost-1.0.0`**. ROOT 만 고치면 아무 변화가 없다(이걸로 한 번 헛돌았다).
- **[참고] 서버는 아웃바운드 HTTPS 차단** — maven 저장소에서 jar 를 못 받는다(SMTP 465 는 열림). 급히 자바 수정분을 반영해야 하면 `.java` 를 서버로 올려 `javac -encoding UTF-8 -cp <WEB-INF/lib jar> -d ...` 로 컴파일해 **`.class` 만 교체**하는 방법이 있다(2026-08-07 실제 사용, `/root/MailUtil.class*.bak` 백업). 어디까지나 임시 — 다음 배포에 덮인다.
- **네이버 계정 조건**: 메일>환경설정>POP3/IMAP 에서 **IMAP/SMTP 사용함**, 2단계 인증 후 **애플리케이션 비밀번호 12자리**, `mail.smtp.user` 는 **아이디만**(@naver.com 붙이면 인증 실패), `mail.from` 은 그 계정과 동일해야 함. 구글은 반대로 전체주소 + 앱비번 16자리 + **587 은 ssl=false 필수**.
- **외부 도메인 첫 발송이 느릴 수 있다**: 네이버끼리는 5초, 아웃룩 첫 발송은 몇 분 끌다 타임아웃 났다가 이후 정상. 재발하면 설정파일의 `mail.smtp.timeout` 만 올리면 된다(코드 수정 불필요).
- **[보안] `mail.properties.sample` 은 git 에 올라간다** — 여기 실제 앱 비밀번호를 적었다가 GitHub 에 노출됐다(커밋 `0c99f13`, winnernet7@gmail.com). 자리표시자로 정리했고, 노출된 구글 앱 비밀번호는 폐기 필요.
- **배포**: 자바(MailUtil) 변경 → **WAR 재빌드 필수.** JSP(evalReportList 도움말 네이버 기준 교체)는 파일 교체로 가능.

### [완료] 적정성평가 Q&A 챗봇 — 지식 DB 전환 + 심평원 교육자료 원문 적재 (2026-08-04)
- **요청**: 「요양병원 수가 실무교육자료」(심평원, 2022 · PDF 339쪽, `D:\워너넷자료\요양병원실무업무자료.pdf`)를 Q&A 답변 자료로 쓰고 **DB에 저장**, **질문 가능**하게. 추가로 ①**질문내용을 카테고리로 나눠 보여주기** ②**어떤 내용이 있고 어떻게 질문할지 가이드** ③**자주하는 질문 순위**.
- **DB 3종 신설** [docs/sql/qna/TBL_QNA.sql] · 데이터 [docs/sql/qna/TBL_QNA_seed.sql] (운영DB 적재 완료, 336건):
  - `TBL_QNA_CAT` 대·중분류(P_CAT_ID로 계층) 47건 — 대분류 12(사내 5 + 원문 7), 중분류 35
  - `TBL_QNA_KB` 지식 336건 — **사내 확정지식 44건(SRC_TYPE='IN', 종전 qnaChat.jsp WQA_KB 배열 그대로 이관)** + **원문 292건('PDF')**. `KIND` = CARD(사내) / QA(질의응답) / ITEM(항목해설) / RAW(표 원문). `WEIGHT` 사내 20 · 원문 3~10, `HIT_CNT`(조회수)로 자주하는 질문 순위 산출. `FULLTEXT … WITH PARSER ngram`(MySQL 8.0.41 확인).
  - `TBL_QNA_LOG` 질문로그 — **`MATCH_YN='N'` 이 곧 "지식에 없어서 못 답한 질문" 목록**(다음 보강 대상).
- **원문 적재 범위(중요)**: 수술코드 목록(별표4 등)·별지 서식·전자문서 파일레이아웃은 **질문거리가 안 되어 제외**. 넣은 것 = 환자평가표 작성매뉴얼 44 · 고시 질의응답 166 · 수가 산정지침 35 · 행정해석 28 · 환자군 급여기준 8 · 관련법령 6 · 청구작성요령 5.
- **[함정·중요] 원문 추출은 `pdftotext -table` 로 한다** (2026-08-04 재작업으로 확정. 초기 `-layout` 판은 폐기):
  - `-layout` 은 **표의 한 행을 여러 줄로 흩어 놓는다** — 질병명은 이 줄, 질병코드는 아래 줄에 찍혀 화면에서 무슨 코드인지 알 수 없었다(사용자 지적). `-table` 은 행을 붙여 준다. 대신 낱말 간격이 넓어지므로 **2칸 이상 공백을 한 칸으로 축약**해 문장을 정상화한다.
  - **2단 표(연번|질의|답변)** — 질의응답 **02절(206~216p)** 과 **행정해석(176~200p)** 은 칼럼을 갈라야 한다. **경계는 표 머리글('답변') 글자 위치로 잡으면 안 된다**(머리글은 칸 가운데 정렬이라 본문 시작과 다름). **본문 '○' 위치의 최빈값**으로 잡는다. ← 이걸 몰라 처음엔 행정해석 분리를 포기하고 RAW 로 뒀었다(지금은 40건으로 정상 분리).
  - **세로 사이드탭**(장 제목을 한 글자씩 세로로 인쇄)은 **글자로 판별 금지** — '수·가·등·원'이 본문 끝에도 온다. 여러 줄이 공통으로 마지막 글자를 두는 **칼럼 위치**로 찾아 자른다.
  - **머리말의 장 표시(Ⅰ·Ⅱ·Ⅲ)는 앞뒤 어디에나 붙는다** — 위치 고정 매칭이면 일부 페이지가 안 걸러지고, 그 줄이 페이지 첫 줄이 되어 **절 제목 인식이 통째로 깨진다**(03~13절이 02절로 뭉쳤던 원인).
  - 질의응답 **절 제목(01.~13.)은 항상 페이지 첫 줄**에 온다 — 이 조건이 없으면 번호로 시작하는 <질문>이 절로 오인됨.
  - **행정해석 질의문은 400자를 넘기도 한다**(TITLE 컬럼 초과) → 제목은 줄이고 원문은 본문 앞에 `[질의]` 로 남긴다.
  - 재추출 절차·스크립트: [docs/tools/qna_pdf/](docs/tools/qna_pdf/README.md) — 원문 교체는 `SRC_TYPE='PDF'` 행만 지우고 다시 넣는다(사내 지식 보존).
- **서버(5계층, /mangr)**: `qnaInit.do`(카테고리+건수+TOP) · `qnaList.do`(카테고리별 질문목록) · `qnaGet.do`(답변+연관질문, 조회수+1, 로그) · `qnaSearch.do`(검색+로그). Mangr_SQL.xml·MangrMapper·MangrService(Impl)·MangrController. DTO 대신 `Map` 반환(조회 전용).
- **검색 랭킹(튜닝 확정)**: ngram MATCH + 문장 전체 LIKE + **낱말별 제목/키워드 가산(핵심)** + `WEIGHT/2` + 조회수. **[함정] 낱말 가산이 없으면** "유치도뇨관 14일"처럼 두 낱말 질문에서 문장 LIKE가 아무 데도 안 걸려 **본문에 그 말이 많은 엉뚱한 항목이 1등**이 된다. 화면의 자동펼침 판정도 **점수 절대값이 아니라 1등/2등 배율(1.35배)** 로만 — 낱말이 많으면 점수가 통째로 커져 절대값 기준은 항상 참이 된다.
- **화면 = 메뉴 1개** [qnacd.jsp](src/main/webapp/WEB-INF/jsp/main/mangr/qnacd.jsp) `/mangr/qnacd.do` — 사이드바 **[적정성평가 Q&A 자료]** (`#adminQnaMenu`, 위너넷 `s_wnn_yn='Y'` 일 때만 표시).
  - 화면 전체를 쓰는 **[좌]분류 · [중]질문목록+검색 · [우]답변** 3단. 답변 표시: 사내=불릿, 원문=줄바꿈 유지, 표원문(RAW)=고정폭 블록.
  - **[확정] 채팅식 배제**(2026-08-04) — 말풍선 주고받기가 아니라 자료를 찾아 읽는 화면이다.
  - **[확정·함정] 관련화면 버튼은 `window.open` 새 창**(고객지원 2종만 사이드바 모달). `location.href` 로 이동하면 안내대로 화면을 열어 본 뒤 Q&A 로 돌아오려고 메뉴를 다시 찾아야 한다(2026-08-04 사용자 지적).
  - **[확정] 첫 화면 안내에 분류 목록을 넣지 않는다** — 왼쪽 기둥과 중복이라 뺐음.
  - 좌측 분류에서 **같은 대분류를 다시 누르면 펼친 중분류가 접힌다**(질문 목록은 유지). 폭은 좌 305px · 중 27%(최대 500) · 우 나머지, 글자 기준 14.6px + 가－/가＋(localStorage 저장), 높이는 **실측**(fitHeight — 고정 calc 금지, 배율·줌마다 어긋남).
  - **[제거] 우측하단 플로팅 챗(tiles/main/qnaChat.jsp)** — 2026-08-04 사용자 요청으로 파일·include 모두 삭제. 되살리려면 git 이력에서 파일을 꺼내 main.jsp 에 include 한 줄만 넣으면 된다(서버 API 는 공용이라 그대로 동작).
- **[함정] 원문의 심볼폰트 글자**: Wingdings 등으로 찍힌 불릿이 사용자영역(U+E000~F8FF)으로 추출돼 화면에서 □(두부)로 깨진다. 시드 생성 때 `▸`·`·`·공백으로 치환하고 U+FFFD 는 제거한다(build.js `PUA`/`fix`).
- **지식 추가/수정은 이제 TBL_QNA_KB 만 고치면 된다**(화면 배포 불필요). 원문을 다시 만들려면 재추출 스크립트가 필요하므로 위 함정 항목을 먼저 확인할 것.
  - **[함정] DB 에서 고쳤으면 시드 파일을 다시 뽑을 것** — `docs/tools/qna_pdf/Export.java` 로 DB→`TBL_QNA_seed.sql` 을 갱신하지 않으면, 나중에 시드를 재적재할 때 고친 내용이 되돌아간다. (시드는 이제 PDF 생성기 산출물이 아니라 **DB 덤프**다.)
- **[확정] 「프로그램 사용법」 답변은 병원 사용자 관점으로 쓴다**(2026-08-04) — 월간보고서 답변이 위너넷 담당자가 하는 일(편집·승인·파일서버 저장) 위주라 병원이 읽으면 할 수 없는 절차를 안내받았다. 열람·확인·목표 등록·문의 중심으로 교체([TBL_QNA_kb_use_20260804.sql](docs/sql/qna/TBL_QNA_kb_use_20260804.sql), 적용 완료). 시뮬레이션은 병원 메뉴 이름인 **적정성-Simulation** 으로 표기.
- **배포**: 자바+XML+JSP 변경 → **WAR 재빌드 + 톰캣 재기동 필수.** DB는 적재 완료(재적재는 seed 파일 상단 DELETE 2줄로 초기화 후 재실행).

### [완료] 월보고서 목록(evalReportList.jsp) — 첨부 PDF 미리보기 (2026-07-31)
- **요청**: 관리자가 메일 보내기 전에 첨부 PDF 내용을 확인할 수 있게.
- **동작 2곳**: ① 목록 첨부 칸 `📎 있음` 클릭(밑줄 표시, 행 클릭=보고서 열기와는 위임 핸들러 `stopPropagation` 으로 분리) ② 메일 발송창 상단 [📄 첨부 PDF 확인] 버튼 — 둘 다 모달(`#erl-pdfModal`, z-index 1500 = 메일창 1400 위)로 PDF 표시.
- **★위너넷(관리자)만**: 일반병원 화면에서는 첨부 칸이 클릭 안 되는 '📎 있음' 표시만(2026-07-31 사용자 확정 — 병원은 보고서 화면에서 열람). 발송창 확인 버튼은 발송창 자체가 관리자 전용이라 별도 분기 불필요.
- **다듬기**: 닫기 버튼 흰배경 전환 시 글자색 미지정으로 '빈 버튼'(흰 글자) — `#erl-pdfModal .erl-phead .erl-btn` 에 color 지정. 메일창 머리줄 버튼 3개는 한 줄 고정(제목 min-width:0, 버튼묶음 nowrap).
- **수법**: 목록 응답에는 경로가 없어(haspdf Y/N 뿐) `loadEvalReport.do` 로 `mst.pdfpath` 를 받고, `/sftp/download.do` 가 attachment 강제라 **fetch blob→objectURL→iframe** (evalReport.jsp `erPdfPreview` 와 동일 — iframe 새 노드 교체·revoke 는 다음 열기·seq 가드 포함). `loadEvalReport.do` 는 문서열람(READ) 기록을 안 남기므로 관리자 미리보기가 '읽음'으로 오기록되지 않음(확인).
- **배포**: JSP만 — WAR 재빌드 불필요.

### [완료] 월보고서(evalReport.jsp) 요약장 확정 3건 — 부족점수 0표시·지표순위·용어 (2026-07-23)
- **부족점수 카드**: 목표 초과 달성으로 gap이 음수면 `0` 표시(renderGoalSummary, `er-gapScore`). 양수만 `+N.N`.
- **우선 개선지표 순위(topGaps) 확정**: 부족분 큰 순 단일정렬 → **진료영역(과정·결과) → 구조영역 → 지역사회복귀율('15')** 3단계 tier, 각 tier 안에서 부족점수 큰 순. topGaps가 Ⅰ-2 도표·Ⅲ 최우선 태그(상위2)·Ⅳ 권고·Ⅴ 시나리오에 공통 사용되므로 전 섹션 순위 일괄 변경됨(최우선 2개도 진료영역에서만 선정).
- **용어 통일**: 사용자 노출 "부족분" → **"부족점수"** (Ⅰ-2 헤더·캡션·비고, Ⅱ 상세표 헤더, Ⅳ 블록 인라인, 핵심진단 ※문구, 빈 메시지). 단 pri_note/diag_note는 편집영역이라 병원별 저장본이 있으면 옛 문구 유지됨. Ⅱ '획득률' 헤더는 사용자 확정으로 **'부족점검'**(값은 그대로 %).
- **구간 표기 "미만" 통일(bndUp 헬퍼)**: 절단 상한(19.99·29.99·0.49·0.24)→실제 경계(20·30·0.5·0.25)로. `0~19.99%`→`0~20미만%`, `26~30명`→`26~30미만명`, `6~9999.99명`→`6명 이상`. 적용: zoneListText(Ⅳ 나열)·zoneRange(Ⅱ·Ⅲ)·simReduceList·simRoomLower·structRiskTxt. 끝자리 9·4 절단형만 보정(PI 0.2/1.6 등 정확 경계는 불변).
- **Ⅲ 배지 앞수치 색**: 미달=빨강(er-b-bad)·만점=파랑(er-b-good). Ⅲ 리드문구 확정: "각 지표별 산정근거 (분모 · 분자 · 현황값 → 표준화구간 → 획득점수), 지표의 개선방향을 정리함."
- **배뇨관리(def_06) "(2026년 2주기 8차 신설)" 문구 제거**: JSP TPL_DEF + 시드 + **운영DB TBL_EVAL_REPORT_TPL 직접 UPDATE 완료**(TEXT엔 없었음).
- **잘라오기 '전체 넣기' 재추가**: 2026-07-22 제거했던 버튼을 사용자 요청으로 복원(모달 erCropAll + 팝아웃 sendAll, 둘 다 확인창 포함).
- **배포**: JSP만 — WAR 재빌드 불필요.

### [완료] 월보고서(evalReport.jsp) 표지 개편 + PDF 그룹별 페이지 분리 (2026-07-23)
- **표지 개편 (여러 차례 사용자 피드백으로 확정된 형태 — 재수정 시 이 확정 먼저 확인)**:
  - 목표등급 배지·"WinCheck⁺ 산출 자료…" 문구 제거 → **"■ 보고서 산출 기준 및 유의사항" 테두리 박스**(불릿 5개, `er-editable` data-key=`cover_notes`)로 대체. 각 불릿은 원본 문서와 동일하게 **`<br>` 고정 줄바꿈 3·3·4·3·3줄**(자연 줄바꿈 아님 — 폭 줄이면 깨짐).
  - 확정 스타일: 본문 14px/보통굵기, 제목 16px·900, 강조 `li b`('2주기 5차…적용') 15px·900, 항목간 여백 0(줄간격 1.95·자간 0.3px만). ※문구(`cover_notes_footer`)는 **박스 밖** 테두리 없이 13px·900 가운데, "심사평가원" 표기.
  - 표지 meta2는 "목표등급 → 현재 종합점수" 순서(원래의 역순). 작성일은 **박스 상단 우측**, 하단에 WinCheck⁺ 로고(`images/winct/wincheck.jpg`, 30px).
  - **[함정] 작성일(`er-coverDate`) 요소 제거 금지** — renderAll()이 el()로 직접 참조, 없으면 표지 렌더 전체 중단. **[함정] 작성일·박스 정렬은 고정폭 컨테이너(`er-cover-noteswrap` 725px)** — `width:fit-content`는 일부 렌더링(인쇄 복제)에서 전체폭으로 풀려 작성일이 페이지 우측 끝으로 감.
- **PDF 구조/과정/결과지표 그룹별 새 페이지 (erPdfGenPreview 분할)**:
  - `_erBreakYs`에서 그룹라벨(`er-grplabel`)을 강제 분할점(force)으로 — 슬라이스 루프에서 남은 공간과 무관하게 라벨 앞에서 끊음. 단 **첫 그룹(구조지표)은 grpSeen 카운트로 제외**(Ⅲ 헤더와 같은 장 — 안 빼면 헤더만 있는 빈 장 생김).
  - **[함정·핵심원인] `.er-indbox`를 분할 후보 sel에 반드시 포함** — 없으면 그룹라벨의 '직전 후보'가 지표제목(er-indhead, 헤더)이 되어 '헤더 바로 뒤 분할 금지' 규칙에 **라벨이 후보에서 통째로 제외**됨(그룹 분리가 아예 안 되던 원인).
  - 문단 단위 후보 추가: `.er-ana/.er-def/.er-plan`(긴 분석박스가 문장 중간 아닌 문단 사이에서 잘리게), `.er-anabar`는 HEAD 취급(분석내용 바만 페이지 끝에 남는 것 방지). 인쇄(window.print)용 print CSS에도 `#er-sec3Body .er-grplabel{page-break-before:always}`(first-child 제외) 추가.
  - 이어지는 장(중간에서 잘려 넘어온 페이지)은 상단 8mm 추가 여백(`contMm`) — 잘린 블록이 페이지 맨 위에 딱 붙지 않게. 여백만큼 그 장의 담는 양(limit)도 줄여 하단 넘침 방지.
  - 모든 절단선은 근처(위 60px) '완전한 흰 여백 줄'로 스냅(`snapWhite`) — 후보 top에서 그대로 자르면 다음 블록 테두리·그림자 1~2px 조각이 이전 장 끝에 남음(총평 박스 잔상).
  - 승인 시 "PDF 첨부하시겠습니까" 확인창 제거 — 승인되면 toast 후 바로 PDF 생성·미리보기(`erPickPdf`). 저장 여부는 미리보기 모달의 [파일서버 저장]/[닫기]에서 결정(사용자 확정).
  - **[확정 2026-07-23] 페이지 하단 빈 여백은 현재 유지** — 그룹·섹션 경계와 박스/카드/총평 통짜 보호가 우선(사용자 선택). "여백 줄여달라" 재요청 시 이 확정 먼저 확인(대안: 큰 블록 문단분할 허용).
- **배포**: JSP만 변경 — WAR 재빌드 불필요(파일 교체), 다음 배포에 포함.

### [완료] 배뇨훈련 점검 H3 오탐 — 규칙적 도뇨(REG_CATH) 제외 누락 (2026-07-19)
- **증상**: 충무요양병원 7월 하옥희 — **규칙적 도뇨 체크(REG_CATH='1') + 배뇨일지 7일**인데 H3(배뇨일지 작성했으나 배뇨훈련프로그램 미체크·분자제외 우려) 오류로 잡힘.
- **원인**: H3 조건이 배뇨훈련 3종 중 **일정배뇨(UR_PLAN)·방광훈련(BLAD_TRAIN)만 `<> '1'` 제외**하고 **규칙적 도뇨(REG_CATH)는 확인 안 함**. 규칙적 도뇨는 분자 인정 로직(Magam_SQL.xml select_CategoryList06·L1550 `OR ( pat.REG_CATH='1' )`)에서 **일수 요건 없이** 인정되는 항목이라, 도뇨 체크 환자가 오탐.
- **조치**: `select_assesCheck`의 H3 블록 2곳(Magam_SQL.xml, 전체 점검용·배뇨훈련 개별 점검용)에 `AND IFNULL(C.REG_CATH,'') <> '1'` 추가. UR_PLAN/BLAD_TRAIN='1'은 이미 제외되므로 일수 1~2일 케이스는 별도 조치 안 함.
- **배포**: XML만 변경 → WAR 재빌드 불필요(파일 교체 가능)하나 MyBatis 매퍼는 기동 시 로드 → **Tomcat 재기동 필요**.
- **관련파일**: Magam_SQL.xml(select_assesCheck, H3 두 블록)

### [완료] 마감업로드(magamFileUpload.jsp) 상단 년도/구분 카드 제거 — 그리드 공간 확보 (2026-07-16)
- **요청**: 년도 셀렉트를 탭(청구샘파일/환자평가표) 줄 맨 앞에 고정 배치, 의미 없는 "구분: 파일선택/폴더선택" 삭제, 상단 카드 공간 제거로 전체를 위로 올려 하단 그리드 공간 확보.
- **조치**: 상단 `card-header` 카드 삭제 → `year_Select`는 `#mg_FlagTab` ul 첫 li로 이동(탭 전환 무관 상시 표시). **`file_Select`는 삭제 아닌 숨김 유지** — JS 2곳(월 클릭 업로드·fn_DataVerify)이 값("2"=폴더선택→webkitdirectory) 분기를 읽음, 숨김 기본값 "1"=파일선택 고정. 기타 숨김 셀렉트(allowedFiles/specode/claimType/insurType/treatType)·samPickNewProg·samPickModeWrap도 숨김 컨테이너로 이동(전부 id 참조라 무영향).
- **추가 (월카드 영역 여백 축소)**: 테마 `.tab-regular .tab-content`의 기본 padding 30px가 월카드 위아래 큰 공백의 원인 → 페이지 한정 `<style>`로 `#mg_FlagTabContent{padding:13px 12px 12px}` + month-card `mb 6px` 오버라이드, tab-regular `mb-3→mb-2`. 하단 그리드 `page_Hight` 400→525, 그리드 카드에 `mb-3`.
- **월카드 높이 = 버튼 개수대로 (사용자 확정)**: 마감완료 월은 버튼 5개(등록/보기/입원현황/수가/🔓열림·잠김), 미마감 월은 4개. min-height/height:100% 통일안을 시도했으나 **사용자가 "버튼에 맞게" 선택 → 카드별 자연 높이**(4버튼 달은 짧게 끝남, 바닥선 불일치 허용). 영역 전체 높이는 가장 긴 카드가 결정. **높이 통일(min-height) 재요청 시 이 확정 먼저 확인.**
- **하단 그리드 = 실측 자동 맞춤 (v5 최종, 시행착오 3개 기록)**: `window.fn_FitGridHeight()` = `innerHeight - layoutTop(문서기준!) - 카드내부여백(실측) - GRID_BOTTOM_RESERVE(12)` 를 scrollBody에 inline !important 적용, **0.8초 setInterval 상시 고정**. `p_Collapse=false`, 그리드 카드 mb-0, dashboard-content padding-bottom 0.
  - **[함정1] 고정 calc() 오프셋 방식 폐기** — 윈도우 배율/브라우저 줌마다 어긋남.
  - **[함정2] top은 반드시 문서기준(r.top+scrollY)** — 화면기준(r.top)만 쓰면 스크롤 시 그리드가 계속 자라는 자가증식 루프(v4까지의 버그).
  - **[함정3] 재적용 스킵 가드는 실제 offsetHeight와 비교** — '직전 목표값'과 비교하면 DataTables 재그리기가 높이를 되돌려도 영원히 스킵(v3 버그).
  - 진단: 콘솔 `[gridFit v5]` 로그 잔존(의도적). 바닥 여백은 GRID_BOTTOM_RESERVE 하나로 조정.
- **[함정] 이 화면 콘솔 소스가 dashboard.do 로 찍힘**: 자료올리기 화면인데 콘솔 준비완료/오류가 dashboard.do:NNNN 으로 표시됨(JSP 수정 시 줄번호만 이동). 반영 여부는 준비완료 줄번호 변화나 `[gridFit]` 로그로 판별.
- **배포**: JSP만 변경 — WAR 재빌드 불필요(파일 교체로 가능), 다음 배포에 포함.

### [완료] 06.배뇨관리 관리항목 마크 3단계(●/○/공란) — DIARY_DAYS 연계 (2026-07-16)
- **요청**: 관리항목(일정배뇨/방광훈련) 표시를 3가지로 — **●**=체크+배뇨일지 3일이상(분자 인정), **○**=①체크했으나 일수 3일미만(1~2일·미기재) ②일수는 기재됐으나 둘 다 미체크(체크누락 의심, 일정배뇨 칸에 표시), **공란**=일수도 없고 체크도 없음(아무것도 안 한 환자). 기존엔 분모-비분자 전원이 ○(planMissYn='X' 링)라 "아무것도 안 한 사람"과 "부분 실시"가 구분 안 됐음.
- **조치**: Magam_SQL.xml `select_CategoryList06`에 `pm.DIARY_DAYS AS diaryDays` 추가 · PatvalDTO `diaryDays` 필드 추가 · assessment.jsp 06블록에 `_urDiaryDays()/_UR_MARK_FULL/_UR_MARK_RING` 헬퍼 신설, urPlan/bladTrain 렌더를 일수 기반 3단계로 교체(**planMissYn 링 로직 제거** — JSP 미참조, SQL의 planMissYn 계산은 잔존·무해). 규칙도뇨(regCath)는 일수 요건 없어 기존 ● 유지.
- **엣지 판정**: 체크+일수 미기재(0) → ○(공란 아님 — 공란은 "미체크+일수없음"만) · 미체크+일수 1~2일 → ○(일수 기재가 있으므로 공란 제외).
- **배포**: 자바(DTO)+XML 변경 → **WAR 재빌드 필수**.
- **관련파일**: Magam_SQL.xml(select_CategoryList06), PatvalDTO.java(diaryDays), assessment.jsp(cate_cd '06' 블록)

### [완료] 월보고서(report.jsp) 목표점수·목표등급 = 차등제(TBL_GRADE_MST) 연계 (2026-07-14)
- **배경**: 월보고서 표지의 "목표등급 3등급 (78점)"이 HTML 하드코딩 고정값이라, 실제 병원 목표(차등제 등록 화면 `TBL_GRADE_MST`)와 따로 놀았음. 청암요양병원처럼 실제 목표가 1등급/88점인데도 보고서는 3등급/78로 오표시.
- **소스**: `TBL_GRADE_MST` (병원+년도+분기별). `GOAL_SCORE`=목표점수(88), `HOSPGRADE`=병원등급(1). 저장/조회는 User_SQL.xml `saveHospGrd`/`selHospGrdList`, assessment.jsp 차등제 폼.
- **조치 (4계층, evalYm→분기 변환)**:
  - Magam_SQL.xml `selectHospGoalGrade` 신설: hosp+START_YY+QTER_FLAG, ACTION_YN='Y', UPD_DTTM DESC LIMIT 1, alias 소문자(goalscore/hospgrade)
  - MagamMapper.java `selectHospGoalGrade(hospCd, startYy, qterFlag)` 추가
  - MagamServiceImpl.loadEvalReport: evalYm(YYYYMM)→startYy=substr(0,4), qterFlag=(mon+2)/3 계산 → res.put("goal", ...)
  - report.jsp: `applyGoalDefault(res.goal)` = cover_goal_score/grade/badge 를 차등제 값으로 **항상 덮어씀(저장 override 무시)** — 목표값은 병원이 직접 등록한 사실값이라 옛 하드코딩 3등급/78 저장분이 마스킹하지 못하게. 마스터 미등록 분기면 미변경. `renderGoalSummary()` 분리해 목표값 확정 후 부족점수·등급표 재계산. HOSPGRADE 빈 분기는 GOAL_SCORE로 gradeOf() 유도.
- **동작**: 병원별 실제 목표(GOAL_SCORE/HOSPGRADE) 자동 표시. 목표를 바꾸려면 차등제 등록 화면에서 수정(월보고서에서 목표 수기수정은 저장돼도 다음 조회 때 마스터값으로 되돌아감).
- **[중요] 형식 확인됨**: QTER_FLAG=숫자문자열("2"), START_YY="2026", HOSPGRADE=숫자("1")=1등급. assessment.jsp monsQuarter/hospcdGrade 셀렉트 value=(idx+1). 서비스 qterFlag=(mon+2)/3.
- **[함정] 안 바뀌면**: ① WAR 재빌드 안 함(자바+XML이라 필수) ② TBL_GRADE_MST에 해당 병원·분기 행 없음(goal=null→미변경) — 둘 다 아니면 select_Eval_Indi 병원코드/분기 확인.
- **배포**: 자바+XML 변경 → **WAR 재빌드 필수**.
- **관련파일**: Magam_SQL.xml(selectHospGoalGrade), MagamMapper.java, MagamServiceImpl.java(loadEvalReport), report.jsp(applyGoalDefault/renderGoalSummary)


### [완료] 샘파일 업로드(TBL_FILES_DATA→각 테이블) 대폭 최적화 — SP_UPLOAD_MAGAM_SAMFILES 재작성 (2026-07-09)
- **증상**: 청구샘파일 업로드가 너무 느림(17,254줄 = 4분16초, 평가파일 417줄 = 11.6초). 병목은 Java INSERT가 아니라 **프로시저의 라인별 처리**.
- **원인 (라인당 반복 비용)**: ① 컬럼 커서가 컬럼(TBL_PATVAL_MST는 240개)마다 lineval 전체를 REPLACE×2+EUC-KR 변환 반복 ② mg_flag='9'는 라인마다 INFORMATION_SCHEMA로 table_update(GROUP_CONCAT) 재계산 ③ 한 줄당 INSERT 1문 → 수천 번 PREPARE/EXECUTE.
- **조치 (DB 프로시저 교체, 앱 로직 무변경)**: [docs/sql/proc/SP_UPLOAD_MAGAM_SAMFILES.sql] = ⓐ 라인 1회만 `@line_bin` 변환 ⓑ 명세서/평가표 라인은 커서 대신 GROUP_CONCAT 셋 기반 1쿼리로 val 목록 생성(TBL_CHUNG_MST 청구서 라인만 원본 커서 유지 — claim_no/M010.1 합계 캡처) ⓒ 같은 그룹 연속 라인 multi-row INSERT 배치(500행/2MB 상한) ⓓ table_update 루프 밖 1회 ⓔ 신규 [SP_EXECUTE_DYNAMIC_SQL2](docs/sql/proc/SP_EXECUTE_DYNAMIC_SQL2.sql) = 기존 실행기의 stmt_text TEXT(64KB 한계) → LONGTEXT (배치 문장이 64KB 초과, **기존 SP_EXECUTE_DYNAMIC_SQL은 다른 호출자용으로 그대로 둠**).
- **함정 2개 (재수정 시 주의)**: ① 배치 INSERT가 64KB 넘으면 구 실행기 stmt_text TEXT에서 잘려 오류 → 반드시 SQL2 사용. ② NOT FOUND 핸들러가 col_done=0/1로 콜/데이터 커서를 구분하는데, 셋 기반 경로는 col_cursor를 안 돌아 col_done=0으로 남음 → **dat FETCH 직전 `SET col_done=1` 필수** (없으면 마지막 줄 중복 처리 → total_cnt+1 → 30000 오류).
- **검증 (가짜 HOSP_CD=99999998로 실데이터 복제, 전 테이블 COUNT+CRC32 체크섬 비교 → 4개 데이터셋 모두 완전 동일)**: GHP 4,260줄 18.0s→2.3s / REP 17,254줄 255.7s→9.3s(27.6배) / 평가 417줄 11.6s→1.7s / 산재 6파일 133줄 0.25s→0.15s.
- **원본 백업**: [docs/sql/proc/SP_UPLOAD_MAGAM_SAMFILES_backup_20260709.sql] (DELIMITER 감싸 실행하면 즉시 원복).
- **부수 변경**: magamFileUpload.jsp 예상시간 공식 80/25줄/초 → 3초+600/120줄/초. (JSP만이라 WAR 재빌드 불필요하나 다음 배포에 포함할 것)
- **후속 (2026-07-09 오후, 대형 파일 90000 실패)**: 4.9MB/2.6만줄 GHP 업로드가 진행바 100% 후 한참 돌다 `90000` 실패. **원인은 자바 쪽 TBL_FILES_DATA 적재** — MyBatis foreach가 2.6만행을 한 문장(6MB+, 파라미터 34만개)으로 생성하다 예외(+ DBCP removeAbandonedTimeout 300초 강제회수 가능성). **조치**: ① [MagamServiceImpl] `insertFilesDataBatch` 신설 = 순수 JDBC 배치(1000행 단위 executeBatch, URL의 rewriteBatchedStatements=true로 multi-row 변환, 단일 커넥션 마지막 commit이라 전체성공/전체취소 유지). `uploadMagamFilesMain`/`uploadMagamFilesOnly` 둘 다 이걸 쓰도록 교체(기존 mapper.uploadMagamFilesMain XML은 잔존·미사용) ② [context-datasource.xml] removeAbandonedTimeout 300→900 (양 프로파일). **주의**: 기존 `jdbcTemplate` 필드는 @Autowired가 없어 null(uploadMagamFilesBatch는 죽은 코드 — 호출하면 NPE) → DataSource 직접 주입 방식 사용. **자바+XML 변경 → WAR 재빌드 필수.**

### [완료] 차등제(01~04) 저장 안됨 + 직전분기 자동복제 + 저장분기 그리드 신설 (2026-07-03)
- **증상 1 (업데이트 안 됨)**: 분기 선택 후 저장해도 값이 이전 그대로. **원인**: `saveHospGrd`가 `INSERT ... ON DUPLICATE KEY UPDATE`인데 TBL_GRADE_MST에 (HOSP_CD,START_YY,QTER_FLAG) UNIQUE 키가 없으면 매 저장이 새 행 추가 → `selectHospGrd`가 `LIMIT 1`(정렬 없음)로 **옛 행**을 읽음. **조치**: [UserServiceImpl.saveHospGrd] = **UPDATE 먼저(`updateHospGrdData` 신설) → 0건이면 INSERT**, `selectHospGrd`에 `ORDER BY UPD_DTTM DESC` 추가. 기존 중복행은 다음 저장 때 전부 같은 값으로 수렴(삭제 불필요).
- **증상 2 (하나 저장했는데 두 분기 생김)**: 컨트롤러 `saveHospGrd.do`가 저장 전에 **직전 분기에도 동일 값 자동복제 저장**(+직전분기 구조영역 재계산). 사용자 요청으로 **제거** — 한 번 저장 = 선택 분기 1건만. `callIndicatorsStructureZone`은 현재 미호출(코드 잔존, 지표 재계산은 화면 fn_CreateData가 수행).
- **신설 (저장분기 그리드)**: assessment.jsp 차등제 폼과 하단 문구 사이에 저장된 분기 목록 그리드(`#grdListWrap`, 전 항목+수정일시, 좌우스크롤). 신규 조회 `selectHospGrdList`(/user/selectHospGrdList.do, 5계층). 동작: **보기 진입=해당년도(평가년월 년도) 내 최종분기 자동선택**(년도 셀렉트는 안 바꿈)·**조회하기=선택한 분기**·그리드는 **선택 년도만 표시**(없으면 "N년 저장된 자료가 없습니다")·행 클릭=폼 적용·저장 후 그리드 자동갱신+우측 카드 유지(fn_CreateData가 card_container 숨기는 것 즉시 복원).
- **배포**: 자바+XML 변경 → WAR 재빌드 필수.

### [완료] 대시보드 500 전 병원 장애 — s_hospid 세션쿠키화 회귀 + 빈 뷰 500 (2026-06-10)
- **증상**: 전 병원에서 `dashboard.do` 가 HTTP 500(흰 화면). 콘솔 `Failed to load ... dashboard.do 500`, 서버로그 `Could not resolve view with name ''`. 개발자 PC만 정상(예전 쿠키 잔존).
- **근본원인 1 (회귀)**: 어제 커밋 `893db73`(06-09 13:40)이 [top.jsp](src/main/webapp/WEB-INF/tiles/main/top.jsp) 에서 `s_hospid`(+s_hospnm/s_conact_gb/s_winconect/s_closeDt1/2) 를 `setCookie(...,1)`(1일 유지) → `setSessionCookie`(세션쿠키)로 변경. **브라우저 종료 시 쿠키 삭제** → 재접속 시 `s_hospid` 쿠키 없음.
- **근본원인 2 (구조적 버그)**: 모든 화면 진입 컨트롤러가 `cookie_value.get("s_hospid").trim()` 호출 → 쿠키 null이면 **NPE → catch → `return ""`** → Spring 빈 뷰 못 찾음 → **500**. 로그인은 세션/ sessionStorage 에만 저장하고 `s_hospid` **쿠키는 top.jsp 병원선택 시에만** 생성되는 구조라, 세션쿠키화가 치명적.
- **조치**:
  1. [top.jsp](src/main/webapp/WEB-INF/tiles/main/top.jsp) L312~ : `setSessionCookie` → `setCookie(...,1)` **원복**(전 병원 즉시 복구)
  2. [UserController.java](src/main/java/egovframework/wnn_medcost/user/web/UserController.java) `main1`(dashboard.do): null-safe(`String s_hospid; if(s_hospid!=null && !trim().isEmpty())`) + 쿠키 없으면 `return ".login/LoginWinCT"`(500 대신 로그인 화면)
  3. **6개 컨트롤러 일괄**: User/Base/Tong/Mangr/Chung/Magam 의 모든 뷰 가드 `return ""`(else+catch, 총 102곳) → `return ".login/LoginWinCT"`. NPE가 나도 catch에서 로그인 화면 반환되어 500 영구 차단. (AJAX 엔드포인트는 `return ""` 미사용 — 영향 없음 확인)
- **주의**: `s_hospid` 를 다시 세션쿠키로 바꾸지 말 것. 로그인 후 `s_hospid` 쿠키를 서버에서 직접 심거나 두 앱 공유 인증토큰을 도입하기 전에는 영구쿠키 유지가 안전. 직접 URL 우회 차단은 컨트롤러의 로그인뷰 반환으로 대체됨.
- **배포**: 자바(.java) 변경 포함 → **WAR 재빌드 후 톰캣 재배포 필수.**
- **[확정 2026-06-10] 직접 URL 접근 차단(로그인 강제) — 안 함**: `…/user/dashboard.do` 를 즐겨찾기로 직접 쓰는 기관/사용자가 실제로 있어, 로그인 강제 시 그들이 매번 로그인해야 함. 사용자 결정으로 **현재(영구 1일 쿠키 + 직접 .do 접근 허용) 유지**. 보안은 쿠키 1일 만료 + 로그아웃 삭제로 최소 확보. **"직접접근 막자/로그인 거쳐야만" 재요청 시 이 방침과 [s_hospid 세션쿠키 금지] 먼저 확인.** 정 강화하려면 s_hospid는 영구 유지한 채 별도 세션쿠키 게이트로만(절대 s_hospid를 세션쿠키화 금지), 로컬 테스트 후 적용.
- **후속 (jsessionid 404 / 크롬 무스타일)**: 세션쿠키(JSESSIONID) 미전송 브라우저(예: 미로그인/쿠키차단 크롬)에서 서버가 전 URL에 `;jsessionid=` 를 붙여(URL 리라이트) 정적 CSS/JS 가 `…css;jsessionid=…` → **404 → 화면 무스타일**. [web.xml](src/main/webapp/WEB-INF/web.xml) 에 `<session-config><tracking-mode>COOKIE</tracking-mode></session-config>` 추가로 URL 리라이트 차단(쿠키 전용 추적). 엣지는 세션쿠키 있어 정상, 크롬은 미로그인/쿠키차단 시 발생. 이 설정으로 쿠키 없어도 정적파일은 정상 로드되어 최소한 스타일은 깨지지 않음. **web.xml 변경도 재배포/재기동 필요.**

### [완료] 마감 샘파일 업로드 — `SP_EXECUTE_DYNAMIC_SQL` "Query was empty" + 핸들러가 진짜오류 은폐 (2026-06-23)
- **증상**: 특정(큰) 청구샘파일 업로드 시 `45000 - SQLSTATE 없음 - MESSAGE 없음` 팝업. 데이터는 정상인데 파일 지우면 다음 줄/파일에서 또 발생. `TBL_DISEASE_MST`, GHP, valsize=78 등에서 반복.
- **근본원인**: [SP_EXECUTE_DYNAMIC_SQL] 이 동적SQL을 `;` 로 분리 실행하는데, **마지막 세미콜론 뒤 개행(`\n`)** 이 빈 문장으로 잡혀 `PREPARE '\n'` → **`Query was empty`(errno 1065)**. 가드가 `LENGTH(stmt_text) > 0` 이라 `"\n"`(길이 1)이 통과. 큰 파일이라 루프 중간 flush(`LENGTH(iuds_sql_nm) > max_insert_length=10MB`)가 발동하며 `;\n` 으로 끝나 노출됨. (작은 파일은 끝이 `TBL_MAGAM_INFO` INSERT의 `;` 라 안 걸림)
- **왜 원인이 안 보였나 (중요)**: 핸들러들이 진짜 오류를 계속 변형/소실시킴.
  1. `SP_EXECUTE_DYNAMIC_SQL` 의 EXIT HANDLER 가 `CONCAT('... ', sql_value)` 에서 **CONCAT 인자 NULL → 전체 NULL → `SIGNAL SET MESSAGE_TEXT = NULL`** → `Variable 'MESSAGE_TEXT' can't be set to the value of 'NULL'` 2차오류로 덮음.
  2. 메인 `SP_UPLOAD_MAGAM_SAMFILES` 의 EXIT HANDLER 는 `NOT FOUND` continue 핸들러가 진단영역을 덮어 `GET DIAGNOSTICS` 가 NULL/빈값 반환 → "SQLSTATE 없음".
- **조치 (확정)**:
  1. **[SP_EXECUTE_DYNAMIC_SQL]** 가드를 `IF stmt_text IS NOT NULL AND stmt_text REGEXP '[^[:space:]]' THEN` 로 변경 → 공백/개행만 있는 조각 스킵(`TRIM` 은 개행 못 지워서 REGEXP 사용). **EXIT HANDLER 3개는 모두 제거(주석)** — 오류는 메인으로 그대로 전파시키는 게 안전(인너는 롤백 기능 없음, 메시지만 변형했음).
  2. **[SP_UPLOAD_MAGAM_SAMFILES] 메인 EXIT HANDLER 복구**: `GET DIAGNOSTICS` + `errcode/errmess`(stage/colVal/lastSQL 디버그필드 포함) + `ROLLBACK`, **RESIGNAL 없음**(앱이 친절한 팝업 띄우게). `NOT FOUND` continue 핸들러는 유지.
- **디버깅 기법(재사용)**: 원인 안 보일 때 ① 메인 errmess 에 `@dbg`(단계마커)·`@sql_val`(실패 동적문장) 추가 → 위치 특정, ② 그래도 안 보이면 **핸들러 전부 주석 + 메인에 `RESIGNAL` 임시추가** → 원본 MySQL 오류를 톰캣 로그/팝업에 그대로 노출. 정리 시 RESIGNAL 제거.
- **주의**: `col_list`/`val_list` 는 LONGTEXT 로 확장해 둠(원래 VARCHAR(4000)). 4000 오버플로우는 이번 원인은 아니었지만 큰 테이블 대비 유지.
- **배포**: 프로시저 2개 `DELIMITER` 재생성. (앱 변경 없음)

## 적정성평가 (assessment.jsp) 요구사항

### [대기] 유치도뇨관 전월 대상자 당월 추적 기능
- **내용**: 전월 유치도뇨관 있는 대상자(제거 대상자 포함)를 당월 화면에서 체크박스로 확인 가능하도록
- **구현방향**: 당월 cate_cd='05' 환자 목록에 "전월에도 유치도뇨관 있었음" 체크박스 컬럼 추가
- **필요작업**: 전월 대상자 조회 SQL 추가 → 당월 목록 렌더링 시 매칭 표시
- **관련파일**: assessment.jsp, Magam_SQL.xml

### [대기] 유치도뇨관 엑셀 업로드 오류 크로스체크 기능
- **내용**: EMR/OCS의 유치도뇨관 삽입 오더 내역을 엑셀로 업로드하면, 평가표 상 삽입일자/제거일자와 자동 대조하여 오류 검출
- **오류 케이스**:
  - 오더에 삽입 있는데 평가표에 누락
  - 오더 삽입일 ≠ 평가표 개시일 (날짜 불일치)
  - 오더에 제거 기록 있는데 평가표에 제거일 미입력
  - 평가표에 있는데 오더에 해당 삽입코드 없음
- **구현방향**: 엑셀 파싱 → 평가표 데이터 조회 → 환자별 매칭 비교 → 오류 목록 표시
- **관련파일**: assessment.jsp, Magam_SQL.xml

### [대기] 특정수가현황(TBL_SPCSUGA_INFO) ↔ 환자평가표 크로스체크
- **내용**: magamFileUpload.jsp에서 업로드한 특정수가현황(M0060 등) 데이터를 환자평가표(TBL_PAT_INDI/assessment)와 대사하여 누락/불일치 검출
- **엑셀 필수컬럼**: 요양기관기호, 입원일, 주민번호, 환자성명, 보험/수가코드(정의코드/청구코드), 내원일(여러개-쉼표/공백 구분 MM-DD 형식)
- **업로드 시 처리규칙**:
  - 내원일 여러개인 경우 개수만큼 행 확장하여 저장
  - 내원일 연도는 같은 행 입원일의 연도로 채움 (MM-DD → YYYY-MM-DD)
  - 엑셀의 요양기관기호와 로그인 병원 일치 여부 확인, 불일치 시 경고(저장은 세션 hosp_cd로 강제)
- **관련파일**: magamFileUpload.jsp, MagamController.java(saveSpcsugaDatas), Magam_SQL.xml, SpcsugaDTO.java, assessment.jsp

### [완료] 06.배뇨관리 — 관리항목 컬럼 + 관리여부 색상화 (2026-05-07)
- **내용**:
  - 그리드 헤더: "배뇨상태 → 관리항목 → 관리여부" 순서, **관리항목**은 colspan=3 으로 [일정한 배뇨 / 방광훈련 / 규칙적 도뇨] 묶음 (2-row header)
  - 각 sub-column: `UR_PLAN='1'` / `BLAD_TRAIN='1'` / `REG_CATH='1'` 일 때 **검정 ◯** 표시 (그 외 공란)
  - 관리여부: `manageYn='Y'` (분자 해당) → "**관리**" 파란색 굵게 / 그 외 → "**제외**" 회색
- **변경파일**:
  - `assessment.jsp` cate_cd === "06" 블록: c_Head_Set 1행 → 2행(object[][]) 변환, columnsSet 에 urPlan/bladTrain/regCath 3컬럼 추가, manageYn render 수정
  - `Magam_SQL.xml` `select_CategoryList06`: SELECT 에 `pm.UR_PLAN AS urPlan`, `pm.BLAD_TRAIN AS bladTrain`, `pm.REG_CATH AS regCath` 추가
  - `PatvalDTO.java`: `urPlan`, `bladTrain`, `regCath` 필드 + getter/setter 추가
- **호환성**: `fn_PrependPatvalChangedColumn` 이 1D/2D c_Head_Set 모두 지원하므로 변경 컬럼 prepend 정상 동작

### [완료] 환자평가표 조회 버튼 활성화 (2026-04-23)
- **상태**: **활성화됨** — `viewTable` 렌더 후 DataTable 버튼 영역(.dt-buttons)에 "환자평가표 조회" 버튼 표시
- **적용 내역**:
  - CSS `#btnPatvalView { display:none !important }` 규칙 제거
  - `fn_AttachPatvalBtnToDt` 의 숨김 블록 제거, 부착+`_show` 클래스 부여 블록 활성화
- **관련파일**: assessment.jsp(fn_AttachPatvalBtnToDt, fn_ShowPatvalModal, _pvBuildHtml 등), Magam_SQL.xml(select_PatvalMst), MagamController.java(/main/select_PatvalMst.do)
- **재숨김이 필요할 때**: `fn_AttachPatvalBtnToDt` 의 `(1) 환자평가표 조회 버튼` 블록을 이전 숨김 로직(pvBtn 원위치 복귀)으로 되돌리고 CSS `#btnPatvalView { display:none !important }` 재추가

### [확정] 유치도뇨관 오류점검 — 필터 없음 + 정보 컬럼 표시 방식 (2026-04-18 최종)
- **최종 방침**: 자동 필터링 대신 사용자가 직접 판단하도록 **모달에 정보 컬럼 표시**
  - 컬럼: 환자ID · 성명 · 입원일 · **환자분류군(PAT_CLASS)** · **평가구분(EVAL_TYPE 코드·설명)** · **유치도뇨관 삽입(INDWELL_CATH)** · 점검항목
  - 평가구분 포맷: "1·입원 평가" / "2·계속 입원 중인 환자 평가" / "3·이전 환자평가표를 적용하는 경우" (PDF 명세서식 2024.7.1.~)
- **필터 상태**: select_assesCheck00/02, select_CathCrossCheck, select_PrevMonthMissing05 **모두 필터 없음** (원상태)
- **모달 UI**: 폭 1400px, 제목바 드래그로 이동 가능
- **관련파일**: Magam_SQL.xml, assessment.jsp (fn_ShowCath05Modal, _cath05EnableDrag, _EVAL_TYPE_MAP)
- **재수정 주의**: 이전에 그리드 필터를 적용했다가 사용자 선택으로 철회함. 재적용 요청 시 이 방침 먼저 확인

### [철회] 유치도뇨관 크로스체크 D7 (2026-04-24 제거)
- **원래 기능**: 오더(M0060) 당월 있으나 당월 평가표 자체가 없는 케이스를 D7 오류로 잡음 (2026-04-23 추가)
- **철회 사유**: "당월 평가표가 없으면 수가가 있어도 무조건 제외" 방침 확정 (2026-04-24 사용자 요청)
- **제거 내역**:
  - Magam_SQL.xml `select_CathCrossCheck`: D7 UNION ALL 블록 전체 삭제
  - assessment.jsp L527, L604: `if (errType !== 'D3' && errType !== 'D7')` → `if (errType !== 'D3')` 로 축소
- **재부활 시 주의**: 사용자 요청으로 제거된 기능이므로, 재도입 요청이 올 때 이 방침 먼저 확인

### [완료] 유치도뇨관 크로스체크 작성일 기준 — D3/D4/D6 검증 기간 (전월 작성일, 당월 작성일] (2026-05-12 최종)
- **배경**: 오더→평가표 크로스체크가 "작성일 이후 오더" + "전월 평가에 속한 오더"까지 오류로 집계 → 차기/전기 평가표에서 반영될 오더를 현재 평가에 책임지우는 문제
- **검증 기간 룰 (모든 D 블록 통일)**: `(전월 평가표 MAX(DOC_DT), 당월 평가표 DOC_DT]`
  - 하한: 전월 평가들 중 **마지막 작성일** 다음날부터 (그 이전 오더는 전월 평가에 반영되었어야 함)
  - 상한: 당월 평가표 작성일까지 (그 이후 오더는 차기 평가에서 반영될 것)
  - 전월 평가표 없으면 하한 미적용 (COALESCE → '00000000')
- **최종 규칙 (작성일 기준, 2026-05-12)**:
  - **D3 (오더O/평가표X)**: `MED_START > 전월 MAX(DOC_DT) AND <= 당월 MAX(PX.DOC_DT)` — 당월 평가표 없으면 상한이 NULL → 자동 제외
  - **D4 (날짜불일치)**: `MED_START > 전월 MAX(DOC_DT) AND <= P.DOC_DT` — 해당 평가표 작성일 이후 + 전월 마지막 평가 이전 오더 모두 검증 제외
  - **D6 (평가표O/오더X, 날짜 단위)**: CAT_IN_1~10 10-way UNION ALL + 기간 필터 (상한 당월 MAX(DOC_DT), 하한 **전월 MAX(DOC_DT)+1일** ← 2026-05-12 MIN→MAX 변경)
  - 당월 평가표 없는 환자는 전 블록에서 자동 제외 (D7 철회와 일관)
- **사례 (2026-05-12 사용자 보고)**: 남서울요양 8월 김창*, 7월 평가 작성일 7/28, 8월 평가 작성일 8/18, 오더 삽입일 7/14 → 7월 평가 기간(7/28까지) 안 기록이므로 8월 D4 오류에서 제외되어야 함. 이전 코드는 상한만 있어 7/14를 8월 D4로 오탐
- **우측하단 수가(M0060) 패널 필터 (assessment.jsp `_cath05RenderSpcsuga`)**:
  - 행 클릭 시 선택 행의 `data-docdt` 캡처 → `_cath05LoadDetail(patId, admitDt, docDt)` → `_cath05RenderSpcsuga(rows, maxDocDt)`
  - `r.medStart > maxDocDt` 행은 렌더링에서 제외 (오류체크 로직과 동일 기준)
- **XML 주의**: `<=` 는 `<![CDATA[ <= ]]>` 로 감싸야 파싱됨. `>=` 는 그대로 OK
- **클라이언트 JS 동작**: D6는 A1/A2가 아니므로 1·입원평가 필터에서 자동 제외됨
- **관련파일**: Magam_SQL.xml(select_CathCrossCheck), assessment.jsp(fn_ShowCath05Modal 행 렌더 L766, _cath05BindRowClick, _cath05LoadDetail, _cath05RenderSpcsuga)

### [대기] 유치도뇨관 오류점검 — 평가구분별 오류 추가/제외 규칙 (2026-04-?? 요청)
- **ADD (새 오류로 잡을 케이스)**:
  - 평가구분 1·입원평가 + **당월 입원환자가 아닌데 입원평가로 체크된 경우** (errType 예: `A1`)
  - 평가구분 1·입원평가 + **당월 이미 입원평가가 있는데 또 입원평가로 체크된 경우** (errType 예: `A2`)
  - **[철회 2026-05-12] 우측 패널 prevMaxDocDt 필터**: 한 번 추가했으나 사용자 요청으로 원복. 우측 수가오더 패널은 **상한(maxDocDt)만** 필터링하고 하한 미적용 — 사용자가 모든 오더를 시각적으로 확인할 수 있게 유지. 오류 검출(SQL)에서만 검증 기간 적용
- **EXCLUDE (오류에서 제외)**:
  - 평가구분 2·계속입원중 환자 중 **전월 평가표 상 유치도뇨관이 제거된 대상자**
  - **당월 평가표가 없는 대상자** (현 _prevMissingData / 전월대상자 당월미존재)
- **3가지 대상자는 오류에서 제외** (요청자 표현)
- **필요 서버 작업**:
  - Magam_SQL.xml: select_assesCheck00(또는 신규) 에 A1/A2 케이스 SELECT UNION
  - 판정 조건: (1) 당월 입원환자는 TBL_IPWON_INFO 로 체크, (2) 당월 입원평가 중복 여부는 TBL_PATVAL_MST evalType='1' 카운트로 체크
  - select_CathCrossCheck 등에서 evalType='2' + 전월 indwellCath 제거 케이스 제외 조건 추가
- **필요 클라이언트 작업**:
  - assessment.jsp fn_ShowCath05Modal 의 _prevMissingData 루프 제거 또는 조건부 스킵
  - A1/A2 errType에 대한 라벨 매핑 추가
- **관련파일**: Magam_SQL.xml, MagamController.java, assessment.jsp

## 샘파일 자동수집 에이전트 (구상)

### [대기] 병원 PC 에이전트 — 샘파일 생성 감지·자동 업로드 (2026-07-03 구상, 착수 보류)
- **배경**: 샘파일이 심평원 제출 후에는 암호화되어 복호화가 복잡 → 제출 전 생성 직후 원본 확보. 위너넷(웹) 미실행 상태에서도 수집되도록 **별도 에이전트 설치 방식** 채택 결정.
- **구성안**: 병원 PC 상주 에이전트(C#/.NET 트레이 + FileSystemWatcher 권장) — 병원별 감시폴더 기준정보, 새 샘파일 감지→쓰기완료 대기→HTTPS 업로드→재시도·중복방지(해시). 서버는 **기존 업로드 경로(/main/uploadMagamFiles.do) 재활용** 검토(에이전트용 토큰 인증만 추가), 병원별 기준정보(HOSP_CD·폴더·토큰)+수신이력 테이블.
- **핵심 체크**: ①개인정보(주민번호 포함) 전송·저장 보안 및 처리 위탁계약 ②병원 방화벽·설치 정책 ③청구SW별 샘파일 생성폴더·파일명 규칙(병원별 상이).
- **상태**: 개발 가능 판단 완료. 착수 시점은 추후 결정.
