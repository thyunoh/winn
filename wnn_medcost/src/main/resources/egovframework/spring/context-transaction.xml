<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
    	http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
    	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd">

	<bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource"/>
	</bean>

	<tx:advice id="txAdvice" transaction-manager="txManager">
		<tx:attributes>
			<tx:method name="*" rollback-for="Exception"/>
		</tx:attributes>
	</tx:advice>

	<aop:config>
		<aop:pointcut id="requiredTx" expression="execution(* egovframework.example.sample..impl.*Impl.*(..))"/>
		<!-- QPS QI 저장(계획서·보고서·주제선정·자원지원) : 머리 저장 뒤 항목을 지우고 다시 넣는다.
		     트랜잭션이 없으면 항목 INSERT 가 실패할 때 새 문서는 머리만 고아로 남고, 고치던 문서는 항목이 지워진 채 남는다.
		     ★이 앱에는 tx:annotation-driven 이 없어 @Transactional 은 동작하지 않는다 — 그래서 여기 pointcut 으로 건다. (2026-09-15) -->
		<aop:pointcut id="qpsQiSaveTx" expression="execution(* egovframework.wnn_medcost.qps.service.impl.QpsServiceImpl.saveQi*(..))"/>

		<!-- ★순서 규칙(스키마) : aop:config 안은 pointcut 전부 → advisor 전부. 섞으면 기동 때 XML 검증에서 컨텍스트가 죽는다. -->
		<aop:advisor advice-ref="txAdvice" pointcut-ref="requiredTx" />
		<aop:advisor advice-ref="txAdvice" pointcut-ref="qpsQiSaveTx" />
	</aop:config>

</beans>
